# journal/

One short markdown log per work session: `YYYY-MM-DD-session-NN.md`. Put the newest summary at the
top of the newest file so a cold pickup is instant. Every commit stages a journal change (the
`.githooks/pre-commit` gate).
