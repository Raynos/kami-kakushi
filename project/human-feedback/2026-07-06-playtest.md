# 2026-07-06 — playtest feedback (async inbox capture)

Feedback captured **in-game** via the F3 capture overlay during the human's
2026-07-06 review of the ADR-139 story DEV surfaces (session
`2026-07-06T11-29-26-a7a4bf`), drained via `/drain-inbox`. Each capture carries
a deterministic save; every item was reproduced against the running game
before it was acted on (R2/PH2).

**Status legend:** 🔲 open · 🔧 in progress · ✅ fixed · 🅿️ parked ·
💬 needs-discussion. **Routing:** bug / UX / feel / visual → fix + log here;
_what the game is_ → also an ADR; general taste → graduate to a living doc.

---

## Feedback log

### FB-122 · story reader too narrow — ✅
**Verbatim:** _"Make this modal wider"_
**Reading:** the reader capped at 72rem; galley's three columns want the whole
viewport.
**Doc-update plan:** none — surface-local.
**Distilled rule:** a compare-across-takes surface earns full-bleed width; the
56ch modal default is for ceremony reading, not side-by-side review.
**Fixed in:** (this drain's commit) — card is now `100vw/100dvh − gutter`,
no rem cap.

### FB-123 · double border under scroll in the reader — ✅
**Verbatim:** _"There's a werid border box with a line here as you scroll, it
feels like theres multiple borders here, one broken and one working"_
**Reading:** the `.frame::after` inner key-line (absolute-positioned) scrolled
away with the content because the framed card was itself the scroll container;
the inset box-shadow frame stayed — two borders, one broken.
**Doc-update plan:** none new — this is the existing "chrome pinned, panes
scroll internally" rule applied.
**Distilled rule:** never make a `.frame` element its own scroll container —
scroll an inner pane; the key-lines belong to the fixed chrome.
**Fixed in:** (this drain's commit) — title + pills pinned, content pane
scrolls.

### FB-124 · identical Tokubei lines read as a broken swap — ✅
**Verbatim:** _"The lines for TOkubei are the same here in all three variants.
that feels like a bug to me in narrative diverge."_
**Reading:** not a code bug (substitution replaces the whole scene) — the DEMO
takes only varied Genemon and copied Tokubei verbatim; but an identical line
across takes is ILLEGIBLE (can't tell a working swap from a broken one).
**Doc-update plan:** takes/README rule below.
**Distilled rule:** in a take-compare surface, a line byte-identical to canon
must READ as "shared with canon" (dimmed + labelled), so deliberate sharing
never looks like a failed substitution (TST4).
**Fixed in:** (this drain's commit) — demo takes now vary Tokubei per
register; the galley reader dims canon-identical lines.

### FB-125 · retire the losing reader variants — ✅
**Verbatim:** _"Now that we've locked in the diverge, you can remove the code
for annotated script & stage read."_
**Reading:** the HR-9 verdict is FIRM: **Galley columns** wins. Retire
annotated + stage per the ADR-075 lifecycle (losers survive in git history +
the HR-9 record).
**Doc-update plan:** HR-9 → ✅, graduates to the archive.
**Fixed in:** (this drain's commit) — reader is galley-only; pills removed.

### FB-126 · cold-open restore line reads as an ugly button — ✅
**Verbatim:** _"omg its such an ugly button lol, wtf. Let's have a really small
subtle link anchored at the bottom, almost like a footer."_
**Reading:** the HD-24 "Returning? Restore a saved game" affordance (shipped
s94) sat right under the wake verb at `fs-small` with an underline — it read as
a second button competing with "Open your eyes". The human wants it demoted to a
tiny, subtle footer anchored at the bottom of the field.
**Placement:** viewport-bottom footer (human's pick via AskUserQuestion, over a
card-bottom variant) — detached from the title card entirely.
**Doc-update plan:** none new — reinforces the existing TST3/§4.8 hierarchy rule
(a secondary affordance must not compete with the primary CTA).
**Distilled rule:** a secondary/opt-in affordance next to a primary CTA belongs
demoted and spatially separated (a footer), not stacked directly under it where
it apes the CTA's weight.
**Fixed in:** (this drain's commit) — `.coldopen-restore` moved out of the card
to a `position:absolute` bottom-anchored footer, `fs-micro`, dimmed
(`color-mix … 55%`), underline held for hover/focus so at rest it reads as a
faint footer link, not a button.

---

## M1 steel-palette review (sessions `13-30-21-1704ec` + `13-38-22-e5ce66`)

The human's live M1 review feedback, drained same-day; the human approved the
full batch ("just do all the proposals… fix them all"). All fixes land in the
FB-127…FB-140 drain commit unless noted.

### FB-127 · chat log loses conversation context — ✅
**Verbatim:** _"The chat panel loses context completely, Every segment here should be having a small UI element that says 'Talking to [name]' … right now its just a context less linear log"_
**Reading:** chats with different NPCs need visible conversation grouping (TST4).
**Distilled rule:** a multi-party transcript must mark interlocutor changes —
speaker colour alone doesn't scope a conversation.
**Fixed in:** (this drain's commit) — a quiet `— with <name> —` kicker inside
the first chat line whenever the partner changes (reconcile-safe
`data-kicker`; resets on filter switch/reset). Known v1 limit: a player line
preceding the NPC's first reply sits above the kicker.

### FB-128 · unique per-character colors — ✅
**Verbatim:** _"I miss the unique colors per character can we bring it back."_
**Reading:** the M1 pigment collapse merged the cast's voice colours.
**Fixed in:** (this drain's commit) — first-class `--v-*` voice tokens
(dark-tuned): narrator muted blue-grey · player bright silver · Sōan teal ·
Chiyo amber · arms rust · official violet · villager sage · lord wisteria;
`VOICE_COLOR` + the log `voice-*` classes consume them.

### FB-129 · narrator vs You too similar — ✅ (fix shared with FB-128)
**Verbatim:** _"The color difference between narrator and 'you' is too little."_
**Fixed in:** the FB-128 values separate them by design (narrator `#9aa4c4`
muted vs player `#e3ecff` bright).

### FB-130 · divider bar too bright — ✅
**Verbatim:** _"This bar here feels really steel bright compared to th rest of the UI"_
**Reading:** the ink-gradient `.brush-rule` inverted light when the M1 text
ramp flipped.
**Fixed in:** (this drain's commit) — now a 2px faint gold hairline
(`--key-dim → --key → --key-dim`), brush mask kept.

### FB-131 · Dwell/Shake choice colours too similar — ✅ · **temper LOCKED**
**Verbatim:** _"Color of dwell on it and Shake it off is too similar"_
**Reading:** +INT (silver) vs +SPD (silver-dim) near-identical under the
3-metal collapse — the T4 failure the plan's fallback anticipated.
**Human steer:** 3-metal THEME stays; attrs get a distinct **5-metal** set.
Candidates built behind a DEV toggle; **the human locked `temper`**
(steel temper-oxide colours: bronze/pale-quench/purple/blue/straw) —
alternates + toggle stripped same-day (ADR-075 zero flag-debt).
**Distilled rule:** role beats hue, but *distinguishability beats collapse* —
a semantic set of N same-family colours must survive side-by-side reading.

### FB-132 · decision prompt needs a call-to-action colour — ✅
**Verbatim:** _"The Do you follow it is a call to action, can we have a call to action colro ?"_
**Fixed in:** (this drain's commit) — VN decision-prompt lines render gold
(value/CTA), not narrator grey.

### FB-133 · red outline on "Open your eyes" off-theme — ✅
**Verbatim:** _"The red outline doesnt fit the theme here"_
**Reading:** `.verb.primary` (any suggested action) carried vermillion —
over-spending the reserved hot accent.
**Fixed in:** (this drain's commit) — primary = gold keyline + inset gold
ring; vermillion retreats to commit/danger (M2 formalizes a danger treatment).

### FB-134 · speaker prefix should be bold — ✅
**Verbatim:** _"The prefix could be bold for the name tbh. Same with You:"_
**Fixed in:** (this drain's commit) — `.vn-speaker` → weight 700, opacity 1.

### FB-135 · player lines need a unique colour — ✅ (fix shared with FB-128)
**Verbatim:** _"What you say should also have a unique color."_

### FB-136 · Settings and DEV buttons misaligned — ✅
**Verbatim:** _"The settings and dev buttons are not aligned"_
**Fixed in:** (this drain's commit) — the DEV chip's fixed bottom now matches
the footer's `--space-1` padding (bottom-aligned with Settings).

### FB-137 · tint attribute names in ±stat lines — ✅
**Verbatim:** _"Can we apply subtle colors to the attribute names and their +1 INT section"_
**Fixed in:** (this drain's commit) — `buildStatLine` wraps each `±N ATTR`
token in its attribute pigment wherever perk mechanics render.

### FB-138 · rake no longer writes to Now — 💬 could not reproduce
**Verbatim:** _"Raking the spilled rice no longer writes to the Now panel"_
**Finding:** verified against the capture's save — rake DOES emit its
ephemeral reward line, and it appears live while Now is the active tab. Now
lines are fleeting by design (15s TTL — FB-115/FB-53): by the time the tab was
opened the lines had aged out, leaving the "Quiet, just now" placeholder. No
change; flag again if the intent is a longer/no TTL.

### FB-139 · × button crowds the modal border — ✅
**Verbatim:** _"Not enough elegant space between X button and borders."_
**Fixed in:** (this drain's commit) — `.modal-close` inset → `--space-4` both.

### FB-140 · About colophon wraps + takes too much space — ✅ (two captures, one fix)
**Verbatim:** _"Line wraps which is a shame, can we change the text or widen the modal a bit."_ + _"This bottom section takes up too much space, can we compact this metadata and reduce the whitespace"_
**Fixed in:** (this drain's commit) — modal 56ch → 62ch (build line no longer
wraps); `.modal-meta` compacted to micro type, near-zero leading — one quiet
colophon.

### FB-141 · only the SPOKEN words should carry the voice colour — ✅
**Verbatim:** _"No only what the character says has a color"_ (round-2 on FB-128)
**Reading:** whole-line voice colouring painted the stage narration ("Sōan …
sits back on his heels.") in the speaker's colour too.
**Fixed in:** (this drain's commit) — VN lines segment into speech/narration
(`writeVnSlice`); the voice rides a per-line `--voice` property landing only on
`.vn-speech` spans + the name prefix; the typewriter writes through the same
segmenter so speech colours as it types.
**Distilled rule:** voice colour marks *utterance*, not the line — narration
inside a dialogue line stays neutral.

### FB-142 · ask-button text should match the rendered player line — ✅
**Verbatim:** _"The color of the word your about to say in the Ask section does not match the color of what it renders in the log, i think we want the same white here"_
**Fixed in:** (this drain's commit) — `.intro-ask` → `--v-player` (these ARE
your words-to-be).

### FB-143 · Genemon's colour vs the CTA gold too similar — ✅
**Verbatim:** _"Genemon's color and the 'Call to action' color are too similar. there's no difference between the two."_
**Fixed in:** (this drain's commit) — steward → drier ochre `#c9975c`; the
decision prompt → `--gold-hi` (brighter, paler). Hue + brightness now separate.

---

## M2 steel-materials review (sessions `14-19-21-8e9d11` + `14-24-12-4ea4cf`)

12 captures, drained in one approved sweep (AskUserQuestion: button cap = shared
~40ch; scope = all 12). All fixes land in the FB-144…FB-155 drain commit.

### FB-144 · unquoted NPC speech not voice-coloured — ✅
**Verbatim:** _"He's clearly saying this text but it's not colored correctly."_
**Reading:** narrative sources may write bare dialogue (no quotation marks);
the FB-141 quote-scoping left those lines neutral.
**Fixed in:** (this drain's commit) — a voiced non-narrator line with no quotes
is ALL speech: the whole span takes the voice colour.

### FB-145 · action buttons too wide — ✅
**Verbatim:** _"In general these buttons are way too wide, it's a bit silly,"_
**Fixed in:** (this drain's commit) — `.slice-do .verb { max-width: 40ch }`
(human pick: one shared cap, tidy left-aligned column).

### FB-146 · manual act should stop auto — ✅
**Verbatim:** _"You should not be able to click and do auto at the same time…"_ + _"Clicking rest or any other action tbh should stop the auto of rake the spilled rice."_ (two captures, one fix)
**Fixed in:** (this drain's commit) — the renderer's player-dispatch wrapper
disarms all three autos (auto-rake / auto-labour / auto-combat) before any
manual act intent (rake/rest/activity/fight/cook/eat/repair). The auto loop's
own dispatches bypass the wrapper, so it can't self-cancel.
**Distilled rule:** a manual act TAKES OVER from automation, never races it.

### FB-147 · rung choices lack colour hints — ✅
**Verbatim:** _"These boxes have no color hints for their choice."_
**Fixed in:** (this drain's commit) — the rare statBonus pick wears its attr
pigment; every other rung choice carries its honest mechanical axis, the
relationship write: warmth-gaining GOLD · even SILVER · warmth-costing SHU.
**Note:** this deliberately telegraphs relational cost (TST4); say the word if
that reads as spoiling the choice.

### FB-148 · vitals number flash headache — ✅
**Verbatim:** _"I'm getting a head ache from this color and font flashing, let's not animate the increase or flash or change color."_
**Fixed in:** (this drain's commit) — the num-pop scale+vermillion keyframes
are retired; steady numerals, the meters carry the motion.
**Distilled rule:** per-tick juice on an auto-grinding counter reads as
strobing, not feedback — animate state changes, not increments.

### FB-149 · DEV chip ≠ Settings height — ✅
**Verbatim:** _"The height of the dev menu is differnet from the height of the settings menu, i want them to be pixel aligned,"_
**Fixed in:** (this drain's commit) — both are exactly 28px border-box
(measured 30.45 vs 28.19 before), bottoms already aligned at the footer inset.

### FB-150 · log scrolls up in chunks — ✅
**Verbatim:** _"This now panel can scroll up smoother, it scrolsl up chunk at a time and its very jarring."_
**Fixed in:** (this drain's commit) — whole-line appends now GLIDE
(scrollTo smooth) with an unpin-guard window so mid-glide positions can't
strand the reader (the FB-77 lag bug stays fixed: typewriter chars + floods
keep the instant jump; reduced-motion always jumps).

### FB-151 · bullet glyph inside ask buttons — ✅
**Verbatim:** _"Why is there a bullet point of a line item inside the box, let's remove that."_
**Fixed in:** (this drain's commit) — the `・` ::before died.

### FB-152 · queued lines pre-show their speaker names — ✅
**Verbatim:** _"IN this rung up box the names of lines that are not rendered pre show like tokubei."_
**Fixed in:** (this drain's commit) — a queued transcript line is fully hidden
(`.vn-pending`) until its typewriter turn; the FB-50 instant-name rule now
applies per line, not per block.

### FB-153 · rung-up ceremony into the beat modal — ✅
**Verbatim:** _"The rung up promoted icon kind of just shows up and fades away… press a button sayign rung up, then render in the modal the icon and add some flavor text like 'You've been promoted to X'"_
**Fixed in:** (this drain's commit) — built as specced: the beat outcome's
control reads **Rung up**; pressing it renders the seal + "You've been
promoted to {rank}." inside the modal (bell rings there), and its Continue
dispatches. The floating overlay is suppressed for that transition (ascension
overlay untouched). The e2e VN walker + the rung unit test updated in-commit.

### FB-154 · button hover unreadable — ✅ (already fixed in M2, 711d0b1)
**Verbatim:** _"The hover state is still not very readable…"_
**Finding:** the capture predates the M2 hover rework (hover now raises the
plate dark with bright silver text — no light fill). Re-check on refresh.

### FB-155 · Settings cursor flickers on hover — 💬 could not reproduce
**Verbatim:** _"Hovering over settings flips the cursor from pointer to hand to pointer toh and etc…"_
**Finding:** probed headlessly — the button node is stable across ticks and
`cursor: pointer` computes on it and its children; no churn, no geometry
shift. Likely the HMR bursts while the tree was being edited mid-play.
Re-check on the committed build; recapture if it persists.

### FB-156 · eat-rice output spams Work, belongs in Now — ✅
**Verbatim:** _"This should not be in work, it should be in now."_ (the "You take a bowl of plain rice. (−2 rice) ×20" line)
**Fixed in:** (this drain's commit) — eat-rice AND cook-meal result lines are
now `ephemeral` (the F58a rake/labour precedent): fleeting Now flavor, never
Work spam. The satiety/HP still banks; only the line fades.

### FB-157 · estate section is border soup; wants a diverge — 🔧 quick fix + routed
**Verbatim:** _"The estate section has too much borders… that's a triple border. I think this whole estate section needs to be redesigned with a diverge."_
**Quick fix:** (this drain's commit) — a card nested inside the framed
do-column drops its own plate frame (key-dim top rule scopes it instead), so
the triple-border read dies today.
**Routed:** the full estate-section redesign joins **M6** as its own diverge
lane (2–3 working variants behind the DEV toggle, ADR-075).

---

## Round-3 captures (session `14-52-42-4d786c`) + the log-history lock

### FB-158 · bare dialogue shows without quotation marks — ✅
**Verbatim:** _"Lol why is the \" missing here, everythinge else that's speech has \" around it."_
**Fixed in:** (this drain's commit) — a voiced non-narrator line whose authored
text carries no quotes is wrapped at DISPLAY time (VN transcript + log), so
dialogue.md's bare teach lines read like every other utterance (and route
through the speech-colour segmenter for free).

### FB-159 · promotion should be HUGE and centred — ✅
**Verbatim:** _"There promotion is supposed to be huge, so let's try to center it in the modal and then also center the contineu button also below it."_
**Fixed in:** (this drain's commit) — the ceremony now overlays the WHOLE
vn-card: 148px seal + kicker + flavour + Continue, all centred (verified at
x=640 dead centre).

### FB-160 · Progress perks gone / FB-161 · story history gone — ✅ (one root cause)
**Verbatim:** _"Inside progress the perk's are gone."_ + _"Lol all story text history is gone ."_
**Root cause:** the log was a 300-entry ring; the auto-rake grind's ephemeral
spam (287 of the surviving 300) evicted the perk boxes and all story lines.
**Human lock (2026-07-06):** durable story/chat/progress history is
**UNBOUNDED — never evicted**; only fleeting (ephemeral) lines cap
(LOG_EPHEMERAL_MAX=100 — they fade in ~15s and need no deep history). The
RENDERER windows the DOM (newest 300 on load/tab-switch; scroll-back lazy
backfill is sanctioned future polish) — core loses nothing.
**Fixed in:** (this drain's commit) — core + tests rewritten to the new
semantics (RED-able: durable-survival + unbounded-history assertions), DOM
window added, fixtures regenerated.

---

## M3 Andon-frame review (session `15-58-24-be06da`)

### FB-162 · DEV chip stranded in the corner whitespace — ✅
**Verbatim:** _"This button is too far in the corner, move it over a bit, away from the whitespace."_
**Fixed in:** (this drain's commit) — the chip's fixed right inset now tracks
the CENTRED shell's edge (`max(.75rem, 50vw − 720px + .75rem)`), so on wide
screens it sits inside the frame.

### FB-163 · no vertical frame lines — ✅
**Verbatim:** _"The lack of vertical lines is weirding me out… we dont want the horizontal lines ending as they do now"_
**Fixed in:** (this drain's commit) — the shell wears side keylines
(`--key-dim` left/right), terminating the header/nav/footer rules into a frame.

### FB-164 · scrollbar too wide, sits on the text — ✅
**Verbatim:** _"The scrollbar that{s custom is too wide, it interacts with the text at the edge and nedes to be thinner."_
**Fixed in:** (this drain's commit) — slim 6px steel scrollbars everywhere a
pane scrolls (silver-faint thumb, transparent track; Firefox `thin` +
WebKit).

### FB-165 · chat kicker out of order — ✅
**Verbatim:** _"Lol the logs are wrong, the with soan is out of order, like the header is in the wrong place"_
**Reading:** the v1 streaming kicker attached to the NPC's reply, so the
player's opening question sat ABOVE its own conversation header.
**Fixed in:** (this drain's commit) — the kicker map is now derived purely
from the entries (lookahead: a player line that opens a run finds its partner
in the NPC's reply), so "— with Sōan —" leads the player's opener. Verified
against the original capture save.

### FB-166 · "why do we even have a rice counter top left" — 💬 design question
**Verbatim:** _"Why do we even have a rice counter on the top left, what value does that have."_
**Status:** bounced back to the human (a what-the-game-shows question, not a
bug): rice is the food/sell stock the early loop banks — the counter is its
only always-visible home. Options if it earns no place: drop it from the
vitals strip at higher rungs, or fold it into the Inventory tab only.
