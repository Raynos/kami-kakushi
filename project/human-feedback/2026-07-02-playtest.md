# 2026-07-02 — playtest feedback (human-steered, live capture)

The human is playing the live v0.3.2 build (`localhost:5173`) and giving taste
direction as they go. This is the **live capture** — feedback recorded verbatim
as it's given so nothing is lost; each item is triaged into a **fix plan** (code)
and a **doc-update plan** (which living doc absorbs the taste).

**The higher-level goal (the human's framing):** capture the human's _taste and
guidelines_ into the living docs so **future roadmap implementations don't need
the same feedback again**. So every item does double duty — fix the instance
_and_ distil the general rule. The distilled rules collect in
**§ Taste distillation** at the bottom and graduate to
[`fun-factor.md`](../../docs/living/fun-factor.md) /
[`ui-design.md`](../../docs/living/ui-design.md) /
[`decisions.md`](../../docs/living/decisions.md).

**Status legend:** 🔲 open · 🔧 in progress · ✅ fixed · 🅿️ parked ·
💬 needs-discussion. **Routing:** bug / UX / feel / visual → fix + log here;
_what the game is_ → also an ADR; general taste → graduate to a living doc.

---

## Feedback log

<!--
Per-item template — fill one block per piece of feedback:

### F1 · <short title> — <status emoji>
**Verbatim:** _"<exactly what the human said>"_
**Reading:** <my interpretation of the intent>
**Doc-update plan:** <which living doc absorbs the taste>
**Distilled rule:** <the general taste guideline, if any → § Taste distillation>
**Fixed in:** <commit hash — we fix inline, so the commit IS the record, not a plan>
-->

### F1 · DEV panel renders a big white block — ✅

**Verbatim:** _"The main page has a big white block because of the dev UI. white
looks ugly, make it the same background everywhere."_
**Reading:** The DEV panel's container background is white/unset instead of
inheriting the game's dark background, so a big white block runs down the whole
right strip and clashes with the woodblock/ink look. Make it match the game
background so there's no white seam. This is a DEV-only surface, but the human
still wants it visually consistent with the game.
**Fix plan (code):** Find the DEV panel component/CSS and set its container/body
background to the game's canonical dark background variable — reuse the existing
`--bg` / ink variable, don't hardcode a colour. (A parallel subagent is applying
this fix now.)
**Doc-update plan:** Add a rule to
[`ui-design.md`](../../docs/living/ui-design.md) (the woodblock/ink UI bible)
that DEV/tooling surfaces must inherit the game's dark background — no
white/browser-default backgrounds anywhere, ever, even on dev-only chrome.
**Distilled rule:** No raw white / browser-default backgrounds anywhere in the
app, including DEV-only tooling and chrome. Every surface inherits the game's
dark ink background by default. White reads as unfinished/ugly and breaks the
woodblock immersion. → § Taste distillation (home: `ui-design.md`).
**Fixed in:** `7642fd7`

### F2 · ?dev=no opt-out + dev panel is shoving the UI off-center — ✅

**Verbatim:** _"I want to be able to open http://localhost:5173/?dev=no and just
not see the dev panel at all. I have a suspicion that the UI is off center
because of the position of the dev panel and that if the dev panel is fully
removed / never rendered / fully hidden that the UI would be different."_
**Reading:** Two linked asks. (1) A `?dev=no` URL query param that FULLY
suppresses the DEV panel — not rendered at all / never mounted in the DOM, not
merely visually hidden. (2) The human suspects the main game UI is off-center
because the DEV panel occupies real layout space (it's a right-hand column in
the layout flow, so the game content is centered in the leftover space, not the
full viewport). Verify that hypothesis: if true, the DEV panel should be a
fixed/overlay element outside the centering layout so the game UI is always
centered on the FULL viewport whether or not the panel is present. The `?dev=no`
case and the off-center fix are related — with the panel gone the layout should
be identical to the panel being an overlay.
**Fix plan (code):** (a) Add a `?dev=no` (or `dev=0`/`dev=false`) query-param
check that gates DEV-panel mounting — when set, the panel is never rendered.
Keep the existing default (panel shows in DEV builds). (b) Investigate whether
the DEV panel is part of the layout flow (e.g. a flex/grid column) that
de-centers the game; if so, make the panel `position: fixed`/overlay so the game
UI centers on the full viewport regardless. Confirm by comparing the layout with
panel present vs `?dev=no`. (Fix agent will be dispatched once the F1 CSS fix
lands, to avoid a same-file race.)
**Doc-update plan:** Add to
[`ui-design.md`](../../docs/living/ui-design.md): the game UI must center on the
FULL viewport independent of any DEV/tooling chrome (dev tooling is an overlay,
never in the centering layout); document the `?dev=no` opt-out as the way to
preview the true player-facing layout. If `?dev=no` becomes a supported dev
affordance worth locking, consider a short note in
[`qa-playtesting.md`](../../docs/living/qa-playtesting.md) too.
**Distilled rule:** DEV/tooling chrome must never affect the player-facing
layout. The game UI centers on the full viewport as if the dev panel doesn't
exist; dev tooling is a fixed overlay, not a layout-flow element. Always provide
a way to preview the true player layout (e.g. `?dev=no`). → § Taste distillation
(home: `ui-design.md`, with a QA note in `qa-playtesting.md`).
**Fixed in:** `7642fd7`

### F3 · "Open your eyes" button not centered in the intro card — ✅

**Verbatim:** _"Open your eyes button is not centered."_
**Reading:** In the cold-open intro card, the title/subtitle/prose all center on
the card's axis but the "Open your eyes" button is left-aligned, breaking the
centered composition. The button should be centered on the same axis as the rest
of the card's content.
**Fix plan (code):** In [`src/ui/styles.css`](../../src/ui/styles.css) (single
stylesheet) find the intro/cold-open card's button (and/or its container) and
center it horizontally — likely the button's wrapper needs `text-align: center`
or the flex container needs `align-items: center` / `justify-content: center` /
`margin: 0 auto`. Match how the title/prose are centered. This is being batched
with the F2 layout fix (same file, same layout concern) into one fix agent to
avoid a same-file race.
**Doc-update plan:** Add to
[`ui-design.md`](../../docs/living/ui-design.md): in a centered card/panel, a
single primary action (button) inherits the card's center axis — don't let it
default to left-aligned. (This can share the F2 ui-design.md layout section.)
**Distilled rule:** In a centered card/panel composition, a single primary
action inherits the card's center axis — a lone button should be centered with
the rest of the content, not left-aligned by default. → § Taste distillation
(home: `ui-design.md`).
**Fixed in:** `7642fd7`

### F4 · Main UI too narrow + DEV panel de-centers it — ✅

**Verbatim:** _"The main UI component is too narrow, too much whitespace left &
right ( could be caused by dev panel ). The main UI component should take up more
of the screen. the dev panel should be a floating panel that's just abstractly on
top somewhere, and when collapsed should be as small as possible, maybe anchored
to the bottom somewhere."_
**Reading:** The DEV panel mount reserved a 16rem right gutter on `#app`
(`app.style.paddingRight`), which shoved the game UI left, created the L/R
whitespace, and (with `body` having no background) exposed a white strip — the
same root cause as F1/F2. The main UI (`.shell`, capped at max-width 980px)
should be wider and take more of the screen. The DEV panel should be a true
floating overlay (position:fixed, reserving NO layout space), bottom-anchored,
and collapse to as small as possible.
**Fix plan (code):** Delete the `#app paddingRight:16rem` gutter in
[`src/ui/dev.ts`](../../src/ui/dev.ts); make the panel bottom-anchored +
`width:fit-content` when collapsed (grow to 15rem expanded); widen `.shell`
max-width 980→1200 and give `body` a dark `--ink` background so margins read as
an intentional woodblock border, not white. (Being implemented now in the main
session as part of the full-viewport app-shell restyle.)
**Doc-update plan:** [`ui-design.md`](../../docs/living/ui-design.md) — the main
UI is a centered paper column (~1200px) on an ink ground that fills the viewport;
DEV/tooling chrome is a fixed overlay reserving no layout space (ties to F2's
rule).
**Distilled rule:** The player UI fills the viewport as a centered paper column
on the ink ground — no cramped narrow column, no raw-white margins. DEV/tooling
chrome is a fixed floating overlay that reserves ZERO layout space and collapses
to minimal. → § Taste distillation (home: `ui-design.md`).
**Fixed in:** `7642fd7`

### F5 · Adopt a fixed header + fixed footer app-shell — ✅

**Verbatim:** _"Other games have a fixed footer on the bottom for UI elements, I
think we would benefit from the fixed header and fixed footer."_
**Reading:** Model the layout like other idle/RPG games: a fixed header pinned to
the top and a fixed footer pinned to the bottom, with the game content filling
the space between. The footer hosts persistent UI controls. (Reference
screenshots the human shared show idle-RPG shells with a header status row and a
footer holding save/settings/version controls.)
**Fix plan (code):** Make `.shell` a full-height flex column (`height:100dvh;
overflow:hidden`): header rows (titlebar/vitals/nav) `flex:0 0 auto` pinned top;
`.workspace` `flex:1; min-height:0` fills the middle; add a new
`<footer class="appbar-footer">` pinned bottom holding the version stamp + the
Settings button (moved out of the titlebar). (Being implemented now in the main
session as part of the full-viewport app-shell restyle.)
**Doc-update plan:** [`ui-design.md`](../../docs/living/ui-design.md) — document
the app-shell contract: fixed header (identity + vitals + nav) / flexible content
/ fixed footer (persistent controls: version, Settings, …).
**Distilled rule:** The game uses a fixed-header / flexible-content /
fixed-footer app shell (like established idle-RPGs). Persistent controls live in
the footer; identity + vitals live in the header; the content region fills the
space between and scrolls internally. → § Taste distillation (home:
`ui-design.md`).
**Fixed in:** `7642fd7`

### F6 · No page scrollbar — size the UI to the viewport — ✅

**Verbatim:** _"There's a damn scroll bar in the main UI, like the UI is not sized
vertically based on the available space. Of course it needs a minimum size, but it
should not have any scroll bars"_
**Reading:** The whole page scrolls because the content (a full-height header plus
a `.log` capped at `max-height:78vh`/`min-height:60vh`) exceeds the viewport.
Instead the app shell should be exactly viewport-height with NO page scrollbar;
the individual content panes (story log, work column) scroll INTERNALLY within the
available space. A minimum size is fine, but no page-level scrollbars.
**Fix plan (code):** `body`/`#app` `overflow:hidden` + `height:100dvh`;
`.workspace` a full-height grid row (`grid-template-rows:minmax(0,1fr);
min-height:0; overflow:hidden`); the `.log` and `.work` panes get `min-height:0;
overflow-y:auto` (drop the `60vh/78vh` caps) so they scroll internally. A
`@media (max-width:720px)` fallback relaxes to natural page flow so mobile still
works. (Being implemented now in the main session as part of the full-viewport
app-shell restyle.)
**Doc-update plan:** [`ui-design.md`](../../docs/living/ui-design.md) — the app
shell is exactly viewport height with no page scroll; content panes scroll
internally; a narrow-screen fallback allows page flow.
**Distilled rule:** The app shell is sized to the viewport (100dvh) with NO page
scrollbar; content panes scroll internally within the available height. Respect a
minimum size and fall back to natural page flow on narrow screens. → § Taste
distillation (home: `ui-design.md`).
**Fixed in:** `7642fd7`

### F7 · Story log pops in instead of scrolling — ✅

**Verbatim:** _"I did not see the story log scroll, it just popped in."_
**Reading:** The story log auto-scrolls to the newest line with an INSTANT jump
(`logSection.scrollTop = logSection.scrollHeight`), so new content appears at the
bottom with no visible scroll motion — it "pops in." It should scroll SMOOTHLY to
follow the newest line so the story is seen to unfold, while still honoring
reduced-motion (OS preference or the in-app `.reduced-motion` class) with an
instant jump for those users.
**Doc-update plan:** [`ui-design.md`](../../docs/living/ui-design.md) (and a nod
in [`fun-factor.md`](../../docs/living/fun-factor.md)) — log/narrative updates
scroll smoothly to follow the newest entry; the motion of the story arriving is
part of the feel.
**Distilled rule:** "Narrative/log content that updates scrolls SMOOTHLY to
follow the newest entry (honoring reduced-motion) — never an instant jump. The
motion of the story arriving is part of the feel." Home doc:
[`ui-design.md`](../../docs/living/ui-design.md) (feel nod in
[`fun-factor.md`](../../docs/living/fun-factor.md)).
**Fixed in:** `f3ad6a9`

### F8 · Move the message/story log to the right column — ✅

**Verbatim:** _"The other games have message log on the right hand side, I think
that's a good approach."_
**Reading:** Reference idle/RPG games put the message log in the right-hand
column; the human wants the same. Swap the workspace so the interactive
work/actions column is on the LEFT and the story log is the RIGHT column. NOTE:
this supersedes the earlier v0.3.1 "story-log-as-left-hero" placement — live
human taste is canon (D-022).
**Doc-update plan:** [`ui-design.md`](../../docs/living/ui-design.md) — the
app-shell layout section: the message/story log occupies the RIGHT column;
work/actions sit on the LEFT (folds into the F4/F5/F6 layout section).
**Distilled rule:** "The message/story log lives in the RIGHT-hand column (the
established idle-RPG convention); the interactive work/actions column sits on the
left." Home doc: [`ui-design.md`](../../docs/living/ui-design.md).
**Fixed in:** `f3ad6a9`

### F9 · Filterable log — bottom tabs to toggle what's shown — ✅

**Verbatim:** _"The full log panel should have some kind of tabs or toggles at
the bottom so we can toggle what is shown. Story ( an important one ),
Actions/Work ( way less important ), Combat, Level ups, perks, unlocks. And
probably more, I actually think that the story is cool so would prefer that to be
a tab. that you can come back to."_
**Reading:** The story/event log (now the right-hand panel) should be FILTERABLE
via a row of tabs/toggles at the BOTTOM of the panel, so the player chooses which
channel(s) to see. Named categories so far: **Story** (the narrative — the human
calls out as important and wants as a returnable tab you can come back to),
**Actions/Work** (explicitly "way less important"), **Combat**, and
**Progression** (level-ups, perks, unlocks) — "and probably more" (e.g.
rewards/koku, system). Key taste signal: the STORY is content worth REVISITING,
not ephemeral scrollback — a first-class tab. This is a NEW UI surface, so D-075
(diverge — 2–3 working variants behind the DEV-panel toggle, live review, an
R-item each) applies; the human also floated "tabs OR toggles," which is exactly
the form-uncertainty diverge resolves. Process/default being confirmed with the
human now. Decision (human, 2026-07-02): DIVERGE — 2–3 working variants behind
the DEV-panel toggle, each an R-item; default view = **Story + a live 'All' you
switch to**.
**Doc-update plan:** `docs/living/ui-design.md` — the log panel carries a bottom
tab/toggle bar to filter channels (Story / Work / Combat / Progression / …);
Story is a persistent, returnable view. If diverged, add the variants to the
D-075 variant log + an R-item per variant in
`project/human-in-the-loop/review.md`.
**Distilled rule:** "The event log is filterable by channel via a bottom
tab/toggle bar (Story / Work / Combat / Progression / …). The STORY is a
first-class, returnable tab — the narrative is content worth revisiting, not
ephemeral scrollback. Work/actions noise is the least important channel." Home
doc: `docs/living/ui-design.md` (story-as-keepable-read nod in `fun-factor.md`).
**Fixed in:** `e7f8cb5`

### F10 · UI too large / not compact enough — ✅

**Verbatim:** _"The font size feels too large to me, the buttons are too large.
Other games have way more compact UI so you can fit more"_ — reinforced: _"the
font size is huge. The footer is huge, the header is huge, the tabs take up lots
of space, the resource info takes up lots of space. The action log and actions
have so little space."_
**Reading:** The whole chrome is oversized — root font, the titlebar, the footer,
the nav tab bar, and the resource/vitals row all eat vertical space, leaving the
log and actions cramped. Established idle-RPGs are far denser. Compact the UI:
shrink the type scale + control heights + spacing so more fits and the content
(log/actions) gets the room. (Being fixed now via the `:root` scale variables +
root font.) NOTE a deliberate a11y trade: control target height drops from 44px
to 32px (still above the WCAG 2.2 24px pointer minimum) — desktop-first density.
**Doc-update plan:** `docs/living/ui-design.md` — the UI is DENSE/compact
(idle-RPG density): a tight type scale, compact control heights, minimal chrome
padding; chrome (header/footer/tabs/resource row) stays minimal so content
dominates.
**Distilled rule:** "The UI is COMPACT and dense like an established idle-RPG — a
tight type scale, small control heights, minimal chrome padding. Chrome
(header/footer/tabs/resource row) must stay minimal; the content (log + actions)
gets the space. Bigger-is-not-better." Home doc: `docs/living/ui-design.md`.
**Fixed in:** `8e229f0`

### F11 · Multi-panel layout (5–7 panels), not everything crammed in 2 — 💬

**Verbatim:** _"The other games have multiple / many panels ( 5-7 ) and it feels
like we only have two. and we are slamming all the information in."_
**Reading:** Reference idle/RPGs lay information out across 5–7 distinct panels
(hero/stats, location, actions, skills, quests/data, message log, events…),
whereas our layout crams everything into two columns (work + log). The human
wants a denser MULTI-PANEL layout. This is a significant structural restructure
and it INTERACTS with the game's reveal-based progression (surfaces intentionally
unlock over rungs — early game is deliberately sparse), so it needs a deliberate
approach (a short plan and/or a D-075 diverge on the panel layout), not an inline
slam. It also overlaps with F9 (the log filter) — story/combat/etc. might become
their own panels rather than tabs. To be scoped with the human.
**Doc-update plan:** `docs/living/ui-design.md` — the layout is a multi-panel
dashboard (5–7 panels) rather than two crammed columns; reconcile with the
reveal-based progression (panels appear as their surface unlocks). Likely also a
`docs/plans/` plan and/or an ADR once the direction is locked.
**Distilled rule:** "Lay information across multiple distinct panels (idle-RPG
dashboard density), not a couple of crammed columns — reconciled with
reveal-based progression so panels appear as their surface unlocks." Home doc:
`docs/living/ui-design.md` (pending a plan/diverge to scope it).
**Fixed in:** —

### F12 · Story text too fast — want slow GBA-style scroll — 🔧

**Verbatim:** _"The text scrolling speed is still way too fast, think more like
slow GBA text scrolling from a old school pokemon game"_
**Reading:** The log's line cascade is too fast; the human wants a slow,
deliberate GBA/old-Pokémon-style text reveal (character-by-character typewriter
feel). KEY scoping: apply this to the STORY/narration channel only — a blanket
cascade slowdown would also drag combat-log spam. Part of the intro/story
cluster.
**Doc-update plan:** `docs/living/ui-design.md` + a feel nod in `fun-factor.md` —
story/narration text reveals slowly (GBA typewriter), honoring reduced-motion.
**Distilled rule:** "Story/narration text reveals slowly, GBA typewriter-style
(not instant, not a fast cascade) — scoped to the story channel, never combat
spam; reduced-motion → instant. Slow text is part of the atmosphere." Home:
`ui-design.md` (feel nod `fun-factor.md`).
**Fixed in:** pending

### F13 · Intro not incremental — let the player respond to the physician — 💬

**Verbatim:** _"The actual intro at the start of the game is not incremental
enough. The physician is a character here, but you don't have the ability to
respond to him in the intro text or talk back to him"_
**Reading:** The opening narration is one-directional and dumped. The physician
(Sōan) is a CHARACTER — the player should be able to respond / talk back, with
the intro revealed incrementally (click-to-advance dialogue) rather than a wall
of text. This is a real feature: a small interactive-dialogue mechanism +
content, likely an ADR (the intro becomes interactive), built with D-075
variants for the presentation. Part of the intro/story cluster.
**Doc-update plan:** `docs/living/ui-design.md` + likely a `docs/plans/` design +
an ADR in `docs/living/decisions.md` — the intro is incremental, interactive
dialogue (respond to Sōan), not a static wall.
**Distilled rule:** "Character dialogue (esp. the intro) is incremental and
interactive — the player advances it beat by beat and can respond / talk back,
not read a wall of text. NPCs the player meets are people to answer, not
narration." Home: `ui-design.md` (+ an ADR once the direction locks).
**Fixed in:** —

### F14 · Cold-open — button jammed on the border + no slow reveal — ✅

**Verbatim:** _"nitpick, not enough border space, open your eyes is right up
against the border. the framing of the border around the box can be more. This
opening page also has no scrolling effect, it has no 5 second delay before
showing the 'open your eyes' button to give you the slow open feel."_
**Reading:** Two parts. (a) NITPICK: the cold-open frame needs more internal
padding — "Open your eyes" sits against the border (the F10 compact pass
over-tightened THIS atmospheric card; it should stay roomy). (b) The opening
needs a SLOW REVEAL: the intro text reveals gradually and the "Open your eyes"
button appears only after a delay (~5s) for the slow-waking feel. (a) is a quick
inline fix; (b) is built as part of the intro cluster, with reveal-style
variants (D-075).
**Doc-update plan:** `docs/living/ui-design.md` — atmospheric surfaces (the
cold-open) keep GENEROUS padding (density applies to the game chrome, NOT the
cold-open); the opening reveals slowly with a delayed CTA.
**Distilled rule:** "Atmospheric/ceremonial surfaces (cold-open, seals) keep
generous breathing room — the compact-density rule is for the game chrome, not
these. The opening reveals slowly with a delayed CTA (the slow-waking beat), not
all-at-once." Home: `ui-design.md`.
**Fixed in:** `c1b6d82`

### F15 · Log scroll happens unseen — cascade AFTER open-eyes — 🔧

**Verbatim:** _"I still don't see any scrolling, I'm wondering if the text
already scrolled before i pressed the open your eyes button lol."_
**Reading:** The smooth log scroll (F7) isn't seen because the story log is
PRE-POPULATED behind the cold-open card while the shell is hidden — so the
cascade/scroll already happened by the time the player opens their eyes. Fix: the
waking narration must cascade AFTER `open_eyes`, while the player is watching,
not before. Part of the intro/story cluster.
**Doc-update plan:** `docs/living/ui-design.md` — reveal beats fire when the
player is WATCHING (post-interaction), never pre-rendered behind a curtain.
**Distilled rule:** "Reveal/animation beats must fire when the player is watching
(after the gating interaction), never pre-run behind a curtain — an unseen
animation is a wasted beat." Home: `ui-design.md`.
**Fixed in:** pending

### F16 · DEV panel — Settings/Variants sub-tabs, recency-ordered, collapsible — ✅

**Verbatim:** _"in the DEV UI have a sub header of two panels ( settings &
variants ) and then front load the most recently introduced variants to the top
of the list, and move the older ones to the bottom. The variants list in dev UI
can also be a collapsed summary of each variant and then you click it and get
more details and the 3 options."_
**Reading:** Restructure the DEV panel: a sub-header with two sections —
**Settings** (the speed/jump/rung/combat dev tools) and **Variants**. The
Variants list shows a COLLAPSED per-surface summary; clicking a surface expands
its details + the 3 options. Most-recently-introduced variant surfaces are
front-loaded to the TOP, older ones sink to the bottom. DEV-only tooling (not a
player surface) → no diverge; just build it. It makes the growing variant set
reviewable.
**Doc-update plan:** none player-facing; note in the `diverge` skill / DEV-panel
docs if any. Mainly a dev-ergonomics build.
**Distilled rule:** (dev-ergonomics, not player taste) "The DEV panel splits
Settings vs Variants; variants are recency-ordered (newest on top) collapsible
summaries that expand to their options — so a long variant list stays
reviewable." Home: the `diverge` skill notes / DEV tooling (not `ui-design.md`).
**Fixed in:** `fac9f7f`

### F17 · Approve cold-open typewriter + number all variants V0–VN — 🔧

**Verbatim:** _"GBA typewriter for variant cold-open reveal approved. Can we go
into all these variants and name them V0 - VN so i can quick refer them"_
**Reading:** Two things. (1) The human APPROVED variant B (GBA typewriter) as the
cold-open reveal prod default → lock it in, strip the fade/ma dead code (D-075).
(2) Give EVERY variant a stable, unique **V-number** (V0…VN, assigned in registry
order so they don't shift when a surface is locked/removed), shown on each option
button and on its collapsed summary row in the DEV panel's Variants tab, so the
human can quick-refer ("lock V19"). Both being built now.
**Doc-update plan:** none player-facing; a dev-ergonomics note in the `diverge`
skill / DEV-panel docs if useful. The taste rule is the "approve → strip dead
code" loop.
**Distilled rule:** "When the human approves a variant, it becomes the prod
default and the unpicked alternates are removed immediately (D-075 zero
flag-debt) — don't let dead variants linger. Variants carry stable V-numbers for
quick human reference." Home: the `diverge` skill / dev-ergonomics (not
`ui-design.md`).
**Fixed in:** pending

### F18 · Persist chosen variants in the URL query params — 🔧

**Verbatim:** _"When a variant is choosen in the dev UI update the query param in
the URL with the variant, when the page loads, read the query params and choose
those variants as per query param, that allows me to edit variants, refresh page
and keep the variants"_
**Reading:** Picking a variant in the DEV panel writes `?<surface>=<variant>` to
the URL (history.replaceState); on load, read the params and apply them via
setVariant so a refresh keeps the selection. Dev-ergonomics; lets the human tweak
→ refresh → keep variants (and share a URL).
**Doc-update plan:** none player-facing; a dev-ergonomics note in the `diverge`
skill / DEV-panel tooling docs.
**Distilled rule:** (dev-ergonomics) "DEV variant selections round-trip through
the URL query params — pick writes the param, load reads it — so a refresh
preserves the review state." Home: the `diverge` skill / dev tooling.
**Fixed in:** pending

### F19 · Story-log text reveals too fast — want a real GBA typewriter — 🔧

**Verbatim:** _"The text for the log panel just flies way too fast. its not like
the GBA Type writer effect, lets change that"_
**Reading:** The story-log line cascade is too fast; the human wants a genuine
GBA-style character-by-character typewriter for the STORY/narration lines (scoped
to that channel — not combat spam). This is the F12 story-typewriter, now urgent.
Reduced-motion → instant.
**Doc-update plan:** `docs/living/ui-design.md` + a feel nod in `fun-factor.md` —
story/narration lines reveal with a real character-by-character typewriter (GBA
feel), scoped to the story channel; reduced-motion → instant.
**Distilled rule:** "Story/narration lines reveal with a real
character-by-character typewriter (GBA feel), not a fast line-flash — scoped to
the story channel, reduced-motion → instant." Home: `ui-design.md` (feel nod
`fun-factor.md`).
**Fixed in:** pending

### F20 · Log filter tabs need an unread indicator — 🔧

**Verbatim:** _"Each tab in the LOG panel should have an unread indicator if
there's more there"_
**Reading:** Each filter tab (Story/Work/Combat/Progress) shows a badge/dot when
it has log entries the reader hasn't seen (new lines arrived in a non-active
channel); switching to that tab clears it.
**Doc-update plan:** `docs/living/ui-design.md` — filtered log channels surface an
unread indicator when new content lands in a channel you're not viewing;
switching to it clears the mark.
**Distilled rule:** "Filtered channels surface an unread indicator when new
content lands in a channel you're not viewing — switching to it clears the mark."
Home: `ui-design.md`.
**Fixed in:** pending

### F21 · Log filter bar — pick C segmented — ✅

**Verbatim:** _"Log filter bar choose C segmented."_
**Reading:** Human verdict — variant C (segmented control) is the log-filter prod
default; A (tabs) + B (chips) to be removed (D-075 strip-on-approve). Being locked
in now.
**Doc-update plan:** none player-facing; folds into the F17 "approve → strip dead
code" loop.
**Distilled rule:** (verdict; folds into the approve→strip-dead-code loop from
F17.)
**Fixed in:** pending

### F22 · Split work-stamina and health into distinct meters + recovery — 💬

**Verbatim:** _"Resting from work and resting from combat should be different
actions, like there's a meter for 'work stamina' and a way to refill it, and then
a health meter and a way to recover health."_
**Reading:** A CORE game-mechanics change (not UI polish): make work-stamina
(spent by labour) and health (spent in combat) distinct resources, each with its
OWN meter AND its own recovery action — "rest from work" ≠ "recover from a fight."
Touches src/core (intents, balance); needs a design note/ADR before building, not
a blind edit.
**Doc-update plan:** `docs/living/prd.md` + an ADR in `docs/living/decisions.md`
once locked — work-stamina and health are distinct resources with distinct
recovery actions.
**Distilled rule:** "Work-stamina and health are distinct resources with distinct
recovery actions — labour drains/refills stamina; combat drains/heals health;
never conflate the two rests." Home: `docs/living/prd.md` + an ADR once locked.
**Fixed in:** —

### F23 · Story log is a flat wall — differentiate narrator / NPC-said / player-said — 🔧

**Verbatim:** _"The story log is just a mountain of text, there's no colors,
there's no emphasis. I want to be able to quickly see - Narrator prose - Something
someone says, - Something I say,"_
**Reading:** The story log needs visual differentiation by VOICE: narrator prose
(muted), something an NPC SAYS (quoted speech — a distinct colour/emphasis), and
something the PLAYER says (their own voice). Shipping now: quoted NPC speech
styled distinct from narrator prose (render-time). "Something I say" (player
speech) doesn't exist in the content yet — it arrives with the interactive intro
(F13), which will tag player lines with a reserved player-voice style. Full
three-way distinction ideally wants speaker-tagged log entries (a small
core/content model) — part of the story-presentation cluster.
**Doc-update plan:** `docs/living/ui-design.md` (§5.1 event-log) — the story log
is legible by VOICE: narrator prose, NPC speech, and player speech each read
distinctly (colour + emphasis).
**Distilled rule:** "The story log is legible by VOICE — narrator prose, NPC
speech, and player speech each read distinctly (colour + emphasis), never a flat
undifferentiated wall." Home: `ui-design.md` (§5.1 event-log).
**Fixed in:** pending

> **Story-presentation cluster:** F12, F13, F15, F19, F23 now form a cohesive
> cluster best designed together — a speaker/voice model (F23) + the GBA
> typewriter (F12/F19) + incremental interactivity (F13) + reveal timing (F15).
> Design them cohesively, not as five isolated inline patches.

### F24 · DEV rung-teleports (R3/R4/R5/R7) freeze the page — ✅

**Verbatim:** _"Note that the buttons in dev ( R3, R4, R5, R7 ) spin the CPU and
take forever, i dont know how you implemented them, but they take a lot of cpu
time and freeze the page."_
**Reading:** The dev rung-teleports (`qa.toRung`) ran a long SYNCHRONOUS sim loop
on the main thread — up to 50_000 `focusedOptimalIntent` + `dispatch` steps, each
a fresh map BFS — brute-forcing every tick from R0 to the target rung. That CPU
spin froze the page. Make the teleport cheap so it lands instantly: grant the
story-half gate flags + a saturated meter and drive the REAL promotion engine ONE
rung per call (O(rungs), ≤ 7 iterations), so each rung still fires its
`rewardOnReach` reveals/unlocks + satiety refill and the landed state is coherent
for review. DEV-only (`__qa`, stripped from prod); the non-dev rung ladder is
untouched.
**Doc-update plan:** `docs/living/qa-playtesting.md` (the `capture-game-states` /
DEV-tooling guide) — add the rule that DEV teleports/fast-forwards must be cheap.
**Distilled rule:** "DEV teleports/fast-forwards must be cheap — never a long
synchronous main-thread sim loop that freezes the page; bound the work or jump
state directly." Home: dev-tooling / the `capture-game-states` QA guide.
**Fixed in:** this commit — pending

---

## Taste distillation — the general rules (graduate to living docs)

> The point of this section: turn one-off feedback into a **standing rule** a
> future implementer will read _before_ building, so the taste is applied by
> default. Each entry names its **home doc** and moves there once settled.

- **No raw white / browser-default backgrounds anywhere** (from F1) — every
  surface in the app inherits the game's dark ink background by default,
  including DEV-only tooling and chrome. White reads as unfinished/ugly and
  breaks the woodblock immersion. **Home doc:**
  [`ui-design.md`](../../docs/living/ui-design.md).
- **DEV/tooling chrome must never affect the player-facing layout** (from F2) —
  the game UI centers on the full viewport as if the dev panel doesn't exist;
  dev tooling is a fixed overlay, not a layout-flow element. Always provide a way
  to preview the true player layout (e.g. `?dev=no`). **Home doc:**
  [`ui-design.md`](../../docs/living/ui-design.md) (with a QA note in
  [`qa-playtesting.md`](../../docs/living/qa-playtesting.md)).
- **A lone primary action inherits its card's center axis** (from F3) — in a
  centered card/panel composition, a single primary action (button) should be
  centered with the rest of the content, not left-aligned by default. **Home
  doc:** [`ui-design.md`](../../docs/living/ui-design.md).
- **The player UI fills the viewport as a centered paper column** (from F4) — no
  cramped narrow column, no raw-white margins; the game UI is a centered paper
  column on the ink ground. DEV/tooling chrome is a fixed floating overlay that
  reserves ZERO layout space and collapses to minimal. **Home doc:**
  [`ui-design.md`](../../docs/living/ui-design.md).
- **Fixed-header / flexible-content / fixed-footer app shell** (from F5) — the
  game uses an established idle-RPG shell: persistent controls live in the footer,
  identity + vitals live in the header, and the content region fills the space
  between and scrolls internally. **Home doc:**
  [`ui-design.md`](../../docs/living/ui-design.md).
- **The app shell is sized to the viewport — no page scrollbar** (from F6) — the
  shell is exactly viewport height (100dvh) with NO page scrollbar; content panes
  scroll internally within the available height. Respect a minimum size and fall
  back to natural page flow on narrow screens. **Home doc:**
  [`ui-design.md`](../../docs/living/ui-design.md).
- **Narrative/log content scrolls smoothly to follow the newest entry** (from
  F7) — log/narrative updates scroll SMOOTHLY to the newest line (honoring
  reduced-motion) rather than an instant jump; the motion of the story arriving
  is part of the feel. **Home doc:**
  [`ui-design.md`](../../docs/living/ui-design.md) (feel nod in
  [`fun-factor.md`](../../docs/living/fun-factor.md)).
- **The message/story log lives in the RIGHT-hand column** (from F8) — the
  established idle-RPG convention: the message/story log occupies the right
  column while the interactive work/actions column sits on the left (supersedes
  the earlier v0.3.1 story-log-as-left-hero placement; live human taste is canon,
  D-022). **Home doc:**
  [`ui-design.md`](../../docs/living/ui-design.md).
- **The event log is filterable by channel via a bottom tab/toggle bar** (from
  F9) — Story / Work / Combat / Progression / … channels toggle what the log
  shows. The STORY is a first-class, returnable tab: the narrative is content
  worth revisiting, not ephemeral scrollback; work/actions noise is the least
  important channel. New UI surface → D-075 diverge applies. **Home doc:**
  [`ui-design.md`](../../docs/living/ui-design.md) (story-as-keepable-read nod in
  [`fun-factor.md`](../../docs/living/fun-factor.md)).
- **The UI is COMPACT and dense like an established idle-RPG** (from F10) — a
  tight type scale, small control heights, minimal chrome padding. Chrome
  (header/footer/tabs/resource row) must stay minimal; the content (log +
  actions) gets the space. Bigger-is-not-better. **Home doc:**
  [`ui-design.md`](../../docs/living/ui-design.md).
- **Lay information across multiple distinct panels (idle-RPG dashboard
  density)** (from F11) — not a couple of crammed columns; reconciled with
  reveal-based progression so panels appear as their surface unlocks. **Home
  doc:** [`ui-design.md`](../../docs/living/ui-design.md) (pending a plan/diverge
  to scope it).
- **Story/narration text reveals slowly, GBA typewriter-style** (from F12) — not
  instant, not a fast cascade; scoped to the story channel, never combat spam;
  reduced-motion → instant. Slow text is part of the atmosphere. **Home doc:**
  [`ui-design.md`](../../docs/living/ui-design.md) (feel nod in
  [`fun-factor.md`](../../docs/living/fun-factor.md)).
- **Character dialogue is incremental and interactive** (from F13) — the intro
  (and NPC dialogue generally) advances beat by beat and the player can respond /
  talk back, not read a wall of text. NPCs the player meets are people to answer,
  not narration. **Home doc:**
  [`ui-design.md`](../../docs/living/ui-design.md) (+ an ADR once the direction
  locks).
- **Atmospheric/ceremonial surfaces keep generous breathing room** (from F14) —
  the cold-open, seals, and similar ceremonial cards stay roomy; the
  compact-density rule is for the game chrome, not these. The opening reveals
  slowly with a delayed CTA (the slow-waking beat), not all-at-once. **Home
  doc:** [`ui-design.md`](../../docs/living/ui-design.md).
- **Reveal/animation beats must fire when the player is watching** (from F15) —
  after the gating interaction, never pre-run behind a curtain; an unseen
  animation is a wasted beat. **Home doc:**
  [`ui-design.md`](../../docs/living/ui-design.md).
- **(dev-ergonomics, not player taste) The DEV panel splits Settings vs
  Variants** (from F16) — variants are recency-ordered (newest on top)
  collapsible summaries that expand to their options, so a long variant list
  stays reviewable. **Home:** the `diverge` skill notes / DEV tooling (not
  `ui-design.md`).
- **(dev-ergonomics, not player taste) Approved variant wins → strip the rest;
  variants carry stable V-numbers** (from F17) — when the human approves a
  variant it becomes the prod default and the unpicked alternates are removed
  immediately (D-075 zero flag-debt); don't let dead variants linger. Every
  variant carries a stable, unique V-number (V0…VN) for quick human reference.
  **Home:** the `diverge` skill notes / dev-ergonomics (not `ui-design.md`).
- **(dev-ergonomics, not player taste) DEV variant selections round-trip through
  the URL query params** (from F18) — picking a variant writes
  `?<surface>=<variant>` to the URL; load reads the params and applies them, so a
  refresh preserves the review state (and the URL is shareable). **Home:** the
  `diverge` skill notes / dev tooling (not `ui-design.md`).
- **Story/narration lines reveal with a real character-by-character typewriter**
  (from F19) — a genuine GBA-feel typewriter, not a fast line-flash; scoped to the
  story channel (never combat spam); reduced-motion → instant. (Sharpens F12.)
  **Home doc:** [`ui-design.md`](../../docs/living/ui-design.md) (feel nod in
  [`fun-factor.md`](../../docs/living/fun-factor.md)).
- **Filtered channels surface an unread indicator** (from F20) — a filter tab
  shows a badge/dot when new content lands in a channel you're not viewing;
  switching to it clears the mark. **Home doc:**
  [`ui-design.md`](../../docs/living/ui-design.md).
- **Work-stamina and health are distinct resources with distinct recovery
  actions** (from F22) — labour drains/refills work-stamina; combat drains/heals
  health; each has its own meter and its own recovery action, and the two rests
  are never conflated. A CORE game-mechanics change, not UI polish. **Home doc:**
  [`prd.md`](../../docs/living/prd.md) + an ADR in
  [`decisions.md`](../../docs/living/decisions.md) once locked.
- **The story log is legible by VOICE** (from F23) — narrator prose, NPC speech,
  and player speech each read distinctly (colour + emphasis), never a flat
  undifferentiated wall. Full three-way distinction wants speaker-tagged log
  entries (a small core/content model); player speech arrives with the interactive
  intro (F13). Part of the story-presentation cluster (F12, F13, F15, F19, F23).
  **Home doc:** [`ui-design.md`](../../docs/living/ui-design.md) (§5.1 event-log).

---

## Doc-update checklist (rollup)

A single place to see every doc edit this session implies, so none is missed at
checkpoint:

- [`ui-design.md`](../../docs/living/ui-design.md) — add the rule that all
  surfaces (including DEV-only tooling/chrome) inherit the game's dark ink
  background; no white / browser-default backgrounds anywhere (F1).
- [`ui-design.md`](../../docs/living/ui-design.md) — add the rule that the game
  UI must center on the FULL viewport independent of any DEV/tooling chrome (dev
  tooling is a fixed overlay, never in the centering layout); document the
  `?dev=no` opt-out as the way to preview the true player-facing layout (F2).
- [`qa-playtesting.md`](../../docs/living/qa-playtesting.md) — if `?dev=no`
  becomes a supported dev affordance, note it as the way to preview the true
  player layout (F2).
- [`ui-design.md`](../../docs/living/ui-design.md) — add the rule that in a
  centered card/panel, a single primary action (button) inherits the card's
  center axis, not a left-aligned default (F3). Can fold into the F2 layout
  entry above.
- [`ui-design.md`](../../docs/living/ui-design.md) — the **app-shell / layout
  section** (F4 + F5 + F6 all fold in here): the player UI fills the viewport as a
  centered paper column (~1200px) on an ink ground, with DEV/tooling chrome a
  fixed overlay reserving no layout space (F4); a fixed-header (identity + vitals
  + nav) / flexible-content / fixed-footer (version, Settings, persistent
  controls) app shell like established idle-RPGs (F5); the shell sized to the
  viewport (100dvh) with NO page scrollbar — content panes scroll internally, with
  a narrow-screen fallback to natural page flow (F6); the message/story log
  occupies the RIGHT column while the interactive work/actions column sits on the
  LEFT (F8, supersedes the v0.3.1 left-hero placement — D-022).
- [`ui-design.md`](../../docs/living/ui-design.md) — add the small log-motion
  rule: narrative/log content that updates scrolls SMOOTHLY to follow the newest
  entry (honoring reduced-motion), never an instant jump; the motion of the story
  arriving is part of the feel (F7). A feel nod also goes in
  [`fun-factor.md`](../../docs/living/fun-factor.md).
- [`ui-design.md`](../../docs/living/ui-design.md) — add the **filterable log**
  surface: the log panel carries a bottom tab/toggle bar to filter channels
  (Story / Work / Combat / Progression / …), with Story a persistent, returnable
  view (F9). This is a NEW UI surface → run D-075 diverge (2–3 working variants
  behind the DEV-panel toggle) and, if diverged, add the variants to the D-075
  variant log + an R-item per variant in
  [`review.md`](../human-in-the-loop/review.md). A story-as-keepable-read nod
  also goes in [`fun-factor.md`](../../docs/living/fun-factor.md).
- [`ui-design.md`](../../docs/living/ui-design.md) — add the **density rule**: the
  UI is COMPACT/dense (idle-RPG density) — a tight type scale, small control
  heights, minimal chrome padding; chrome (header/footer/tabs/resource row) stays
  minimal so the content (log + actions) gets the space (F10).
- [`ui-design.md`](../../docs/living/ui-design.md) — add the **multi-panel
  layout** direction: information laid across 5–7 distinct panels (idle-RPG
  dashboard density) rather than two crammed columns, reconciled with
  reveal-based progression (panels appear as their surface unlocks); likely needs
  a `docs/plans/` plan and/or an ADR + a D-075 diverge once the direction is
  locked (F11).
- [`ui-design.md`](../../docs/living/ui-design.md) — add the **slow story-text
  rule**: story/narration text reveals slowly, GBA typewriter-style (not instant,
  not a fast cascade), scoped to the story channel (never combat spam) and
  honoring reduced-motion (instant). A feel nod also goes in
  [`fun-factor.md`](../../docs/living/fun-factor.md) (F12).
- [`ui-design.md`](../../docs/living/ui-design.md) — add the **incremental
  interactive dialogue** direction: the intro (and NPC dialogue) is incremental
  and interactive — the player advances it beat by beat and can respond / talk
  back, not a static wall. Likely also a `docs/plans/` design + an ADR in
  [`decisions.md`](../../docs/living/decisions.md) once the direction locks (F13).
- [`ui-design.md`](../../docs/living/ui-design.md) — add the **atmospheric
  breathing-room** rule: atmospheric/ceremonial surfaces (cold-open, seals) keep
  generous padding (the compact-density rule is for the game chrome, not these);
  the opening reveals slowly with a delayed CTA (F14).
- [`ui-design.md`](../../docs/living/ui-design.md) — add the **watch-the-reveal**
  rule: reveal/animation beats fire when the player is watching (after the gating
  interaction), never pre-run behind a curtain (F15).
- `diverge` skill notes / DEV-panel tooling docs — the DEV panel splits Settings
  vs Variants; variants are recency-ordered (newest on top) collapsible summaries
  that expand to their options (dev-ergonomics, not player-facing; NOT
  `ui-design.md`) (F16).
- `diverge` skill notes / DEV-panel tooling docs — an approved variant becomes
  the prod default and the unpicked alternates are stripped immediately (D-075
  zero flag-debt); every variant carries a stable V-number (V0…VN) shown on its
  option button + collapsed summary row for quick human reference
  (dev-ergonomics, not player-facing; NOT `ui-design.md`) (F17).
- `diverge` skill notes / DEV-panel tooling docs — DEV variant selections
  round-trip through the URL query params (pick writes `?<surface>=<variant>`,
  load reads + applies it) so a refresh preserves the review state
  (dev-ergonomics, not player-facing; NOT `ui-design.md`) (F18).
- [`ui-design.md`](../../docs/living/ui-design.md) — sharpen the story-text rule
  (F12): story/narration lines reveal with a REAL character-by-character
  typewriter (GBA feel), not a fast line-flash — scoped to the story channel,
  reduced-motion → instant. Feel nod in
  [`fun-factor.md`](../../docs/living/fun-factor.md) (F19).
- [`ui-design.md`](../../docs/living/ui-design.md) — add the **unread-indicator**
  rule: filtered log channels show a badge/dot when new content lands in a channel
  you're not viewing; switching to it clears the mark (F20).
- [`prd.md`](../../docs/living/prd.md) + an ADR in
  [`decisions.md`](../../docs/living/decisions.md) — add the **split
  work-stamina / health** mechanic: distinct resources with distinct recovery
  actions (labour drains/refills stamina; combat drains/heals health), each with
  its own meter; needs a design note/ADR before building (CORE change, F22).
- [`ui-design.md`](../../docs/living/ui-design.md) (§5.1 event-log) — add the
  **log-by-voice** rule: narrator prose, NPC speech, and player speech each read
  distinctly (colour + emphasis), never a flat wall; full three-way distinction
  wants speaker-tagged entries (part of the story-presentation cluster with F12,
  F13, F15, F19) (F23).
- [`qa-playtesting.md`](../../docs/living/qa-playtesting.md) — add the
  **cheap-DEV-teleport** rule: DEV teleports/fast-forwards must be cheap — never a
  long synchronous main-thread sim loop that freezes the page; bound the work or
  jump state directly (F24, fixed).
