# PRD V2 — change plan

**Date:** 2026-06-26 · Driven by all 79 human decisions (prd_human_feedback Blocks L+M). **474 changes · 50 new subsections · 20 new ADRs (D-023–D-042).** Raw: [raw/2026-06-26-prd-v2-plan.json](raw/2026-06-26-prd-v2-plan.json).

## Rewrite order

1. §1 — author the new §1.6.4 (sequential two-phase model + three combat tracks) FIRST as the canonical conceptual home, then apply the §1 spine edits (hybrid gate, budget=floor, Combat Rank rename, name-reclaim split, E3, design-staggered reveals) and point §1.6.3/§1.5.1/§1.12 at §1.6.4 to avoid three divergent descriptions.

2. §2 — fix the SYSTEM SHAPES that everything else consumes: delete CombatDeedsPool → three tracks, the combat-reveal ladder + weapon roster, per-skill perks, the hybrid-gate model, the sequential phase model, multi-backend save shape, mechanical weather, intra-line dialogue, no-revealQueue, top-level tabs. (Defers all numbers to §4.)

3. §4 — the heaviest numeric re-derivation, done after the shapes exist: the rung-meter law (third curve), the one combat-fed character-level curve, the hybrid good/great/excellent triples back-solved against the fixed deed inventory, the 70/30 + seasonal re-derivation, satiety/durability/weapon/win-rate terms, cross-pillar combos, and the budget=FLOOR phase-split pacing tables. §4.5/§4.6/§4.8 are the riskiest and must tie out together (numbers stay provisional per D-021).

4. §6 — make GameState, the registry set, the save/RNG/determinism contracts, and the verifier MATCH §2/§4: multi-backend save, RNG cursors + day-keyed weather, Math.pow ban, character.level + subEngines + dialogue-flags (additive), no revealQueue field, and the V2 verifier invariants (gate-monotonicity, accrual tie-out, per-perk magnitude, real-name denylist, trade-post-combo proof).

5. §3 — rewrite the tier tables against the settled model: rungs gate on rung-meter+story, relocate pillar deeds to phase 2, capstones open phase 2, add the combat-reveal-ladder + nav-tab rows, delete the duplicate G6 name line, apply the Combat Rank/macron sweep.

6. §5 — reflect the narrative SHAPE last among the design sections: two-phase narration, name-reclaim split + conditional epilogue, E3 Prosperous, Daikan stub, one find-spot, the Naoyuki beat, rescoped ZERO-gift language, and all name renames (consistent with the §6 denylist).

7. §7 — re-cut the milestone roadmap once §2/§4/§6 are stable: budget=floor, full multi-backend save in M0, rung-meter in M1, the M2a→M5 combat ladder spread, hybrid gate + phase-2 grind in M3, E3 + combos in M5, fonts/license/credits/content-rating in M7, fun-proxy gating, new risk rows.

8. Final cross-section CONSISTENCY sweep — enforce the watch-list (naming, the single character-level curve, the ≥30-min floor shared by rung-meter + pacing + verifier, deeds-all-in-phase-2, revealed-pillar set T0=2/T1=3/T2=3, weapon-roster params, RNG shape, save-selector, E3 scope, trade-≤⅓-post-combo, budget-as-floor everywhere).


## New ADRs (D-023–D-042)

- **D-023** — Sequential per-tier progression — Phase 1 (climb rungs) then Phase 2 (estate-influence/pillar grind); pillar DEEDS gated to Phase 2  *(← FU7, FU11, Q30, Q7)*
- **D-024** — The rung-meter accrual law — numeric per-rung-reset meter, threshold = ≥30-min floor × eligible curated-activity rate, AND-gated with story milestones  *(← FU6, Q30, Q28)*
- **D-025** — Three clean combat tracks de-conflated — character level←combat-XP (HP/attr/satietyMax), Arms pillar←recognised deeds, Combat Rank meter←per-rung curated activities; one defined combat-level curve + per-mob MobDef.level  *(← FU14, Q1, Q47, FU15, FU5)*
- **D-026** — Incremental combat + growing weapon roster + combat-reveal ladder — T0 starts with exactly ONE weapon; +2/+3/+4 per tier (~9-10); R3→R4→R5→weapon-L10→2nd line T1/3rd T2, one reveal per beat  *(← Q15, FU12, FU13, Q17)*
- **D-027** — Bounded per-skill combat perks — relaxes the absolute no-labour→combat wall to ~2-8 small stackable perks via a separate skillCombatBonus channel, NO global cap; conditioning stays the zero-stat enablement gate; verifier flips ==0 → small-magnitude  *(← Q6, FU8, Q28)*
- **D-028** — Hybrid good/great/excellent tier-gate — replaces simple thresholds: good in ALL revealed pillars, great in 2-3, excellent in 1-2 (T0 2-pillar special), NO overflow; per-pillar-per-tier overhaul vs the fixed deed inventory; trade ≤⅓ survives as the only structural cap  *(← Q7, FU10, FU11)*
- **D-029** — Budget is a FLOOR not a ceiling — a longer OSRS-rough minimum-grind model; active-only with tab-open auto-resolve + auto-repeat ('leave it running'); pacing gate fails on undershoot only  *(← FU18, FU23)*
- **D-030** — Multi-backend redundant save layer — IndexedDB+localStorage+sessionStorage, atomic write, app-identity magic field, monotonic save-counter newest-wins + timestamp tiebreaker (documented core-lint exemption), additive backwards-compatible schema; built full in M0  *(← Q37, FU1, FU2, Q44, Q45, Q46)*
- **D-031** — Broader cross-pillar combos as the T2 anti-slump — multiple pillar pairs / larger magnitude, computed POST trade-clamp, excluded from the gate-threshold check, verifier-proven never to breach ⅓; paired with seasonal-reward rotation  *(← Q22, FU20)*
- **D-032** — No fixed quest-type budget — supersedes D-012's lean-4; PEST/HUNT/CLEAR/DEFEND are the T0 starter set, author whatever quests fit each stage, more/interesting welcome at later tiers  *(← Q23)*
- **D-033** — Estate stage E3 authored in v1 — estate grows E0→E3 ('Prosperous'), folded into the G-rungs as a koku/Arms sink; E4-E5 remain parked  *(← Q8)*
- **D-034** — Graded durability bands — 4 bands on weapon attackPower (75+/50+/1+/0 → 1.0/0.9/0.75/0.55), fixed wear per fight, armour bands on defense, repair restores; NEVER auto-unequip / never weaponless  *(← Q33, FU17)*
- **D-035** — Satiety throttles combat — satietyRate multiplier on attackPower (flat above ~0.7 → ~0.5 floor, separate combat coefficient); the locked 20-35% first-fight win-rate re-measured 'at adequate satiety (≥~0.7)'  *(← Q31, FU16, Q47)*
- **D-036** — Name-reclaim split — reclaiming 'Tahei' is Origin-gated at O5 (earned + MISSABLE, conditional epilogue); the Otsuru/Tama TRUTH stays spine-guaranteed at G6 for every player  *(← Q5, Q25, Q40)*
- **D-037** — Design-staggered reveals, NO runtime reveal-queue (supersedes Q17's queue framing); distinct activities (Crafting, Quests) surface as top-level nav tabs, not nested panels  *(← FU4, Q17, Q10)*
- **D-038** — Determinism hardening — per-named-stream RNG cursors {seed,cursors:{combat,loot,seasonal,worldgen}} + stateless day-keyed weather/lunar (not stored); ban Math.pow/exp/log/trig in core (integer-pow, whitelist sqrt)  *(← Q2, Q3, FU3, Q36)*
- **D-039** — Intra-line dialogue branching in v1 — flat choices[]/ChoiceId with locksLineIds[]/flags effects; data-not-scripting, deterministic, only chosen-flags persist; content/dialogue.ts  *(← Q34, FU22)*
- **D-040** — V1 ending = castle-town / Daikan first-contact stub — drops the porter/Kaidō-guild first-contact framing (it re-ran spent T2 content)  *(← Q24)*
- **D-041** — Bundled asset set corrects the 'no asset pipeline' claim — self-hosted OFL fonts, inline-SVG load-bearing motifs (emoji cosmetic-only), a small synthesized Web Audio + original/CC0 audio set; commit-SHA build stamp + About/Credits + LICENSE + itch content descriptors  *(← Q38, Q50, Q52, Q54, Q51, Q53)*
- **D-042** — Real-name fictionalization & lint — rename Toyama/Konoe (Q27), Mago/Naozane/Obaa Sato (Q11/Q39), allow-list Nihonbashi (Q12); a §6.6 real-name denylist lint prevents recurrence  *(← Q27, Q39, Q11, Q12, Q28)*

## Cross-section consistency watch-list

- Character combat-level curve defined ONCE (new §4 subsection) and referenced everywhere: hpMax=40+8·characterLevel (§4.6.1), satietyMax=base+per-level growth (Q47, §2.3/§4.4), +1 attr point / 2 levels (§4.4), on-kill XP = MobDef.level·COMBAT_XP_K (§4.6.5). No section may re-derive its own 'level' concept or combat power silently couples.
- Rung-meter thresholds back-solved from the SAME ≥30-min floor that §4.8 pacing uses, and asserted by the §6.6 gate-monotonicity check — the old discrete 'Estate Service x1/x2' marks (§4.8) and the §3.0 'RANK' trigger must both become the numeric meter + story AND-gate.
- Two-phase split: the ENTIRE §4.2.1 deed inventory (T0 ~560 ip Estate / ~350 Arms etc.) accrues in PHASE 2 only — agreement required across §1.6.3, §2.16(e), §3 capstone rows (R7/V7/G7), §4.2.1/§4.8, §5 transition-gate narration, and §6.4 (no extra phase flag — derivable from current rung).
- Hybrid gate bands re-derived against the FIXED deed inventory and tying out in §4.8 + the §6.6 accrual-tie-out verifier; the revealed-pillar set T0=2 (Arms+Estate) / T1=3 (+Office) / T2=3-4 must match the §3 reveal schedule AND the §2.16(e) four-bar-panel reveal (no 'good in ALL' check against an unrevealed pillar).
- Weapon roster: count (~9-10; T0 starts 1, +2/+3/+4) and per-weapon archetype params (baseSpeed/reach/targetCount + signature) byte-identical across §2.8/§2.10, §3 reveal rows, §4.6, §6.5 items.ts — the single baseSpeed=1.0 in §4.6.1/§4.6.2 MUST become per-weapon.
- RNG shape { seed, cursors:{combat,loot,seasonal,worldgen} } identical in §6.3/§6.4/§6.7/§7; purge { seed, counter } and 'split child RNGs'. Weather/lunar are DERIVED via stateless deriveDayKeyed (NOT stored) — only market-saturation persists.
- Save selector: the monotonic save-COUNTER is the real newest-wins selector; the save-layer timestamp is only the tiebreaker (a documented core-lint exemption — metadata, not game logic); plus the app-identity magic field — must agree across §6.4 stored metadata, §6.8 selector, §6.1 lint-exemption, §7 M0 DoD.
- Three combat tracks stay lexically + mechanically distinct and 'Combat Standing'→'Combat Rank' project-wide: track 1 'character (combat) level' (kills/XP), track 2 'Arms pillar' (deeds), track 3 'Combat Rank meter' (curated rung activities). 'Standing' = the 官威 pillar only. Reconflation is the single likeliest regression (old §2.8 'Combat Level = a Combat Deeds pool' fused all three).
- NO revealQueue field in GameState (FU4 supersedes Q17) — §2.1/§3.0/§6.4 must frame reveal staggering as a design property of the authored schedule, not stored runtime state.
- Conditioning stays the ZERO-stat enablement gate in BOTH the three-track model and the per-skill-perk model — the bounded skillCombatBonus must not become a back-door that bypasses conditioning's weak→capable gate; keep them orthogonal in §2.8/§4.5/§4.6.1.
- Budget figures (≈28.5h v1; T0 4.5h / T1 8h / T2 16h; ≥30-min/rung) read as FLOORS/minimums everywhere — §4.8.0/§4.8.4, §4.9 LOCKED list, §1.6.3, §7.1.2, the M6 pacing gate (fails on undershoot only), and risk R2.
- E3 scope: estate grows E0→E3 in v1 (E4-E5 parked) — consistent across §1.5.1/§1.7.1, §2.17, §4.7.5 (cost row), §3.6/§3.7.1, §5 T2.4/T3, §7.1.1/§7.1.3; the AGI glyph fix 体→敏 (§4.4) rides along the name sweep.
- Trade ≤⅓ stays a HARD structural cap and cross-pillar combos are computed POST-clamp + EXCLUDED from the gate-threshold check — agreement across §1.6.1, §4.2.3/§4.3.1, §6.6 (verifier-proven a combo can never breach ⅓ or satisfy a required pillar); T0 has NO trade strand (opens T1).
- First-fight win-rate 20-35% is re-specified 'at adequate satiety (≥~0.7)' wherever it appears (§4.6.6, §2.8, §7 M2a DoD) so the locked criterion and the satiety throttle agree.
- Name renames applied consistently and lint-guarded: Toyama/Konoe→invented (Q27), Mago/Naozane/Obaa Sato→non-colliding (Q11/Q39), Nihonbashi allow-listed (Q12) — across §1.8, §5 cast, §6.6 denylist; field-lad 'Mago' must not collide with antagonist 'Magobei'.

## Coverage
ALL 79 V2 decisions are mapped to PRD §1-§7. Block L Q1-Q56 (all 56) are covered — Q1-Q56 each appear in at least one change-map id, including the late presentation/deploy items Q48-Q56 (a11y/contrast, fonts, license, content-rating, credits, world-registry, perf). Block M FU1-FU23 (all 23) are covered, including FU9 (fun-proxy), FU15 (MobDef.level), FU19 (win-rate bands), FU21 (satiety floor), FU5 (M0 GameState skeleton). NO UNMAPPED decisions. Q43 is a DUPLICATE of Q37 (multi-backend save) and is folded there. CONTRADICTIONS are all pre-resolved by D-022 (newest-block-wins / annotate-don't-delete), not left open: (1) FU4 supersedes Q17's 'serialize reveals into a runtime one-per-beat QUEUE' → design-staggered, NO stored revealQueue; (2) FU8 supersedes Q6's 'small CAPPED' → per-skill perks with NO hard global cap (small per-perk magnitude); (3) FU13 extends Q15's '3 weapon lines' → ~9-10 weapons across 3 archetype lines; (4) FU16/FU21 + Q31 override the master-sheet 'labour-only throttle, floor 0.3' → combat IS throttled, floor ~0.5; (5) Q5 supersedes Q40's 'keep naming on the G6 spine' → Tahei name-reclaim moves to the missable Origin O5 (Q25 resolved alongside); (6) Q10 overrides the master-rec 'nested panels' → top-level nav tabs; (7) Q8 overrides the master-rec 'cap at E2' → E3 authored in v1 (forces §5 T2.4 'E2→early E3', §3.7.1 'E3→E4 at T3', and §4.7.5/§7 'E0→E2-only' to re-align to E0→E3 with E4-E5 parked); (8) the signed Block-M FU18 (FLOOR) overrides the resolution-log 'proposed-lean' FU18 (CEILING); (9) the signed FU20 (BROADER combos) overrides the 'single capped Name-only rebate' lean. One naming overload to resolve consistently (not a contradiction in intent): Q9 renames the Arms rank-gate 'Combat Standing'→'Combat Rank' while the newer FU14 informally still writes 'Combat-Standing rung-meter' — standardize on 'Combat Rank' project-wide ('Standing' = the 官威 pillar only).


## Per-section summary


### §1 — Vision, Pillars, Factions, World & Endgame (70 changes)
Establish the V2 conceptual spine: SEQUENTIAL two-phase per-tier (climb rungs → estate-influence/pillar grind), HYBRID good/great/excellent gate, THREE clean combat tracks, INCREMENTAL combat (1 weapon → growing roster), bounded per-skill perks, budget=FLOOR, Combat Standing→Combat Rank, Tahei-name-reclaim split (O5 missable / G6 truth guaranteed), E3 authored in v1, design-staggered reveals, real-name renames. Annotate D-006/D-008/D-011/D-012/D-013 and the Round-A reversal log; add the V2-deltas/D-022-governing note.
- NEW: §1.6.4 — Sequential per-tier model + the three combat tracks (the canonical conceptual home §1.5.1/§1.6.3/§1.12 reference)
- NEW: §1.5.1 sub-note — Combat progression: incremental weapons + the reveal ladder

### §2 — Systems & Mechanics Catalog (52 changes)
Flip system shapes to V2: delete the conflated CombatDeedsPool → three independent tracks; incremental combat + staggered reveal ladder (1 weapon at T0); per-skill perk channel (relaxes the no-cross-feed wall); hybrid tier-gate (no overflow, trade ≤⅓ survives); multi-backend save; satiety throttles combat; graded durability bands; mechanical ±10% weather (day-keyed/derived); intra-line dialogue choices[]; top-level Crafting/Quests tabs; no runtime reveal-queue; no quest-type budget; corrected asset register (inline-SVG motifs + curated audio + self-hosted fonts).
- NEW: The three combat tracks (§2.8)
- NEW: Combat-reveal ladder (incremental) (§2.8)
- NEW: Weapon roster (~9-10 across v1) (§2.10)
- NEW: Per-skill perks — bounded labour→combat cross-feed (§2.7)
- NEW: Sequential per-tier progression — rungs(phase 1)→pillar grind(phase 2) (§2.15/§2.16)
- NEW: Hybrid tier-gate — good/great/excellent (§2.16)
- NEW: Multi-backend save layer (§2.19)
- NEW: About / Credits surface (§2.21)

### §3 — Incremental Unlock Ladder (UI-as-progression) (38 changes)
Reframe every tier table to the two-phase model: rungs gate on the per-rung-reset rung-meter + story (AND-gate); pillar DEEDS relocated out of R5/R6/V2/V4/G1/G5 into phase 2; capstones OPEN phase 2; tier-up = the hybrid pillar profile. Add the combat-reveal-ladder track parallel to §3.5 nav-reveal; Crafting/Quests become top-level nav rows (R4/R5); delete the duplicate G6 name-reclaim spine line; Combat Standing→Combat Rank + macron sweep.
- NEW: The two-phase per-tier model + rung-meter law + phase-2 tier-gate (after §3.0)
- NEW: The combat-reveal ladder track (after §3.5)

### §4 — Combat, Progression & Balance Model (40 changes)
Balance overhaul + the biggest numeric re-derivation: hybrid good/great/excellent triples back-solved against the fixed deed inventory (no simple ratios); the rung-meter accrual law (the missing third curve); one combat-fed character-level curve driving HP/satietyMax/attr-points/on-kill-XP; per-skill perk channel (skillCombatBonus); satiety→combat throttle; graded durability bands; per-weapon archetype params; win-rate bands; broadened post-clamp cross-pillar combos; budget=FLOOR pacing tables split into phase-1 rungs + phase-2 grind; E3 cost row; Math.pow→integer-pow; ~18-19K NET held koku.
- NEW: §4.0.1 Progression architecture — three tracks, two phases
- NEW: §4.1.x The rung-meter accrual law
- NEW: §4.3.1 Cross-pillar combos (T2 anti-slump)
- NEW: §4.5.4 Per-skill perk tracks
- NEW: §4.6.x Satiety→combat throttle
- NEW: §4.6.x Graded durability bands
- NEW: §4.6.x Weapon roster & archetype params + combat-reveal ladder
- NEW: §4.6.x Win-rate bands
- NEW: §4.6.x Retreat resolution
- NEW: §4.8.1b Phase-2 pillar-grind block (per tier)

### §5 — Act-by-Act Narrative & Content (36 changes)
Reflect V2 in narrative shape only: two-phase per-tier narration with deeds gated to phase 2; the Otsuru/Tama truth spine-guaranteed at G6 vs the missable Origin-O5 Tahei name-reclaim (and conditional epilogue); E2→E3 Prosperous; T3 stub = castle-town/Daikan first-contact (drop porter-guild); one co-located weir-jizō find-spot + 'presumed-dead→back-from-the-dead'; a Naoyuki rivalry→respect beat; rescoped ZERO-gift Origin language; name renames (Mago/Naozane/Obaa Sato/Toyama/Konoe); no quest-type budget; incremental weapon-line FOUND-gear flavour.
- NEW: Per-tier progression-shape callout (T0.1/T1.1/T2.1)
- NEW: Naoyuki T1/early-T2 interstitial beat (T1.2 + T1.5)
- NEW: Origin track & the missable name-reclaim (binding note, T2.2/T2.5)

### §6 — Tech Architecture & Data Model (64 changes)
Bring the data-model, determinism, save and verifier contracts to V2: multi-backend redundant atomic save (IndexedDB+localStorage+sessionStorage, magic field, monotonic-counter newest-wins + timestamp tiebreaker as a documented core-lint exemption, additive schema); per-named-stream RNG cursors + stateless day-keyed weather; Math.pow ban; three-track GameState (character.level) + subEngines + dialogue choice-flags (additive at milestone); NO revealQueue field; hybrid-gate storage; per-skill perks registry field; intra-line dialogue registry; world.ts/beliefBeasts.ts; hardened verifier (gate-monotonicity, accrual tie-out, real-name denylist, per-perk-magnitude flip, trade-post-combo proof); bundled-assets correction; About/Credits surface.
- NEW: §6.1.1 Bundled assets (self-hosted OFL fonts · curated audio · inline SVG · build stamp)
- NEW: §6.4.1 Three combat tracks & the sequential phase gate (data view)
- NEW: §6.7.1 Stateless day-keyed derivations (weather/lunar/festivals)
- NEW: §6.8.1 Multi-backend redundant save + newest-wins arbitration
- NEW: §6.8.2 Additive backwards-compatible schema & migration safety net
- NEW: §6.6.1 V2 verifier invariants

### §7 — Milestone Roadmap, v1 Scope & Deployment (85 changes)
Re-cut the roadmap under D-022: budget→FLOOR (regression fails on undershoot only); FULL multi-backend save built in M0 (only the itch cross-origin-iframe test deferred to M7); numeric rung-meter + sequential phase-1/phase-2 from M1; combat reveal-ladder spread M2a→M5 (R3 sheds its UI-dump); hybrid gate + phase-2 grind in M3; 2nd weapon line + intra-line dialogue + Naoyuki beat in M4; 3rd line + E3 + cross-pillar combos in M5; verifier flips + a11y + inline-SVG/audio + fun-proxy gating in M6; self-hosted fonts + LICENSE + About/Credits + itch content descriptors in M7; new risk rows (fun, perf/memory, bounded-perks).
- NEW: §7.1.4 Per-tier shape (phase 1 → phase 2)
- NEW: §7.2.0 Combat reveal ladder → milestone map
- NEW: §7.3.1 Assets (the acknowledged small set)
- NEW: §7.3.2 About/Credits, licensing & content rating


---

## Detailed per-section change-maps


### §1 — Vision, Pillars, Factions, World & Endgame (lines 46-834)

- **[edit]** `§1 header blockquote (lines 48-56)` — Retitle DRAFT→PRD V2 banner. Replace the 'simple per-tier required-pillar thresholds (floor+overflow rejected)' settled-locks line with the V2 spine: HYBRID good/great/excellent pillar gate; SEQUENTIAL per-tier model (climb rungs → then grind the estate's pillars → tier-up); budget=FLOOR / long OSRS-rough grind identity; reveals design-staggered one-at-a-time. Note V2 supersedes V1 per D-022 (most-recent-wins).  <sub>← Q7, FU10, FU7, FU18, FU4, D-022</sub>
- **[edit]** `§1.1 Vision (58-84)` — Weave in the budget=FLOOR identity: a long, OSRS-rough grind ('leave it auto-running, check progress' — active-only). Reframe 'rung by sweat-bought rung' to the sequential per-tier motion (climb the rungs, THEN grind the estate up, THEN tier-up). Keep the UI-reveal-as-progression line but add reveals come one-at-a-time by design.  <sub>← FU18, FU23, FU7, FU4</sub>
- **[edit]** `§1.2 pillar 3 — Lean and high-impact (100-104)` — Reconcile 'deliberately small' with FU18: lean = no fluff / no half-built FEATURES + park-don't-delete (KEEP), but the GRIND itself is generous and long (OSRS-rough, budget=floor). Drop the '~4 quest types' budget (Q23: NO quest-type budget — author whatever fits, more quests welcome). Keep the T0-T2 / ~5-mobs / ~6-8-node cut-set but stop framing content as 'small'.  <sub>← FU18, Q23 · ADR</sub>
- **[rewrite]** `§1.2 pillar 4 — Combat first-class (106-116)` — Three changes: (a) replace 'no labour→combat training-rate cross-feed' with BOUNDED per-skill perks (each skill ~2-8 small stackable combat perks; conditioning stays the zero-stat gate; skills make you 'more capable'); (b) reframe 'five classic-RPG combat systems (character combat level…)' into the THREE clean tracks — combat-XP→character level + HP/attribute points; recognised deeds→the Arms pillar; per-rung curated activities→the Combat Rank rung-meter; (c) combat is now INCREMENTAL: T0 starts with exactly ONE weapon, a growing roster + a combat-reveal ladder.  <sub>← Q1, Q6, FU8, FU14, Q15, FU13, FU12 · ADR</sub>
- **[edit]** `§1.2 pillar 6 — No hidden edge (123-126)` — Keep porter's-knot + dream = ZERO mechanical bonus (identity flavour). Add a clarifying clause: 'no hidden edge' means no edge of BIRTH/GIFT/MEMORY — distinct from labour SKILLS, which now grant small bounded combat perks (earned, not innate).  <sub>← Q6, FU8, Q12</sub>
- **[edit]** `§1.2 pillar 2 — Fractal incrementality (94-98)` — Add: reveals are DESIGN-staggered one-at-a-time (the unlock schedule is authored so reveals are inherently single — no runtime reveal-queue) and obey a general NO-UI-DUMPS principle; distinct activities (Crafting, Quests) surface as their own top-level nav tabs, not nested panels.  <sub>← FU4, Q17, Q10</sub>
- **[edit]** `§1.3 Premise & tone (138-158)` — Light: weave the 'presumed dead → back from the dead' framing into the premise (he was grieved as dead). Otherwise keep tone unchanged.  <sub>← Q11</sub>
- **[edit]** `§1.4 protagonist intro (160-168)` — The Tahei name no longer simply 'returns' — reclaiming 'Tahei' is ORIGIN-GATED at O5: earned and MISSABLE (a player who skips the Origin track may never reclaim it). Distinguish this from the Otsuru/Tama TRUTH, which is spine-guaranteed at G6. Keep fixed-male + 'Tama' borrowed name.  <sub>← Q5 · ADR</sub>
- **[rewrite]** `§1.4 mediocre-start contract (170-176)` — Replace 'Combat capacity is gated entirely behind labour-built conditioning … with no labour→combat training-rate cross-feed' with: conditioning stays the zero-stat ENABLEMENT gate, but labour skills now grant small bounded per-skill combat perks (more reps → more capable). Add satiety throttles combat ('eat before you fight'): the humbling/near-fatal first fight win-rate is measured at adequate satiety (≥~0.7).  <sub>← Q6, FU8, Q31, FU16 · ADR</sub>
- **[edit]** `§1.4 no-hidden-edge guardrails — 'Growth is grind, not gift' bullet (189-190)` — Keep porter's-knot/dream ZERO-bonus bullets. Edit 'Growth is grind, not gift' to: returning memory/instinct grants no starting stat/recipe/tool/combat bonus, BUT trained labour skills DO grant small bounded combat perks — the distinction is gift-vs-work, not labour-vs-combat.  <sub>← Q6, FU8, Q12</sub>
- **[rewrite]** `§1.5.1 'Two earned standing meters' paragraph (215-222)` — Replace the 'Estate Service gates labour promotions; Combat Standing gates martial ones' framing with the SEQUENTIAL model: Phase 1 = climb the rungs via CURATED per-rung activities (1-to-many designed activities, not a repeat-counter) tracked by a per-rung-reset NUMERIC rung-meter (a real §4 curve) AND story milestones (AND-gate; 'awaiting X' when one lags). Phase 2 = after the FINAL rung, the estate-influence/pillar grind unlocks → the hybrid gate tiers up. Rename 'Combat Standing' → 'Combat Rank' (the martial rung-meter sub-track); 'Estate Service' = the labour rung-meter sub-track. Pillar DEEDS gated to phase 2.  <sub>← FU7, FU6, Q30, Q9, FU14 · ADR</sub>
- **[edit]** `§1.5.1 conditioning-gate sentence (218-219)` — Replace 'Labour conditioning is a one-way enablement gate on combat rungs (it grants ZERO combat stat bonus)' with: conditioning stays the zero-stat enablement gate, but each labour skill grants small stackable combat perks (~2-8 per skill, no global cap, small magnitudes).  <sub>← Q6, FU8 · ADR</sub>
- **[edit]** `§1.5.1 capstone sentence (221-222)` — Rewrite 'The capstone rung confirms (does not first-open) the higher macro tiers — the relevant pillars have gated those in parallel.' Under the sequential model the capstone rung OPENS phase 2 (the estate-influence/pillar grind); the hybrid per-pillar profile is then what tiers up — pillars are NOT grown in parallel across the rungs.  <sub>← FU7, FU11, Q7 · ADR</sub>
- **[rewrite]** `§1.5.1 T0 ladder table rows R3-R6 (237-240)` — Overlay the combat-reveal ladder + sequential phasing: R3 = the SINGLE starter weapon + bare auto-resolve loop + retreat + Bestiary (combat-XP→character level begins); R4 = durability bands surface with the loot→craft loop, Crafting becomes a top-level tab; R5 = stance slot unlocks, pest/hunt/clear/defend quest types (Quests a top-level tab); MOVE 'Arms deeds begin accruing'(R5)/'first Arms standing recorded'(R6) OUT of phase 1 into PHASE 2 (deeds gated post-final-rung) — curated combat activities still feed the Combat Rank rung-meter during the rungs, but Arms PILLAR deeds don't accrue until phase 2.  <sub>← FU12, Q15, Q1, Q10, FU7, FU14, FU17 · ADR</sub>
- **[edit]** `§1.5.1 '~8 rungs' / titles paragraph (224-226) + T0 intro (228-230)` — Note the growing weapon roster: T0 starts with 1 weapon and unlocks +2 across the tier (FU13: T0+2 / T1+3 / T2+4, ~9-10 across v1). Keep the ~8-rung lean ladder shape and humble titles.  <sub>← FU13, Q15</sub>
- **[edit]** `§1.5.1 T1/T2 ladder tables & intros (243-270)` — Weave the incremental combat reveal across tiers: T1 unlocks +3 weapons and the 2nd combat LINE (gated on a Combat Rank rung); T2 unlocks +4 weapons and the 3rd line. Rung-meter resets per rung each tier; each tier's Arms-pillar deeds accrue only in that tier's phase 2.  <sub>← FU13, FU12, FU7</sub>
- **[edit]** `§1.5.1 estate-physical-growth blockquote (272-279)` — Author estate stage E3 for v1: estate grows E0→E3 (un-park E3). Add an E3 'Prosperous/Recovering+' stage description; update the parked note to E4-E5 only. Keep the 'physical growth runs ahead of top personal rank, gated on pillars + low rank floor + cost' framing.  <sub>← Q8 · ADR</sub>
- **[keep]** `§1.5.2 VILLAGE web (281-302)` — Keep the gentle per-node reputation-web shape and 'never gates the climb' as-is; only the Obaa Sato rename (handled in §1.8) touches it.  <sub>← Q39</sub>
- **[edit]** `§1.5.3 ORIGIN rung-walk (316-321)` — Fix the conflation 'his name set down (the Tama-payoff capstone, at G6)': the O5 capstone = reclaiming the name 'Tahei' (Origin-gated, earned + MISSABLE); the Otsuru/Tama TRUTH is SEPARATE and spine-guaranteed at G6. Re-sequence the rung walk so O5 = name-reclaim, not the G6 truth.  <sub>← Q5 · ADR</sub>
- **[edit]** `§1.5.3 Origin payoff + 'Hard guardrail' (323-334)` — Rescope 'ZERO mechanical gift' to the backstory/MEMORY reveal only: remembering grants ZERO. The new-relationship morale buff (modest global skill-XP) and origin trade-tie speedup (the ~10-15% time-to-tier accelerant) are LEGITIMATE present-day-relationship mechanics that STAY — they are earned new relationships, not retroactive gifts from remembering.  <sub>← Q12</sub>
- **[keep]** `§1.5.3 Jinpachi re-add (319-321)` — Keep father Jinpachi re-added; reunion resolves at T2 with the optional T4 emotional callback.  <sub>← Q5</sub>
- **[rewrite]** `§1.5.4 'Separate earned standing meters' paragraph (350-356)` — Rename 'Combat Standing' → 'Combat Rank'. Reframe the estate spine's gating as the per-tier rung-meter (FU6, per-rung-reset) + story milestones (sequential phase 1), with the four-pillar grind as the separate phase-2 tier-gate. Explicitly separate the THREE combat-fed tracks so they can't collapse into one bar: character level & HP/attrs (combat-XP), the Arms pillar (deeds), the Combat Rank rung-meter (per-rung curated activities). Keep Village Reputation + Origin Ties as the optional side-track meters.  <sub>← Q9, FU6, FU7, FU14 · ADR</sub>
- **[edit]** `§1.5.4 House-Influence multiplier note (353-356) + synergies (363-366)` — Keep the ~10-15% side-faction multiplier (Q12 confirms the speedup/buff is legitimate). No change in magnitude; just ensure it reads as a real accelerant, not a memory gift.  <sub>← Q12</sub>
- **[edit]** `§1.6.1 Arms pillar row (392)` — Clarify Arms grows on recognised DEEDS (the influence economy, phase-2 gated) — distinct from character level (fed by combat-XP) and the Combat Rank rung-meter (fed by per-rung curated activities). One kill feeds character level; a recognised deed feeds Arms; rung activities feed the meter.  <sub>← FU14, FU7</sub>
- **[edit]** `§1.6.1 trade-cap blockquote (397-404)` — Keep the trade sub-engine ≤⅓ cap as a HARD structural cap (FU20 — combos computed POST-clamp, verifier-proven). Add: cross-pillar combos are BROADER in V2 (multiple pillar pairs, larger magnitude) but never breach the trade clamp; the T2 anti-slump uses BOTH seasonal-reward rotation AND cross-pillar combos.  <sub>← FU20, Q20, Q22</sub>
- **[rewrite]** `§1.6.2 Accrual (406-418)` — State that pillar accrual (achievement JUMPS + seasonal JUDGED RESULTS on a new high-water mark) belongs to the PHASE-2 estate-influence track — pillar DEEDS do NOT accrue while climbing the rungs (gated post-final-rung, FU7), preventing 'half the rungs, maxed deeds'. Add: weather/festivals modulate judged results mechanically, bounded ±10%; bulk-sale saturation damper applies progressively per-unit. Keep up-only + rare recoverable per-pillar dents.  <sub>← FU7, Q35, Q42 · ADR</sub>
- **[rewrite]** `§1.6.3 intro paragraph (420-431)` — Replace 'tier gating = per-tier REQUIRED PILLARS via SIMPLE THRESHOLDS' with the HYBRID per-pillar-per-tier gate: good in ALL revealed pillars, great in 2-3, excellent in 1-2 (semantics: good=expected baseline, great=really strong, excellent=above-and-beyond); T0 is a 2-pillar special case; NO overflow substitution. Add the SEQUENTIAL framing: tier-up = climb all rungs (phase 1: rung-meter + story) THEN clear the phase-2 hybrid pillar profile. Note the per-pillar-per-tier thresholds need a full OVERHAUL (not simple ratios), back-solved against the fixed deed inventory — exact numbers deferred to §4. Keep trade ≤⅓ as the only surviving structural cap.  <sub>← Q7, FU10, FU7, Q30, FU20 · ADR</sub>
- **[rewrite]** `§1.6.3 five-tier table — 'Required pillars (simple thresholds)' column (433-439)` — Recast the column to 'Pillar profile (good/great/excellent)' per tier: T0 = 2-pillar special (Arms+Estate good, one excellent); T1 = good in all revealed + Office added, great in 2-3; T2 = Estate+Office great/excellent, Arms good; T3 = Office+Name excellent; T4 = Name+Office excellent (national banzuke on all four). Add a column/note that the rung-meter + story milestones precede the pillar gate (phase 1 → phase 2).  <sub>← Q7, FU10, FU7 · ADR</sub>
- **[edit]** `§1.7 world table — find-spot rows (466, 468)` — Consolidate to ONE find-spot: the jizō AT the weir where he was found. Merge 'The Boundary-Stone / Jizō' and the weir reference of 'The River, Ford & Weir' so there is a single found-here location; the one capped residual-ambiguity offering lives at that single jizō-at-the-weir.  <sub>← Q11 · ADR</sub>
- **[edit]** `§1.7 intro (446-453)` — Light: note world areas are a data-driven registry (content/world.ts) with unlock predicates — reinforces the existing data-driven framing.  <sub>← Q55</sub>
- **[edit]** `§1.7.1 (mirror of estate-stage note)` — Update the parked-stages reference: E3 is now authored in v1 (estate E0→E3); only E4-E5 remain parked (cross-ref §1.5.1).  <sub>← Q8</sub>
- **[edit]** `§1.7.1 'Kaidō Porters' & Transport Guild' node (481)` — Remove 'the T3 stub cliff-hanger is this one node's first-contact'. The v1 stub ending is NOT the porter-guild node — drop the porter-guild ending framing (Q24); re-point the cliff-hanger to the castle-town / Daikan's-Office first-contact.  <sub>← Q24 · ADR</sub>
- **[edit]** `§1.7.1 'Daikan's Office (castle-town officialdom)' node (485)` — Mark this node as the v1 STUB-ENDING first-contact beat (the castle-town / Daikan first-contact cliff-hanger on which v1 closes).  <sub>← Q24</sub>
- **[edit]** `§1.7.1 Edo conduit node (486)` — Rename 'rusui Konoe' to an invented surname (Konoe is a real great-house name).  <sub>← Q27</sub>
- **[edit]** `§1.7.1 Edo conduit node — Nihonbashi (486)` — Keep the Nihonbashi/banzuke payoff; annotate Nihonbashi as an explicitly ALLOW-LISTED real place name (Q12).  <sub>← Q12</sub>
- **[edit]** `§1.8 cast — Kurosawa Naoyuki (513)` — Add the explicit T1 / early-T2 rivalry→grudging-respect BEAT so the G5 ally-flip against Rival House Tomita reads as earned, not abrupt.  <sub>← Q26</sub>
- **[edit]** `§1.8 cast — 'Sota & Mago' (519)` — Rename the teen field-labourer 'Mago' (collision); pick a clean non-colliding period name.  <sub>← Q11</sub>
- **[edit]** `§1.8 cast — 'Clerk Naozane' (566)` — Rename Naozane off the Naoyuki collision (non-locked NPC).  <sub>← Q39</sub>
- **[edit]** `§1.8 cast — 'Obaa Sato' (535) + §1.5.2/§1.7 references` — Rename Obaa Sato off the Sayo collision (non-locked NPC); update the herb-stall references in §1.7 (462) and §1.5.2.  <sub>← Q39</sub>
- **[edit]** `§1.8 cast — 'Junkenshi Toyama Saemon-no-jō' (567) + §1.11` — Rename the touring-inspector's surname 'Toyama' to an invented one (real surname); update §1.11 references.  <sub>← Q27</sub>
- **[edit]** `§1.8 cast — 'Otsuru (the real Tama)' (555)` — Note the Otsuru truth/living-proof is spine-guaranteed at G6 (Q5); keep the Otsuru name lock and the costly/incomplete reunion.  <sub>← Q5</sub>
- **[edit]** `§1.8 cast — 'Jinpachi' (549)` — Keep Jinpachi reunion at T2; cross-ref that the Tahei NAME-reclaim is a separate Origin O5 capstone (missable), not the father reunion.  <sub>← Q5</sub>
- **[edit]** `§1.9 side-thread (A) — lost-child truth (576-584)` — Keep the Otsuru truth spine-guaranteed at G6 (Q5). Add 'presumed dead → back from the dead' framing; consolidate the two find-spot references to the single jizō-at-the-weir (Q11).  <sub>← Q5, Q11 · ADR</sub>
- **[edit]** `§1.9 side-thread (B) — origin thread (586-596)` — State the Tahei name-reclaim is Origin-gated at O5, earned + MISSABLE (Q5). Keep family reunions resolving at T2. Reaffirm the morale buff + trade-tie ~10-15% speedup are legitimate accelerants (Q12), while the memory reveal itself grants ZERO.  <sub>← Q5, Q12 · ADR</sub>
- **[edit]** `§1.9 'Per-tier side-quest list (lean)' header (605)` — Drop the '(lean)' / budget framing — no quest budget; author whatever quests fit each stage, more/interesting quests welcome esp. later tiers.  <sub>← Q23</sub>
- **[edit]** `§1.9 per-tier side-quest table (607-612)` — Add a T1 (or early-T2) Naoyuki rivalry→grudging-respect beat (Q26). Keep T2 reunions incl. Jinpachi. Reflect the Tahei name-reclaim as the Origin O5 capstone (missable, Q5).  <sub>← Q26, Q5, Q23</sub>
- **[rewrite]** `§1.10 '~4 v1 quest types' paragraph (639-641)` — Remove the ~4 quest-type budget (Q23 supersedes D-012's lean-4). PEST/HUNT/CLEAR/DEFEND remain the early core, but escort/patrol/bounty/duel/investigate/etc. are no longer hard-parked — author whatever quest types fit each stage; more and more-interesting quests welcome, especially later tiers.  <sub>← Q23 · ADR</sub>
- **[edit]** `§1.10 residual-ambiguity paragraph (628-629)` — Update the single residual-ambiguity beat location to the consolidated jizō-at-the-weir (Q11). Keep ≤1 cap + the open-ended/dawning-dread philosophy.  <sub>← Q11</sub>
- **[edit]** `§1.11 antagonists/endgame intro + T3 row (643-656)` — State v1's ENDING beat explicitly: v1 closes on the castle-town / Daikan first-contact cliff-hanger (the T3 stub), not the porter-guild. Rewrite any stale T3-as-full-tier framing; drop the porter-guild ending framing. The full T3/T4 endgame stays the roadmap vision below.  <sub>← Q24 · ADR</sub>
- **[edit]** `§1.11 T2 antagonist row + Succession (654, 690-691)` — Tie the G5 Naoyuki ally-flip vs Tomita to the new T1/early-T2 rivalry→respect beat (Q26) so it reads as earned.  <sub>← Q26</sub>
- **[edit]** `§1.11 endgame prose — rusui Konoe / inspector Toyama (656, 683-687)` — Rename 'rusui Konoe' and the touring inspector's surname 'Toyama' to invented names (Q27) wherever they appear in the §1.11 narrative.  <sub>← Q27</sub>
- **[edit]** `§1.11 epilogue tableau (693-694)` — The 'a true name (Tahei)' epilogue element is now CONDITIONAL: name-reclaim is gated on completing the Origin O5 track (missable, Q5) — the epilogue reflects whether the player earned it.  <sub>← Q5 · ADR</sub>
- **[edit]** `§1.11 power-late-game paragraph (668-678)` — Note T2 anti-slump / late-game variety leans on cross-pillar combos + seasonal reward rotation (Q22/Q20/FU20); keep trade OUT of the finale spine.  <sub>← Q22, Q20, FU20</sub>
- **[rewrite]** `§1.12 'Systemically — multi-screen UI' paragraph (706-712)` — Reveals are DESIGN-staggered one-at-a-time (no runtime reveal-queue) + obey NO-UI-DUMPS (Q17); distinct activities (Crafting, Quests) appear as top-level nav tabs (Q10); the per-tier rank ladders run the SEQUENTIAL phase-1 (rungs)→phase-2 (estate-grind) model (FU7). Keep 'unlock reads as plot, never silent menu growth.'  <sub>← FU4, Q17, Q10, FU7 · ADR</sub>
- **[rewrite]** `§1.12 'T0 — Estate ladder reveals' bullets (714-723)` — Rewrite per the combat-reveal ladder + sequential phasing: R3 = single starter weapon + bare auto-resolve + retreat + Bestiary, character level begins (combat-XP); R4 = durability bands + Crafting top-level tab; R5 = stance slot + Quests top-level tab + pest/hunt/clear/defend; MOVE 'Arms deeds begin accruing/recorded' out of R5/R6 into PHASE 2 (post-R7); R7 capstone OPENS phase 2 (the four-pillar/influence grind), and the hybrid pillar gate is what tiers up to T1.  <sub>← FU12, Q15, Q1, Q10, FU7, FU14 · ADR</sub>
- **[add-new]** `new bullet block after §1.12 T0 reveals (after 723)` — Add an explicit combat-reveal ladder summary: R3 weapon+loop+retreat → R4 durability → R5 stance → weapon-L10 ability/item slots → 2nd combat line at T1, 3rd at T2; plus the growing weapon roster (T0+2 / T1+3 / T2+4, ~9-10 across v1). One reveal per beat.  <sub>← FU12, FU13, Q15</sub>
- **[rewrite]** `§1.12 'active-only with no offline progress' paragraph (733-743)` — Keep active-only / no offline (FU23). Add: tab-open AUTO-RESOLVE + AUTO-REPEAT give the 'leave it running, check progress' feel (active-only); auto-producers stay T3+. Reframe the time/budget model: the ~28.5h budget is a FLOOR not a ceiling — a longer OSRS-rough grind (interleave, don't brick-wall).  <sub>← FU23, FU18 · ADR</sub>
- **[edit]** `§1.12 shapes + multiplier sentences (740-742)` — Keep the estate steep-geometric / village gentle / origin O5 shape variety + the ~10-15% multiplier (Q12). Add the rung-meter (FU6) as the per-rung numeric curve that drives phase-1 reveals.  <sub>← Q12, FU6</sub>
- **[rewrite]** `§1.13 'No labour→combat cross-feed' guardrail (758-759)` — REVERSE to bounded per-skill perks: each skill grants ~2-8 small stackable combat perks/flat bonuses (no global cap); conditioning stays the zero-stat enablement gate; replace 'ZERO combat stat or training-rate bonus / soft hidden edge killed outright' with 'small per-perk magnitude — labour skills make you more capable; big combat power stays combat-only.'  <sub>← Q6, FU8 · ADR</sub>
- **[edit]** `§1.13 'Fictionalise real names' guardrail (760-763)` — Add Nihonbashi to the ALLOW-LIST (Q12). Record the renames: Toyama + Konoe (Q27), Mago (Q11), Naozane + Obaa Sato (Q39); keep Jinpachi.  <sub>← Q12, Q27, Q11, Q39</sub>
- **[add-new]** `new bullet in §1.13 (after 757)` — Add a guardrail: the budget is a FLOOR not a ceiling — a longer OSRS-rough grind with enough grinding content; interleave, never brick-wall; 'leave it auto-running' (active-only + tab-open auto-resolve/auto-repeat).  <sub>← FU18, FU23 · ADR</sub>
- **[add-new]** `new bullet in §1.13 (after 762)` — Add a guardrail: combat is incremental + never weaponless — T0 starts with 1 weapon, a growing roster (FU13); graded durability bands degrade attackPower but NEVER auto-unequip (Q33/FU17).  <sub>← FU13, Q15, Q33, FU17</sub>
- **[edit]** `§1.13 'Standing & Office pillar kanji' guardrail (768-771) + all §1 occurrences` — Rename the Arms rank-gate 'Combat Standing' → 'Combat Rank' project-wide (Q9), so 'Standing' = the 官威 pillar only. Macronize gōshi (currently 'goshi') and rōnin forms project-wide.  <sub>← Q9</sub>
- **[edit]** `§1.14 intro + ADR table (778-796)` — Add a V2-deltas note: V2 decisions add ADRs D-016…D-022; D-022 is GOVERNING (V2 decisions supersede conflicting prior locks, most-recent-wins, annotate-don't-delete); D-021 = freeze means locked-intent-only (PD-1: §1 vision + acceptance criteria frozen; §4 numbers / §7 M2-M7 stay provisional).  <sub>← D-022, PD-1, D-021 · ADR</sub>
- **[edit]** `§1.14 D-008 row (789)` — Amend: per-tier gating SIMPLE thresholds → HYBRID good/great/excellent (Q7/FU10) under the SEQUENTIAL phase-1/phase-2 model (FU7); trade ≤⅓ survives as the only structural cap.  <sub>← Q7, FU10, FU7 · ADR</sub>
- **[edit]** `§1.14 D-011 row (792)` — Amend: combat earns standing + conditioning gate + NO cross-feed → BOUNDED per-skill perks (Q6/FU8); add the THREE clean combat tracks (FU14: character level / Arms deeds / Combat Rank rung-meter).  <sub>← Q6, FU8, FU14 · ADR</sub>
- **[edit]** `§1.14 D-012 row (793)` — Amend: drop the ~4-quest budget (Q23 — no quest budget); combat now incremental + bigger weapon roster (Q15/FU13); budget = FLOOR / longer grind (FU18); estate E3 authored in v1 (Q8).  <sub>← Q23, Q15, FU13, FU18, Q8 · ADR</sub>
- **[edit]** `§1.14 D-013 row (794)` — Amend (cross-ref §6): multi-backend redundant save (Q37/FU1-3), per-named-stream RNG cursors + day-keyed stateless weather (Q2/FU3), ban Math.pow→integer-pow (Q36), active-only + tab-open auto-resolve/auto-repeat (FU23); presentation: a11y correctness, --ink-soft contrast, self-hosted OFL fonts, inline-SVG motifs (Q18/Q48/Q52/Q38).  <sub>← Q37, FU1, Q2, FU3, Q36, FU23, Q18, Q48, Q52, Q38</sub>
- **[edit]** `§1.14 D-006 row (787)` — Amend: the Tahei reveal is now Origin-gated at O5 — earned + MISSABLE (Q5); the Otsuru/Tama TRUTH stays spine-guaranteed at G6.  <sub>← Q5 · ADR</sub>
- **[edit]** `§1.14 'Resolved by Round A' list (798-824)` — Annotate the 🔁 reversals: 'Tier-gate = simple per-tier thresholds' → hybrid good/great/excellent (Q7/FU10); 'estate stages E3-E5 parked' → E3 authored in v1 (Q8). Add: Combat Standing rank-gate renamed → Combat Rank (Q9); single find-spot consolidated at the jizō-at-the-weir (Q11).  <sub>← Q7, FU10, Q8, Q9, Q11 · ADR</sub>
- **[NEW]** §1.6.4 — Progression structure: the sequential per-tier model + the three tracks @ new subsection after §1.6.3 — Formalize the V2 progression spine in one load-bearing place that §1.5.1, §1.6.3 and §1.12 reference. Define: (1) SEQUENTIAL per-tier — Phase 1 climbs the rungs via CURATED per-rung activities tracked by a per-rung-reset NUMERIC rung-meter (a real §4 curve, ≥30-min floor back-solved) AND story milestones (AND-gate); Phase 2 = after the final rung, the estate-influence/pillar grind unlocks → the hybrid gate tiers up; pillar DEEDS gated to phase 2. (2) The THREE clean combat tracks (kills/combat-XP→character level + HP/attrs; recognised deeds→Arms pillar; per-rung curated activities→the Combat Rank rung-meter) — stating what one kill writes to each. (3) The hybrid good/great/excellent profile (Q7) as the phase-2 tier-gate. Note exact curves/thresholds deferred to §4.  <sub>← FU7, FU6, Q30, FU14, Q7, FU10, Q1, Q9</sub>
- **[NEW]** §1.5.1 sub-note — Combat progression: incremental weapons + the reveal ladder @ new subsection/note after the §1.5.1 T2 ladder paragraph (after line 270) — Capture at the vision level that combat is now a real INCREMENTAL progression surface: the growing weapon roster (T0 starts with 1; +2 T0 / +3 T1 / +4 T2; ~9-10 across v1, each archetype + signature ability) and the combat-reveal ladder (R3 weapon+loop+retreat → R4 durability bands → R5 stance → weapon-L10 ability/item slots → 2nd line T1, 3rd line T2), one reveal per beat. Detail lives in §2.8/§4, but §1 must state the shape since combat moved from 'surfaced from T0' to 'an incremental ladder.'  <sub>← Q15, FU13, FU12</sub>

### §2 — Systems & Mechanics Catalog (prd.md lines 822-1711)

- **[edit]** `§2 preamble — "Genuinely-open items" (~lines 831-833)` — Strike the line declaring the gate shape SETTLED as "simple thresholds + trade ≤⅓ ... NO floor/overflow arithmetic." Replace with: gate model is now the HYBRID good/great/excellent distribution (per-pillar-per-tier overhaul, §2.16); trade ≤⅓ stays the one hard structural cap; floor/overflow still rejected (no overflow-substitution).  <sub>← Q7, FU10 · ADR</sub>
- **[edit]** `§2 System inventory table (~lines 840-865)` — Update rows to V2: 2.4 (coin numbers deferred to M4, placeholder); 2.7 (character level COMBAT-fed only; per-skill perks add small combat texture); 2.8 (combat INCREMENTAL — one weapon at T0; THREE clean tracks; not a one-beat dump); 2.16 (hybrid good/great/excellent gate, not simple thresholds); 2.19 (MULTI-BACKEND save, not single-IndexedDB). Also add §2.8 + §2.19 to the "most load-bearing" note.  <sub>← Q1, Q6, Q7, Q13, Q15, Q37, FU8, FU13, FU14</sub>
- **[edit]** `§2.1(a)/(e) UI-reveal engine` — Add the GENERAL no-UI-dumps principle and design-staggered reveals: the unlock schedule is authored so reveals are inherently ONE-AT-A-TIME (no two collide on a beat). State there is NO runtime reveal-queue (supersedes the earlier Q17 "serialize into a deterministic one-per-beat queue" note); genuine multi-element single-feature reveals are bespoke one-offs designed per case with the human. Reveal cadence is a gameplay/UI DESIGN responsibility, not runtime machinery.  <sub>← FU4, Q17 · ADR</sub>
- **[edit]** `§2.1(b)/(c) reveal data shape` — Note that distinct activities (Crafting, Quests) reveal as their own TOP-LEVEL nav tabs (not nested panels); the main screen stays the active labour/deeds/combat loop. RevealableEntry 'tab'/'navLink' kinds carry these top-level activities.  <sub>← Q10</sub>
- **[edit]** `§2.2(c) WorldClock data shape` — weather & lunarPhase are DERIVED via a pure stateless deriveDayKeyed(seed,'weather'|'lunar',day) helper — NOT stored WorldClock fields; nothing weather/lunar persists (only the day index does). The fractional dtTicks remainder accumulates in the app tick loop so tick() only ever receives whole integer ticks.  <sub>← Q3, FU3, Q2</sub>
- **[rewrite]** `§2.3 Soft stamina / satiety (a)-(c)` — Satiety throttles BOTH labour AND combat (was framed labour-only). Curve: flat above ~0.7 satietyMax, kneeing to a ~0.5 floor (STAMINA_RATE_FLOOR ≈ 0.5); modest drain per action; rest fully refills. Combat uses a SEPARATE satietyRate coefficient from the labour floor (tune independently). satietyMax = base + per-(combat-)level growth. Never a hard wall.  <sub>← Q31, Q17, Q47, FU16, FU21 · ADR</sub>
- **[edit]** `§2.4(b)/(c)/(e) Resources & currencies` — Coin/market numbers (koku↔coin spread, sinks, MarketState) DEFERRED to M4 as a placeholder, not frozen. Strike any '+ caps' — resource counts stay UNBOUNDED (counts-only GameState). Keep no-market-in-T0 (village shops = the first market, T1). The market-saturation damper applies PROGRESSIVELY per-unit on bulk sales (each unit walks the price down — legible, un-gameable). Note koku is comfortable/NET.  <sub>← Q13, Q14, Q29, Q42</sub>
- **[edit]** `§2.5 Auto-producers` — Keep strictly T3+. Clarify the "leave it running for hours, check progress" feel comes from tab-open AUTO-RESOLVE combat + AUTO-REPEAT labour (FU23), NOT auto-producers; v1 is active-only (no offline accrual). Auto-producers remain a late convenience distinct from auto-repeat.  <sub>← FU23, FU18</sub>
- **[edit]** `§2.6 Gathering / labour (a)/(b)/(e)` — Add tab-open AUTO-REPEAT labour (active-only) as the grind convenience, distinct from the LATE auto-gather toggle / auto-producer (§2.5). Grind depth is a FLOOR not a ceiling — a longer OSRS-rough grind with enough grinding content; interleave content, don't brick-wall. §4.8 becomes a minimum-grind model.  <sub>← FU23, FU18 · ADR</sub>
- **[rewrite]** `§2.7(a)/(c) attribute & level model` — Character level = its OWN stored track, fed by COMBAT XP ONLY (labour/deeds NEVER raise it). HP + satiety capacity + attribute points scale off combat level (hpMax/satietyMax/+1 attr per 2 levels are §4 numbers). SCOPE OUT (or keep tiny) the "compounding skill-XP multiplier" so combat level can't drive labour XP (kills the combat→labour loop).  <sub>← Q1, FU14, Q47 · ADR</sub>
- **[rewrite]** `§2.7(c) the "no-hidden-edge guard"` — REPLACE "combat skills have no input edge from labour skills (no cross-feed field exists)" with: every skill (labour incl.) has a PERK/flat-bonus track (~2-8 perks OR ~3-8 small flat stat bonuses) unlocked by leveling it; perks add a small combat bonus through a SEPARATE skillCombatBonus channel (NOT an attribute, NOT character level). Stackable, NO hard global cap — bounded by small per-perk magnitude + incremental skill unlock + holistic enemy/drop scaling (risk accepted). Conditioning stays the ZERO-stat enablement gate (the one exception). SkillDef gains perks[].  <sub>← Q6, FU8 · ADR</sub>
- **[edit]** `§2.7(e) skills reveal cadence` — Replace "2-3 weapon lines at T0" with: exactly ONE starter weapon at T0; the weapon roster grows incrementally (T0 +2 / T1 +3 / T2 +4; ~9-10 across v1). Skills surface INCREMENTALLY per rung/tier — this incremental unlock is the real bound on labour→combat cross-feed (can't front-load).  <sub>← Q15, FU13, FU8</sub>
- **[rewrite]** `§2.8(a)/(c) — the conflated Combat-Level/CombatDeedsPool` — DELETE "Combat Level (a Combat Deeds pool: combat-XP + a deed-count)" and the CombatDeedsPool type. Replace with THREE INDEPENDENT tracks: (1) kills/combat-XP → CHARACTER LEVEL (HP + attribute points, Q1); (2) recognized DEEDS → the ARMS pillar (the §4.2.1 influence economy); (3) per-rung CURATED activities → the COMBAT-STANDING rung-meter (§2.15). State exactly what one kill writes (level only — never the rung-meter, never Arms directly).  <sub>← FU14, Q1, Q30 · ADR</sub>
- **[rewrite]** `§2.8(a)/(e) — incremental combat + reveal ladder` — Combat is INCREMENTAL: T0 starts with EXACTLY ONE weapon. Rewrite the §2.8(e) R3 dump ("drill yard + Combat panel + idle-combat all at once") into the staggered ladder, one reveal per beat: R3 = single starter weapon + bare auto-resolve loop + retreat → durability bands at R4 (with the loot→craft loop) → stance slot at R5 → ability/item slots at the first weapon-line L10 milestone → 2nd weapon line at T1 → 3rd at T2.  <sub>← Q15, FU12, FU13, Q17 · ADR</sub>
- **[edit]** `§2.8(c) Combatant / encounter data` — Add per-WEAPON archetype params (baseSpeed / reach / targetCount / attack profile) + a signature ability — distinct identity now lives on the weapon, not only on Stance. The crude carrying-pole is a 0th IMPROVISED weapon (not a line). Sketch CombatEncounterState (added additively at its M2/M5 milestone, not pre-declared in M0 per FU5).  <sub>← FU13, Q34, FU5</sub>
- **[edit]** `§2.8(b)/(c) durability in the sim` — Weapon attackPower scaled by 4 GRADED durability bands (75+/50+/1+/0 → 1.0/0.9/0.75/0.55), FIXED wear per FIGHT (cheap, replay-stable). NEVER auto-unequip — a weapon stays equipped & functional even at 0 (the 0.55 floor); never weaponless. Armour bands identically on defense.  <sub>← Q33, FU17 · ADR</sub>
- **[edit]** `§2.8(b)/(c) satiety→combat throttle` — Add a satietyRate multiplier on attackPower (lighter on attackSpeed), flat above ~0.7 satietyMax → ~0.5 floor; a SEPARATE combat coefficient. "Adequate satiety" = ≥~0.7, where the LOCKED 20-35% first-fight win-rate is re-measured (re-spec it "at adequate satiety"); bound the throttle so the floor only costs a few win-rate points (never below ~15%).  <sub>← Q31, FU16, Q47 · ADR</sub>
- **[edit]** `§2.8(b)/(d) retreat semantics` — Retreat = a CLEAN escape valve: keep HP + loot, modest clock cost, NEVER dents Influence. Abandoning a DEFEND deed = a failed defend (a small recoverable Arms dent). Add headless win-rate bands at R3/V2/V5/G1/G5 as a 2nd pacing proxy (numbers tune-later, FU19).  <sub>← Q16, FU19</sub>
- **[edit]** `§2.8(a) conditioning framing` — Keep conditioning as the ZERO-stat, one-way labour-built enablement gate (the weak→capable gate), but note it is now the EXCEPTION: other labour skills feed small combat texture via per-skill perks (§2.7). Conditioning grants ZERO combat stat / training-rate bonus.  <sub>← Q6, FU8</sub>
- **[edit]** `§2.8(e) + §2.15(a) — Combat-Standing / Combat-Rank naming` — FLAG a naming conflict for the rewrite to resolve: Q9 wanted the Arms rank-gate renamed "Combat Standing" → "Combat Rank" (so "Standing" = 官威 only); the newer FU14 still calls the per-rung meter the "Combat-Standing rung-meter." Newest-block-wins → keep "Combat Standing" for the rung-meter OR apply "Combat Rank"; pick ONE term and apply project-wide.  <sub>← Q9, FU14 · ADR</sub>
- **[edit]** `§2.9(c) MobDef / belief registry` — Add an explicit per-mob `level` field on MobDef (hand-tunable; defaults ~ the dangerRing's expected character-level) feeding the on-kill combat-XP path. Move the BeliefBeast registry to content/beliefBeasts.ts (separate from grindable mobs).  <sub>← FU15, Q3</sub>
- **[edit]** `§2.10(a)/(c)/(e) loot, equipment, durability` — Durability = GRADED bands (4 bands on weapon attackPower; armour bands on defense), NEVER auto-unequip, repair/re-craft restores to max, FIXED wear per fight. EquipDef weapons carry archetype params (baseSpeed/reach/targetCount) + signature ability; the weapon roster GROWS (~9-10 across v1; T0 +2 / T1 +3 / T2 +4). Durability bands reveal at R4 with the loot→craft loop.  <sub>← Q33, FU17, FU13, FU12 · ADR</sub>
- **[edit]** `§2.11 Crafting` — Crafting reveals as a TOP-LEVEL nav tab (Q10). The market-saturation damper applies progressively per-unit on bulk sales (Q42). Repair/re-craft restores weapon/armour durability to max (FU17). Trade/coin numbers (incl. the silk meibutsu economics) deferred to M4 (Q13).  <sub>← Q10, Q42, FU17, Q13</sub>
- **[edit]** `§2.12(a)/(c) dialogue branching` — Intra-line dialogue branching IS in v1: a flat choices[] list per node (choices[] + ChoiceId); a choice's effect = locksLineIds[]/flags. DATA, not scripting; deterministic (no RNG); only chosen-flags persist (save-light). Authored in content/dialogue.ts. Add choices[]/ChoiceId to the TextLine data shape.  <sub>← Q34, FU22 · ADR</sub>
- **[rewrite]** `§2.12(a)/(c) quest-type budget` — REMOVE the fixed "~4 v1 quest types" cap and the "Escort/patrol/... parked for T2+" budget framing. Author whatever quests fit each stage; the 4 grind types (PEST CONTROL/HUNT/CLEAR/DEFEND) are just the T0 STARTING set, not a cap; more/interesting quests welcome, especially later tiers (supersedes D-012's "lean 4"). Drop the "parked" cap from the Quest type union.  <sub>← Q23 · ADR</sub>
- **[edit]** `§2.12(d)/(e) quests as deeds` — DEFEND remains the canonical combat Arms standing-earner. Clarify that some quest/deed completions are the per-rung CURATED activities feeding the Combat-Standing rung-meter in PHASE 1, while pillar DEEDS accrue in PHASE 2 (FU7). Quest log = a top-level tab (Q10).  <sub>← FU7, FU14, Q10</sub>
- **[edit]** `§2.13(a)/(c) lore & rumours` — BeliefBeast registry → content/beliefBeasts.ts (Q3). Co-locate the single residual-ambiguity boundary-jizō at the weir/ford (Q11 — one find-spot; "presumed dead → back from the dead"). Rumours stay design-staggered/organic per-tier (aligns with FU4, no all-at-once dump).  <sub>← Q3, Q11, FU4</sub>
- **[edit]** `§2.14(a)/(c)/(d) world simulation` — Weather hazards & festivals carry MECHANICAL effects, bounded ±10% on labour/combat rates (+ festival economic beats). Weather/lunar DERIVED from the day-keyed RNG sub-stream (deriveDayKeyed), not stored. World-sim content (SeasonRules / Festival / WeatherHazard) authored in content/world.ts (add content/festivals.ts row), generated/verified like the other registries.  <sub>← Q35, Q3, FU3, Q55 · ADR</sub>
- **[rewrite]** `§2.15(a) ESTATE ladder + (c) data shape` — SEQUENTIAL per-tier (FU7): PHASE 1 = climb the rungs (R0→R7) via CURATED per-rung activities (1-to-many designed activities per rung, NOT a single repeat-counter) tracked by the rung-meter + story; PHASE 2 = the estate-influence/pillar grind UNLOCKS after the FINAL rung → tier-up. Pillar DEEDS are gated to phase 2 (not accruing across rungs — prevents "half the rungs, maxed deeds"). Rung promotion = AND-gate (numeric rung-meter ≥ threshold AND story flags; surface "awaiting X" when one lags). Revise "capstone confirms the tier": the capstone rung confirms phase 1; the phase-2 hybrid pillar gate is the actual tier-gate.  <sub>← FU7, FU6, Q30, FU11 · ADR</sub>
- **[edit]** `§2.15(a)/(c) rung-meter law` — Both meters (EstateService, Combat-Standing) are NUMERIC and PER-RUNG-RESET; each rung's threshold = (≥30-min floor × that rung's eligible-activity rate), back-solved like the koku column so meter and floor stay in lockstep. Combat-Standing rung-meter is fed by per-rung curated activities, NOT raw kills/XP (FU14). Add rungActivityTags tagging which activities advance which rung; double-counting allowed but each stream sums independently.  <sub>← Q30, FU6, FU7, FU14 · ADR</sub>
- **[edit]** `§2.15(a) ORIGIN support track` — Rescope the "ZERO mechanical bonus": returning MEMORY (the backstory reveal) grants NO retroactive stat/recipe bonus, but the present-day relationship pride buff (modest global skill-XP) + the ~10-15% time-to-tier speedup STAY (Q12). Reclaiming the name "Tahei" = the Origin O5 capstone — EARNED and MISSABLE (Origin-gated, Q5); the Otsuru/Tama TRUTH stays spine-guaranteed at G6 (§5). Add Nihonbashi to the fictional allow-list.  <sub>← Q12, Q5 · ADR</sub>
- **[edit]** `§2.15(a) conditioning note (combat rungs)` — Conditioning stays the ZERO-stat one-way enablement gate on combat rungs; note that OTHER labour skills now grant small per-skill perks (Q6/FU8) — conditioning alone grants zero combat stat/training-rate bonus.  <sub>← Q6, FU8</sub>
- **[rewrite]** `§2.16(a) tier-gate model + the tier table` — REPLACE "PER-TIER REQUIRED PILLARS via SIMPLE THRESHOLDS ... NO floor/overflow (formula REJECTED)" with the HYBRID gate: per-pillar-per-tier thresholds — GOOD in ALL revealed pillars · GREAT in 2-3 · EXCELLENT in 1-2. Semantics: good = expected baseline · great = really strong · excellent = above-and-beyond. Per-tier revealed-pillar set: T0 = 2 (Arms+Estate) SPECIAL CASE ("good in both + excellent in 1", no phantom 3rd), T1 = 3, T2 = 3. NO overflow-substitution (floor only). Trade ≤⅓ stays the one hard structural cap. Rewrite the table's "Required pillars (simple thresholds)" column to the good/great/excellent distribution per tier.  <sub>← Q7, FU10 · ADR</sub>
- **[edit]** `§2.16(a)/(c) tier-up composition + TierGate type` — The Q7 hybrid pillar distribution is a SEPARATE phase-2, capstone-only tier-gate, ANDed with the capstone rung-meter+story (FU11); pillar deeds gated to phase 2 (FU7). Rewrite the TierGate data shape: carry per-pillar good/great/excellent bands + a DISTRIBUTION predicate (not a single requiredPillarThresholds map; delete "no floor/overflow fields exist" line — replace with the distribution semantics).  <sub>← FU11, FU7, Q7, FU10 · ADR</sub>
- **[edit]** `§2.16(a) accrual & dents` — Add the below-high-water seasonal RESTORE branch for dents: a small seasonal restore lifts a dented pillar back toward its untouched high-water WITHOUT advancing the high-water ("self-heals, never a wipe"). Keep the two-shape accrual (achievement jumps + judged results, new-high-water-only) + per-event caps.  <sub>← Q32</sub>
- **[add-new]** `new para in §2.16(a) (T2 anti-slump)` — Add BROADER cross-pillar combos as the T2 anti-slump device (multiple pillar pairs / larger magnitude). Computed AFTER the trade-≤⅓ clamp; counted INSIDE the deeds budget + per-event cap; EXCLUDED from the gate-threshold check (a combo can NEVER substitute for being "good in ALL"); trade-≤⅓ stays HARD (verifier-proven it can't breach ⅓ via a combo). A narrow §4.3 no-leakage exception.  <sub>← Q22, FU20 · ADR</sub>
- **[edit]** `§2.16(b) four-bar panel` — Pillar bars show DISTANCE-TO-NEXT-GATE; number-flash uses the §2 gain/loss tokens (gain=--ai, loss=--beni); vermilion reserved for rank-up/seal beats.  <sub>← Q21</sub>
- **[edit]** `§2.16(a) tier table — T3 row` — Update the T3 "stub in v1" row: v1 ends on the castle-town / Daikan FIRST-CONTACT (magistrate's office, stone walls) — drop the porter-guild framing; rewrite the stale T3 header (detail in §5).  <sub>← Q24</sub>
- **[edit]** `§2.17(a)/(c)/(e) estate growth` — Author E3 for v1: estate grows E0→E3 (un-park E3 "Prosperous" + its §4.7.5 cost row). EstateStage id (E0…E3 in v1); E4–E5 parked. E3 authored in M5 as a koku/Arms sink folded into the G-rungs (build/authoring cost only — play-time budget is a FLOOR per FU18). Update the "E0–E2" references throughout the subsection.  <sub>← Q8, FU18 · ADR</sub>
- **[rewrite]** `§2.19(a)/(c)/(e) save/load` — REPLACE "Canon: IndexedDB ... single autosave; localStorage note superseded" with MULTI-BACKEND redundant save: IndexedDB primary + localStorage + sessionStorage. ATOMIC write (one put, never clear-then-rewrite). On save, write all available backends; on load, read ALL backends and pick NEWEST by a monotonic SAVE-COUNTER (the real selector) + a save-layer timestamp tiebreaker (a documented core-lint exemption — metadata, NOT game logic; the deterministic core stays clock-free). Calm "couldn't save — export a backup" notice on any rejection. Survives itch cross-origin-iframe partition/eviction. Built FULL in M0 (FU1).  <sub>← Q37, FU1, FU2, Q44 · ADR</sub>
- **[edit]** `§2.19(a)/(c) schema & integrity` — Save schema is BACKWARDS-COMPATIBLE / ADDITIVE (protobuf/thrift-style: new fields optional with defaults, never remove/repurpose) → migrations become rare; keep raw-backup + rollback + a future-version guard as the safety net (never load a forward-version backend — fall through to newest readable, then reject-to-recovery). Add an app-identity MAGIC field (app:'kami-kakushi') + reject-to-recovery on a bad/foreign id.  <sub>← Q45, Q46, FU2 · ADR</sub>
- **[edit]** `§2.19(c) persisted shape + M0 skeleton` — RNG = { seed, cursors:{combat,loot,seasonal,worldgen} } (per-named-stream monotonic cursors, NOT a single counter). Persist market-saturation ONLY (weather/lunar are DERIVED, not stored). Nest estateWealth.subEngines{land,treasury,trade} (storage for the trade-≤⅓ clamp + per-strand high-water). M0 skeleton = {hp,satiety,attributePoints} + character.level(=1) + satietyMax-at-floor; do NOT pre-declare subEngines/CombatEncounterState/dialogue choices — add additively at their milestone.  <sub>← Q2, Q3, FU3, Q34, FU5</sub>
- **[edit]** `§2.19(b) save-safety / robustness` — Add explicit CONFIRM on destructive actions (import / fresh-start, plus the genuinely-unrecoverable rare actions: no-respec attribute allocation, rare-material consume, narrative-route choices) + an auto PRE-OVERWRITE snapshot. Note Math.pow is banned (integer-pow) so exported saves replay byte-identically cross-engine.  <sub>← Q19, Q49, Q36</sub>
- **[edit]** `§2.20(b)/(c) content verifier` — Add verifier checks: gate-monotonicity & ceiling (no rung needs more than its tier can grant), accrual tie-out (deeds sum to the gate share within tolerance), real-name DENYLIST lint, trade-can-never-breach-⅓-via-combo proof, each combat track sums INDEPENDENTLY. FLIP the no-hidden-edge check from "labour→combat == 0" to "each per-skill perk is SMALL-magnitude (not zero, not a global ≤CAP)" with conditioning still asserted ZERO. Add the §6.1 Math.pow/exp/log/trig lint (whitelist sqrt).  <sub>← Q28, FU8, Q6, FU20, FU14, Q36, Q27 · ADR</sub>
- **[edit]** `§2.20(a) gen:docs / single-source` — Tag duplicated derived values "illustrative — see generated"; align gen-docs paths (docs/balance/ + docs/content/); the world-sim content/world.ts registry is generated/verified like the others.  <sub>← Q41, Q55</sub>
- **[edit]** `§2.21(a)/(c) art register` — Load-bearing period motifs (pillar / season / rarity marks) = INLINE SVG (consistent across OSes); emoji COSMETIC-only. This corrects the "no asset pipeline" claim (one acknowledged small motif+audio asset set).  <sub>← Q38 · ADR</sub>
- **[edit]** `§2.21(a)/(c) audio` — "GOOD audio": mixed SYNTHESIZED Web Audio + original/CC0 samples (the ONE acknowledged small curated audio set); correct the register to "text + emoji + CSS + a small curated audio set"; mute toggle stays. Licensing/credits surfaced (s2-50).  <sub>← Q50 · ADR</sub>
- **[edit]** `§2.21(a)/(b)/(c) accessibility` — Do the low-cost a11y correctness items now: a persistent quiet a11y entry point from minute one; an ARIA live-region scoped to narration+milestone ("polite"); a large-textScale reflow case + a screen-reader acceptance pass. Functional/hint text → --ink-soft (passes WCAG AA on all paper surfaces); --ink-faint decorative-only; darken the meter fill for contrast. Extend A11ySettings with live-region scope.  <sub>← Q18, Q48</sub>
- **[edit]** `§2.21(a)/(c) fonts` — Self-host the OFL fonts (kill Google dynamic-subsetting — it breaks offline + the itch relative-base); bundle the OFL license; clear the Reserved-Font-Name rule.  <sub>← Q52</sub>
- **[rewrite]** `§2.22 review checklist` — Update the flagged items to V2: item 1 (simple-threshold gate → HYBRID good/great/excellent, no overflow, trade ≤⅓ still hard); item 6 (lean skill/quest set → exactly ONE weapon at T0 + growing roster ~9-10 + per-skill perks + NO fixed quest-type budget). Add new flagged items: the THREE combat tracks; the SEQUENTIAL phase-1/phase-2 progression; the MULTI-BACKEND save layer.  <sub>← Q7, Q23, Q15, Q6, FU7, FU14, FU13, Q37</sub>
- **[edit]** `§2.16 + cross-§2 terminology` — Macronize gōshi / rōnin project-wide; confirm 官威 (kan'i) = Standing & Office. Resolve the "Combat Standing" vs "Combat Rank" overload (see s2-20).  <sub>← Q9</sub>
- **[NEW]** The three combat tracks @ new subsection inside §2.8 (after §2.8(c)) — Crisply tabulate the three INDEPENDENT combat-fed tracks and what one kill / deed / rung-activity writes to each: (1) kills + combat-XP → character level (HP + attribute points, Q1); (2) recognized DEEDS → the Arms pillar (the influence economy); (3) per-rung curated activities → the Combat-Standing rung-meter (§2.15). Replaces the deleted conflated CombatDeedsPool.  <sub>← FU14, Q1, Q30</sub>
- **[NEW]** Combat-reveal ladder (incremental) @ new subsection inside §2.8 (after the new three-tracks subsection) — Enumerate the staggered, one-per-beat combat reveal order with the trigger kind per step: R3 starter weapon + auto-resolve loop + retreat (combat rung) → R4 durability bands (loot→craft) → R5 stance slot → weapon-line L10 ability/item slots (weapon-skill milestone) → 2nd line T1 / 3rd line T2 (combat rung), signatures at L25/L50. Kills the R3 UI-dump.  <sub>← FU12, Q15, Q17, Q33, Q16</sub>
- **[NEW]** Weapon roster (incremental, ~9-10 across v1) @ new subsection inside §2.10 (after §2.10(c)) — List the growing period-appropriate weapon roster (T0 starts with 1; unlocks T0 +2 / T1 +3 / T2 +4) with each weapon's archetype params (baseSpeed / reach / targetCount / attack profile) + a signature ability; crude pole = a 0th improvised weapon, not a line. Replaces "2-3 weapon lines at T0".  <sub>← FU13, Q15</sub>
- **[NEW]** Per-skill perks — bounded labour→combat cross-feed @ new subsection inside §2.7 (after §2.7(c)) — Specify the per-skill perk/flat-bonus track (~2-8 perks or ~3-8 small flat bonuses per skill, unlocked by leveling it), the SEPARATE skillCombatBonus channel (not attribute, not level), stackable with NO hard global cap (bounded by small magnitude + incremental skill unlock + holistic scaling, risk accepted), and conditioning as the zero-stat exception. Replaces the §2.7/§4.5.3 "== 0 no-cross-feed" wall.  <sub>← Q6, FU8</sub>
- **[NEW]** Sequential per-tier progression — rungs (phase 1) → pillar grind (phase 2) @ new subsection bridging §2.15 / §2.16 (e.g. lead-in to §2.16, or new §2.15 closing subsection) — Author the two-phase model: Phase 1 climb the rungs via curated per-rung activities + the numeric per-rung-reset rung-meter + story (AND-gate); Phase 2 the estate-influence/pillar grind unlocks after the final rung and the Q7 hybrid pillar distribution is the real tier-gate. Pillar deeds gated to phase 2. Defines the rung-meter accrual law (threshold = ≥30-min floor × eligible-activity rate).  <sub>← FU7, FU6, FU11, Q30, Q7</sub>
- **[NEW]** Hybrid tier-gate — good / great / excellent @ new subsection inside §2.16 (replacing the simple-threshold gate paragraph) — State the distribution rule (good in ALL revealed pillars, great in 2-3, excellent in 1-2), the semantics (baseline / really-strong / above-and-beyond), the per-tier revealed-pillar set (T0=2 special case, T1=3, T2=3), no overflow-substitution, and the per-pillar-per-tier threshold overhaul against the fixed deed inventory.  <sub>← Q7, FU10</sub>
- **[NEW]** Multi-backend save layer @ new subsection inside §2.19 (after §2.19(c)) — Specify the backend abstraction (IndexedDB + localStorage + sessionStorage), atomic write, app-identity magic field, the monotonic save-counter newest-wins selector (+ save-layer timestamp tiebreaker as a documented core-lint exemption), reject-to-recovery, additive schema, and itch cross-origin-iframe survival. Built full in M0.  <sub>← Q37, FU1, FU2, Q45, Q46, Q44</sub>
- **[NEW]** About / Credits surface @ new subsection inside §2.21 (or a small §2.20 sibling) — Define a small About/Credits surface: authorship, the commit-SHA build stamp, font/audio attributions, clean-room attestation; note the license split (permissive code MIT/Apache-2.0 + reserved game content) and itch content descriptors as the deploy-checklist link (detail in §7).  <sub>← Q54, Q51, Q53</sub>

### §3 — Incremental Unlock Ladder (UI-as-progression) (prd.md lines 1712-2231)

- **[rewrite]** `§3 intro canon-banner (lines ~1730-1744)` — Replace the superseded canon claims. Swap 'simple per-tier required-pillar thresholds (no floor/overflow)' for the Q7 HYBRID tier-gate (good in ALL revealed pillars / great in 2-3 / excellent in 1-2; T0 2-pillar special; NO overflow). Add the SEQUENTIAL two-phase per-tier model (FU7), the THREE clean combat tracks (FU14), INCREMENTAL combat starting with 1 weapon + a growing roster (Q15/FU13), the BOUNDED labour->combat per-skill perks (Q6/FU8), budget = a FLOOR/longer-OSRS-grind (FU18), active-only with tab-open auto-resolve+auto-repeat (FU23), and DESIGN-staggered one-at-a-time reveals/no runtime queue (FU4). Keep up-only + minor recoverable dents but add the Q32 below-high-water self-heal nuance.  <sub>← FU7, Q7, FU10, FU14, Q15, FU13, Q6, FU8, FU18, FU23, FU4, Q32 · ADR</sub>
- **[rewrite]** `§3.0 trigger-taxonomy table (lines ~1757-1762)` — Rewrite the RANK row: a rung advances on the per-rung RUNG-METER (curated per-rung activities, per-rung-reset, FU6) crossing its threshold AND its STORY milestones (Q30 AND-gate) — NOT on raw XP/kills/deeds. Rewrite the PILLAR row: pillar deeds accrue only in PHASE 2 and the PILLAR predicate now gates the TIER-UP (the Q7 hybrid profile), not individual rungs. Add a line naming the three combat-fed tracks (kills/XP->character level; deeds->Arms pillar; curated activities->rung-meter; FU14). Rename 'Combat Standing' -> 'Combat Rank' (Q9).  <sub>← Q30, FU6, FU7, Q7, FU14, Q9 · ADR</sub>
- **[edit]** `§3.0 'Two cross-cutting rules' (lines ~1769-1778)` — Add a third cross-cutting rule for FU4: the unlock SCHEDULE is designed so reveals are inherently one-at-a-time (no runtime reveal-queue — this supersedes the Q17 'serialize into a deterministic queue' framing); genuine multi-element single-feature reveals are bespoke one-offs. Fold in Q17 'no-UI-dumps' (stagger slowly/gently) and Q10 (distinct activities = top-level tabs, not nested panels). Reinforce that the incremental skill-unlock ORDER is what bounds the Q6/FU8 labour->combat cross-feed.  <sub>← FU4, Q17, Q10, Q6, FU8 · ADR</sub>
- **[edit]** `§3.1 cold open (lines ~1782-1797)` — Keep substantially. Confirm framing is consistent with Q11 ('presumed dead -> back from the dead'; jizō/find-spot co-located at the weir — primarily §1/§5, cross-check only). No mechanical change; the dream-fragment stays ZERO-bonus.  <sub>← Q11</sub>
- **[rewrite]** `§3.2 T0 intro paragraph (lines ~1801-1810)` — Reframe 'driven by two meters: Estate Service + Combat Standing' to the per-rung RUNG-METER fed by curated per-rung activities (FU6/FU7), with combat-flavored rungs fed by combat curated activities. State T0 is PHASE 1 (climb R0->R7) and that the four-bar Influence panel reveals at R7 = ENTRY to PHASE 2 (the estate-influence/pillar grind), where pillar DEEDS first accrue. Rename Combat Standing->Combat Rank; macronize 'goshi'->'gōshi'. Estate stage span E0->E1 unchanged.  <sub>← FU7, FU6, FU14, Q9 · ADR</sub>
- **[rewrite]** `§3.2 R3 row (line ~1817)` — De-dump per the FU12 combat-reveal ladder: R3 reveals ONLY the Drill Yard (fractal post->rack->slots), the Combat panel with a BARE auto-resolve loop + RETREAT (Q16 clean escape), the ONE starter weapon (Q15/FU13 — T0 starts with exactly one), Equipment & Inventory, and the Bestiary. REMOVE stance/ability/item-slot from R3 (they reveal later — see the new combat-reveal subsection). Surface the 'eat before you fight' satiety->combat coupling here (Q31/FU16). Frame auto-resolve as the active-only 'leave it running' loop (FU23).  <sub>← FU12, Q15, FU13, Q16, Q31, FU16, FU23, FU4 · ADR</sub>
- **[edit]** `§3.2 R4 row (line ~1818)` — Add the durability-bands reveal at R4 (graded 4-band, never auto-unequip; Q33/FU17) tied to the loot->craft loop. Reframe 'simple Crafting tab' as a TOP-LEVEL nav tab (Q10), revealed as its own beat (FU4). Keep Naoyuki's grudging vouch (seeds the Q26 rivalry->respect arc).  <sub>← FU12, Q33, FU17, Q10, Q26 · ADR</sub>
- **[rewrite]** `§3.2 R5 row (line ~1819)` — REMOVE 'Arms deeds begin accruing here' — pillar deeds are gated to PHASE 2 (FU7); R5 combat feeds character-level + the rung-meter only. Reframe the Quest log as a TOP-LEVEL nav tab (Q10). Reframe 'the four v1 quest types' as the T0 CORE/starter quest set (PEST/HUNT/CLEAR/DEFEND), explicitly NOT a cap — more/varied quests authored later (Q23). Add the stance-slot reveal at R5 (FU12). DEFEND is the future Arms-deed activity but its deeds accrue in phase 2.  <sub>← FU7, Q10, Q23, FU12, FU14 · ADR</sub>
- **[rewrite]** `§3.2 R6 row (line ~1820)` — REMOVE/relocate 'THE FIRST COMBAT-EARNED HOUSE STANDING is recorded / first Arms achievement-jump enters the books' — Arms deeds accrue in PHASE 2 (FU7), not mid-climb. Keep Workshops/Granary + the village-tier-opens + road-out. Add the 2nd T0 weapon-roster unlock here (FU13: T0 unlocks 2 weapons total beyond the starter's line).  <sub>← FU7, FU14, FU13 · ADR</sub>
- **[rewrite]** `§3.2 R7 row (line ~1821)` — Reframe R7 as END OF PHASE 1 / ENTRY TO PHASE 2. Rung gate = rung-meter + story ONLY (drop '+ PILLAR (Arms+Estate thresholds)'). R7 REVEALS the four-bar Influence panel, which OPENS phase 2 (pillar deeds start accruing); the actual T0->T1 TIER-UP is the separate phase-2 Q7 hybrid gate (T0 2-pillar special: Arms + Estate). Rewrite the diegetic line away from 'four standings, rising' (implies already-accrued) toward 'now, raise the house itself' (phase 2 begins).  <sub>← FU7, Q7, FU11, Q30, FU14 · ADR</sub>
- **[edit]** `§3.2 'T0 deliberately withheld' note (line ~1823)` — Keep; ensure consistency with the phase model (pillar grind is phase 2). Confirm coin/market still T1 (Q29), component-crafting T1+, auto-producers T3+ (FU23).  <sub>← FU7, Q29, FU23</sub>
- **[edit]** `§3.2.1 spine, R4->R5 row (line ~1854)` — Change 'Arms deeds begin to accrue' -> the R5 night-watch post is granted but Arms-pillar DEEDS accrue in phase 2 (FU7); R5 combat feeds character-level + the rung-meter. Rename Combat-Standing->Combat Rank.  <sub>← FU7, FU14, Q9 · ADR</sub>
- **[edit]** `§3.2.1 spine, R5->R6 + R6->R7 rows (lines ~1856-1857)` — R7 gate = rung-meter + story (phase 1 complete); move the Arms+Estate PILLAR requirement to the phase-2 tier-up. Reframe the Munenori capstone: he names the bailiff (ends phase 1) and points at raising the house's standing (phase 2) before 'Now: the valley.'  <sub>← FU7, Q7, Q30 · ADR</sub>
- **[edit]** `§3.2.1 prose + table (lines ~1837, 1855)` — Macronize 'goshi'->'gōshi'; rename all 'Combat-Standing'/'Combat Standing' -> 'Combat Rank' (Q9). No structural change.  <sub>← Q9</sub>
- **[keep]** `§3.3 T0 estate rooms (lines ~1865-1888)` — Keep as-is (T0 = E0->E1, untouched by Q8 which adds E3 in T2). Recruit Tokujirō stays a light roster card. Only macron/Combat-Rank renames if any term appears (none in this block).  <sub>← </sub>
- **[rewrite]** `§3.4 T1 intro + two-track block (lines ~1891-1918)` — Add the two-phase model for T1: climb V0->V7 (phase 1; rung-meter + story; pillar deeds NOT accruing) then phase 2 = grind pillars to the T1 hybrid gate (now 3 revealed pillars: Arms/Estate/Office — good in all, great in 2-3, excellent in 1-2). Note the Office BAR may reveal during phase 1 but ACCRUES in phase 2. Add that the mechanical weather/festival ±10% layer first reveals this tier (Q35). Rename Combat Standing->Combat Rank. Keep the village side-track unchanged (still ~10-15% accelerant).  <sub>← FU7, Q7, FU14, Q35, Q9 · ADR</sub>
- **[edit]** `§3.4 V2 row (line ~1924)` — Reframe 'combat-earned Arms standing at valley scale (Arms jump)' — V2 combat feeds character-level + the rung-meter; Arms DEEDS accrue in T1 phase 2 (FU7). Add T1's 2nd weapon-LINE opening (FU12: 2nd line at T1) plus the +3 weapon-roster growth across V-rungs (FU13). Rename Combat Standing->Combat Rank.  <sub>← FU7, FU14, FU12, FU13, Q9 · ADR</sub>
- **[edit]** `§3.4 V4 row (line ~1926)` — Reframe 'the Standing & Office pillar lights for the FIRST time... first Office achievement-jump': the Office BAR reveals here as a story beat (Magobei's skim resolved), but Office DEEDS accrue in T1 phase 2 (FU7). Keep the headman roll-up + rigged-box motif.  <sub>← FU7, Q7 · ADR</sub>
- **[add-new]** `§3.4, a Naoyuki beat woven into V5 or V6 (lines ~1927-1928)` — Add one low-cost interstitial Naoyuki beat (rivalry -> grudging respect) in a T1 rung so the G5 ally-flip reads as earned (Q26). Narrative-only, ZERO mechanical effect.  <sub>← Q26</sub>
- **[rewrite]** `§3.4 V7 capstone row (line ~1929)` — Reframe V7 as end-of-phase-1; drop '+ PILLAR' from the rung gate (rung = rung-meter + story). The T1->T2 TIER-UP is the phase-2 Q7 hybrid gate (Arms+Estate+Office). Keep Region-map open + Tomita/Akagi surfacing.  <sub>← FU7, Q7, Q30 · ADR</sub>
- **[edit]** `§3.4 quest/folklore references + withheld note (lines ~1922-1934)` — Reframe quest-type references so the 4 grind types are the starter set, more/varied quests welcome (Q23). Confirm weather/festival mechanical layer (Q35) is a T1 reveal in the withheld/rumours note. Rename any Combat Standing.  <sub>← Q23, Q35, Q9</sub>
- **[edit]** `§3.4.1 spine, V3->V4 row (line ~1951)` — Change 'the Lord records the house's first Office standing' -> the Office bar reveals; Office DEEDS accrue in T1 phase 2 (FU7). Rename Combat-Standing->Combat Rank across §3.4.1.  <sub>← FU7, Q9 · ADR</sub>
- **[edit]** `§3.4.1 spine, V6->V7 capstone row (line ~1954)` — Tier-up = phase-2 hybrid gate (Arms+Estate+Office); the V7 rung itself gates on rung-meter + the 'clean your room' story beat. Adjust the granter prose accordingly.  <sub>← FU7, Q7 · ADR</sub>
- **[add-new]** `§3.5 nav-reveal table — two new rows (lines ~1974-1983)` — Add 'Crafting' as a TOP-LEVEL nav tab revealed at R4 and 'Quests' as a TOP-LEVEL nav tab revealed at R5 (Q10), each its own one-per-beat reveal (FU4/Q17). These currently appear only as in-rung panels in §3.2 with no nav-track entry.  <sub>← Q10, FU4, Q17 · ADR</sub>
- **[edit]** `§3.5 nav-reveal table — House (R7) + Castle-town (T2->T3) rows (lines ~1979, 1983)` — Reframe the House/Influence screen diegetic line at R7 as entering PHASE 2 ('a page to raise the house itself'). Ensure the Castle-town screen STUB row reads as the Daikan/castle-town first-contact cliff-hanger (Q24), with no porter-guild framing.  <sub>← FU7, Q24</sub>
- **[rewrite]** `§3.6 T2 intro + three-track block (lines ~1991-2033)` — Add the two-phase model for T2: climb G0->G7 (phase 1) then phase 2 = grind to the T2 hybrid gate (now 4 revealed pillars incl. Name; good all/great 2-3/excellent 1-2; no overflow). Office accrual stays a phase-2 emphasis ('win it socially'). Change estate stage span 'E2 Recovering -> early E3' to 'E2 -> E3 (Prosperous)' reflecting the un-parked full E3 stage in v1 (Q8). Rename Combat Standing->Combat Rank. Keep the village + Origin side-tracks as optional accelerants.  <sub>← FU7, Q7, Q8, FU14, Q9 · ADR</sub>
- **[edit]** `§3.6 G1 + G5 rows (lines ~2038, 2042)` — Reframe G1/G5 combat: feeds character-level + rung-meter; Arms DEEDS accrue in T2 phase 2 (FU7). Add T2's 3rd weapon-LINE opening (FU12: 3rd line at T2) + the +4 weapon-roster growth across G-rungs (FU13). Rename Combat Standing->Combat Rank.  <sub>← FU7, FU14, FU12, FU13, Q9 · ADR</sub>
- **[edit]** `§3.6 G2 row (line ~2039)` — Disentangle the rung gate from the Origin-track-open gate: the G2 RUNG promotion gates on rung-meter + story; the ORIGIN side-track OPEN is the doubly-earned condition (STORY dream-memory AND travel-standing) revealed AT G2 (F11/§1.5.3), not a four-pillar gate. Keep substance; reword so the new phase model isn't confused with the travel-standing/Origin gate.  <sub>← FU7, Q5, FU11</sub>
- **[edit]** `§3.6 G4 row (line ~2041)` — Mark the Kuzuhara river-works/resettlement as the E3-stage estate-fabric lever, landing E3 'Prosperous' in v1 (Q8). Keep grief-work/temple-register framing (no rite).  <sub>← Q8 · ADR</sub>
- **[rewrite]** `§3.6 G6 row (line ~2043)` — DELETE the name-reclaim clause from the SPINE diegetic line ('You have a name again: Tahei. You set it down quietly') — the G6 spine carries ONLY the Otsuru truth-reveal (guaranteed). Tahei reclaiming his name is Origin-O5-gated and MISSABLE (Q5/Q25/Q40). In the 'what reveals' column, clarify the name-claim is on the optional O5 track, not guaranteed at G6.  <sub>← Q5, Q25, Q40 · ADR</sub>
- **[edit]** `§3.6 G6 + G7 trigger columns (lines ~2043-2044)` — Rung gates = rung-meter + story; relocate the 'win-the-region' Estate+Office PILLAR requirement to the phase-2 TIER-UP gate (Q7). G7 = end of phase 1; T2->T3 tier-up = phase-2 hybrid gate (4 pillars). Keep rivals-dethroned + castle-town-invites-the-house (Q24 stub).  <sub>← FU7, Q7, Q30, Q24 · ADR</sub>
- **[edit]** `§3.6.1 spine table + prose (lines ~2057-2087)` — Rename Combat-Standing->Combat Rank. Reframe G2->G3..G6->G7 so rung promotions gate on rung-meter + story and the capstone PILLAR requirement is the phase-2 tier-up. Keep the Naoyuki ally-flip at G5->G6 (now earned by the new T1 beat s3-19). Clarify the G2 Origin-open is narrative/travel-standing, not a phase-pillar gate.  <sub>← FU7, Q7, Q9, Q26 · ADR</sub>
- **[edit]** `§3.6.2 Origin ladder hard-rules + O-rows (lines ~2097-2113)` — Keep O5 name-reclaim Origin-gated/missable (already Q5-correct). Rescope the 'ZERO mechanical gift' phrasing to 'no retroactive stat/recipe bonus from the backstory' — the ~10-15% speedup + skill-XP buff stay (Q12). Add Nihonbashi to the fictional allow-list reference if cited.  <sub>← Q5, Q12</sub>
- **[edit]** `§3.7.1 T3 stub sketch (lines ~2140-2168)` — Confirm the Daikan/castle-town first-contact framing (Q24); no porter-guild content. Add a one-line forward note that T3 carries the same two-phase model (FU7). Keep auto-producers at C1 (T3+, FU23) and the marriage/adoption lever at C5.  <sub>← Q24, FU7, FU23</sub>
- **[edit]** `§3.7.2 T4 roadmap sketch (lines ~2170-2197)` — Keep. Note the E#/E0-E5 namespace disambiguation still holds; E3 is now in-v1 (Q8) but E4-E5 stay parked. The national-banzuke authored ending stays; forward two-phase consistency noted.  <sub>← Q8, FU7</sub>
- **[edit]** `§3.8 cross-references & deferrals (lines ~2201-2212)` — Add deferrals: the rung-meter curve/per-rung thresholds (FU6), the Q7 hybrid-gate thresholds per-pillar-per-tier (FU10), the combat-reveal-ladder weapon params + signature abilities (FU13), and the durability bands (FU17) all go to §4. Note Math.pow ban / integer-pow determinism applies to any §3-implied curve (Q36/FU). Keep kan'i resolution note.  <sub>← FU6, FU10, FU13, FU17, Q36</sub>
- **[rewrite]** `§3.9 review checklist (lines ~2214-2231)` — Rewrite for V2. Item 1 (R7 PILLAR vs RANK trigger mix) is now RESOLVED by FU7's two-phase model: rungs gate on rung-meter + story; tier-up gates on the phase-2 Q7 pillar profile. Items 2-5 stay resolved. Add fresh V2 confirm-items: the combat-reveal-ladder cadence (FU12), the phase-2 grind length under FU18 (budget = floor), and the weapon-roster reveal points (FU13).  <sub>← FU7, Q7, FU12, FU18, FU13</sub>
- **[edit]** `global within §3 (all tier tables + §3.0)` — Replace-all 'Combat Standing'/'Combat-Standing' -> 'Combat Rank' and '*goshi*' -> '*gōshi*' (Q9). Add a one-line note at the Skills-tab reveal (R2) that each skill has a perk track revealed by leveling it, and the incremental skill-unlock ORDER is the real bound on the bounded labour->combat cross-feed (Q6/FU8); conditioning stays the zero-stat gate.  <sub>← Q9, Q6, FU8</sub>
- **[NEW]** The two-phase per-tier model (rungs -> the estate-influence grind) + the rung-meter law + the phase-2 tier-gate @ new subsection after §3.0 (before §3.1) — Make FU7 concrete and cross-cutting before the tier tables: PHASE 1 = climb the rungs (R0->R7 / V0->V7 / G0->G7) via CURATED per-rung activity sets (1-to-many, NOT a repeat-counter), tracked by the per-rung RUNG-METER (FU6: per-rung-reset; threshold back-solved from the >=30-min floor x that rung's eligible-activity rate) AND its story milestones (Q30 AND-gate, with an 'awaiting X' state when one lags); pillar DEEDS do NOT accrue here. PHASE 2 = after the final rung, the estate-influence/pillar grind unlocks ('really level up the estate') and pillar deeds start accruing; tier-up requires the Q7 HYBRID profile across revealed pillars (good in ALL / great in 2-3 / excellent in 1-2; T0 2-pillar special; NO overflow; FU10 thresholds -> §4). State the THREE clean combat tracks (FU14) and that the rung-meter is fed by curated activities only (not XP/kills/deeds). Note budget = a FLOOR (FU18) and the active-only auto-resolve/auto-repeat loop (FU23).  <sub>← FU7, FU6, FU11, Q30, Q7, FU10, FU14, FU18, FU23</sub>
- **[NEW]** The combat-reveal ladder (the staggered reveal of combat functionality + the weapon roster) @ new subsection after §3.5 (parallel to the nav-reveal track) — Author the FU12 combat-reveal ladder as its own laddered track (like §3.5 is for nav), so combat never UI-dumps: R3 = 1 starter weapon + bare auto-resolve loop + RETREAT (Q16) -> durability BANDS at R4 (graded 4-band, never auto-unequip; Q33/FU17) -> STANCE slot at R5 -> ABILITY + ITEM slots at the first weapon-line L10 milestone -> 2nd weapon LINE at T1, 3rd at T2 (FU12). Lay out the WEAPON ROSTER growth (Q15/FU13: T0 starts with 1, unlocks +2; T1 +3; T2 +4; ~9-10 across v1) and which rung/tier each reveals, plus the trigger KIND per step (combat rung / character-level / weapon-skill milestone / story). State the satiety->combat coupling (Q31/FU16) and that each step is one-reveal-per-beat (FU4). Numbers/params -> §4.  <sub>← FU12, Q15, FU13, Q33, FU17, Q16, Q31, FU16, FU14, FU4</sub>

### §4 — Combat, Progression & Balance Model (lines 2236–3116)

- **[rewrite]** `§4 STATUS/intro block (2236–2259)` — Re-status from 'DRAFT — awaiting human review / nothing locked' to 'PRD V2 — incorporates the human-signed Block L (Q1–56) + Block M (FU1–23), governed by D-022 (newest-block-wins)'. Critically, edit the 'shapes are canon and NOT tunable' list (2251–2252): REMOVE 'no labour→combat cross-feed' (now a bounded per-skill-perk channel, Q6/FU8) and 'no-floor/overflow gating' (now the hybrid good/great/excellent gate, Q7/FU10) — both former 'canon shapes' are explicitly reversed by V2. Keep trade ≤⅓ and the jump+seasonal accrual shape as still-canon.  <sub>← Q6, Q7, FU8, FU10, D-022 · ADR</sub>
- **[rewrite]** `§4 REBALANCE NOTE (2254–2259)` — The ~28.5h budget is now a FLOOR, not a target/ceiling (FU18): the saga can/should be LONGER — a long OSRS-rough grind, interleaved, never brick-walled; §4.8 becomes a MINIMUM-grind model. The 4.5/8/16h tier figures and the ≥30-min/rung floor are minimums, not caps. Annotates D-016.  <sub>← FU18 · ADR</sub>
- **[edit]** `§4.0 master-scale table + notes (2288–2304, 2296)` — T0 held-koku band: drop the 3–5K-held double-count; restate to ~18–19K NET held using the 'yields-already-net' model (Q14). Update the '~21K produced (~3–5K held)' table cell to '~21K produced → ~18–19K NET held' and the 'end-T0 held koku ≈ a few K' prose to ~18–19K. T3 row already reads 'Castle-town (stub)' per Q24 — keep.  <sub>← Q14, Q24</sub>
- **[add-new]** `new subsection §4.0.1 after §4.0` — Author 'Progression architecture — three tracks, two phases'. THREE independent tracks (FU14): (1) character level ← combat XP/kills only → HP + attribute points (Q1); (2) the rung-meter ← per-rung CURATED activities (a separate authored set, NOT pillar deeds, NOT raw kills); (3) the four influence pillars ← deeds + seasonal. TWO phases per tier (FU7): Phase 1 = climb rungs R0→R7 via rung-meter + story (+ curated activities + throughput); Phase 2 = the estate-influence/pillar grind unlocks AFTER the final rung → the hybrid tier-gate → tier-up. Pillar DEEDS are gated to Phase 2 (prevents 'half the rungs, maxed deeds').  <sub>← FU7, FU14, Q1, Q30 · ADR</sub>
- **[rewrite]** `§4.1 shape paragraph (2317–2326)` — Retitle §4.1 from 'SIMPLE bands — NO floor/overflow' to 'HYBRID good/great/excellent tier-gate (the Phase-2 tier-gate)'. New shape: GOOD in ALL revealed pillars (breadth floor — reverses the old 'no global floor'), GREAT in 2–3, EXCELLENT in 1–2; NO overflow/substitution (this half of the old rule survives). Per-tier revealed-pillar set: T0 = 2 (Arms+Estate) special case = good in both + excellent in 1, no phantom 3rd; T1 = 3 (+Office); T2 = 3. Semantics: good = expected baseline, great = really strong, excellent = above-and-beyond. This is the Phase-2 tier-gate (separate from the per-rung rung-meter+story gate, FU11).  <sub>← Q7, FU10, FU11, FU7 · ADR</sub>
- **[rewrite]** `§4.1 threshold table (2330–2336)` — Replace the single-threshold-per-pillar table with a per-pillar-per-tier GOOD/GREAT/EXCELLENT triple table, re-derived against the fixed deed inventory — NOT simple 0.2×/0.5× ratios (FU10 explicitly rejects ratios). Map the old named threshold to roughly the gate reference; author the triples (T0–T2 ship; T3/T4 stub) in balance.ts. Only the first 3 tiers are load-bearing for v1.  <sub>← Q7, FU10 · ADR</sub>
- **[edit]** `§4.1 framing/derivation/levers notes (2338–2358)` — Rewrite the 'why magnitudes survived', 'reading the drift', and levers prose to the hybrid model and the Phase-1/Phase-2 split: the rung-meter+story gate the rungs; this hybrid pillar distribution is the Phase-2 tier-gate. Drift (Arms+Estate early → Office+Name late) still holds. Levers = the good/great/excellent triples + the great/excellent counts.  <sub>← Q7, FU10, FU11, FU7</sub>
- **[add-new]** `new subsection §4.1.x after §4.1 (the rung-meter law)` — Author 'The rung-meter accrual law' (FU6): per-rung-RESET meter; each rung's threshold = (≥30-min floor × that rung's eligible curated-activity rate), back-solved exactly like the §4.8.1 koku column so meter and time floor stay in lockstep; AND-gate — promote only when meter ≥ threshold AND all story flags set, surfacing 'awaiting X' when one side lags (FU6/Q30). Fed by the per-rung CURATED activity set (separate from the pillar-deed inventory — FU7). Rename the combat rung-meter 'Combat Standing' → 'Combat Rank' (Q9; 'Standing' reserved for the 官威 Standing & Office pillar); 'Estate Service' meter name unchanged.  <sub>← FU6, FU7, Q30, Q9 · ADR</sub>
- **[edit]** `§4.2 accrual-model framing (2362–2380)` — Reframe: pillar accrual (deeds + seasonal) is the PHASE-2 grind (FU7), not a per-rung accrual. Keep the 70/30 deeds/seasonal split (LOCKED). Add a forward-reference to the T2 cross-pillar combos exception (§4.3.1, FU20).  <sub>← FU7, FU20</sub>
- **[rewrite]** `§4.2.1 deed inventory + itemizations + cadence (2382–2453)` — Four interlocking edits: (a) deeds accrue in PHASE 2 only (FU7) — recompute the cadence claim, which is no longer 'one act every ~4.5–5 min across the whole tier' but tier-relative within the phase-2 window (Q20: T0~5/T1~8/T2~13 min); (b) the 70%-of-gate itemizations must re-sum against the new HYBRID gate (good/great/excellent), not the old single threshold (FU10); (c) Q29: re-itemize T0 Estate (~560 ip) with LAND/TREASURY deeds ONLY — DELETE the 6 trade contracts (trade strand opens at T1, village shops = first market); (d) Q23: more/interesting quests welcome (esp. later tiers) — the inventory may grow beyond the 'fixed' set, but every gate still ties out.  <sub>← FU7, FU10, Q29, Q23, Q20 · ADR</sub>
- **[edit]** `§4.2.1 / R5 Arms snapshot note` — Note that Q29's instruction to 'pull Arms-deed onset to R4 so R6/E2 clears on deeds' is SUPERSEDED by FU7 — all pillar deeds (Arms included) are gated to Phase 2, so there is no per-rung Arms-deed onset to fix. Remove the per-rung Arms-deed onset framing.  <sub>← FU7, Q29</sub>
- **[edit]** `§4.2.2 seasonal judged-result model (2455–2579)` — Reconcile with FU7/FU10: (a) the seasonal stream is part of the PHASE-2 pillar grind — clarify the pillar high-water appraisal toward the tier-gate runs during Phase 2 (seasons still tick in Phase 1 for weather/festival flavour but don't accrue pillars); reconcile the 8-season tie-out (which assumed seasons spread across the whole tier) with the phase-2 window; (b) re-derive JUDGE_K = SEASONAL_SHARE·(gate reference) against the new HYBRID gate magnitudes rather than the old single threshold. Keep the high-water-mark rule, sqrt shaping, TIER_REF normalizers, and 30% share.  <sub>← FU7, FU10 · ADR</sub>
- **[edit]** `§4.2.2 per-season hook (cross-ref to §4.2.4)` — Add a reference that the per-season hook also runs the Q32 below-high-water dent-restore branch (a small seasonal restore lifting a dented value toward its untouched high-water WITHOUT advancing high-water).  <sub>← Q32</sub>
- **[edit]** `§4.2.3 trade ≤⅓ cap (2581–2609)` — KEEP the ≤⅓ HARD invariant verbatim. Add two clarifications: T0 has NO trade strand (Q29 — opens at T1); and FU20 cross-pillar combos are computed AFTER the trade clamp and can never breach ⅓ (verifier-proven, §6.6).  <sub>← Q29, FU20</sub>
- **[edit]** `§4.2.4 recoverable dents (2611–2623)` — Add the Q32 explicit below-high-water restore branch: a small seasonal restore regrows a dented `value` toward its untouched `highWater` without advancing highWater ('self-heals within ~1–2 seasons, never a wipe'). Keep DENT_FRACTION=0.10, scripted-only, never touches highWater.  <sub>← Q32</sub>
- **[edit]** `§4.3 pillar conversion weights (2627–2655)` — Three edits: (a) unify the `combatLevel` term to the single combat-fed CHARACTER level (Q1/FU14); (b) the 'no cross-pillar leakage except roll-ups' rule gains a narrow, broadened T2 exception — cross-pillar combos (Q22/FU20, see §4.3.1); (c) note the labour-skill combat bonus (FU8) is a SEPARATE additive channel (§4.5.4), NOT a §4.3 conversion weight — §4.3 still routes deeds→pillars with no skill/level→Influence-direct edge.  <sub>← Q1, FU14, Q22, FU20, FU8 · ADR</sub>
- **[add-new]** `new subsection §4.3.1 after §4.3` — Author 'Cross-pillar combos (T2 anti-slump exception)' (Q22/FU20): BROADER than the lean proposal — multiple pillar pairs / larger magnitude. State which pairs combo, the magnitude, and the proof they don't breach invariants: computed AFTER the trade clamp, kept below trade-≤⅓, EXCLUDED from the hybrid gate-threshold check (a combo can NEVER satisfy 'good in ALL'), bounded within (or as a rebate on) the 70/30 + 0.04 envelope; §6.6 verifier-proven. Pairs with the seasonal-reward-rotation as the dual T2 anti-slump device.  <sub>← Q22, FU20 · ADR</sub>
- **[edit]** `§4.4 attribute system (2659–2688)` — Clarify the cross-feed wall: the no-labour→combat-VIA-ATTRIBUTES wall STAYS (attributes remain combat-fed only; labour skills never grant STR/AGI), BUT a bounded labour→combat channel now exists via per-skill perks (a separate `skillCombatBonus` addend, never an attribute — §4.5.4, Q6/FU8). Unify the level term: the combat-fed CHARACTER level grants +1 attr point / 2 levels + HP (Q1/FU14). Keep no-respec-in-v1 LOCKED.  <sub>← Q6, FU8, Q1, FU14 · ADR</sub>
- **[rewrite]** `§4.5.3 no-cross-feed wall callout box (2739–2749)` — FLIP the '==0 / labour skill can NEVER grant a combat-feed attribute' type-wall into the bounded per-skill-perk model (FU8): every skill (labour + combat) has a perk / flat-bonus track (~2–8 perks each) unlocked by leveling it; labour-skill perks MAY include a small `skillCombatBonus` (a separate additive term — NOT an attribute, NOT character level). Stackable, NO hard global cap; bounded by small per-perk magnitudes + incremental skill unlock + holistic balance (gear/level/attrs/enemy-scaling grow together); risk accepted. Conditioning stays the ZERO-stat enablement gate; big combat power stays combat-only (Q1).  <sub>← Q6, FU8, Q1 · ADR</sub>
- **[add-new]** `new subsection §4.5.4 after §4.5.3` — Author 'Per-skill perk tracks (the bounded labour→combat channel)' (FU8): the data model — each skill carries a perk track; labour perks may grant the small `skillCombatBonus`; magnitudes/cadence (perks unlocked by leveling) as balance.ts levers. The §6.6 verifier flips from asserting the cross-feed edge '== 0' to asserting each perk magnitude is small/bounded (no global ≤CAP, per-perk smallness) — Q28 alignment.  <sub>← FU8, Q28 · ADR</sub>
- **[edit]** `§4.5.3 milestone table + no-respec box (2724–2733, 2751–2755)` — Reconcile the L10 '+1 attribute (combat skills only)' row and the milestone-choice no-respec box with the new perk model: labour-skill milestones may now grant a bounded combat perk (via skillCombatBonus, not an attribute); milestone-choice no-respec rule unchanged (LOCKED).  <sub>← FU8, Q6</sub>
- **[rewrite]** `§4.6.1 combatant model (2773–2782)` — Augment effective stats: attackPower = (weaponBase + 1.2·STR + gearAtk + skillBonus + skillCombatBonus) · satietyRate · durabilityBand; defense gains an armour durabilityBand multiplier. Add satietyRate (FU16), skillCombatBonus (FU8), durabilityBand (FU17) terms. Add the missing satietyMax formula = base + per-level growth off the combat-fed level (Q47/FU21). Unify characterLevel = the combat-fed character level (Q1/FU14).  <sub>← FU16, FU8, FU17, Q47, Q1, FU14 · ADR</sub>
- **[add-new]** `new subsection after §4.6.1 (Satiety→combat throttle)` — Author the satiety→combat throttle (FU16/Q31): a `satietyRate` multiplier on attackPower (a lighter one on attackSpeed), flat above ~0.7·satietyMax → knee to a ~0.5 floor; a SEPARATE combat coefficient from the labour STAMINA_RATE_FLOOR (tune independently); 'adequate satiety' = ≥~0.7, the reference at which the LOCKED 20–35% first-fight win-rate is now measured; bounded so the floor never pushes win-rate below ~15%. Satiety throttles combat ('eat before you fight').  <sub>← Q31, FU16, Q47 · ADR</sub>
- **[add-new]** `new subsection in §4.6 (Graded durability bands)` — Author graded durability bands (Q33/FU17): 4 integer bands on weapon attackPower (75+/50+/1+/0 → ×1.0/0.9/0.75/0.55); FIXED wear per FIGHT (cheap ticks, replay-stable — not per swing); armour bands identically on defense; repair/re-craft restores to max; NEVER auto-unequip / never weaponless (it's an auto-battler). All thresholds/multipliers in balance.ts.  <sub>← Q33, FU17 · ADR</sub>
- **[rewrite]** `§4.6.5 on-kill rewards (2812–2818)` — Clean THREE-track separation (FU14): one kill writes (1) combat XP → CHARACTER level (HP + attr points) via mobLevel·COMBAT_XP_K; (2) weapon-skill XP → the equipped weapon line. It does NOT bump the rung-meter (per-rung curated activities do) and does NOT accrue a pillar deed EXCEPT a *recognized* clear/defend → Arms (Phase 2). mobLevel = an explicit per-mob `MobDef.level` field, defaulting ~ the dangerRing's expected character-level (FU15). Rewrite to stop conflating level with any deeds pool.  <sub>← FU14, FU15, Q1 · ADR</sub>
- **[edit]** `§4.6.6 mediocre-start & soft-setback (2820–2839)` — (a) First-fight win-rate 20–35% now tagged 'at adequate satiety (≥~0.7)' (Q31/FU16); (b) the 'no labour conditioning bonus enters these numbers' line: conditioning stays the zero-stat gate, but a small bounded skillCombatBonus now exists (FU8) — first-fight numbers measured without heavy perk stacking; (c) keep the soft-setback shape LOCKED; (d) cross-ref the new win-rate-bands sub.  <sub>← Q31, FU16, FU8, Q16 · ADR</sub>
- **[add-new]** `new subsection after §4.6.6 (Win-rate bands)` — Author 'Win-rate bands (the 2nd pacing proxy)' (Q16/FU19): target headless win-rates at R3/V2/V5/G1/G5 — fresh-at-rung humbling (~30–45%, R3's 20–35% the anchor) and comfortable after that rung's intended training/gear (~80%+), tightening slightly per tier; wired into the M3/M6 fun-proxy harness alongside the pacing floor. Tune-later magnitudes.  <sub>← Q16, FU19</sub>
- **[add-new]** `new subsection near §4.6.6 (Retreat resolution)` — Author retreat resolution (Q16): retreat = clean escape — keep HP/loot, modest clock cost, NEVER dents Influence; abandoning a DEFEND/defence-deed counts as a failed defend (the only Influence movement on a loss is the scripted Arms dent, §4.2.4).  <sub>← Q16</sub>
- **[add-new]** `new subsection in §4.6 (Weapon roster + combat-reveal ladder)` — Author 'Weapon roster & archetype params + the combat-reveal ladder' (FU13/FU12/Q15): T0 STARTS with 1 weapon, then unlocks T0 +2 / T1 +3 / T2 +4 (~9–10 weapons across v1); 3 archetype LINES (spear/sword/staff) each with §4.6 params (baseSpeed / reach / targetCount) + a signature ability; reveal ladder = R3 (1 weapon + bare auto-resolve + retreat) → R4 durability bands (with loot→craft) → R5 stance slot → first weapon-L10 milestone (ability + item slots) → 2nd line at T1 → 3rd line at T2. One reveal per beat (FU4/Q17 design-staggered, no UI dump).  <sub>← FU13, FU12, Q15 · ADR</sub>
- **[edit]** `§4.7.1 gathering/labour yields (2850–2882)` — (a) Yields are 'already net' (Q14) — reconcile the throughput tie-out: lifetime-produced ≈21K → ~18–19K NET held (not 3–5K); (b) pin the labour satietyRate/STAMINA_RATE_FLOOR (FU21: flat above ~0.7·satietyMax → ~0.5 floor; modest drain/action; rest refills); (c) add a bounded ±10% MECHANICAL weather/festival modifier (Q35) derived from a day-keyed STATELESS `deriveDayKeyed(seed,'weather',day)` helper (FU3) — not persisted.  <sub>← Q14, FU21, Q35, FU3</sub>
- **[edit]** `§4.7.4 auto-producers (2900–2909)` — Reaffirm auto-producers are T3+ ONLY and ACTIVE-ONLY (no offline accrual). Add the FU23 note: v1's 'leave it running, check progress' feel comes from TAB-OPEN auto-resolve combat + auto-repeat labour (active-only), NOT an offline layer.  <sub>← FU23</sub>
- **[edit]** `§4.7.5 building/upgrade costs (2911–2927)` — Extend the estate ladder E0→E2 to E0→E3 (Q8): un-park E3 'Prosperous' and add its stage row with koku/material cost + Estate/Arms pillar floor + rank floor. Per FU18, E3 folds into the later G-rungs as an extra koku/Arms sink (NO new rung). Update the affordability tie-out for the ~18–19K NET held figure.  <sub>← Q8, FU18, Q14 · ADR</sub>
- **[edit]** `§4.7.2/§4.7.3 + market notes` — Coin/market numbers (koku↔coin spread, one sink, MarketState) are M4 PLACEHOLDERS, not frozen (Q13); resource counts stay unbounded (strike any '+caps'). Bulk sales apply the saturation damper PROGRESSIVELY per-unit (Q42 — each unit walks the price down; legible, un-gameable).  <sub>← Q13, Q42</sub>
- **[rewrite]** `§4.8 intro + §4.8.0 headline beats (2931–2957)` — Reframe the whole budget as a FLOOR not a ceiling (FU18): 4.5/8/16h and the ≥30-min/rung floor are MINIMUMS; the saga can be LONGER (OSRS-rough grind, interleave, don't brick-wall); §4.8 = a minimum-grind model. Add the win-rate-bands beat (Q16/FU19) to the headline table; tag the first-fight win-rate 'at adequate satiety' (Q31). Mark time-to-T1/T2 as floors.  <sub>← FU18, Q16, FU19, Q31 · ADR</sub>
- **[rewrite]** `§4.8.1 T0 rank-by-rank table (2959–2993)` — Reshape for FU7 sequential phases: the rung table is PHASE 1 — each rung's 'gating cost to leave it' becomes the rung-meter threshold (FU6) + story flags + the per-rung curated activity + throughput, NOT accumulating pillar deeds. Remove the per-rung Estate/Arms deed entries. Add a PHASE-2 block (after R7): the pillar-deed + seasonal grind to the hybrid gate (good in Arms+Estate + excellent in 1). Redo the totals: tier time = phase-1 rungs + phase-2 grind ≥ the 4.5h FLOOR. Rename 'Combat Standing x1/x2' → 'Combat Rank x1/x2' (Q9).  <sub>← FU7, FU6, FU10, Q30, Q9, FU18 · ADR</sub>
- **[rewrite]** `§4.8.2 (T1) + §4.8.3 (T2) pacing tables (2995–3042)` — Apply the same FU7 phase split (phase-1 rungs via rung-meter/story + curated activities; phase-2 pillar grind to the hybrid gate). Budget-as-floor (FU18). T2 weaves in the cross-pillar combos anti-slump (FU20/Q22) and the E3 sink (Q8). Macronize 'ronin' → 'rōnin' and any gōshi/rōnin forms (Q9). Office 25× step retained.  <sub>← FU7, FU18, FU20, Q8, Q9, FU6, FU10 · ADR</sub>
- **[edit]** `§4.8.4 budget-at-a-glance (3044–3059)` — Restate the per-tier and v1-total hours as FLOORS/minimums (FU18), and note the tier total now = phase-1 rungs + phase-2 grind (FU7). Keep the ≥30-min floor and ≤2–3× never-balloon rule as binding constraints.  <sub>← FU18, FU7</sub>
- **[rewrite]** `§4.9 levers index + HARD INVARIANTS + LOCKED lists (3063–3113)` — (a) ADD levers: rung-meter per-rung thresholds, the good/great/excellent triples + great/excellent counts, per-skill perk magnitudes/cadence, satietyRate coeff, durability band multipliers, MobDef.level defaults, weapon archetype params (baseSpeed/reach/targetCount + signatures), win-rate bands, combo pairs/magnitude, E3 cost. (b) REWRITE the HARD INVARIANTS list (3094–3098): 'simple thresholds, no floor/overflow' → hybrid good/great/excellent (breadth floor, no overflow); 'no labour→combat cross-feed' → bounded per-skill perks (wall relaxed, Q6/FU8); reaffirm active-only/no-offline (FU23). (c) Update the LOCKED list + 'do-not-re-open' note: budget is now a FLOOR (FU18); the V2 Block-L/M decisions supersede (D-022).  <sub>← Q7, FU10, Q6, FU8, FU18, FU23, D-022, FU6, FU16, FU17, FU13, FU15, Q16, FU19, FU20, Q8 · ADR</sub>
- **[edit]** `§4.1 / §4.5.4 / §4.9 (verifier alignment)` — Align §4 with the Q28-hardened §6.6 verifier: gate-monotonicity/ceiling now checks the hybrid triples + the rung-meter thresholds; accrual tie-out checks deeds sum to the (new hybrid) gate share within tolerance; the cross-feed assertion flips from '== 0' to 'each perk magnitude bounded/small' (FU8); combos excluded from the gate-threshold check (FU20).  <sub>← Q28, FU8, FU10, FU20, FU6</sub>
- **[edit]** `§4.0 and throughout §4 (illustrative tags)` — Tag the duplicated/derived balance numbers 'illustrative — see generated (balance.ts / docs/balance/, docs/content/)' and align the gen-doc paths (Q41), so no value is hand-maintained twice.  <sub>← Q41</sub>
- **[NEW]** §4.0.1 Progression architecture — three tracks, two phases @ after §4.0 — Master reframe: 3 independent tracks (character level←combat XP; rung-meter←per-rung curated activities; influence pillars←deeds+seasonal) and 2 phases per tier (Phase 1 climb rungs; Phase 2 pillar grind to the hybrid gate; deeds gated to Phase 2).  <sub>← FU7, FU14, Q1, Q30</sub>
- **[NEW]** §4.1.x The rung-meter accrual law @ after §4.1 — FU6 rung-meter: per-rung-reset, threshold = ≥30-min floor × eligible-activity rate (back-solved like the koku column), AND-gate with story ('awaiting X'), fed by the separate curated per-rung activity set; rename Combat Standing→Combat Rank (Q9).  <sub>← FU6, FU7, Q30, Q9</sub>
- **[NEW]** §4.3.1 Cross-pillar combos (T2 anti-slump exception) @ after §4.3 — FU20/Q22 broader combos: pillar pairs, magnitude, computed post-trade-clamp, below trade-≤⅓, excluded from the gate check, verifier-proven; the dual T2 anti-slump device with the seasonal-reward-rotation.  <sub>← Q22, FU20</sub>
- **[NEW]** §4.5.4 Per-skill perk tracks (the bounded labour→combat channel) @ after §4.5.3 — FU8 data model: per-skill perk tracks, labour perks may grant a small skillCombatBonus (separate addend), stackable, no global cap, small magnitudes, risk accepted; verifier flipped ==0 → bounded.  <sub>← FU8, Q6, Q28</sub>
- **[NEW]** §4.6.x Satiety→combat throttle @ after §4.6.1 — FU16/Q31: satietyRate multiplier on attackPower (lighter on attackSpeed), flat >~0.7→~0.5 floor; separate combat coefficient; 'adequate satiety' = the win-rate reference; bounded ≥~15%.  <sub>← Q31, FU16, Q47</sub>
- **[NEW]** §4.6.x Graded durability bands @ in §4.6 (after the satiety throttle) — Q33/FU17: 4 bands on weapon attackPower (75+/50+/1+/0→1.0/0.9/0.75/0.55), fixed wear per fight, armour bands on defense, repair restores, never auto-unequip.  <sub>← Q33, FU17</sub>
- **[NEW]** §4.6.x Weapon roster & archetype params + combat-reveal ladder @ in §4.6 (near §4.6.5/§4.6.6) — FU13/FU12/Q15: T0 starts 1 weapon → +2/+3/+4 per tier (~9–10); 3 archetype lines w/ params + signatures; reveal ladder R3→R4→R5→weapon-L10→T1 2nd line→T2 3rd, one per beat.  <sub>← FU13, FU12, Q15</sub>
- **[NEW]** §4.6.x Win-rate bands (2nd pacing proxy) @ after §4.6.6 — Q16/FU19: target headless win-rates at R3/V2/V5/G1/G5 (fresh ~30–45%, trained ~80%+), wired into the fun-proxy harness.  <sub>← Q16, FU19</sub>
- **[NEW]** §4.6.x Retreat resolution @ near §4.6.6 — Q16: retreat = clean escape (keep HP/loot, modest clock cost, never dents Influence); abandoning a defence-deed = a failed defend.  <sub>← Q16</sub>
- **[NEW]** §4.8.1b Phase-2 pillar-grind block (per tier) @ within/after §4.8.1 (and mirrored in §4.8.2/§4.8.3) — FU7: after the final rung, the pillar-deed + seasonal grind to the hybrid tier-gate; the tier total = phase-1 rungs + phase-2 grind ≥ the per-tier FLOOR (FU18).  <sub>← FU7, FU18, FU10</sub>

### §5 — Act-by-Act Narrative & Content (prd.md lines 3102-3888)

- **[edit]** `§5 T0.1 Overview (line 3124); also T2.3 (line 3527)` — Macronize 'Kurosawa *goshi* house' -> '*gōshi*' and run a gōshi/rōnin macron sweep across §5 (rōnin already correct at 3525/3632). Pure romanization fix.  <sub>← Q9</sub>
- **[edit]** `§5 T0.1 felt-arc / 'Combat is live this tier' (lines 3136-3138)` — Reframe 'the drill yard, Combat panel, Bestiary, the first fight all surface inside T0' from an all-at-once statement to INCREMENTAL combat reveal: combat is live this tier but its UI/systems surface one beat at a time (starter weapon + bare auto-resolve loop + retreat at the first-fight beat; durability/stance/slots/Equipment/Inventory/Bestiary stagger later) — no UI-dump. Daily texture stays peaceful-labour dominant (keep).  <sub>← Q15, FU12, FU13, Q17</sub>
- **[edit]** `§5 T0.1 transition gate (lines 3130-3133)` — Encode the FU7 two-phase shape: Phase 1 = climb the R-rungs via curated per-rung activities + the rung-meter + story; Phase 2 = the estate-influence/pillar grind unlocks AFTER the final rung -> the T0->T1 tier-up. Pillar DEEDS are gated to phase 2 (not accruing across the rung climb). Recast 'Required pillars: Arms + Estate' as the hybrid tier-gate (T0 = the 2-pillar special case: good in both revealed pillars + excellent in 1; NO overflow) rather than flat thresholds. Keep Estate stage span E0->E1.  <sub>← FU7, FU6, FU14, Q30, Q7, FU10 · ADR</sub>
- **[edit]** `§5 T0.2 beats 4-5 (lines 3155-3162)` — De-dump the combat reveal. Beat 4: keep the improvised carrying-pole as the 0th weapon for the humbling fight. Beat 5 ('Begging for drills'): reveal ONLY the drill yard + Combat panel + the FIRST REAL starter weapon (a spear, per FU13) + the bare auto-resolve loop + retreat — strike the simultaneous 'Equipment/Inventory, and the Bestiary' dump. Spread Equipment/Inventory + durability bands to R4 (loot->craft), stance to R5, ability/item slots to the first weapon-line L10 milestone. Note T0 then unlocks +2 weapons total (starts with the one).  <sub>← Q15, FU12, FU13, Q17</sub>
- **[edit]** `§5 T0.2 beats 7-8 (lines 3169-3176)` — Apply the FU14 three-clean-tracks separation + FU7 phase-gating: the gate-watch / pest-clears advance the Combat-Standing rung-meter (phase 1) and feed character level via kills — NOT the Arms pillar. Reword 'Your Arms deeds begin to accrue' and 'the first combat-earned Arms standing is now recorded' so combat in the rung climb raises Combat Standing (rung-meter), while the Arms PILLAR deeds (the influence economy) open only in the phase-2 estate-grind. Keep the diegetic 'the gate held, and the books say so' framing.  <sub>← FU14, FU7, Q1, Q30 · ADR</sub>
- **[edit]** `§5 T0.4 roster (line 3205)` — Rename field-lad 'Mago' to an invented non-colliding name (collides with antagonist Magobei and the porter's-knot homophone). Update any downstream porter's-knot-beat references to the groom/field-lad accordingly.  <sub>← Q11, Q39, Q28</sub>
- **[edit]** `§5 T0.2 beat 1 (line 3147), beat 3 (line 3154), T0.7 porter's-knot (line 3266)` — Q12 rescope of the T0 'ZERO bonus' clue-tags: clarify the dream-fragment and the porter's-knot identity clue carry no RETROACTIVE stat/recipe gift FROM THE REVEAL ITSELF — distinct from the (later-opening, T2) optional Origin accelerant. Keep the clues as identity flavour; reword 'ZERO bonus' to 'no stat gift from the clue' for consistency with the rescoped Origin-track language.  <sub>← Q12</sub>
- **[keep]** `§5 T0.3 antagonist 'The Inherited Debt' (lines 3182-3199)` — No V2 decision touches the T0 villain-less debt arc (rigged-box explicitly ABSENT in T0; no conspiracy). Keep as-is.  <sub>← </sub>
- **[edit]** `§5 T1.1 Overview + transition gate (lines 3291-3299)` — Encode the FU7 two-phase shape for T1: Phase 1 = climb V0->V7 via curated per-rung activities + rung-meter + story; Phase 2 = estate-influence/pillar grind after V7 -> the T1->T2 tier-up; pillar deeds phase-2-gated. Recast 'Required pillars: Arms + Estate, first Office' as the hybrid gate across the 3 revealed pillars (good in all; great in 2-3; excellent in 1-2). Keep Estate stage span E1->E2.  <sub>← FU7, FU6, FU14, Q30, Q7, FU10 · ADR</sub>
- **[edit]** `§5 T1.2 beats 2-3 (lines 3306-3312)` — Apply FU14: 'combat keeps pace clearing valley pests' and 'combat-earned Arms standing at valley scale' -> combat clears advance Combat Standing (the rung-meter, phase 1) + character level via kills; reserve Arms PILLAR deeds for the phase-2 estate-grind.  <sub>← FU14, FU7</sub>
- **[add-new]** `§5 new beat in T1.2 (interstitial after beat 5/6) + a Naoyuki entry in T1.5` — Author ONE low-cost Naoyuki 'rivalry -> grudging respect' beat in T1/early-T2 (e.g. he fights alongside the MC putting out a valley fire / securing a road and grudgingly acknowledges him), bridging the T0 seed and the G5 rival->ally flip so the flip reads as earned. Add a matching Naoyuki line to the T1.5 NPC beats.  <sub>← Q26</sub>
- **[edit]** `§5 T1.2 allegiance note (lines 3331-3335); also the multi-route resolutions in T0.3/T1.3/T2.3` — Note that the Tama-vs-farmhand leaning and the multi-route antagonist (Peaceful/Assertive/Partial) choices are authored as intra-line dialogue choices (choices[]/ChoiceId, content/dialogue.ts; data not scripting; deterministic; only chosen-flags persist). Light cross-ref; data shape stays §2/§6's job.  <sub>← Q34, FU22</sub>
- **[edit]** `§5 T1.5 (line 3375), Market/Shop Row (line 3395), T3.7 'keep ... as seed' (line 3759)` — Rename 'Obaa Sato' (herbalist/wise-woman, village folklore-keeper) to an invented name off the Sayo near-rhyme; update all three references. Locked names unchanged.  <sub>← Q39</sub>
- **[edit]** `§5 T1.6 Areas — 'Boundary-Stone / Jizō' + 'The River, Ford & Weir' (lines 3399-3401)` — Q11 ONE find-spot: co-locate the boundary-stone/jizō AT the weir/ford so 'where he was found' is a single place (merge the two currently-separate 'where he was found' entries into one co-located weir-jizō find-spot).  <sub>← Q11</sub>
- **[edit]** `§5 T1.7 boundary-stone offering (lines 3429-3430) + belief->cause table row (line 3447)` — Re-site the lone residual-ambiguity token (the unidentified-hand offering) at the co-located weir-jizō find-spot; keep it as the ONE capped ambiguous token (≤1 cap holds).  <sub>← Q11</sub>
- **[edit]** `§5 T0.7 intro (line 3263), T1.7 intro (line 3417), T2.7 intro (line 3642)` — Q23 no-quest-budget: remove any lean/capped framing; add a one-line note that there is NO fixed quest-type budget — author whatever quests fit each stage, with more/interesting quests welcome especially at later tiers; the '4 grind types' is just the T0 starting set, not a cap.  <sub>← Q23</sub>
- **[add-new]** `§5 T1.7 and T2.7 side-quest lists` — Author additional example side-quests at the later tiers to demonstrate Q23's 'more/interesting quests welcome esp. later tiers' (the concrete quest set is fleshed out in build; §5 lists representative additions). Keep all optional, none gate the spine.  <sub>← Q23</sub>
- **[edit]** `§5 T2.1 Overview stage span (line 3468) + T2.4 header/buildings/parked note (lines 3557, 3569, 3576-3577)` — Q8 author E3 for v1: change 'E2 Recovering -> early E3' to 'E2 Recovering -> E3 Prosperous'; update the T2.4 'Stages E3–E5 ... are parked' note to 'E3 Prosperous is authored in v1; E4–E5 parked'. Estate now grows E0->E3 in v1.  <sub>← Q8, FU18 · ADR</sub>
- **[edit]** `§5 T2.1 Overview + T2.2 preamble (lines 3462-3469, 3475-3485)` — Encode the FU7 two-phase shape for T2: Phase 1 = climb G0->G7 via curated per-rung activities + rung-meter + story; Phase 2 = estate-influence/pillar grind after G7 -> the T2->T3 tier-up; pillar deeds phase-2-gated. Recast 'Required pillars: Estate + Office rising; Arms secures roads' as the hybrid gate (good in all 3 revealed pillars; great in 2-3; excellent in 1-2).  <sub>← FU7, FU6, FU14, Q30, Q7, FU10 · ADR</sub>
- **[edit]** `§5 T2.2 beat 7 'The lost-child truth resolves (alliance-broker)' (lines 3507-3512)` — Q5 climax split: the lost-child TRUTH (Otsuru; 'the MC is not her') stays SPINE-GUARANTEED at G6 for every player. Move the Origin reunions + Tahei reclaiming his true name OFF the spine onto the OPTIONAL, MISSABLE Origin O5 capstone — delete the guaranteed G6 name-flavour line ('Tahei claims his true name' is no longer guaranteed here). Beat 7 spine = only the Otsuru truth; the name-reclaim is the Origin payoff.  <sub>← Q5 · ADR</sub>
- **[edit]** `§5 T2.2 'dream rule (binding) at payoff' note (lines 3515-3520)` — Q12 rescope: the returning MEMORY grants narrative ACCESS only and no RETROACTIVE stat/recipe gift — but the optional Origin side-track itself is a ~10–15% accelerant + skill-XP buff (per §1.5.4/§2.15), framed as a present-day relationship reward, NOT a gift from remembering. Reword 'ZERO mechanical bonus' so it scopes to the reveal, not the track.  <sub>← Q12, Q5</sub>
- **[edit]** `§5 T2.5 Origin cast — Jinpachi (lines 3582-3588), Oyuki (line 3590), Denbei (lines 3593-3594)` — Q11 'presumed dead -> back from the dead': reword Jinpachi's 'grieved him as away/lost' and Oyuki's 'grieved him' to 'presumed him dead' so the reunion reads as 'back from the dead' (parallels Otsuru + the kamikakushi master-lie). Q12: rescope the Jinpachi/Denbei porter's-knot 'ZERO bonus' tags (no retroactive gift from the reveal; the Origin accelerant is the separate side-track reward). Keep Jinpachi's reunion warm and un-stacked (not a third debt-bondage arc).  <sub>← Q11, Q12</sub>
- **[keep]** `§5 T2.2 beat 5 + T2.5 Kuzuhara cast note + Items-flagged #4 (lines 3498-3503, 3605-3609, 3878-3882)` — Kuzuhara has NO personal tie to the MC and NO survivor/co-investigator character — already correct per canon (origin uncovered via dream -> Sawatari-juku family). No change.  <sub>← Q11</sub>
- **[edit]** `§5 T2.7 'The origin reunions' (lines 3644-3647)` — Q5: state the origin reunions are the OPTIONAL Origin O5 side-track (missable; the Tahei name-reclaim lands HERE, not on the spine), while the Otsuru truth is the guaranteed spine payoff. Q12: rescope 'access-only, ZERO gift' to 'the reveal carries no retroactive gift; the track is the optional ~10–15% accelerant; never gates the spine'.  <sub>← Q5, Q12</sub>
- **[rewrite]** `§5 T3 header box 'STUB in v1' (lines 3666-3672)` — Q24: rewrite the stale stub framing. The single v1-buildable T3 node = the CASTLE-TOWN / DAIKAN first-contact (the sealed summons + presenting credentials at the Daikan's Office + the bare rented townhouse beachhead mirroring the T0 kura cold-open). DELETE the 'Kaidō Porters' & Transport Guild first-contact' as the stub (it re-ran spent T2 content). The T2 capstone opens onto this one castle-town teaser, then ends.  <sub>← Q24 · ADR</sub>
- **[edit]** `§5 T3.2 Main-quest beats (lines 3686-3704)` — Align with Q24: mark beats 1-2 (the summons + the townhouse beachhead) as the v1-BUILT stub node and beats 3-7 as authored-forward (not built in v1). The forward beats already describe the Daikan/castle-town arc — keep, but flag the build boundary at beat 2.  <sub>← Q24</sub>
- **[edit]** `§5 T3.3 (line 3710), T3.5 'Clerk Naozane' (line 3732), T3.3 reveal chain (line 3712)` — Rename 'Clerk Naozane' (the reachable conscience / documentary-proof clerk) to an invented name off the Naoyuki collision; update all three references. Locked names unchanged.  <sub>← Q39</sub>
- **[edit]** `§5 T3.1 stage span (line 3684), T3.4 header (line 3720), T3.7 'Parked — later' (lines 3756-3757)` — Q8: with E3 now reached in v1's T2, T3-forward starts at E3 already attained; change the 'Parked — later ... estate stages E3–E5 in full' to 'E4–E5' (E3 un-parked). T3.1 'E3 Prosperous -> E4 Fortified Seat' stays as the forward span.  <sub>← Q8</sub>
- **[edit]** `§5 T4.1 (line 3771), T4.2 beats 1/3/5/6 (lines 3787, 3790, 3796, 3798), T4.3 (line 3811), T4.4 (line 3818)` — Q27: swap the real surnames for invented ones — 'Junkenshi Toyama Saemon-no-jō' (the touring inspector) and 'Rusui Konoe Settsu-no-suke' (the capital pen-pal proxy). Keep the generic offices/titles (junkenshi / rusui / Settsu-no-suke style honorific). Prevents the §6.6 real-name lint recurrence.  <sub>← Q27, Q28</sub>
- **[edit]** `§5 T4.5 epilogue tableau (lines 3829-3843)` — Q5: make the 'a true name — Tahei' element of the epilogue CONDITIONAL on completing the optional Origin O5 track (missable). A player who skips Origin still gets the warm restored-and-ranked ending and KNOWS the Otsuru truth, but has not reclaimed 'Tahei'. The name-reclaim is the Origin payoff, not a guaranteed ending element.  <sub>← Q5 · ADR</sub>
- **[edit]** `§5 cross-references — 'Numbers belong in §4' (lines 3849-3852)` — Expand the deferral list to name the new V2 §4 surfaces §5 defers to: the rung-meter accrual curve (FU6), the three combat tracks + character-level XP curve (FU14/Q1), the hybrid good/great/excellent gate thresholds (FU10/Q7), the E3 cost row (Q8), graded durability bands (FU17), the satiety->combat term (FU16), and the T2 cross-pillar combos (FU20). §5 keeps only the story shape.  <sub>← FU6, FU14, FU10, Q8, FU17, FU16, FU20</sub>
- **[edit]** `§5 cross-references — 'Rung copy belongs in §3' (line 3853)` — Note that §3 now also owns the staggered combat-reveal ladder (FU12) and the per-rung curated-activity sets (FU7); §5 fixes only the story shape of each ladder and the phase-1/phase-2 narrative arc.  <sub>← FU12, FU7</sub>
- **[edit]** `§5 'Items flagged for the human' #5 (lines 3883-3885)` — Update 'the chosen T3 stub node (Porter-Guild first-contact)' to the castle-town/Daikan first-contact (per Q24); update the parked/cut lists to reflect E3 un-parked (E4–E5 parked) per Q8.  <sub>← Q24, Q8</sub>
- **[edit]** `§5 T2.6 Areas — 'The Kaidō Porters' & Transport Guild' (lines 3618-3619)` — Clarify the porter/transport guild remains a T2 trade-backbone node (Kanta's favour run / sekisho pass-tiers / route-risk) and is NOT the T3 cliff-hanger stub (the stub is now the castle-town/Daikan first-contact per Q24).  <sub>← Q24</sub>
- **[keep]** `§5 conditioning-gating framing (T0.6/T1.6/T2.6 'conditioning-gated'); first-fight narration (T0.2 beat 4)` — Conditioning stays the zero-stat enablement gate (Q6/FU8) — the per-skill perk cross-feed and the satiety->combat throttle are §4 mechanics that do not change §5 narration. The humbling first fight 'survives by luck not skill' stays valid (the 20-35% win-rate is now measured 'at adequate satiety' — a §4 number, not §5 text).  <sub>← Q6, FU8, Q31, FU16</sub>
- **[edit]** `§5 T1.6 mob roster FOUND-gear (line 3411), T2.6 FOUND blades (line 3632)` — Light note that the growing weapon roster (FU13: T0 +2 / T1 +3 / T2 +4) surfaces partly as the FOUND-gear flavour already present (worn kodachi, fitted yari), with the new weapon LINES unlocking one per tier (sword at T1, staff at T2); §5 keeps the FOUND-gear flavour, the full roster/params live in §2/§3.  <sub>← FU13, Q15</sub>
- **[NEW]** Progression shape — climb the rungs (Phase 1), then the estate-influence grind (Phase 2) @ A short binding callout added to each tier Overview (T0.1, T1.1, T2.1) — Encode the FU7 sequential per-tier model in narrative terms: Phase 1 = climb the rungs via curated per-rung activities + the rung-meter + story; Phase 2 = the estate-influence/pillar grind unlocks ONLY after the final rung, then drives the tier-up. State that pillar DEEDS are gated to Phase 2 (preventing 'half the rungs but maxed deeds'). This is the story-shape; the curves/thresholds are §3/§4's job.  <sub>← FU7, FU6, Q30, FU14</sub>
- **[NEW]** Naoyuki T1/early-T2 interstitial beat (rivalry -> grudging respect) @ New beat in T1.2 (main-quest interstitial) with a paired entry in T1.5 (key NPC & dialogue beats) — Bridge the T0 rivalry seed and the G5 rival->ally flip with one low-cost beat so the conversion reads as earned (Q26). Keep it lean: a shared danger where Naoyuki grudgingly acknowledges the MC.  <sub>← Q26</sub>
- **[NEW]** Origin track & the missable name-reclaim (binding note) @ A binding note attached to T2.2 (beat 7 area) / T2.5 — Lock the Q5 climax structure in one place: the Otsuru/Tama TRUTH is spine-guaranteed at G6 for every player; the Origin reunions + Tahei reclaiming his true name are the OPTIONAL, MISSABLE Origin O5 capstone; the Origin track is an optional ~10–15% accelerant + skill-XP buff (Q12 rescope — no retroactive gift from the memory itself). Disambiguates spine vs side-track and the conditional epilogue.  <sub>← Q5, Q12</sub>

### §6 — Tech Architecture & Data Model (lines 3873–4341)

- **[edit]** `§6.0 intro (lines 3889–3896)` — Update the one-line save-model description from 'IndexedDB + base64, versioned minimal-state, migrations' to 'MULTI-BACKEND redundant save (IndexedDB + localStorage + sessionStorage), atomic write, app-identity magic field, additive backwards-compatible schema, monotonic-counter newest-wins selector'. Also add to the §6 scope list: the three combat tracks, intra-line dialogue data model, and the bounded per-skill cross-feed.  <sub>← Q37, FU1, Q45, Q44, Q46, Q34, Q6, FU8</sub>
- **[edit]** `§6.0 ADR note (lines 3898–3899)` — §6 is no longer ONLY an 'elaboration of D-013'. Note it now also IMPLEMENTS the V2 decisions under D-022 (governing, newest-wins): multi-backend save (Q37), additive schema (Q45), RNG per-named-stream cursors (Q2), Math.pow ban (Q36), the bounded labour->combat verifier flip (Q6), the feature-rich data model incl. intra-line dialogue & subEngines (Q34), and the no-runtime-reveal-queue design rule (FU4). These are real reversals/new locks, not just buildable detail.  <sub>← D-022, Q37, Q45, Q2, Q36, Q6, Q34, FU4 · ADR</sub>
- **[edit]** `§6.1 ESLint rules (lines 3918–3925)` — Add a new lint rule: BAN Math.pow / Math.exp / Math.log / trig in core (whitelist Math.sqrt); all growth-curve powers use integer-power-by-repeated-multiplication. Rationale: byte-identical cross-engine replay + portable exported saves (kills the 1-ULP hole).  <sub>← Q36 · ADR</sub>
- **[edit]** `§6.1 pure-core boundary lint (lines 3921–3923)` — Keep Date.now/performance.now BANNED in src/core/, but document an explicit SAVE-LAYER exemption: persistence/ may call Date.now for the save-layer timestamp used only as the newest-wins TIEBREAKER (metadata, not game logic) — the deterministic core stays clock-free. Name this as a recorded core-lint exemption.  <sub>← FU2, Q2 · ADR</sub>
- **[add-new]** `new subsection after §6.1 (§6.1.1)` — Bundled-assets note that CORRECTS the 'no asset pipeline' claim: self-host the SIL OFL fonts (no Google dynamic subsetting; bundle the OFL license; works offline + with itch relative-base); a small curated audio set (synthesized Web Audio + original/CC0 samples) bundled into dist/; inline SVG for load-bearing period motifs. Build injects a commit-SHA build stamp.  <sub>← Q52, Q50, Q38, Q54 · ADR</sub>
- **[edit]** `§6.2 core module table — core/rng row (line 3977)` — Rewrite to: persisted per-named-stream cursors `{ seed, cursors:{combat,loot,seasonal,worldgen} }` (NOT a single counter, NOT child-RNG-by-splitting); plus a pure stateless day-keyed helper `deriveDayKeyed(seed,'weather',day)` for weather/lunar that is NOT a persisted cursor.  <sub>← Q2, Q3, FU3 · ADR</sub>
- **[edit]** `§6.2 core module table — core/influence row (line 3983)` — Replace 'simple per-tier required-pillar thresholds (no floor/overflow formula)' with: HYBRID good/great/excellent per-pillar-per-tier gate (good in ALL revealed pillars, great in 2-3, excellent in 1-2; T0 2-pillar special; NO overflow); pillar DEEDS gated to phase-2 (FU7); nested estateWealth.subEngines{land,treasury,trade} clamp; cross-pillar combos computed POST trade-clamp.  <sub>← Q7, FU10, FU7, Q34, Q22, FU20 · ADR</sub>
- **[edit]** `§6.2 core module table — core/skills row (line 3980)` — Add: each skill (labour + combat) carries a per-skill PERKS track (~2-8 perks / small flat combat bonuses, unlocked by leveling it). This is the bounded labour->combat channel; the real bound is incremental skill unlock (skills reveal per rung/tier), not a global cap. Conditioning stays the ZERO-stat enablement gate.  <sub>← Q6, FU8 · ADR</sub>
- **[edit]** `§6.2 core module table — core/combat row (line 3978)` — Expand: combat-XP -> character.level track (Q1); per-mob MobDef.level (FU15); graded durability bands on attackPower/defense (FU17); satiety throttle satietyRate on attackPower (FU16); clean retreat (Q16); the staggered reveal of stance/ability/item slots (FU12); a bigger weapon roster with archetype params + signature ability (FU13); CombatEncounterState save-resume (Q34).  <sub>← Q1, FU14, Q33, FU17, Q31, FU16, Q16, FU12, Q15, FU13, FU15, Q34 · ADR</sub>
- **[edit]** `§6.2 core module table — core/unlock row (line 3982)` — Add: reveal staggering is a DESIGN property of the authored unlock schedule (one-at-a-time by construction) — there is NO stored runtime reveal-queue. Genuine multi-element single-feature reveals are bespoke one-offs designed per case.  <sub>← FU4, Q17 · ADR</sub>
- **[edit]** `§6.2 src tree — app/ line (line 3961)` — Note the app tick loop ACCUMULATES the fractional dtTicks remainder and hands only WHOLE-INTEGER ticks to core tick(); it also drives auto-resolve combat + auto-repeat labour while the tab is active (the 'leave it running' feel), still active-only.  <sub>← FU3, Q2, FU23</sub>
- **[edit]** `§6.2 src tree — persistence/ line (line 3962)` — Update to: multi-backend (IndexedDB + localStorage + sessionStorage) + atomic write + app-identity magic field + monotonic-counter newest-wins selector + the additive-schema migration chain & raw-backup. Still side-effectful, never imported BY core.  <sub>← Q37, FU1, Q44, Q46, Q45 · ADR</sub>
- **[add-new]** `§6.2 core module table — new row core/ranks (after core/influence, line 3983)` — Add a module (or extend core/state) for the per-rung-reset rung-meter, the AND-gate (meter >= per-rung threshold AND story flags, surfacing 'awaiting X' when one lags), and the phase-1 (climb rungs via curated activities) -> phase-2 (estate-influence/pillar grind unlocks) gate per tier.  <sub>← Q30, FU6, FU7, FU14 · ADR</sub>
- **[edit]** `§6.3 tick() contract (lines 4019–4036)` — Specify dtTicks is a WHOLE INTEGER (the app loop accumulates the fractional remainder); add deriveDayKeyed weather/festival resolution to the per-day scheduler (mechanical ±10%); per-tick now includes auto-resolve combat + auto-repeat labour (active-only, no offline).  <sub>← FU3, Q2, Q35, FU23</sub>
- **[keep]** `§6.3 Intent union — advance_dialogue + combat_action (lines 4009–4010)` — Keep: `advance_dialogue { lineId, choiceId? }` already satisfies the intra-line dialogue data model (FU22), and `combat_action` already carries retreat (Q16). Add a one-line note that a dialogue choice writes only chosen-flags (locksLineIds[]/flags effects), deterministic, save-light.  <sub>← Q34, FU22, Q16</sub>
- **[edit]** `§6.3 per-season scheduler note (lines 4033–4035)` — Reflect FU7 sequential per-tier: the seasonal judged Influence appraisal accrues pillar deeds only in PHASE 2 (after the final rung) — deeds do not accrue across the rung-climb. Keep the up-only/new-high-water-mark rule.  <sub>← FU7 · ADR</sub>
- **[edit]** `§6.4 stored GameState — rng field (line 4059)` — Change `rng: RngState // seed + counter/stream` to the per-named-stream cursor shape `{ seed, cursors:{combat,loot,seasonal,worldgen} }`; cross-ref §6.7. Weather is NOT stored (derived day-keyed).  <sub>← Q2, FU3 · ADR</sub>
- **[edit]** `§6.4 stored GameState — character field (lines 4065–4066)` — Add `level` (combat-fed, stored, floor 1) and `combatXp` to character; HP and attribute-points scale off level. Note satietyMax is DERIVED from level (base + per-level growth), not stored. M0 skeleton = {hp, satiety, attributePoints, character.level=1}.  <sub>← Q1, FU5, FU14, Q47 · ADR</sub>
- **[add-new]** `§6.4 stored GameState — new field after resources/producers (line 4064)` — Add a stored market-saturation field (e.g. `market: { saturation: Record<ResourceId,number> }`) — the only non-derivable economy state; weather/lunar are derived, belief-beasts are content. Persist saturation ONLY.  <sub>← Q3, FU3, Q42</sub>
- **[edit]** `§6.4 stored GameState — influence field (line 4069)` — Nest `subEngines:{ land, treasury, trade }` (each value + highWater) on the estateWealth pillar entry, so the trade-<=1/3 clamp has real storage; combos are computed against this clamped value.  <sub>← Q34, FU20 · ADR</sub>
- **[edit]** `§6.4 stored GameState — ranks field (line 4072)` — Clarify the per-tier ranks entry stores the PER-RUNG-RESET rung-meter values: estateService and combatStanding are the meters; combatStanding is fed by per-rung CURATED activities (not raw kills/XP). The phase-1/phase-2 marker is derivable from current rung (no extra stored field needed).  <sub>← Q30, FU6, FU7, FU14 · ADR</sub>
- **[edit]** `§6.4 stored GameState — combat? field + surrounding prose (line 4079)` — Note that combat (CombatEncounterState), estateWealth.subEngines, and dialogue choice-flags are ADDITIVE OPTIONAL fields added at their milestone (M3/M5), NOT in the M0 skeleton (Q45 additive schema); only {hp,satiety,attributePoints,character.level} ship at M0.  <sub>← FU5, Q34, Q45</sub>
- **[add-new]** `§6.4 stored GameState — explicit callout near `unlocked` (line 4076)` — Add an explicit NON-field callout: there is NO `revealQueue` in GameState. Reveal staggering is a design property of the authored unlock schedule (FU4), not stored runtime state — `unlocked: Set<SurfaceId>` is the only reveal state. This supersedes Q17's 'deterministic one-per-beat queue'.  <sub>← FU4, Q17 · ADR</sub>
- **[edit]** `§6.4 computed/selectors examples (lines 4085–4090)` — Add derived selectors: `satietyMax(state)` (base + per-level growth, Q47); `durabilityBand(state,slot)` from the stored durability integer (Q33/FU17); `gateProfile(state)` (good/great/excellent distribution vs the revealed pillars, Q7/FU10); effective combat power INCLUDING per-skill perks (FU8). All pure, excluded from save.  <sub>← Q47, Q33, FU17, Q7, FU10, Q6, FU8</sub>
- **[edit]** `§6.4 'Why this split matters' prose (lines 4092–4094)` — Reinforce with the ADDITIVE-schema rule: not only do you migrate only non-derivable fields, but new stored fields are added as OPTIONAL with defaults (never removed/repurposed), so most schema growth needs no migration at all. Cross-ref §6.8.  <sub>← Q45, FU5</sub>
- **[add-new]** `§6.5 registry table — new rows (after enemies.ts, line 4112)` — Add `content/world.ts` (SeasonRules / Festival / WeatherHazard — mechanical ±10%, day-keyed) and `content/beliefBeasts.ts` (belief-creatures, kept OUT of grindable spawn tables). Keep enemies.ts grounded-only.  <sub>← Q55, Q3, Q35</sub>
- **[edit]** `§6.5 registry table — dialogues.ts row (line 4114)` — Rename to `content/dialogue.ts`; add `choices[]` + `ChoiceId` with `locksLineIds[]`/flags effects — intra-line branching as flat DATA (not scripting), deterministic, only chosen-flags persist.  <sub>← Q34, FU22 · ADR</sub>
- **[edit]** `§6.5 registry table — skills.ts row (line 4109)` — Add a per-skill PERKS field (~2-8 perks / small flat combat bonuses, gated to skill levels); document that the verifier asserts small per-perk magnitude (not ==0, not a global cap).  <sub>← Q6, FU8 · ADR</sub>
- **[edit]** `§6.5 registry table — items.ts row (line 4110)` — Specify the bigger weapon roster (~9-10 across v1; T0 starts with 1, then +2 T0 / +3 T1 / +4 T2), each weapon carrying archetype params (baseSpeed / reach / targetCount) + a signature ability. 3 archetype lines (spear/sword/staff) anchor it.  <sub>← Q15, FU13 · ADR</sub>
- **[edit]** `§6.5 registry table — ranks.ts + activities.ts rows (lines 4107, 4118)` — Add per-rung CURATED activity sets (tagged by rung) that feed the rung-meter, AUTHORED SEPARATELY from the pillar-deed inventory; encode the combat-reveal ladder (R3 weapon+auto-loop+retreat -> R4 durability -> R5 stance -> weapon-L10 ability/item slots -> 2nd line T1 / 3rd T2); ranks carry rung-meter threshold + AND-gate.  <sub>← FU6, FU7, FU12, FU14 · ADR</sub>
- **[edit]** `§6.5 registry table — influence.ts row (line 4119)` — Replace 'per-tier required-pillar thresholds' with the per-pillar-per-tier good/great/excellent band triples (the V2 overhaul, balanced against the fixed deed inventory); add the phase-2 deed gating and the cross-pillar combos (post-clamp, third-pillar, excluded from gate checks).  <sub>← Q7, FU10, FU7, FU20, Q22 · ADR</sub>
- **[edit]** `§6.5 registry table — enemies.ts row (line 4112)` — Add an explicit per-mob `level` field (MobDef.level, defaults ~ the dangerRing's expected character-level) feeding the on-kill XP path; reiterate belief-creatures live in beliefBeasts.ts not here.  <sub>← FU15, Q3</sub>
- **[edit]** `§6.5 registry table — balance.ts row (line 4121)` — Expand 'the single home for tunables' to include the new V2 curves/constants: rung-meter thresholds, good/great/excellent gate bands, per-skill perk magnitudes, durability bands (75+/50+/1+/0 -> 1.0/0.9/0.75/0.55), satiety throttle (flat >=~0.7 -> ~0.5 floor), weather ±10%, combat-XP->level curve, mobLevel defaults — all integer-pow only (no Math.pow).  <sub>← FU6, FU10, FU8, FU17, FU16, FU21, Q35, FU14, Q36</sub>
- **[edit]** `§6.5 Rewards-object prose (lines 4123–4126)` — Note `pillarDeltas` (deed accrual) is PHASE-2-gated (FU7) — deeds don't grant pillar value during the rung-climb. Rewards stay one bus; the dialogue choices[] effects ride the same bus.  <sub>← FU7, FU22</sub>
- **[edit]** `§6.5 registry table — quests.ts row (line 4115)` — Note NO quest-type budget: quests are authored per stage (the '4 grind types' is just the T0 starting set, not a cap); more/interesting quests welcome in later tiers.  <sub>← Q23</sub>
- **[rewrite]** `§6.6 verifier invariant — 'no labour->combat cross-feed' (line 4141)` — FLIP the machine check: conditioning still grants ZERO combat stats (stays the gate), BUT each per-skill perk grants a SMALL bounded combat bonus — the verifier asserts each perk's MAGNITUDE is small (a per-perk magnitude check), NOT ==0 and NOT a single global <=CAP. Remove the absolute '==0 no-cross-feed' wall.  <sub>← Q6, FU8 · ADR</sub>
- **[add-new]** `§6.6 verifier invariants list (after line 4142)` — Add: gate-monotonicity/ceiling (no rung/tier requires more than its tier can grant); accrual tie-out (deeds sum to the gate share within tolerance); per-rung-meter monotonicity (the rung-meter target exists and is reachable from its eligible activities).  <sub>← Q28, FU6 · ADR</sub>
- **[add-new]** `§6.6 verifier invariants list (after line 4142)` — Add a real-name DENYLIST lint (flags real surnames/places — Toyama, Konoe, real daimyō/place names); maintained alongside the macron lint. Allow-list: naturalized exonyms + the invented allow-list incl. Nihonbashi.  <sub>← Q28, Q27, Q39, Q12</sub>
- **[edit]** `§6.6 verifier — trade-<=1/3 invariant (line 4138)` — Strengthen: trade <=1/3 of Estate & Wealth holds EVEN AFTER cross-pillar combos — combos are computed POST trade-clamp, land on a third pillar (Name), and are EXCLUDED from gate-threshold checks; the verifier proves trade can never breach 1/3 via a combo.  <sub>← Q22, FU20 · ADR</sub>
- **[add-new]** `§6.6 verifier invariants list (after line 4142)` — Add the hybrid gate-distribution check: good in ALL revealed pillars, great in 2-3, excellent in 1-2, the T0 2-pillar special case, NO overflow-substitution; and assert combos are excluded so a combo can't satisfy a required pillar.  <sub>← Q7, FU10, FU20 · ADR</sub>
- **[edit]** `§6.6 macron/romaji lint (lines 4143–4146)` — Add gōshi / rōnin to the macron examples; add Nihonbashi to the fictional allow-list; cross-link the real-name denylist (s6-38). Note the 'Combat Standing'->'Combat Rank' rename so the lint examples track the new term.  <sub>← Q9, Q12</sub>
- **[edit]** `§6.6 generated-docs paths (lines 4148–4153)` — Align gen-docs output paths to docs/balance/ + docs/content/; tag duplicated illustrative numbers 'illustrative — see generated' so hand-typed derived values can't silently drift.  <sub>← Q41</sub>
- **[rewrite]** `§6.7 The one seeded RNG (lines 4157–4173)` — Rewrite: `state.rng = { seed, cursors:{combat,loot,seasonal,worldgen} }` — per-named-stream MONOTONIC cursors (replace `{seed,counter}` and the 'derive child RNGs by splitting' model). splitmix64 stays. Add the stateless day-keyed helper `deriveDayKeyed(seed,'weather',day)` for weather/lunar (NOT a persisted cursor). tick() consumes whole-integer dtTicks; Math.random banned in core; cross-ref the Math.pow ban (§6.1).  <sub>← Q2, Q3, FU3, Q35, Q36 · ADR</sub>
- **[rewrite]** `§6.8 header + primary store (lines 4176–4183)` — Rewrite from 'IndexedDB single autosave' to MULTI-BACKEND redundant save: IndexedDB primary + localStorage + sessionStorage; on save, atomic write to all available backends; on load, read ALL backends and pick the newest. Survives itch cross-origin-iframe partition/eviction. Build the FULL layer in M0 (FU1).  <sub>← Q37, FU1 · ADR</sub>
- **[add-new]** `new §6.8 subsection (atomic write)` — Atomic-write rule: one atomic put per backend, never clear-then-rewrite; on ANY write rejection (quota/private-mode/torn write) show a calm persistent 'couldn't save — export a backup' banner. No silent loss.  <sub>← Q44</sub>
- **[add-new]** `new §6.8 subsection (newest-wins arbitration)` — Newest-save selector: a MONOTONIC save-counter is the real selector; a save-LAYER timestamp (documented core-lint exemption — metadata, not game logic) is the tiebreaker; NEVER load a forward-schemaVersion backend (fall through to the newest READABLE backend, then reject-to-recovery). Equal/skewed-clock handled by the counter.  <sub>← FU2, Q2, Q45 · ADR</sub>
- **[add-new]** `new §6.8 subsection (magic field)` — App-identity magic field `app:'kami-kakushi'` for a fast reject of foreign/corrupt blobs; on a bad/stale id, reject-to-recovery (offer re-import / fresh start), never a silent half-load.  <sub>← Q46</sub>
- **[rewrite]** `§6.8 migration policy (lines 4192–4197)` — Make ADDITIVE backwards-compatible schema the PRIMARY mechanism (protobuf/thrift-style: new fields optional with defaults, never remove/repurpose) -> migrations become RARE. Keep the ordered migration chain + raw pre-migration backup + future-version guard ONLY as the safety net for unavoidable structural changes. Supersedes 'ordered forward migrations' as the default.  <sub>← Q45, FU5 · ADR</sub>
- **[edit]** `§6.8 persist-only list (lines 4186–4191)` — Update the persisted-surface list: rng CURSORS (not seed+counter), market saturation, character.level + combatXp, estateWealth.subEngines, dialogue chosen-flags, the rung-meter values — and note additive optional fields added per-milestone.  <sub>← Q2, Q3, Q1, Q34, FU22, FU5</sub>
- **[add-new]** `new §6.8 subsection (save-safety)` — Save-safety: explicit confirm on destructive actions (import / fresh-start, and only genuinely-unrecoverable rare actions) + an automatic pre-overwrite snapshot (extends the multi-backend write) before any overwrite.  <sub>← Q19, Q49</sub>
- **[edit]** `§6.8 export/import (lines 4184–4185)` — Keep base64 export/import as the manual backstop, but make it a PERIODICALLY-NUDGED first-class backup (the matrix+nudge posture) so silent autosave loss is caught.  <sub>← Q37</sub>
- **[edit]** `§6.9 multi-screen nav (lines 4218–4221)` — Add Crafting and Quests as separate TOP-LEVEL nav tabs (not nested panels). State the principle: the main screen stays the active labour/deeds/combat loop; any distinct non-critical activity gets its own top-level tab (still revealed one-at-a-time).  <sub>← Q10 · ADR</sub>
- **[edit]** `§6.9 data-driven surfaces / reveal (lines 4214–4217)` — Replace any 'serialized one-per-beat queue' implication with: reveals are DESIGN-staggered one-at-a-time (no UI dumps), driven by the authored unlock schedule — no runtime queue. Each first-time reveal still pushes a diegetic log line.  <sub>← Q17, FU4 · ADR</sub>
- **[edit]** `§6.9 art register (lines 4226–4228)` — Update 'text + emoji + CSS' to add: inline SVG for load-bearing period motifs (pillar/season/rarity marks, identical across OSes; emoji COSMETIC-ONLY) + a small curated audio set (synthesized Web Audio + original/CC0). Corrects the 'no asset pipeline' claim.  <sub>← Q38, Q50 · ADR</sub>
- **[edit]** `§6.9 active-only loop (lines 4233–4235)` — Add: while the tab is OPEN, auto-resolve combat + auto-repeat labour run unattended for hours (the 'leave it running, check progress' feel). Still strictly active-only — no offline catch-up; auto-producers stay T3+.  <sub>← FU23, FU18</sub>
- **[edit]** `§6.9 responsive desktop+mobile (lines 4222–4225)` — Add the mobile IA rule: per-tier PRIMARY tabs on the bottom bar (current-tier core), older tabs in an overflow drawer in reveal order.  <sub>← Q19</sub>
- **[edit]** `§6.9 art register / number-flash (lines 4226–4232)` — Add (cross-ref ui-design.md): pillar bars show distance-to-next-gate; gain/loss number-flash uses the §2 gain/loss tokens; vermilion reserved for rank-up/seal beats; functional text uses --ink-soft (WCAG AA).  <sub>← Q21, Q48</sub>
- **[edit]** `§6.11 a11y — live region / entry (lines 4287–4291)` — Scope the ARIA live region to narration + milestones ('polite'); add a persistent quiet a11y entry point from minute one; add a large-textScale reflow case + a screen-reader acceptance pass to the checklist.  <sub>← Q18</sub>
- **[edit]** `§6.11 a11y — contrast (new bullet)` — Add a contrast bullet: functional/hint text uses --ink-soft (passes WCAG AA on every paper surface); --ink-faint is decorative-only; darken the meter fill for contrast.  <sub>← Q48</sub>
- **[edit]** `§6.11 language-of-parts (lines 4292–4294)` — Add gōshi/rōnin (and the 'Combat Rank' term) to the lang-tagging examples; reiterate the §6.6 lint flags untagged CJK.  <sub>← Q9</sub>
- **[edit]** `§6.12 conventions table (lines 4304, 4305, 4307)` — Update the 'one RNG' row to per-named-stream cursors + day-keyed weather + Math.pow ban; the 'Active-only' row to add auto-resolve/auto-repeat; the 'Single source of truth' row to add the hardened verifier (gate-monotonicity, accrual tie-out, real-name lint, per-perk magnitude).  <sub>← Q2, Q3, Q36, FU23, Q28, FU8</sub>
- **[rewrite]** `§6.13 ADR header (lines 4313–4318)` — Rewrite: §6 now implements V2 decisions beyond D-013 — record the governing D-022 plus the specific new/changed locks it carries: multi-backend save (Q37), additive schema (Q45), RNG cursors (Q2), Math.pow ban (Q36), bounded cross-feed verifier flip (Q6), feature-rich data model incl. intra-line dialogue & subEngines (Q34), no-runtime-reveal-queue (FU4), asset-set correction (Q50/Q52/Q38). §6 is no longer 'elaboration only'.  <sub>← D-022, Q37, Q45, Q2, Q36, Q6, Q34, FU4, Q50, Q52, Q38 · ADR</sub>
- **[edit]** `§6.13 'Flagged — ALL RESOLVED 2026-06-25' items 1-4 (lines 4320–4339)` — Annotate the superseded resolutions (newest-wins, don't delete): item 1 IndexedDB single autosave -> SUPERSEDED by multi-backend (Q37); item 2 splitmix64+named sub-streams -> REFINED to persisted per-named-stream cursors (Q2); item 3 ordered forward migrations -> SUPERSEDED by additive-schema-first / migrations rare (Q45); item 4 tier stored -> KEEP.  <sub>← Q37, Q2, Q45 · ADR</sub>
- **[add-new]** `§6.5 surfaces.ts row + §6.1.1 build` — Add an About/Credits surface (content/surfaces.ts) carrying authorship, the commit-SHA build stamp, font/audio attributions, and the license note; the build injects the commit SHA.  <sub>← Q54, Q51, Q50, Q52</sub>
- **[NEW]** §6.1.1 Bundled assets (self-hosted OFL fonts · curated audio · inline SVG · build stamp) @ new subsection after §6.1 (toolchain) — Corrects the 'no asset pipeline' canon: enumerate the ONE small curated asset set — self-hosted OFL fonts (no Google dynamic subsetting; OFL license bundled; works offline + itch relative-base), a small synthesized Web Audio + original/CC0 audio set, inline SVG load-bearing motifs — and the commit-SHA build stamp injection.  <sub>← Q52, Q50, Q38, Q54</sub>
- **[NEW]** §6.4.1 Three combat tracks & the sequential per-tier phase gate (data-model view) @ new subsection after §6.4 (GameState stored vs computed) — Make the FU14 separation explicit at the data level: character.level/combatXp (kills/combat-XP only) · the Arms pillar (deeds, phase-2-gated) · the combatStanding rung-meter (per-rung curated activities) — exactly what one kill writes to each — and the phase-1 (climb rungs) -> phase-2 (estate/pillar grind) gate, with deeds locked to phase 2. Replaces the conflated old Combat Level/CombatDeedsPool model.  <sub>← Q1, FU14, FU7, Q30, FU6</sub>
- **[NEW]** §6.7.1 Stateless day-keyed derivations (weather / lunar / festivals) @ new subsection after §6.7 (RNG) — Define `deriveDayKeyed(seed,'weather',day) -> value` as a PURE stateless helper (not a persisted cursor): mechanical ±10% weather/festival modifiers reproduced from seed+day, so nothing weather-related needs storing and replays stay byte-identical.  <sub>← Q3, FU3, Q35</sub>
- **[NEW]** §6.8.1 Multi-backend redundant save + newest-wins arbitration @ new subsection within the rewritten §6.8 — Spec the IndexedDB+localStorage+sessionStorage redundant write, atomic write + 'couldn't save' banner, the app-identity magic field + reject-to-recovery, and the monotonic-counter newest-wins selector (save-layer timestamp tiebreaker, forward-version fall-through). Built in full at M0.  <sub>← Q37, FU1, FU2, Q44, Q46</sub>
- **[NEW]** §6.8.2 Additive backwards-compatible schema & migration safety net @ new subsection within the rewritten §6.8 — Spec additive-optional-fields-with-defaults as the PRIMARY schema-growth mechanism (migrations rare), with the ordered migration chain + raw pre-migration backup + future-version guard + pre-overwrite snapshot as the safety net.  <sub>← Q45, FU5, Q19</sub>
- **[NEW]** §6.6.1 V2 verifier invariants @ new subsection within §6.6 (content verifier) — Collect the Q28 hardening + the V2-driven invariant changes: gate-monotonicity/ceiling, accrual tie-out, per-rung-meter reachability, the FLIPPED per-skill-perk magnitude check (small, not ==0), the trade-<=1/3-holds-post-combo proof, the hybrid good/great/excellent gate-distribution check (combos excluded), and the real-name denylist lint.  <sub>← Q28, Q6, FU8, Q22, FU20, Q7, FU10, Q27, Q39</sub>

### §7 — Milestone Roadmap, v1 Scope & Deployment (lines 4316–4867)

- **[rewrite]** `§7 preamble blockquote (≈4364–4366) + §7.1.2` — The 'saga ≈28.5 h … LOCKED … treated as a release acceptance criterion, not a lever' line becomes a FLOOR/minimum-grind statement: ≈28.5 h is a MINIMUM, the game can/should be LONGER (a long OSRS-rough grind); only the ≥30-min per-rung floor + per-tier MINIMUMS are gate-enforced (undershoot fails, overshoot is fine/desired). §4.8 reframed as a minimum-grind model.  <sub>← FU18 · ADR</sub>
- **[edit]** `§7 preamble 'Canon honoured throughout' (≈4360–4362)` — Keep 'active-only v1 — NO idle/offline layer' but add: tab-open AUTO-RESOLVE + AUTO-REPEAT give the 'leave it running' feel (still active-only, no offline progress).  <sub>← FU23, FU18</sub>
- **[edit]** `§7 preamble 'single seeded RNG (§6.2, §6.7)' (≈4359)` — Restate as: one seed → per-named-stream persisted cursors {combat,loot,seasonal,worldgen} + a stateless day-keyed weather/lunar derivation; integer-pow only (Math.pow banned by lint).  <sub>← Q2, FU3, Q36</sub>
- **[edit]** `§7 preamble ADR-D-021 note (≈4354–4357)` — Cite D-022 as governing (the Block L/M V2 decisions supersede conflicting prior locks) and note FU18 annotates D-016 (budget→floor); M0/M1 still the only committed build order, M2–M7 provisional and now re-cut.  <sub>← D-022, FU18 · ADR</sub>
- **[edit]** `§7.1.1 'What v1 IS' opening paragraph (4394–4398)` — T3 stub framing → 'castle-town / Daikan first-contact' (drop the porter/Kaidō-guild framing); state the G6 Otsuru/Tama TRUTH is spine-guaranteed while the 'Tahei' name-reclaim is Origin-gated/missable at O5; estate now grows E0→E3.  <sub>← Q24, Q5, Q8</sub>
- **[edit]** `§7.1.1 cut-set table 'Rank ladders' row (4404)` — Note each tier = PHASE 1 (the ~8 rungs, each gated on a numeric rung-meter [FU6] + story milestone via AND-gate) → PHASE 2 (the estate-influence/pillar grind unlocks after the final rung). Rung promotion is rung-meter AND story.  <sub>← FU6, FU7, Q30 · ADR</sub>
- **[edit]** `§7.1.1 cut-set table 'Quest types' row (4406)` — Keep the 4 TYPES (PEST/HUNT/CLEAR/DEFEND) as the taxonomy but drop the quest-COUNT budget: author as many quests as fit each stage, more/interesting ones welcome (esp. later tiers).  <sub>← Q23 · ADR</sub>
- **[edit]** `§7.1.1 cut-set table 'Skills (LOCKED v1 set)' row (4408)` — Replace '2–3 weapon lines' with the INCREMENTAL weapon roster (~9–10 weapons across 3 archetype lines: T0 starts with 1 / unlocks 2 · T1 +3 · T2 +4; each = archetype params + signature ability). Add: each skill carries bounded per-skill PERKS (small labour→combat bonuses, FU8); fishing surfaces at T1.  <sub>← Q15, FU13, FU8, Q4 · ADR</sub>
- **[edit]** `§7.1.1 cut-set table 'Estate stages' row (4409)` — E0→E2 becomes E0→E3 (E3 authored in M5, folded into the G-tier spend; E4–E5 parked).  <sub>← Q8 · ADR</sub>
- **[edit]** `§7.1.1 cut-set table 'House Influence' row (4410)` — Replace 'simple per-tier required-pillar thresholds' with the HYBRID good/great/excellent tier-gate (good in ALL revealed pillars, great in 2–3, excellent in 1–2; T0 2-pillar special; NO overflow). Pillar DEEDS gated to PHASE 2; the per-pillar-per-tier thresholds are an OVERHAUL (not simple ratios).  <sub>← Q7, FU10, FU7, FU14 · ADR</sub>
- **[rewrite]** `§7.1.1 cut-set table 'Save' row (4412)` — 'IndexedDB single autosave' → MULTI-BACKEND redundant save (IndexedDB + localStorage + sessionStorage), atomic write, app-identity magic field + reject-to-recovery, monotonic-counter+timestamp newest-wins selector, backwards-compatible additive schema; base64 export/import retained.  <sub>← FU1, Q37, Q44, Q45, Q46 · ADR</sub>
- **[add-new]** `§7.1.1 cut-set table — new 'Combat' row (after the Skills row, ≈4408)` — Add a row for incremental combat: auto-resolve auto-battler (active-only); the FU12 reveal ladder (R3 weapon+loop+retreat → R4 durability bands → R5 stance → weapon-L10 ability/item slots → 2nd line T1 / 3rd T2); satiety-throttled (FU16); graded 4-band durability (FU17); three clean tracks (combat-XP→level, deeds→Arms, curated activities→Combat-Rank meter, FU14).  <sub>← Q15, FU12, FU13, FU14, Q31, FU16, Q33, FU17 · ADR</sub>
- **[rewrite]** `§7.1.2 'The locked pacing target' (4414–4423)` — Reframe the whole subsection from CEILING to FLOOR: ≈28.5 h is a minimum-grind floor, not a target/ceiling; the game can/should run longer (OSRS-rough grind, interleave don't brick-wall). Keep the ≥30-min per-rung floor and per-tier MINIMUMS as verify-gate criteria, but the M6 regression fails ONLY on undershoot (rung too fast / tier under budget), never on overshoot. The auto-resolve/auto-repeat 'leave it running' loop is what makes the longer grind palatable.  <sub>← FU18, FU23 · ADR</sub>
- **[edit]** `§7.1.3 parked table 'Estate stages E3–E5' row (4432)` — Un-park E3: it ships in v1 (authored M5); only E4–E5 stay parked. Rename the row to 'Estate stages E4–E5'.  <sub>← Q8 · ADR</sub>
- **[edit]** `§7.1.3 'Hard guardrails' blockquote (4440–4444)` — Replace 'no labour→combat training-rate cross-feed' with: BOUNDED labour→combat via small per-skill perks (no global cap, small per-perk magnitude); conditioning stays the ZERO-stat enablement gate. Keep 'trade ≤⅓' but add that cross-pillar combos are computed POST-clamp and never move the trade ratio or satisfy a required pillar (FU20).  <sub>← Q6, FU8, FU20 · ADR</sub>
- **[add-new]** `new subsection §7.1.4 'Per-tier shape (phase 1 → phase 2)' after §7.1.3` — Author the sequential per-tier model at scope level: Phase 1 = climb the rungs via curated per-rung activities (1-to-many, not a repeat-counter) + the numeric rung-meter + story milestones; Phase 2 = the estate-influence/pillar grind unlocks after the final rung → hybrid tier-gate → tier-up. Pillar deeds gated to phase 2 (prevents 'half the rungs, maxed deeds'). Plus the three clean combat tracks (combat-XP→level, deeds→Arms, curated activities→Combat-Rank meter).  <sub>← FU7, FU6, FU10, FU14 · ADR</sub>
- **[edit]** `§7.2 granularity note blockquote (4455–4461)` — Add: each content tier is internally PHASE 1 (rungs via rung-meter) then PHASE 2 (pillar grind + hybrid gate); reveals are DESIGN-staggered one-per-beat (NO runtime queue, FU4); combat reveals follow the FU12 ladder spread across M2a→M5 with combat sub-slices added to M4 (2nd weapon line) and M5 (3rd line). M2a/M2b stay the combat-ENGINE milestones.  <sub>← FU7, FU4, FU12, FU6, FU13, FU18 · ADR</sub>
- **[add-new]** `new subsection/table §7.2.0 'Combat reveal ladder → milestone map' (after the granularity note, ≈4461)` — Add a small table pinning which combat element reveals in which milestone: R3 starter weapon+auto-resolve+retreat→M2a; R4 durability bands + 2nd T0 weapon→M2b; R5 stance→M3; first weapon-L10 ability/item slots→M3; 2nd weapon line (+3 T1 weapons)→M4; 3rd weapon line (+4 T2 weapons)→M5. One reveal per beat.  <sub>← FU12, FU13, Q17 · ADR</sub>
- **[edit]** `M0 'Lands' RNG bullet (4475)` — rng = {seed, cursors:{combat,loot,seasonal,worldgen}} persisted, whole-integer dtTicks; plus a stateless deriveDayKeyed(seed,'weather',day) helper outside GameState.rng; integer-pow only, Math.pow banned by the §6.1 lint.  <sub>← Q2, FU3, Q36</sub>
- **[edit]** `M0 'Lands' reveal-engine bullet (4475–4476)` — State explicitly: NO runtime reveal-queue — a synchronous one-shot rewards/unlock bus; the reveal SCHEDULE is design-staggered so reveals are inherently one-at-a-time (FU4 reverses the Q17 queue).  <sub>← FU4 · ADR</sub>
- **[rewrite]** `M0 'Lands' save bullet (4483–4484)` — 'IndexedDB single autosave' → the FULL multi-backend save layer: backend-abstraction over IndexedDB+localStorage+sessionStorage, atomic redundant write, app-identity magic field with reject-to-recovery, monotonic-counter+timestamp newest-wins selector, backwards-compatible additive schema; base64 export/import + migration scaffold. Defer ONLY the itch cross-origin-iframe survival TEST to M7.  <sub>← FU1, FU2, Q37, Q44, Q45, Q46 · ADR</sub>
- **[edit]** `M0 'Lands' cold-open bullet + 'Goal' (4464–4465, 4480–4482)` — Note the M0 stored GameState surface = {hp, satiety, attributePoints} + character.level (=1 floor) + satietyMax-at-floor; do NOT pre-declare subEngines/CombatEncounterState/dialogue (additive at their milestone).  <sub>← FU5, Q1, Q47</sub>
- **[edit]** `M0 'Definition of done' (4486–4491)` — Determinism hash covers the new stored surface incl. character.level; save round-trip exercises ALL backends with the newest-wins selector via the monotonic save-counter; document that the save-layer timestamp is a core-lint EXEMPTION (metadata in persistence, not core) so the deterministic core stays clock-free.  <sub>← FU1, FU2, FU5</sub>
- **[edit]** `M0 phase-2 (4496)` — Add character.level (floor 1) + satietyMax-at-floor to the M0 surface; rng field = {seed, cursors:{combat,loot,seasonal,worldgen}}; the deriveDayKeyed weather helper; ban Math.pow → integer-pow; keep the skeleton minimal (no speculative subEngines/combat/dialogue).  <sub>← FU3, FU5, Q1, Q47, Q36</sub>
- **[edit]** `M0 phase-3 (4497)` — Make explicit: the rewards/unlock bus is synchronous one-shot with NO runtime reveal-queue and NO dequeue rule; one flag/threshold flip reveals exactly one surface; the staggering is by design-schedule, not a queue.  <sub>← FU4 · ADR</sub>
- **[rewrite]** `M0 phase-5 'Build the save spine' (4499)` — Rewrite to the multi-backend redundant atomic write + magic-field + monotonic-counter+timestamp newest-wins selector + reject-to-recovery + additive (never remove/repurpose) schema, over a backend abstraction (IndexedDB+localStorage+sessionStorage); the timestamp is the documented core-lint exemption; persistence still never imported by core.  <sub>← FU1, FU2, Q37, Q44, Q45, Q46 · ADR</sub>
- **[edit]** `M0 phase-1/phase-6 boundary rules (4495, 4500)` — Boundary lint also bans Math.pow in core (FU/Q36); note the save-layer timestamp as a documented exemption to the no-Date.now-in-core rule (it lives in persistence, never in reduce/tick).  <sub>← Q36, FU2</sub>
- **[edit]** `M0 phase-7/DoD + M1 (4501–4502)` — Confirm the M0/M1 buildable surface: add a persistent quiet accessibility entry point from minute one (Q18) and note the deferred perf/memory acceptance criterion (Q56) to be added once M0/M1 profiling gives real numbers.  <sub>← Q18, Q56</sub>
- **[edit]** `M1 'Goal' + 'Lands' (4506–4515)` — Add the numeric rung-meter (FU6, per-rung-reset, AND-gated with story) as the R0→R2 promotion mechanism, fed by curated per-rung activities (FU7 phase 1). Promotion = meter≥threshold AND story flags; surface 'awaiting X' when one lags.  <sub>← FU6, FU7, Q30 · ADR</sub>
- **[add-new]** `M1 'Lands' + a new phase (≈4523)` — Add tab-open AUTO-RESOLVE + AUTO-REPEAT for labour activities (active-only, no offline) — the 'leave it running' feel that supports the longer-grind floor.  <sub>← FU23, FU18</sub>
- **[add-new]** `M1 — new phase after phase-7 (≈4531)` — Add a fun-proxy instrumentation phase, REPORT-ONLY (dead-time, reward/unlock cadence, always-a-visible-next-goal, first-5-min hook; deed-cadence T0 ~5 min; thresholds from fun-factor.md; mirrors the ≥30-min pacing harness — exists at M1, gates at M6).  <sub>← Q4, FU9</sub>
- **[edit]** `M1 phase-1 + phase-2 (skills registry, 4525–4526)` — skills.ts seeds the per-skill PERK field (bounded labourSkillCombatBonus / perks, ~2–8 per skill, FU8) even though the combat EFFECT lands at M2; conditioning remains the zero-stat gate (no bonus).  <sub>← Q6, FU8 · ADR</sub>
- **[edit]** `M1 'Lands' + phase-3 (world-clock/seasons, 4514, 4527)` — Add weather as a mechanical ±10% day-keyed modifier (deriveDayKeyed sub-stream) surfacing with seasons.  <sub>← Q35, FU3</sub>
- **[rewrite]** `M1 phase-4 'Wire the Estate Service meter' (4528)` — Replace the single 'Estate Service standing meter' with the numeric RUNG-METER (FU6): each R0→R1→R2 gate = meter≥per-rung threshold AND story flags; pillar DEEDS do NOT accrue here (gated to phase 2, FU7); rooms still reveal separately.  <sub>← FU6, FU7, Q30 · ADR</sub>
- **[edit]** `M1 'Definition of done' + phase-7 (4517–4521, 4531)` — Headless run reaches R2 via the rung-meter; the per-rung threshold curve is instrumented against the ≥30-min floor; fun-proxies present but report-only; satiety throttle is flat above ~0.7 → ~0.5 floor (FU21).  <sub>← FU6, FU9, FU21</sub>
- **[rewrite]** `M2a 'Goal' + 'Lands' (4533–4546)` — STRIP the UI-dump: at R3 reveal ONLY the single starter weapon + bare auto-resolve loop + retreat (FU12). Stance/ability/item slots, Equipment, Inventory, Bestiary do NOT reveal at R3 (move to M2b/M3 per the ladder). Rename 'idle auto-resolve' → 'auto-resolve (active-only) + auto-repeat' (FU23). Keep auto-battler determinism + isolation.  <sub>← FU12, Q17, Q15, FU23 · ADR</sub>
- **[edit]** `M2a 'Lands' combat-tracks + phase-1 (4544–4545, 4557)` — Replace the conflated CombatDeedsPool with the THREE clean tracks (FU14): kills/combat-XP→character.level (HP + attribute points, Q1, via the hpMax=40+8·level / +1 attr per 2 levels curve); deeds→Arms pillar (gated to phase 2); curated activities→the Combat-Rank rung-meter (Q9 rename 'Combat Standing'→'Combat Rank'). mobLevel sourced from MobDef.level (FU15).  <sub>← FU14, Q1, Q9, FU15 · ADR</sub>
- **[edit]** `M2a 'Lands' + 'DoD' win-rate (4541–4543, 4548–4552)` — The first-fight win-rate 20–35% is measured 'at adequate satiety (≥~0.7 satietyMax)'; combat is satiety-throttled via a satietyRate multiplier on attackPower (lighter on attackSpeed, floor ~0.5, bounded so win-rate never drops below ~15%, FU16). Setback shape stays LOCKED.  <sub>← Q31, FU16, Q47 · ADR</sub>
- **[edit]** `M2a 'Lands' no-cross-feed + DoD + phase-3/4 (4545–4546, 4552, 4559–4560)` — RELAX the no-cross-feed wall: bounded per-skill PERKS MAY enter combat (small magnitude, no global cap, FU8); the assertion flips from '==0' to 'perks within magnitude bounds'; conditioning stays the ZERO-stat enablement gate (its own ==0 assertion kept).  <sub>← Q6, FU8 · ADR</sub>
- **[edit]** `M2a phase-1 + phase-5 reveal (4557, 4561)` — Combat data model uses the three-track types (drop CombatDeedsPool); integer-pow; satietyRate term in §4.6.1. Phase-5 reveals ONLY Combat panel + Drill Yard + Combat nav + starter weapon + retreat at R3 (no stance/ability/item/Equipment/Inventory/Bestiary).  <sub>← FU14, FU12, Q36, FU16 · ADR</sub>
- **[edit]** `M2b 'Lands' + phase-2 durability (4571–4574, 4584)` — Graded durability = 4 BANDS on weapon attackPower (75+/50+/1+/0 → 1.0/0.9/0.75/0.55), fixed wear per fight, NEVER auto-unequip; armour bands on defense; repair restores. Surfaces at R4 per the reveal ladder (replaces binary unequip-at-0).  <sub>← Q33, FU17, FU12 · ADR</sub>
- **[edit]** `M2b phase-1 registries (4583)` — enemies.ts authors an explicit MobDef.level (FU15, default ~dangerRing expected level); beliefBeasts.ts is a SEPARATE scaffold registry (Q3); add the content/world.ts world-sim registry (Q55).  <sub>← FU15, Q3, Q55</sub>
- **[add-new]** `M2b 'Lands' (≈4571)` — The SECOND T0 weapon unlocks during T0 (FU13: T0 unlocks 2 total) — author the 2nd T0 weapon here (or early M3) per the reveal cadence, distinct from the M2a starter.  <sub>← FU13 · ADR</sub>
- **[rewrite]** `M3 'Goal' + 'Lands' (4592–4604)` — Re-cut around sequential per-tier (FU7): phase-1 = rungs R4→R7 via the rung-meter + curated activities + story; phase-2 (AFTER the R7 capstone) = the pillar grind UNLOCKS → four-pillar accrual + the four-bar panel reveal + the HYBRID good/great/excellent tier-gate (T0 2-pillar special) → T0→T1. Pillar DEEDS (incl. Arms from DEFEND and the R6 combat-Arms beat) are gated to phase 2.  <sub>← FU7, Q7, FU10, FU14 · ADR</sub>
- **[edit]** `M3 'Lands' accrual-model + phase-1 (4599–4602, 4614)` — Replace 'simple per-tier required-pillar thresholds' with the hybrid gate (Q7/FU10): good in ALL revealed pillars, great in 2–3, excellent in 1–2; T0 2-pillar special; NO overflow; the §4 thresholds are a per-pillar-per-tier OVERHAUL. Pillar accrual + the four-bar panel move into the PHASE-2 sub-slice; dent self-heal is a below-high-water seasonal restore (Q32); cross-pillar combos computed post-clamp (FU20).  <sub>← Q7, FU10, FU7, Q32, FU20 · ADR</sub>
- **[edit]** `M3 'Lands' combat reveals (≈4596)` — Add: stance slot reveals at R5; the first weapon-line L10 milestone reveals the ability/item slots (FU12 ladder).  <sub>← FU12 · ADR</sub>
- **[edit]** `M3 'Lands' + phase-3 quests (4598–4599, 4616)` — R5 reveals the Quest log + 4 quest TYPES, but with NO quest-count budget (author more quests, Q23). DEFEND remains the Arms earner — but Arms-pillar accrual is gated to PHASE 2 (FU7), so R5 reveals the type while accrual begins in phase 2.  <sub>← Q23, FU7, FU14 · ADR</sub>
- **[edit]** `M3 phase-4 (R6) + phase-5 (R7) (4617–4618)` — R6's 'first combat-earned Arms standing recorded' moves into phase 2 (deeds gated); R6/R7 stay story/rung beats. R7 is the FINAL phase-1 rung; crossing it OPENS phase 2 (pillar grind + four-bar panel reveal); the hybrid tier-gate (not simple thresholds) gates T0→T1.  <sub>← FU7, FU14, Q7, FU10 · ADR</sub>
- **[add-new]** `M3 — new phase + DoD (≈4619)` — Add a fun-proxy MEASUREMENT pass at M3 (Q4) still report-only; DoD asserts the hybrid-gate semantics, that pillar deeds are gated to phase 2, and that quest count is unbounded.  <sub>← Q4, FU9, FU7, FU10, Q23 · ADR</sub>
- **[edit]** `M4 'Lands' (4625–4635)` — Add the combat sub-slice: the 2nd weapon line (+3 T1 weapons, FU13) + valley-scale combat reveals; reflect per-tier phase-1 (V0→V7 rungs via rung-meter) → phase-2 (T1 pillar grind + hybrid gate, Office bar lights in phase 2) → T1→T2; pillar deeds gated to phase 2; fishing surfaces at V2 (Q4); coin (mon) numbers authored here (Q13); mechanical ±10% weather/festival layer deepens (Q35).  <sub>← FU13, FU12, FU7, FU10, FU14, Q4, Q13, Q35 · ADR</sub>
- **[add-new]** `M4 'Lands' + a new phase (≈4650)` — Land the intra-line dialogue ENGINE (FU22: choices[]+ChoiceId, locksLineIds[]/flags effects, deterministic, data-not-scripting, only chosen-flags persist) — first dialogue-rich beats incl. the Naoyuki T1 rivalry→grudging-respect beat (Q26) so the G5 ally-flip is earned.  <sub>← FU22, Q34, Q26 · ADR</sub>
- **[edit]** `M4 'Definition of done' + phases (4637–4651)` — Add: 2nd weapon line unlocks; dialogue choices are deterministic (only chosen flags persist); the T1 hybrid tier-gate replaces the simple Arms/Estate/Office thresholds; spine-only run still reaches the gate (unchanged) but via the phase-1/phase-2 structure.  <sub>← FU13, FU22, FU10, FU7 · ADR</sub>
- **[edit]** `M5 'Lands' (4655–4671)` — Add the combat sub-slice: the 3rd weapon line (+4 T2 weapons, FU13) at region scale; reflect per-tier phase-1 (G0→G7 rungs) → phase-2 (T2 pillar grind + hybrid gate) → T2→T3; pillar deeds gated to phase 2.  <sub>← FU13, FU12, FU7, FU10 · ADR</sub>
- **[edit]** `M5 'Lands' estate (≈4665) + phase-5 (4688)` — Author E3: estate grows E2→E3 (Q8) folded into the G4–G7 estate spend (no new rung) — the top v1 estate stage / extra koku+Arms sink.  <sub>← Q8, FU18 · ADR</sub>
- **[edit]** `M5 'Lands' name/payoff (4668–4670) + phase-6 (4689)` — The G6 Otsuru/Tama TRUTH stays spine-guaranteed; the 'Tahei' name-reclaim is Origin-gated at O5 and MISSABLE (not spine-guaranteed) (Q5); returning memory still grants access only, ZERO mechanical gift (Q12).  <sub>← Q5, Q12 · ADR</sub>
- **[add-new]** `M5 'Lands' + a phase (≈4689)` — Author the T2 anti-slump cross-pillar combos (Q22/FU20): BROADER combos (multiple pairs / larger magnitude) computed POST trade-clamp, trade ≤⅓ stays HARD, combos excluded from gate-threshold checks and counted inside the 70% deeds budget; plus the seasonal-reward rotation. Extend the intra-line dialogue engine to the Origin reunions (FU22).  <sub>← Q22, FU20, FU22 · ADR</sub>
- **[edit]** `M5 phase-5 + 'Lands' T3 stub (4671, 4688)` — T3 stub framing → castle-town / Daikan first-contact (drop the porter/Kaidō-guild framing, Q24).  <sub>← Q24</sub>
- **[edit]** `M5 'Definition of done' (4673–4680)` — Assert: E3 reached; name-reclaim O5-gated/missable while G6 truth fires on the spine; cross-pillar combos post-clamp + trade ≤⅓ hold; the T2 hybrid tier-gate gates T2→T3.  <sub>← Q8, Q5, FU20, FU10 · ADR</sub>
- **[rewrite]** `M6 'Goal' + 'Lands' balance pass (4694–4709)` — Tune to the FLOOR: T0≈4.5 h / T1≈8 h / T2≈16 h are MINIMUMS, every rung ≥~30 min; the pacing regression fails ONLY on undershoot (too fast / under budget), never overshoot; §4.8 = minimum-grind model (FU18).  <sub>← FU18 · ADR</sub>
- **[edit]** `M6 'Lands' content-verifier list (4701–4703) + phase-4 (4723)` — Replace 'no labour→combat cross-feed' with 'bounded per-skill perk magnitude (perks within bounds; conditioning ==0)'; replace simple-threshold gate checks with the hybrid good/great/excellent gate + NO-overflow; add Q28 hardening (gate-monotonicity/ceiling, rung-meter monotonicity, accrual tie-out, real-name lint); cross-pillar combos post-clamp + trade ≤⅓; world registry (Q55) id-resolution.  <sub>← Q6, FU8, Q7, FU10, Q28, FU20, Q55 · ADR</sub>
- **[edit]** `M6 'Lands' T3 stub + phase-5 (4704–4705, 4724)` — T3 stub = castle-town / Daikan first-contact (drop porter/Kaidō-guild framing, Q24); still no T3 ladder/auto-producers/marriage lever reachable.  <sub>← Q24</sub>
- **[edit]** `M6 'Lands' accessibility + phase-6 (4706, 4725)` — Add Q18 specifics: a persistent quiet a11y entry point (already from minute one), ARIA live-region scoped to narration+milestone ('polite'), a large-textScale reflow case, a screen-reader acceptance pass; functional text uses --ink-soft (WCAG AA), --ink-faint decorative-only (Q48).  <sub>← Q18, Q48</sub>
- **[edit]** `M6 'Lands' art/feel + phase-7 (4707–4709, 4726)` — Update the register to 'text + emoji + CSS + INLINE SVG (load-bearing pillar/season/rarity motifs, Q38, emoji cosmetic-only) + a small curated audio set (synthesized Web Audio + original/CC0 samples, Q50)'; corrects the 'no asset pipeline' claim.  <sub>← Q38, Q50 · ADR</sub>
- **[edit]** `M6 phase-2 + phase-3 (4721–4722)` — FLOOR semantics in the balance pass + pacing gate: tune so the grind is AT LEAST the budget + ≥30-min floor; the verify gate FAILS on too-fast only (no upper-bound failure).  <sub>← FU18 · ADR</sub>
- **[add-new]** `M6 — new phase (≈4727)` — Promote the fun-proxies from report-only to a GATING check at M6 (Q4/FU9) — the mitigation for the new fun risk row.  <sub>← Q4, FU9</sub>
- **[edit]** `M7 'Lands' + 'Definition of done' save (4733–4742)` — 'IndexedDB single autosave' → multi-backend redundant save; add the deferred itch cross-origin-iframe save-SURVIVAL test on Chromium AND WebKit (FU1/Q37); export/import round-trip retained.  <sub>← FU1, Q37 · ADR</sub>
- **[add-new]** `M7 — new phase (≈4746)` — Self-host the OFL fonts (kill Google dynamic-subsetting, bundle the OFL license, honour the Reserved-Font-Name rule) — works offline + on itch's relative-base (Q52); confirm zero font network calls in the smoke test.  <sub>← Q52</sub>
- **[add-new]** `M7 — new phase (≈4746)` — Add a LICENSE file before release: permissive code (MIT/Apache-2.0) + reserved game content (all-rights-reserved or CC-BY-NC) (Q51).  <sub>← Q51</sub>
- **[edit]** `M7 phase-2 build-provenance (4747)` — Expand the commit-SHA stamp into a full About/Credits surface (authorship, commit-SHA build stamp, font/audio attributions, clean-room attestation, license + content-descriptor pointers) (Q54).  <sub>← Q54</sub>
- **[edit]** `M7 phase-5 itch page (4750)` — Declare itch content descriptors (mild thematic: child-disappearance, drowning, debt) as a deploy-checklist step (Q53).  <sub>← Q53</sub>
- **[edit]** `M7 'Lands' + smoke tests (4733–4742, 4749)` — Bundle inline SVG + self-hosted fonts + the small synth/CC0 audio set in the static build; the offline smoke test asserts ZERO network calls (fonts now self-hosted).  <sub>← Q38, Q50, Q52</sub>
- **[edit]** `§7.2.1 milestone summary table (4755–4765)` — Update one-line goals: M2a 'auto-resolve' (drop 'idle'); M3 'phase-1 rungs + phase-2 pillar grind / hybrid gate → T0'; M4 '+2nd weapon line + intra-line dialogue + Naoyuki beat'; M5 '+3rd weapon line + E3 + combos'; M6 '+ inline-SVG/audio + a11y + fun-gate'; M7 '+ self-host fonts/license/credits/descriptors'. Add a §-systems note for the new decisions.  <sub>← FU12, FU13, FU7, FU10, Q8, Q26, Q38, Q50, Q18, Q4, Q52, Q51, Q54, Q53</sub>
- **[edit]** `§7.3 Deployment save bullet (4787–4791)` — 'IndexedDB single autosave' → multi-backend redundant save (IndexedDB+localStorage+sessionStorage), atomic write, magic field, monotonic-counter+timestamp newest-wins; base64 export remains the portable backstop; backwards-compatible additive schema (migrations rare).  <sub>← FU1, Q37, Q45 · ADR</sub>
- **[add-new]** `new subsection §7.3.1 'Assets (the acknowledged small set)' in §7.3` — Document the one acknowledged small asset set that corrects 'no asset pipeline': self-hosted OFL fonts (Q52), inline SVG load-bearing motifs (Q38), a small synthesized Web Audio + original/CC0 audio set (Q50) — all bundled, offline-capable, no network.  <sub>← Q38, Q50, Q52 · ADR</sub>
- **[add-new]** `new subsection §7.3.2 'About/Credits, licensing & content rating' in §7.3` — Author: the in-product About/Credits surface (authorship, commit-SHA build stamp, font/audio attributions, clean-room attestation, Q54); the license split (permissive code + reserved content, Q51); the itch content descriptors (child-disappearance, drowning, debt, Q53).  <sub>← Q54, Q51, Q53</sub>
- **[edit]** `§7.3 'How to ship' checklist (4798–4803)` — Add deploy-checklist steps: set itch content descriptors; verify fonts are self-hosted (no Google/network); verify the About/Credits + commit-SHA stamp is present; confirm the LICENSE file exists; run the multi-backend save-survival smoke on Chromium + WebKit.  <sub>← Q53, Q52, Q54, Q51, FU1, Q37</sub>
- **[rewrite]** `§7.4.1 risk row R2 (balance-tuning) (4818)` — Reframe to the FLOOR: the grind is a MINIMUM not a target; the §4.8 curve is derived to be AT LEAST the floor; the headless regression fails on undershoot only; tuning lengthens the grind / interleaves content rather than hitting a fixed total.  <sub>← FU18 · ADR</sub>
- **[edit]** `§7.4.1 risk row R3 (save migration) (4819)` — Add mitigations: multi-backend redundant write + monotonic-counter+timestamp newest-wins selector + backwards-compatible additive schema (never remove/repurpose) + magic-field reject-to-recovery further reduce save-loss; cross-origin-iframe survival tested at M7.  <sub>← FU1, Q37, Q45, Q46 · ADR</sub>
- **[edit]** `§7.4.1 risk row R4 (art/feel) (4820)` — Correct 'no asset pipeline' to 'a small curated asset set (inline SVG motifs + synth/CC0 audio + self-hosted fonts)'; still low-risk; the M6 polish + capture-game-states audit catch feel regressions.  <sub>← Q38, Q50, Q52</sub>
- **[add-new]** `§7.4.1 risk table — new row R6 (fun)` — Add the fun risk row: 'the LOCKED grind ships balanced but reads as a slog' — mitigated by the M1 report-only / M6 gating fun-proxies (Q4/FU9) and the FU18 interleave-don't-brick-wall + auto-resolve/auto-repeat 'leave it running' feel.  <sub>← Q4, FU9, FU18, FU23</sub>
- **[add-new]** `§7.4.1 risk table — new row R7 (perf/memory)` — Add the deferred perf/memory criterion as a tracked risk: a sustained-runtime per-tick + render-ms budget to be set after M0/M1 profiling (Q56).  <sub>← Q56</sub>
- **[add-new]** `§7.4.1 risk table — new row R8 (bounded perks / combat spread)` — Add: the bounded per-skill perks (no global cap) + holistic balance (gear/level/attrs/enemy-scaling grow together) carry accepted balance risk (FU8); the combat reveal-ladder spread across M2a→M5 must stay one-per-beat (no UI-dump) — mitigated by the FU12 design-staggered schedule + the verifier perk-magnitude bound.  <sub>← FU8, FU12</sub>
- **[edit]** `§7.5 §7-decisions→ADR (4839–4845)` — Update: V2 §7 now REFLECTS the Block L/M decisions (D-022 governing) — the budget→FLOOR reframe (FU18) annotates D-016; the proposed D-017 execution plan is updated for the re-cut milestones (full multi-backend save in M0, rung-meter + sequential phase-1/phase-2 in M1+, combat reveal-ladder spread M2a→M5 with combat sub-slices in M4/M5, E3 in M5, fun-proxies + a11y + assets + license + credits + content-rating at M6/M7). §7 still opens no NEW design decisions but carries the V2 reshape.  <sub>← D-022, FU18, FU1, FU6, FU7, FU12, FU13, Q8, Q4, Q9 · ADR</sub>
- **[keep]** `§7.5 'Flagged for the human — ALL RESOLVED' items 1–5 (4852–4866)` — Keep: M2a/M2b stay the fixed combat-ENGINE milestones; no pre-planned descope (full T0–T2); itch free/PWYW + relative base; local verify gate / manual upload — all still hold under V2 (combat sub-slices in M4/M5 are additive, not a re-split of M2).  <sub>← FU18, FU12</sub>
- **[edit]** `§7.0 / §7.1.1 prose name references (4368–4386, 4394–4398)` — Reconcile §7's named references against the final §5 rename list (Toyama/Konoe→invented Q27; Naozane + Obaa Sato renamed Q39; T0 field-lad off 'Mago' Q11; AGI glyph 体→敏) at integration — flag, no specific edit yet.  <sub>← Q27, Q39, Q11</sub>
- **[NEW]** §7.1.4 Per-tier shape (phase 1 → phase 2) @ after §7.1.3 (PARKED/CUT) — Make the SEQUENTIAL per-tier model legible at scope level: Phase 1 = climb the rungs via curated per-rung activities + the numeric rung-meter + story; Phase 2 = the estate-influence/pillar grind unlocks after the final rung → the hybrid good/great/excellent tier-gate → tier-up. Pillar deeds gated to phase 2. Plus the three clean combat tracks (combat-XP→level, deeds→Arms, curated activities→Combat-Rank meter).  <sub>← FU7, FU6, FU10, FU14</sub>
- **[NEW]** §7.2.0 Combat reveal ladder → milestone map @ after the §7.2 granularity note (≈line 4461) — A small table pinning which combat element reveals in which milestone (R3 weapon+loop+retreat→M2a; R4 durability + 2nd T0 weapon→M2b; R5 stance→M3; weapon-L10 ability/item slots→M3; 2nd weapon line +3 T1 weapons→M4; 3rd line +4 T2 weapons→M5) so the FU12/FU13 stagger has an unambiguous build home and no milestone UI-dumps.  <sub>← FU12, FU13, Q17</sub>
- **[NEW]** §7.3.1 Assets (the acknowledged small set) @ inside §7.3 (Deployment), after the export/import save bullet — Document the one acknowledged small asset set that corrects the 'no asset pipeline' claim: self-hosted OFL fonts (offline + itch relative-base), inline SVG load-bearing motifs (pillar/season/rarity), and a small synthesized Web Audio + original/CC0 audio set — all bundled, no network.  <sub>← Q52, Q38, Q50</sub>
- **[NEW]** §7.3.2 About/Credits, licensing & content rating @ inside §7.3 (Deployment), after §7.3.1 — Author the in-product About/Credits surface (authorship, commit-SHA build stamp, font/audio attributions, clean-room attestation), the license split (permissive code MIT/Apache-2.0 + reserved game content ARR or CC-BY-NC, a LICENSE file before M7), and the itch content descriptors (child-disappearance, drowning, debt) as a deploy-checklist step.  <sub>← Q54, Q51, Q53</sub>

### Cross-cutting NEW SYSTEMS (spanning §1, §2, §3, §4, §6) — the WHERE + shape

- **[rewrite]** `Canonical home: NEW §1.6.4 + a §3 phase-2 stage per tier. Cross-ref rewrites: §1.6.3, §2.15(a), §2.16(e), §3.2/§3.4/§3.6 capstone rows, §4.1, §4.2.1, §4.8.1/.2/.3.` — SEQUENTIAL per-tier progression. Replace the current PARALLEL model (deeds accrue across all rungs; capstone rung 'confirms' a tier already gated by pillars) with a two-phase structure. PHASE 1: climb rungs R0->R7 (V0->V7, G0->G7) gated by the numeric rung-meter (cross-2) + story flags ONLY — pillar deeds do NOT accrue here. PHASE 2: clearing the final rung UNLOCKS the estate-influence/pillar grind ('really level up the estate'); grind the four pillars to the hybrid gate (cross-6) -> tier-up. Section touchpoints: NEW §1.6.4 states the model; §1.6.3 table 'Required pillars' column reframed as the phase-2 gate; §2.15(a) rewrite 'capstone confirms... pillars gated in parallel' -> 'capstone OPENS phase 2'; §2.16(e) rewrite 'pillars accrue from early deeds' -> 'pillar deeds accrue in phase 2 only'; §4.2.1 achievement JUMPS + §4.6.5 Arms-deed are phase-2-gated; §4.8 pacing tables split each tier's hours into phase-1 rung-climb + phase-2 grind; §3 each tier gains a phase-2 'estate-influence grind' stage after its capstone rung.  <sub>← FU7, FU11, Q30, Q7 · ADR</sub>
- **[add-new]** `Canonical home: NEW §4 subsection (e.g. §4.1bis 'The rung-meter accrual law'). Cross-ref rewrites: §2.15(a)/(c), §3.0 trigger taxonomy 'RANK' row, §3.2.1 spine, §4.8.1/.2/.3 'Estate Service x1/x2' notation, §6.6 verifier.` — The rung-meter accrual law (the missing third balance curve). Define a numeric meter: per-rung-RESET; each rung's threshold = (>=30-min floor x that rung's eligible curated-activity rate), back-solved the same way §4.8.1's koku column is; AND-gate (promote only when meter >= threshold AND all that rung's story flags set), with an 'awaiting X' read when one side lags. There are TWO such meters per tier — Estate Service (labour rungs) and Combat Rank (martial rungs, renamed per Q9, cross-11). Fuel = a CURATED per-rung activity set (1-to-many designed activities, NOT a single repeat-counter, NOT the §4.2.1 pillar-deed inventory). §2.15 'two earned standing meters' becomes this numeric law; §3.0 'RANK' trigger and §3.2.1 spine map each rung to (meter threshold AND story); §4.8 pacing tables replace the discrete 'Estate Service x1' marks with the meter target; §6.6 gains a gate-monotonicity check against the rung targets (Q28).  <sub>← FU6, Q30, FU7, Q9, Q28 · ADR</sub>
- **[rewrite]** `Canonical home: NEW §4 subsection 'The three combat tracks + the character combat-level curve'. Rewrites: §2.8(a)/(c), §2.7(a)/(c)/(d), §4.4 (attr-points), §4.6.1 (hpMax), §4.6.5 (on-kill XP), §6.4 (character.level field).` — THREE clean combat tracks, de-conflated. Rewrite §2.8's 'character Combat Level = a Combat Deeds pool (combat-XP + deed-count of kills/clears/defends)' and delete the CombatDeedsPool data shape. The three independent stored tracks, one kill/deed writes to exactly one: (1) kills/combat-XP -> character.level (Q1, combat-fed ONLY; drives HP + attribute points); (2) recognized DEEDS -> the Arms pillar (§4.2.1, the influence economy); (3) per-rung curated activities -> the Combat-Rank rung-meter (cross-2). NEW §4 subsection defines the character combat-level XP->level curve (reuse §4.5.1 geometric shape, slower, fed by combat ONLY) so hpMax=40+8*characterLevel (§4.6.1), satietyMax-grows-with-level (Q47), and +1 attribute point / 2 levels (§4.4) all hang off ONE defined curve. mobLevel = explicit MobDef.level (FU15) is the on-kill XP source (§4.6.5, content/enemies.ts). Scope out / keep tiny the §2.7a 'compounding skill-XP multiplier' to avoid a combat->labour loop. Add character:{level} to the M0 stored surface (§6.4, FU5).  <sub>← FU14, Q1, Q47, FU15, FU5 · ADR</sub>
- **[rewrite]** `Canonical home: NEW §3 'combat-reveal ladder' subsection + NEW §4.6 'weapon roster' subsection. Rewrites: §2.8(a)/(b)/(e), §2.7(e), §2.10(a)/(e), §3.2 R3 row + R4/R5 rows, §4.6.1/§4.6.2.` — Incremental combat-progression ladder + a ~9-10 weapon roster. Replace 'combat goes live at R3 dumping the whole system' and '2-3 weapon lines at T0' (§2.7e/§2.8/§3.2). NEW §3 combat-reveal ladder (one reveal per beat, FU12): R3 = ONE starter weapon + bare auto-resolve loop + retreat -> R4 = durability bands (with the loot->craft loop, cross-ref FU17) -> R5 = stance slot -> first weapon-line L10 milestone = ability/item slots -> new weapons revealed rung/tier-to-tier. NEW §4.6 roster subsection (FU13): T0 unlocks 2 weapons, T1 +3, T2 +4 (~9-10 across v1; T0 still STARTS with the one starter; crude carrying-pole = a 0th improvised weapon, not a line). Each weapon = archetype params (baseSpeed / reach / targetCount / attack profile) + a signature ability; §4.6.1/§4.6.2's single baseSpeed=1.0 becomes per-weapon. §2.10 weapon roster + staggered Equipment/Inventory reveal; §3.2 R3 row rewritten to the single-weapon beat.  <sub>← Q15, FU12, FU13, Q17, FU17 · ADR</sub>
- **[rewrite]** `Canonical home: REWRITE §4.5.3 (-> per-skill perk tracks). Rewrites: §2.7(c)/(d), §4.4 no-cross-feed lines, §4.6.1 combatant model, §6.6 verifier, §4.9 levers/invariants.` — Per-skill perk system = bounded labour->combat cross-feed (Q6/FU8). Replace the §4.5.3 'no-cross-feed wall' (labour-skill+combat-attribute = type error) and the §6.6 '== 0 cross-feed' assertion. EVERY skill (combat + labour) gets a perk/flat-bonus track (~2-8 perks OR ~3-8 small flat stat bonuses, unlocked by leveling it); perks STACK, NO hard global cap. The combat bonus enters via a NEW separate additive term (skillCombatBonus on effectiveStats, §4.6.1) — NOT an attribute (avoids the §4.4 edge), NOT character level (Q1). Bound = small per-perk magnitude + incremental skill unlock (skills reveal per rung/tier, can't front-load) + holistic scaling. §6.6 verifier flips from '== 0' to 'each perk magnitude is small'. Conditioning STAYS the zero-stat enablement gate (don't let perks bypass it). Remove the 'no labour->combat cross-feed' HARD INVARIANT line from §4.9; annotate (don't delete) the prior canon.  <sub>← FU8, Q6, Q28 · ADR</sub>
- **[rewrite]** `Canonical home: REWRITE §4.1 (canonical). Cross-ref rewrites: §1.6.3, §2.16(a)-gating-block + TierGate data shape, §4.2.1 (re-derive deed inventory), §4.8 tie-out, §6.6 verifier, §4.9 invariants.` — Hybrid good/great/excellent tier-gate (Q7/FU10), reversing 'simple thresholds, NO floor/overflow' (D-016/F3) — annotate prior canon, don't delete. §4.1 rewritten as the canonical home: per-pillar-per-tier good/great/excellent triples (good = expected baseline, great = really strong, excellent = above-and-beyond); distribution = good in ALL revealed pillars, great in 2-3, excellent in 1-2; T0 2-pillar special ('good in both + excellent in 1', no phantom 3rd); NO overflow-substitution. The triples need a DEDICATED rebalance against the fixed §4.2.1 deed inventory (NOT simple 0.2x/0.5x ratios). Per-tier REVEALED-pillar set: T0=2 (Arms+Estate), T1=3, T2=3 — must match the §3 reveal schedule + §2.16(e) panel reveal. §1.6.3 / §2.16 gating blocks rewritten from 'SIMPLE THRESHOLDS' to the hybrid distribution; TierGate data shape changes from Partial<Record<pillar,threshold>> to good/great/excellent bands; remove 'no floor/overflow fields exist'. This hybrid gate IS the phase-2 tier-gate (cross-1).  <sub>← Q7, FU10, FU11, Q30 · ADR</sub>
- **[rewrite]** `Canonical home: REWRITE §6.8 + NEW §6.8.1 (backend architecture). Rewrites: §6.7 (RNG), §6.4 (RngState/character.level/save-meta/magic), §6.3 (dtTicks), §6.1 (Math.pow lint), §6.5 (registries), §6.6 (verifier).` — Multi-backend save layer + RNG cursors + determinism bans (Q37/FU1-5/Q2/Q3/Q36/Q44/Q45/Q46). §6.8 rewrite: redundant write to IndexedDB + localStorage + sessionStorage; atomic write (Q44, never clear-then-rewrite) + calm 'export a backup' notice; app-identity magic field (Q46) with reject-to-recovery; newest-wins on load = monotonic save-COUNTER (real selector) + save-layer timestamp (tiebreaker, a documented core-lint exemption per FU2); backwards-compatible ADDITIVE schema (Q45, migrations rare) + raw-backup/rollback safety net; never load a forward-version backend. NEW §6.8.1 = the backend-abstraction architecture (built in full at M0, FU1). §6.7 rewrite: rng = { seed, cursors:{combat,loot,seasonal,worldgen} } (FU3/Q2) replacing { seed, counter }; delete 'split child RNGs from parent counter'; add a stateless deriveDayKeyed(seed,'weather',day) helper (NOT a persisted cursor, FU3/Q3). §6.4: RngState shape, character.level (FU5), saveCounter+saveTimestamp+app magic as metadata; do NOT pre-declare subEngines/CombatEncounterState/dialogue (Q45, additive at their milestone). §6.3: dtTicks whole-integer (remainder app-side). §6.1: ban Math.pow/exp/log/trig -> integer-pow + lint (Q36). §6.5: add content/dialogue.ts (FU22), content/world.ts (Q55), content/beliefBeasts.ts + festivals (Q3/Q55). §6.6: real-name lint + accrual tie-out + gate-monotonicity (Q28).  <sub>← Q37, FU1, FU2, FU3, FU5, Q2, Q3, Q36, Q44, Q45, Q46, Q28, Q55 · ADR</sub>
- **[edit]** `Canonical home: §2.12(c). Cross-ref: §6.4 (flags persist), §6.5 (content/dialogue.ts), §6.3 (advance_dialogue keep-as-is).` — Intra-line dialogue branching (Q34/FU22). §2.12(c) TextLine gains a flat choices[] (+ ChoiceId); each choice's effect = locksLineIds[] / flagsSet. Data not scripting; deterministic (no RNG); save-light (only chosen-flags persist in GameState.flags, §6.4 — no new heavy storage; add the choices shape additively to content/dialogue.ts at M5, FU5/Q45). §6.3 advance_dialogue intent already carries choiceId? — keep-as-is, flag it as load-bearing for FU22.  <sub>← Q34, FU22, FU5, Q45</sub>
- **[edit]** `§2.1(a)/(b)/(e), §3.0 (cross-cutting rules), §6.4 (NO revealQueue field).` — Design-staggered reveals — NO runtime reveal-queue (FU4), resolving the conflict where the Q30/Q17 master-log said 'simultaneous reveals serialize into a deterministic one-per-beat queue'. FU4 supersedes (newest-wins): the unlock SCHEDULE is designed so reveals are inherently one-at-a-time (staggered by construction); genuine multi-element single-feature reveals are bespoke one-offs designed per case. §2.1 must NOT add a revealQueue[] to GameState; §3.0's fractal-incrementality rule stays but is framed as a design discipline, not a queue mechanism; reveal cadence is a gameplay/UI design responsibility. (Also reinforces NO-UI-DUMPS, Q17, across §3's R3 combat reveal per cross-4.)  <sub>← FU4, Q17, Q30 · ADR</sub>
- **[edit]** `§4.8 (all), §4.8.0 headline beats, §4.8.4, §4.9 LOCKED list, §1.6.3, §7.1.2, §2.5/§2.8 (auto-resolve/auto-repeat).` — Budget is a FLOOR, not a ceiling (FU18) + tab-open auto-resolve/auto-repeat (FU23). Reframe §4.8 from a fixed-total pacing model to a MINIMUM-grind model: the ~28.5h (T0 4.5 / T1 8 / T2 16) becomes a floor; the game can be a longer OSRS-rough grind (interleave, don't brick-wall). §4.8.0/§4.8.4 'LOCKED budget' -> 'LOCKED minimum'; §4.9's LOCKED-list and §7.1.2 acceptance criterion annotated (don't delete; D-016 annotated). v1 stays active-only (FU23): tab-open idle auto-resolve combat + auto-repeating labour give the 'leave it running, check progress' feel — §2.8 'auto-continue toggle' and §2.6 labour loop carry this; auto-producers stay T3+. This interacts with cross-1: each tier's floor splits into phase-1 rung-climb + phase-2 grind.  <sub>← FU18, FU23, FU7 · ADR</sub>
- **[edit]** `§2.8, §2.15, §3.0/§3.2.1, §4.x rung-meter, §4.9, §6.5 content/ranks.ts.` — Terminology unification (Q9): rename the combat rung-gate 'Combat Standing' -> 'Combat Rank' project-wide, so 'Standing' refers only to the 官威 Standing-&-Office pillar. The Combat-Rank rung-meter (cross-2 / FU14 track 3) must NOT be reconflated with the Arms pillar (track 2) or with character combat-level (track 1). Macronize goshi/ronin per Q9. Keep the three combat tracks lexically distinct everywhere: 'character (combat) level' (track 1), 'Arms pillar' (track 2), 'Combat Rank meter' (track 3).  <sub>← Q9, FU14</sub>
- **[NEW]** §1.6.4 — Sequential per-tier progression (Phase 1: climb the rungs → Phase 2: the estate-influence grind) @ §1.6, immediately after §1.6.3 (the tiers/gating subsection) — The conceptual home of the two-phase model: Phase 1 = climb rungs via curated activities + the rung-meter + story (pillar deeds NOT accruing); Phase 2 = the pillar grind unlocks after the final rung -> the hybrid tier-gate -> tier-up. Anchors cross-1, referenced by §2.15/§2.16/§3/§4.1/§4.8.  <sub>← FU7, FU11, Q30, Q7</sub>
- **[NEW]** §3.x — The two-phase tier structure + the per-tier Phase-2 estate-influence grind stage @ §3: a short addition to §3.0 (how-to-read), plus a Phase-2 'estate-influence grind' block appended after each tier's capstone rung (§3.2 R7, §3.4 V7, §3.6 G7) — Makes the rungs-then-grind structure concrete in the unlock ladder: phase-1 rung tables stay (gated by rung-meter+story), and a NEW phase-2 stage per tier hosts the pillar-deed grind to the hybrid gate. Where the §4.2.1 deeds actually fire.  <sub>← FU7, Q30, Q7, Q23</sub>
- **[NEW]** §3.x — The combat-reveal ladder (cross-tier, one reveal per beat) @ §3, a new subsection after §3.2.1 (or a standalone cross-tier table referenced by §3.2/§3.4/§3.6) — The staggered schedule for combat functionality: R3 starter weapon+loop+retreat -> R4 durability -> R5 stance -> weapon-L10 ability/item slots -> new weapons per rung/tier. Anchors cross-4; replaces the §3.2 R3 'dump everything' row.  <sub>← Q15, FU12, Q17, FU17</sub>
- **[NEW]** §4.1bis — The rung-meter accrual law (the third balance curve) @ §4, a new subsection adjacent to §4.1 (before §4.2 accrual) — Defines the numeric Estate-Service / Combat-Rank meters: per-rung-reset, threshold back-solved from the >=30-min floor x curated-activity rate, AND-gate with story flags. The third curve §4 currently lacks (alongside pillars and skill-XP). Anchors cross-2.  <sub>← FU6, Q30, FU7, Q28</sub>
- **[NEW]** §4.x — The character combat-level XP→level curve + the three combat tracks @ §4, a new subsection (e.g. §4.5.0 before the per-skill curves, or §4.6.0 before the combat math) — Defines the combat-only XP->characterLevel curve that hpMax/satietyMax/attribute-points all depend on, and a one-kill-writes-to-each disambiguation table for the three tracks (level / Arms pillar / Combat-Rank meter). Anchors cross-3; reconciles §4.6.1/§4.6.5/§4.4/Q47.  <sub>← FU14, Q1, Q47, FU15</sub>
- **[NEW]** §4.6.x — The weapon roster (~9-10 weapons: archetype params + signature abilities) @ §4.6, a new subsection after §4.6.1/§4.6.2 (the combatant model & cadence) — Per-weapon auto-battler params (baseSpeed/reach/targetCount/attack profile) + signature ability for the growing roster (T0 2 / T1 +3 / T2 +4); turns the single baseSpeed=1.0 into per-weapon data. Anchors cross-4 / FU13; feeds content/items.ts.  <sub>← FU13, Q15</sub>
- **[NEW]** §4.5.3 (rewritten) / §4.5.4 — Per-skill perk tracks (bounded labour→combat cross-feed) @ §4.5, rewriting §4.5.3 in place (or a new §4.5.4 superseding the no-cross-feed wall) — Per-skill perk/flat-bonus tracks (~2-8 each, stackable, no global cap) routing a small combat bonus through a new additive skillCombatBonus term (not attribute, not level); replaces the type-level no-cross-feed wall with small-per-perk-magnitude. Anchors cross-5 / FU8/Q6.  <sub>← FU8, Q6, Q28</sub>
- **[NEW]** §6.8.1 — The multi-backend save-layer architecture @ §6.8, a new subsection (the backend abstraction built in full at M0) — The backend-abstraction + atomic write + magic field + monotonic-counter/timestamp newest-wins selector + reject-to-recovery layer (FU1) — the M0 deliverable that the rewritten §6.8 prose describes structurally. Anchors cross-7.  <sub>← FU1, Q37, FU2, Q44, Q45, Q46</sub>

### COVERAGE CHECK — all 79 V2 decisions (Block L Q1–Q56 + Block M FU1–FU23) mapped to PRD §1–§7

- **[rewrite]** `§2.7, §2.8(a)/(c), §4.4, §4.5, §4.6.1, §6.4` — SUBSTANTIVE. Character level = its own STORED track fed by COMBAT-XP only; HP+attribute points scale off it. Rewrite §2.8's conflated 'Combat Level = CombatDeedsPool (combat-XP + deed-count)' and §2.7a 'character level grants...'; add character.level to §6.4 stored surface; §4.4 '+1 attr pt/2 levels' and §4.6.1 hpMax=40+8·characterLevel now reference a defined combat-fed curve.  <sub>← Q1, FU14, FU5 · ADR</sub>
- **[rewrite]** `§2.7c/d, §4.4, §4.5.3, §4.6.1, §1.13, §6.6` — SUBSTANTIVE + CONTRADICTS canon. Relax the absolute no-labour→combat lock into a BOUNDED cross-feed. Rewrite the four places that hard-state 'no cross-feed / type-error / ==0': §2.7c guard, §2.7d ties, §4.4 rule line, §4.5.3 no-cross-feed wall + the 2026-06-25 type-level box, §1.13 guardrail; flip §6.6 verifier from '==0' to small-magnitude. NOTE: Q6's 'small CAPPED' is SUPERSEDED by FU8 (per-skill perks, NO hard global cap) — reconcile newest-wins.  <sub>← Q6, FU8 · ADR</sub>
- **[rewrite]** `§2.7e, §2.8(a)/(e), §3.2 (R3 row), §4.6, new combat-ladder subsection` — SUBSTANTIVE + CONTRADICTS '2-3 weapon lines at T0'. Combat is INCREMENTAL: T0 starts with exactly ONE weapon; weapons/styles unlock rung/tier (≥1 new per tier). Reconcile §2.7e/§2.8 'Lean core lines at T0 (2-3 weapon lines)' down to one. NOTE: Q15 '3 lines total' is EXTENDED by FU13 (~9-10 weapons) — 3 archetypes, growing roster.  <sub>← Q15, FU12, FU13 · ADR</sub>
- **[rewrite]** `§4.1, §1.6.3, §2.16` — SUBSTANTIVE + REVERSES D-016/F3. Replace 'SIMPLE bands — NO floor/overflow' tier-gate with HYBRID specialization: good in ALL revealed pillars, great in 2-3, excellent in 1-2; T0 2-pillar special; NO overflow. §4.1 header/table, §1.6.3 gating, §2.16 all currently state the now-rejected 'simple thresholds, no floor/overflow' as canon — must be rewritten. Thresholds via FU10 overhaul.  <sub>← Q7, FU10, FU11 · ADR</sub>
- **[rewrite]** `§2.15, §3.2.1/§3.4.1/§3.6.1, new §4 rung-meter subsection, §6.6` — SUBSTANTIVE. Rung promotion gates on BOTH a NUMERIC rung-meter (a real §4 curve fed by rung-specific activities) AND story milestones (AND-gate). Adds a THIRD balance curve §4 lacks. §2.15 currently implies a single arbitrary/flag promotion; earned-transition spines map to flags but need the meter overlay. Double-counting allowed.  <sub>← Q30, FU6, FU7, FU11 · ADR</sub>
- **[edit]** `§4.6.6, §2.8(b), §4.8/§7 (win-rate proxy)` — SUBSTANTIVE. Retreat = clean escape (keep HP/loot, modest clock cost, never dents Influence; abandoning a defend-deed = failed defend). Add headless win-rate bands at R3/V2/V5/G1/G5 as a 2nd pacing proxy. §4.6 has no retreat-resolution rule today.  <sub>← Q16, FU19</sub>
- **[rewrite]** `§2.10, §4.6/§4.7 (durability math)` — SUBSTANTIVE + reverses the binary-unequip rec. GRADED durability bands; weapon stays equipped/functional even at 0 — never auto-unequip/weaponless. §2.10 'durability that wears and is repaired' needs the band model; numbers from FU17.  <sub>← Q33, FU17 · ADR</sub>
- **[edit]** `§4.0, §4.2.1, §4.8.1/§4.8.4` — SUBSTANTIVE (numbers). Koku is comfortable/NET — finish T0 holding ~18-19K; drop the 3-5K double-count. Restate the §4.0 held band and the §4.8 tie-out so per-rung rates are net of upkeep.  <sub>← Q14</sub>
- **[edit]** `§4.1 (T0 Estate gate proof), §4.2.1, §4.8.1, §2.4` — SUBSTANTIVE (bookkeeping). Keep no-market-in-T0; re-itemize the T0 Estate gate with LAND/TREASURY deeds only (no trade contracts); pull Arms-deed onset to R4 so R6/E2 clears on deeds not seasonal luck. Village shops stay first market (T1).  <sub>← Q29, Q7</sub>
- **[edit]** `§2.4, §4.7, §7 (M4)` — SUBSTANTIVE (minor). Defer coin/market numbers to M4 (placeholder, not frozen); strike the stray '+ caps' so resource counts stay unbounded (matches counts-only save).  <sub>← Q13</sub>
- **[add-new]** `§2.14, §4 (weather magnitudes), §6.7/§6.5` — SUBSTANTIVE. Weather + festivals are MECHANICAL, bounded ±10% on labour/combat rate + festival economic beats, derived from a day-keyed RNG sub-stream. §2.14 currently lacks mechanical magnitudes; needs a §4 weather/festival lever block.  <sub>← Q35, Q3, Q55, FU3</sub>
- **[edit]** `§4.2.4, §4.2.2` — SUBSTANTIVE (minor). Dent self-heal = a small below-high-water seasonal restore branch that never advances the high-water. Add to §4.2.4 dents + the §4.2.2 seasonal formula.  <sub>← Q32</sub>
- **[add-new]** `§4.2.2, §4.3 (no-leakage exception), §4.8.3, §5 T2` — SUBSTANTIVE + carves an invariant exception. T2 anti-slump = BOTH seasonal-reward-rotation AND cross-pillar combos (a narrow §4.3 no-leakage exception). EXTENDED by FU20 (broader combos; trade-≤⅓ stays HARD).  <sub>← Q22, FU20 · ADR</sub>
- **[rewrite]** `§2.17, §4.7.5, §3.6, §5 T2.4, §7.1.1/§7.1.2, §3.7.1` — SUBSTANTIVE + un-parks scope + CONTRADICTS current docs. Author an E3 estate stage for v1 → estate grows E0→E3. CONTRADICTION: §5 T2.4 says 'E2 → early E3', §3.7.1 puts 'E3→E4' at T3, §7/§4.7.5 only author E0→E2, master-sheet rec said 'cap at E2'. Newest (Q8) wins — fully author E3 in v1, push T3 to E4+, add the §4.7.5 cost row.  <sub>← Q8, FU18 · ADR</sub>
- **[edit]** `§4.7 (market/saturation), §2.11/§2.4` — SUBSTANTIVE (minor). Saturation damper applies progressively PER-UNIT on bulk sales (each unit walks the price down — legible, un-gameable). Bulk sell-N/craft-N UI is an implementation nicety.  <sub>← Q42, Q13</sub>
- **[edit]** `§4.6.1, §4.6.6, §2.3, §2.8` — SUBSTANTIVE + perturbs a LOCKED criterion. Combat IS satiety-throttled ('eat before you fight'); add a satiety term to §4.6.1 attackPower; re-spec the locked 20-35% first-fight win-rate as 'at adequate satiety'; floor ~0.5. CONTRADICTION: master-sheet rec was labour-only throttle floor 0.3 — Block L Q31 + FU16/FU21 override to combat-throttled floor 0.5.  <sub>← Q31, FU16 · ADR</sub>
- **[edit]** `§2.3(c), §4.4/§4.6.1, §6.4` — SUBSTANTIVE. satietyMax grows with (combat) level from the start (not a flat stub). Add the formula (base + per-level growth, per FU21) to §2.3/§4.  <sub>← Q47, FU21, FU5</sub>
- **[edit]** `§2.1, §4 (STAMINA_RATE_FLOOR), §2.3, §3 (reveal staggering)` — SUBSTANTIVE. STAMINA_RATE_FLOOR ~0.5 + a GENERAL no-UI-dumps principle (stagger ALL reveals one-per-beat). CONTRADICTION: Q17's master-rec 'serialize into a deterministic one-per-beat QUEUE' is SUPERSEDED by FU4 (NO runtime queue — design-staggered). Floor 0.3→0.5 per Q31/FU21.  <sub>← Q17, FU4, Q31 · ADR</sub>
- **[rewrite]** `§6.7, §6.3, §6.4, §2.19/§6.8, §6.13` — SUBSTANTIVE. RNG = per-named-stream persisted cursors {seed, cursors:{combat,loot,seasonal,worldgen}} + whole-integer dtTicks. Rewrites §6.7's 'state.rng = {seed, counter}' single-counter model and the §6.13#2 framing; §6.3 tick contract takes integer ticks (remainder app-side).  <sub>← Q2, FU3, Q36 · ADR</sub>
- **[edit]** `§6.4, §6.5, §2.14, §2.9` — SUBSTANTIVE. Persist market-saturation only; flag-encode estate stage; DERIVE weather/lunarPhase from a day-keyed RNG sub-stream; belief-beasts get content/beliefBeasts.ts. Trims §6.4 stored surface; §2.9 already separates BeliefBeast registry (confirm).  <sub>← Q3, Q2, Q55, FU3</sub>
- **[add-new]** `§6.4, §2.12, §2.16 (subEngines), §6.5` — SUBSTANTIVE. Feature-rich data model: intra-line dialogue branching IS in v1; nest estateWealth.subEngines{land,treasury,trade} (storage for the trade-≤⅓ clamp); sketch CombatEncounterState. RECONCILE with FU5: declared in v1 but added ADDITIVELY at their milestone (M3/M5), NOT pre-declared at M0.  <sub>← Q34, FU22, FU5, Q45 · ADR</sub>
- **[edit]** `§6.1 (lint), §4.5.1, §4.6, §4.7 (all power curves)` — SUBSTANTIVE + cross-cutting. Ban Math.pow → integer-pow-by-repeated-multiplication + a §6.1 core lint (whitelist sqrt). Touches EVERY §4 formula using ^ (xpForLevel, growth curves) — easy to leave a stale x^n; flag for the §4 sweep.  <sub>← Q36, Q2</sub>
- **[edit]** `§6.8, §2.19` — SUBSTANTIVE. Atomic autosave write (one put, never clear-then-rewrite) + a calm persistent 'couldn't save — export a backup' notice on any rejection. §6.8/§2.19 robustness is LOAD-side only today.  <sub>← Q44, FU1</sub>
- **[rewrite]** `§6.8, §2.19, §7 (M0 DoD), §7.3 (deploy test)` — SUBSTANTIVE + reverses 'single IndexedDB'. Multi-backend redundant save (IndexedDB + localStorage [+ sessionStorage]); on load read ALL, newest-wins. Rewrites §6.8 'Primary store: IndexedDB ... single autosave slot'. Q43 is a duplicate of this. M7 still tests itch-embed survival on Chromium+WebKit.  <sub>← Q37, Q43, FU1, FU2 · ADR</sub>
- **[edit]** `§6.8 (migrations), §2.19` — SUBSTANTIVE. Backwards-compatible additive (protobuf/thrift-style) save schema — optional defaulted fields, never remove/repurpose → migrations rare. Reframe §6.8 'ordered migrations' as the rare-case safety net + keep raw-backup/rollback.  <sub>← Q45, FU1, FU5 · ADR</sub>
- **[edit]** `§6.8, §2.19` — SUBSTANTIVE (minor). Add an app:'kami-kakushi' magic field (fast-reject foreign/corrupt) + reject-to-recovery on a stale/broken id (no silent overwrite).  <sub>← Q46, Q44</sub>
- **[edit]** `§6.6 (verifier)` — SUBSTANTIVE. §6.6 verifier gains gate-monotonicity/ceiling, accrual tie-out (deeds sum to gate share), and a real-name denylist lint. NOTE: the verifier's '==0 cross-feed' assertion must FLIP to small-magnitude per Q6/FU8 — keep them consistent.  <sub>← Q28, Q27, Q7, Q14, FU8</sub>
- **[edit]** `§6.5, §2.14` — SUBSTANTIVE (minor). World-sim content (SeasonRules/Festival/WeatherHazard) → a content/world.ts registry, generated/verified like the others (data-as-code).  <sub>← Q55, Q35, Q3</sub>
- **[edit]** `§4.0/§4.x, §6.6, §6.5 (gen-docs paths)` — SUBSTANTIVE (minor). Tag duplicated derived values 'illustrative — see generated'; pick Name's §4.0 display band-top + fix the gate gloss; align gen-docs paths (docs/balance/ + docs/content/); add the §6.6 verifier assertion when balance.ts lands.  <sub>← Q41</sub>
- **[rewrite]** `§5 (G6 + O5 beats), §3.6.2 (Origin O5), §1.9, §1.5.3` — SUBSTANTIVE + resolves Q25+Q40. Otsuru/Tama TRUTH = spine-guaranteed at G6 (every player); reclaiming the name 'Tahei' = Origin-gated at O5 (earned, missable). DELETE the guaranteed-G6 name flavour line. CONTRADICTION: Q40 master-rec 'keep naming on the G6 spine' is SUPERSEDED by Q5 (name → O5 only).  <sub>← Q5, Q25, Q40, Q12 · ADR</sub>
- **[rewrite]** `§5 T3 header+beats, §3.7.1, §1.11/§1.7.1` — SUBSTANTIVE. v1 ends on the castle-town/Daikan first-contact (magistrate's office, stone walls); rewrite the stale §5 T3 header and DELETE the porter-guild framing (it re-ran spent T2 content).  <sub>← Q24</sub>
- **[add-new]** `§5 T1.5/T2.5 (+T1.2/T2.2), §1.5.1/§1.8` — SUBSTANTIVE (add). Add one T1/early-T2 Naoyuki beat (rivalry → grudging respect) so the G5 ally-flip reads as earned. Currently the arc is seeded T0 then absent until G5.  <sub>← Q26</sub>
- **[edit]** `§5 T0 (find-spot + T0.5), §1.9, §1.8 (rename field-lad)` — SUBSTANTIVE. One find-spot (co-locate the jizō at the weir/ford); 'presumed dead → back from the dead' applied consistently; rename the T0 field-lad off 'Mago'. FLAG: 'Mago' collides with T1 antagonist 'Magobei' — pick a clearly distinct name.  <sub>← Q11, Q39</sub>
- **[edit]** `§1.8 cast, §5 (T3/T4 apex chars), §1.13/A6, §6.6` — SUBSTANTIVE + cross-cutting. Swap the real surnames (Toyama, Konoe) for invented ones; the Q28 real-name lint prevents recurrence. Touches §1.8 + every §5 mention — flag for a name sweep.  <sub>← Q27, Q28, Q39</sub>
- **[edit]** `§1.8 cast, §5 (T3/T0), §4.4 (AGI glyph)` — SUBSTANTIVE (minor). Rename non-locked Naozane + Obaa Sato (off Naoyuki/Sayo collisions); re-glyph AGI 体→敏 (§4.4 table currently shows AGI (体)); add the Konoe replacement cast row. Locked names unchanged.  <sub>← Q39, Q27</sub>
- **[edit]** `§1.5.3, §1.9, §3.6.2, §6.6 (allow-list), §5 T2` — SUBSTANTIVE (minor). Rescope Origin 'ZERO mechanical gift' to the backstory/identity reveal only (the ~10-15% speedup + skill-XP buff STAY); add Nihonbashi to the §6.6 fictional allow-list; T2 social verbs stay deed-framed.  <sub>← Q12, Q5</sub>
- **[edit]** `§2.12, §1.11, §7.1.1, §5 (per-tier side-quests)` — SUBSTANTIVE + supersedes D-012. NO fixed quest-type budget — '4 grind types' is just the T0 starting set, not a cap; author whatever quests fit each stage (more/interesting welcome, esp. later tiers). Reword §2.12/§7.1.1's 'the 4 types' framing.  <sub>← Q23, FU18 · ADR</sub>
- **[edit]** `§2.1, §3.2/§3.4 (R4/R5 reveal rows), §6.9, §2.11/§2.12` — SUBSTANTIVE. Distinct activities (Crafting, Quests) = their own TOP-LEVEL nav tabs (main screen stays the active labour/deeds/combat loop), not nested panels. CONTRADICTION: master-sheet P2 rec said 'nested panels' — Block L Q10 overrides to top-level tabs. Still revealed one-at-a-time (Q17/FU4).  <sub>← Q10, Q17, Q30 · ADR</sub>
- **[add-new]** `§7 (M1/M3/M6 tasks + risk row), §4.8, fun-factor.md/qa-playtesting.md` — SUBSTANTIVE. Wire fun-proxies into M1/M3/M6 + a fun risk row ('the locked grind ships balanced but reads as a slog'). FLAG: primary detail lives in fun-factor.md/qa-playtesting.md (external) — the PRD touch is the §7 milestone tasks + §7.4 risk. Report-only at M1, gates at M6 (FU9).  <sub>← Q4, FU9, Q20</sub>
- **[edit]** `§6.9 (responsive), §6.11/§2.21, §6.8/§2.19 (save-safety)` — SUBSTANTIVE. Mobile = per-tier primary tabs on the bottom bar + an overflow drawer (older screens in reveal order); save-safety = explicit confirm on destructive actions + an auto pre-overwrite snapshot.  <sub>← Q19, Q49, Q37</sub>
- **[edit]** `§6.11, §2.21` — SUBSTANTIVE (partly already in §6.11). Do the low-cost a11y correctness items now: persistent quiet a11y entry from minute one, ARIA live region scoped to narration+milestone ('polite'), large-textScale reflow case, a screen-reader acceptance pass. §6.11 already has live region/landmarks/skip-link/lang-tags — confirm + extend.  <sub>← Q18</sub>
- **[edit]** `§6.9/§6.11, ui-design.md` — SUBSTANTIVE (minor). Functional/hint text → --ink-soft (passes WCAG AA on all paper surfaces); --ink-faint decorative-only; darken the meter fill. FLAG: primary owner is ui-design.md; PRD touch is the presentation register.  <sub>← Q48</sub>
- **[edit]** `ui-design.md (primary), §2.1/§2.16 (panel), §4.1` — SUBSTANTIVE (minor). Pillar bars show distance-to-next-gate; gain/loss flash uses the §2 tokens (gain=--ai, loss=--beni); vermilion reserved for rank-up/seal. FLAG: mostly a ui-design.md change — easy to miss in a PRD-only sweep; the §4.1 distance-to-gate read is the PRD hook.  <sub>← Q21</sub>
- **[edit]** `§4.8 (pacing), fun-factor.md (primary), §7` — SUBSTANTIVE (minor). Deed-cadence proxy → tier-relative (T0~5 / T1~8 / T2~13 min); the broad reward/number-jump clause carries the no-dead-time guard; the slower T2 budget stays intentional. FLAG: primary owner fun-factor.md.  <sub>← Q20, FU9, Q4</sub>
- **[edit]** `§2.8(e)/§2.15 (rename), §1.6 (官威 pillar), §3.2.1 (rung names), project-wide` — SUBSTANTIVE + cross-cutting. Rename the Arms rank-gate 'Combat Standing' → 'Combat Rank' (so 'Standing' = the 官威 pillar only); macronize gōshi + one rōnin form project-wide. NOTE: reconcile with FU14's 'Combat-Standing rung-meter' wording → 'Combat Rank rung-meter'. Flag for an all-sections terminology sweep.  <sub>← Q9, FU14</sub>
- **[edit]** `§6.9 (art register), §2.21, ui-design.md` — SUBSTANTIVE. Inline SVG for load-bearing period motifs (pillar/season/rarity marks — identical across OSes); emoji cosmetic-only. §6.9 'text + emoji + CSS' register adds inline SVG.  <sub>← Q38</sub>
- **[edit]** `§2.21 (audio), §6.9, §7 (M6, R4 'no asset pipeline' fix), §7.3` — SUBSTANTIVE + corrects K1/D-013 'no asset pipeline'. 'Good audio' = mixed synthesized Web Audio + original/CC0 samples (the one acknowledged small asset set); correct R4's 'no asset pipeline' to 'text+emoji+CSS + a small curated audio set'.  <sub>← Q50, Q54 · ADR</sub>
- **[edit]** `§6.9/§6.1 (fonts/build), §7.3 (deploy), ui-design.md` — SUBSTANTIVE. Self-host the OFL fonts (kill Google dynamic-subsetting — breaks offline + itch relative-base); bundle the OFL license; clear the Reserved-Font-Name rule.  <sub>← Q52</sub>
- **[add-new]** `§7.3 (deploy/license), §7 M7` — SUBSTANTIVE (new). License = permissive code (MIT/Apache-2.0) + reserved game content (all-rights-reserved or CC-BY-NC); add a LICENSE file before M7. No license section exists today.  <sub>← Q51</sub>
- **[add-new]** `§2.21/§6.9 (new surface), §7.3, §6.10` — SUBSTANTIVE (new). Add a small About/Credits surface (authorship, commit-SHA build stamp, font/audio attributions, clean-room attestation). New in-product surface — pre-satisfies attribution for Q50/Q52/Q38.  <sub>← Q54, Q50, Q52, Q38</sub>
- **[add-new]** `§7.3 (deploy checklist), §7 M7` — SUBSTANTIVE (new minor). Declare itch content descriptors (mild thematic: child-disappearance, drowning, debt) as a §7.3/M7 deploy-checklist step.  <sub>← Q53</sub>
- **[edit]** `§7.4 (risk register), §7.1.2` — SUBSTANTIVE (minor). Defer a perf/memory acceptance criterion (per-tick + render ms budget) to M0/M1 profiling; note the intent in the risk register now (add the R7 row once real numbers exist).  <sub>← Q56</sub>
- **[edit]** `§5 G6, §3.6.2 (O5)` — NO-OP standalone — RESOLVED BY Q5. A player who skips Origin does NOT reclaim the name at G6 (name is Origin-gated O5); only the Otsuru truth is guaranteed at G6. Folded into the Q5 rewrite.  <sub>← Q5, Q25</sub>
- **[edit]** `§5 G6/O5 prose, §3` — NO-OP standalone — RESOLVED BY Q5 (climax de-duplication: G6 carries the truth, O5 carries the name). CONTRADICTION: Q40's own master-rec 'keep naming on the guaranteed G6 spine' is SUPERSEDED by Q5. Folded into Q5.  <sub>← Q5, Q40</sub>
- **[keep]** `§6.8/§2.19/§7.3` — NO-OP — DUPLICATE of Q37 (itch cross-origin storage survival). Covered by the multi-backend save rewrite (Q37/FU1). No separate change.  <sub>← Q43, Q37</sub>
- **[edit]** `§6.8/§2.19 (save-safety), §6.9 (UI confirms), §2.7/§4.4 (attr allocation)` — SUBSTANTIVE (minor). Confirm only the genuinely-unrecoverable rare actions (no-respec attribute allocation, rare-material consume, narrative-route choices); record the action-class policy; the fast loop stays frictionless.  <sub>← Q49, Q19</sub>
- **[edit]** `§7 (M0 DoD), §6.8, §2.19` — SUBSTANTIVE. Build the FULL multi-backend save layer in M0 (backend-abstraction + atomic write + magic field + newest-wins selector); rich per-system fields added additively later. Adds an M0 deliverable/DoD bar.  <sub>← FU1, Q37, Q44, Q45, Q46</sub>
- **[edit]** `§6.8 (save selector), §6.7/§6.1 (clock-free core + lint exemption), §2.19` — SUBSTANTIVE. Newest-save selector = a save-layer Date.now (a DOCUMENTED core-lint exemption — metadata, not game logic) + a monotonic save-counter as the real selector, timestamp as tiebreaker; never load a forward-version backend. The deterministic core stays clock-free.  <sub>← FU2, Q37, Q2, Q45</sub>
- **[rewrite]** `§6.7, §6.3, §6.4, §2.14` — SUBSTANTIVE. RNG = {seed, cursors:{combat,loot,seasonal,worldgen}} (persisted, monotonic per stream); weather/lunar = a PURE stateless deriveDayKeyed(seed,'weather',day) helper (NOT a persisted cursor); dtTicks remainder accumulates in the app loop. Implements Q2/Q3; rewrites §6.7.  <sub>← FU3, Q2, Q3, Q35</sub>
- **[rewrite]** `§2.1 (UI-reveal engine), §3 (unlock schedule), §6.9` — SUBSTANTIVE + CONTRADICTS Q17's queue reading. NO runtime reveal-queue — design the unlock schedule so reveals are inherently one-at-a-time (staggered by construction); multi-element single-feature reveals are bespoke one-offs. Reveal cadence is a design responsibility, not a runtime queue. Supersedes 'serialize into a one-per-beat queue.'  <sub>← FU4, Q17, Q34 · ADR</sub>
- **[edit]** `§6.4 (M0 stored skeleton), §7 (M0)` — SUBSTANTIVE. M0 GameState = {hp,satiety,attributePoints} + character.level (=1 floor) + satietyMax-at-floor; do NOT pre-declare subEngines/CombatEncounterState/dialogue — add additively at their milestone (reconciles with Q34's 'in v1').  <sub>← FU5, Q1, Q47, Q34, Q45</sub>
- **[add-new]** `new §4 rung-meter subsection, §4.8.1 (R0-R2 thresholds), §2.15, §3.2.1, §6.6` — SUBSTANTIVE (new balance curve). Rung-meter law: per-rung-RESET; each rung's threshold = (≥30-min floor × that rung's eligible-activity rate), back-solved like the koku column; AND-gate (meter ≥ threshold AND story flags), 'awaiting X' read when one lags. The third §4 curve does not exist today.  <sub>← FU6, Q30, Q9, Q28, Q10 · ADR</sub>
- **[rewrite]** `§3 (ladder structure), §4.8 (parallel→sequential accrual), §2.15, §2.16/§4.1, §4.2.1, §5` — SUBSTANTIVE + BIG structural shift. SEQUENTIAL per-tier: Phase 1 climb the rungs via CURATED per-rung activities (1-to-many, NOT a repeat-counter) + rung-meter + story → Phase 2 estate-influence/pillar grind unlocks AFTER the final rung → tier-up. Pillar DEEDS gated to phase 2 (prevents 'half the rungs, maxed deeds'). Reshapes §4.8.1's parallel-accrual model; resolves FU11.  <sub>← FU7, Q30, Q7, FU11 · ADR</sub>
- **[rewrite]** `§4.5.3, §4.4, §2.7, §4.6.1, §6.6, §1.13` — SUBSTANTIVE + CONTRADICTS canon stated 3x. Bounded labour→combat via PER-SKILL perks (each skill ~2-8 perks / small flat bonuses), STACKABLE with NO hard global cap (risk accepted); bound = small magnitudes + incremental skill unlock + holistic scaling. Replace the §4.5.3/§4.4 ==0 wall + §6.6 '==0' assertion with a 'small magnitude' check. CONTRADICTION: supersedes Q6/FU8-proposed 'hard global ≤CAP' — final = NO global cap. Conditioning stays the zero-stat gate.  <sub>← FU8, Q6, Q1, Q28 · ADR</sub>
- **[edit]** `§7 (M1 report-only, M6 gate), §4.8, fun-factor.md` — SUBSTANTIVE. Fun-proxies (dead-time, reward/unlock cadence, visible-next-goal, first-5-min hook) instrumented at M1 as REPORT-ONLY (gate at M6); set + thresholds from fun-factor.md, deed-cadence T0 ~5 min. Implements Q4.  <sub>← FU9, Q4, Q20, Q16</sub>
- **[rewrite]** `§4.1, §1.6.3, §2.16` — SUBSTANTIVE + threshold overhaul. Hybrid-gate thresholds need a per-pillar-per-tier OVERHAUL (NOT simple ratios — good=0.2/great=0.5 'breaks'), balanced against the FIXED deed inventory. Semantics: good=expected baseline, great=really strong, excellent=above-and-beyond. The §4.1 table is fully re-derived; §2.16/§1.6.3 made consistent. Implements Q7.  <sub>← FU10, Q7 · ADR</sub>
- **[edit]** `§3 (gate composition), §4.1, §2.15` — SUBSTANTIVE but FOLDED — resolved by FU7. Gate composition: rung-meter + story gate EACH rung (story only at capstones; mid-rungs RANK-only); the Q7 hybrid pillar distribution is the SEPARATE phase-2 tier-gate. State the composition predicate explicitly so the build can evaluate a tier transition deterministically.  <sub>← FU11, FU7, Q30, Q7</sub>
- **[add-new]** `§3.2 (R3 row rewrite), §2.7e/§2.8e, new combat-reveal-ladder subsection` — SUBSTANTIVE. Combat-reveal ladder (one reveal per beat): R3 = starter weapon + bare auto-resolve + retreat → durability bands at R4 (loot→craft) → stance at R5 → ability/item slots at the first weapon-L10 milestone → 2nd line T1, 3rd T2. Rewrites §3.2's R3 row and §2.8's M2a 'dump panel+stance+ability+item+retreat+weapon+Equipment+Inventory+Bestiary at once'.  <sub>← FU12, Q15, Q17, Q33, Q16, Q30, Q1 · ADR</sub>
- **[add-new]** `§2.8/§2.7e (weapon lines), §4.6 (weapon params), §5 (per-tier unlocks), §6.5 (weapon registry)` — SUBSTANTIVE + expands Q15. MORE weapons (growing roster): T0 unlocks 2 · T1 +3 · T2 +4 (~9-10 across v1; T0 still starts with 1). Author a period set (yari/uchigatana/kodachi/bō/kusarigama/naginata/tanbō/kanabō/yumi…), each with archetype params (baseSpeed/reach/targetCount) + a signature ability. CONTRADICTION nuance: 3 archetype LINES (Q15) but ~9-10 weapons (FU13) — reconcile.  <sub>← FU13, Q15 · ADR</sub>
- **[rewrite]** `§2.8 (rewrite CombatDeedsPool), §4.6.5, §4.2.1, new §4 rung-meter, §2.15` — SUBSTANTIVE. THREE clean combat tracks: kills/combat-XP → character level (HP+attr points); deeds → the Arms pillar; per-rung curated activities → the Combat-Rank rung-meter. Rewrite §2.8's conflated 'Combat Level = CombatDeedsPool (XP + deed-count)'; §4.6.5 states exactly what one kill writes to each track.  <sub>← FU14, Q1, Q30, Q47, Q9 · ADR</sub>
- **[edit]** `§2.9 (MobDef +level), §4.6.5 (on-kill XP), §6.5 (content/enemies.ts)` — SUBSTANTIVE (minor). mobLevel = an explicit per-mob MobDef.level field (hand-tunable; defaults ~ the dangerRing's expected character-level). §2.9c MobDef currently has no level field but §4.6.5 references mobLevel.  <sub>← FU15, Q1, Q15</sub>
- **[edit]** `§4.6.1 (add term), §4.6.6 (win-rate), §2.3` — SUBSTANTIVE. Satiety→combat = a satietyRate multiplier on attackPower (lighter on attackSpeed), flat above ~0.7 satietyMax → ~0.5 floor; 'adequate satiety' = ≥~0.7 where the locked 20-35% first-fight win-rate is measured; a SEPARATE coefficient from the labour floor; bound so the floor never pushes win-rate below ~15%. Implements Q31.  <sub>← FU16, Q31, Q47, Q17, Q16</sub>
- **[rewrite]** `§2.10, §4.6/§4.7 (durability math + band table)` — SUBSTANTIVE. Durability = 4 integer bands on weapon attackPower (75+/50+/1+/0 → 1.0/0.9/0.75/0.55); FIXED wear per FIGHT (cheap, replay-stable); armour bands identically on defense; repair/recraft restores; never auto-unequip. Implements Q33 with numbers.  <sub>← FU17, Q33, Q2, Q36</sub>
- **[rewrite]** `§4.8 (pacing reframe), §7.1.2 (locked target reframe), §1, §7.4` — SUBSTANTIVE + REVERSES D-016/the locked 'budget as target'. The ~28.5h budget is a FLOOR not a ceiling — a longer OSRS-rough grind; §4.8 becomes a MINIMUM-grind model; interleave content, don't brick-wall. CONTRADICTION: §7.1.2/§4.1 currently call the per-tier hours a LOCKED acceptance ceiling, and the FU18 master-proposal said 'CEILING' — the human REVERSED to FLOOR. Reframe both as a minimum.  <sub>← FU18, Q8, Q23, FU23 · ADR</sub>
- **[edit]** `§4.6.6/§4.8 (win-rate proxy), §7 (M3/M6 harness)` — SUBSTANTIVE (minor, tune-later). Win-rate bands: each key combat rung humbling-fresh (~30-45%), comfortable after that rung's training/gear (~80%+); R3 20-35%→~85% set; wire V2/V5/G1/G5 into the M3/M6 fun-proxy harness.  <sub>← FU19, Q16, Q31, Q4</sub>
- **[add-new]** `§4.3 (no-leakage exception), §4.2.3 (trade cap), §6.6 (proof), §4.8.3, §5 T2` — SUBSTANTIVE. BROADER cross-pillar combos (multiple pairs / larger magnitude — more T2 variety, a wider §4.3 exception). The trade-≤⅓ cap stays HARD — combos computed AFTER the trade clamp; the §6.6 verifier PROVES trade can never breach ⅓ via a combo. Excluded from the Q7 gate-threshold check.  <sub>← FU20, Q22, Q7, Q6 · ADR</sub>
- **[edit]** `§2.3 (satiety curve), §4 (stamina function), §4.6.1` — SUBSTANTIVE. Labour satiety throttle: flat above ~0.7 satietyMax, knee to a ~0.5 floor; modest drain per action; rest fully refills; satietyMax = base + small per-level growth. Defines §2.3's currently-unspecified soft-throttle function.  <sub>← FU21, Q31, Q47, Q17</sub>
- **[edit]** `§2.12 (dialogue), §6.4/§6.5 (content/dialogue.ts), §5 (dialogue beats)` — SUBSTANTIVE. Intra-line dialogue = a flat choice list per node (choices[] + ChoiceId); a choice's effect = locksLineIds[]/flags. Data, not scripting; deterministic; only chosen-flags persist (save-light). Implements Q34's branching with an authoring shape.  <sub>← FU22, Q34</sub>
- **[edit]** `§2.2 (active-only clock), §2.5 (auto-producers T3+), §2.8 (auto-resolve), §6.9, §4.8/§7` — SUBSTANTIVE. v1 stays active-only (no offline): with the tab OPEN, idle auto-resolve combat + auto-repeating labour run unattended for hours — the 'leave it running, check progress' feel comes from auto-resolve/auto-repeat, NOT an offline layer. Auto-producers stay T3+. Reconciles FU18's long-grind with the active-only lock; add the auto-repeat affordance to §2.8/§2.6.  <sub>← FU23, FU18</sub>
- **[NEW]** §4.x — Rung-meter accrual law (the third balance curve) @ new subsection in §4, after §4.5 (per-skill curves) / before §4.6 combat — Defines the per-rung-reset numeric rung-meter: per-crossing increment, the front-loaded-then-ramp threshold curve back-solved against the ≥30-min floor, the AND-gate with story flags, and the R0-R2 (then V/G) thresholds. This curve does not exist in the PRD today and is required by Q30/FU6/FU7/FU14.  <sub>← Q30, FU6, FU7, FU14</sub>
- **[NEW]** §3.x / §4.x — Combat-progression reveal ladder @ new subsection in §3 (after §3.2.1) and/or §4.6, cross-linked from §2.8 — The rung-by-rung staggered schedule (one reveal per beat): R3 weapon+loop+retreat → R4 durability → R5 stance → weapon-L10 ability/item slots → 2nd line T1, 3rd T2, plus per-step trigger kind. Replaces §2.8/§3.2's all-at-once M2a dump.  <sub>← Q15, FU12, FU13, Q17, FU4</sub>
- **[NEW]** §2.7 / §4.5 — Per-skill perk track (bounded labour→combat) @ extend §2.7c/§4.5.3 (perks) — effectively a rewrite of the milestone-perk model — Each skill (combat+labour) gets a stackable perk/flat-bonus track (~2-8 each), small-magnitude, NO hard global cap, unlocked by leveling; replaces the type-level ==0 no-cross-feed wall. Conditioning stays the zero-stat gate. Required by Q6/FU8.  <sub>← Q6, FU8</sub>
- **[NEW]** §4.1 — Hybrid good/great/excellent tier-gate (overhaul) @ rewrite §4.1 (the SIMPLE-bands table) + a per-pillar-per-tier threshold matrix — Replace the rejected 'simple thresholds, no floor/overflow' gate with the hybrid distribution (good in ALL · great 2-3 · excellent 1-2; T0 2-pillar special; NO overflow) and a fully re-derived per-pillar-per-tier threshold set balanced against the fixed deed inventory. Required by Q7/FU10.  <sub>← Q7, FU10</sub>
- **[NEW]** §2.8 / §4.6 — Three combat tracks + weapon roster registry @ rewrite §2.8 framing; add a weapon-archetype table in §4.6 / a content registry note in §6.5 — State the three clean tracks (level/Arms/rung-meter) and author the ~9-10-weapon roster (archetype params + signature ability). Replaces the conflated CombatDeedsPool. Required by FU14/FU13.  <sub>← FU14, FU13, Q1, Q15</sub>
- **[NEW]** §2.21 / §6.9 — About/Credits surface @ new subsection under §2.21 (presentation) or §6.9, referenced from §7.3 — An in-product About/Credits surface (authorship, commit-SHA build stamp, font/audio attributions, clean-room attestation) — pre-satisfies attribution for the SVG/audio/font decisions.  <sub>← Q54, Q50, Q52, Q38</sub>
- **[NEW]** §7.3 — License, content descriptors & deploy checklist @ new subsections under §7.3 deployment — License split (permissive code + reserved content, LICENSE before M7), itch content descriptors (child-disappearance/drowning/debt), and the multi-backend save-survival test on Chromium+WebKit. Required by Q51/Q53/Q37/Q52.  <sub>← Q51, Q53, Q37, Q52</sub>