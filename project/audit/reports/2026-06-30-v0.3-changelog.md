# Kamikakushi v0.3 — changelog (the spine-closes + breadth + fork-retire build)

> **Built:** 2026-06-29 → 06-30, autonomously, via the overnight `/loop`, from `path-to-v0.3` **Part 2**
> (the three movements: re-baseline T0-M1/M2 → the T0-M3 macro spine → the T0-M4 breadth). **v0.2 = the floor.**
> `npm run verify` GREEN throughout (**99 → 239 tests**). Full detail: the session-19 journal
> (`project/journal/2026-06-29-session-19-v03-part2-build.md`).
>
> **Scope rule honored:** none of the human's design/taste calls were made for them — every balance number ships
> **liquid (D-059), tune by playtest**; the deferred design questions are routed to **R4**. The one locked-ADR
> contradiction that remained (D-056, the DEMO/REAL fork) was **executed** mid-loop only after the human confirmed
> the direction (ship real pacing, keep the DEV tools).

## What v0.3 adds over v0.2

| Area | v0.3 | Why it matters |
|---|---|---|
| **⭐ The macro spine CLOSES** | The R7 capstone opens **Phase 2**: estate **deeds** bank into the live **House-Influence 家産** pillar (per-deed-capped), the **season judges** your high-water (70/30, ±10%, never net-negative), and at **Excellent** the **manual Ascend** fires the **BIG T0→T1 ceremony**. | v0.2 was "a beautiful chassis with no engine." v0.3 is the engine — the loop demonstrably closes (proven by an end-to-end real-intent test, not just teleports). |
| **The contiguous T0 ladder R0…R7** | The thin **R4→R7** rungs (Kura-warden → House-servant → Steward's man → Trusted of the house), each a reveal-as-plot beat; the v0.1 R3 dead-end is fixed (R3→R4 needs real gate-watch duty, `combat-blooded`). | The estate tier is now a complete climb, not a stub that strands at R3. |
| **Combat re-baseline (D-050)** | **HP carries** between fights and **heals only by eating** (cook a meal); the loss/level-up/promotion free-heals are gone. No-stance-dominated (Pareto lever test). No-stranding (eat+repair reaches L2, 8 seeds). | "Eat before you fight" is now a real, legible decision; a fight is something you might lose. |
| **T0-M4 breadth** | A diegetic **mentor** (Genemon teaches raking→koku as reveal-as-plot in the log, D-039); the 2nd weapon is **looted + crafted** (the drillmaster grant retired, D-052); a first **quest** (drive off the crop-raiders, D-037); a tiny capped **market** (D-008); a **walkable estate map** (`move_to`, D-065). | The estate gains texture beyond the labour grind — and each seam is proven to work within a real playthrough. |
| **M2·8 — DEMO/REAL fork RETIRED (D-056)** | One re-derived profile: **R0 ≈ 5-min cold-open**, the climb rungs **≈ 10–15 min** (sim-verified R0 4.88 / R1 10.0 / R2 12.1); T0 is ≥30-floor-EXEMPT (the floor gates from T1). Review velocity = the **DEV speed toggle** (kept permanent). | No more "demo balance leaks to players"; the build runs at its real, intended pace. |
| **The cold-open re-sequenced** | Genemon greets with **greet + the stakes** on wake; the koku-teaching + promise land **after** your first rake (reveal-as-plot), as the +koku shows. | The battery flagged the old wake as a 5-line monologue dump; it's now a lean hook. |

## Quality — how v0.3 was vetted

- **8-lens fidelity battery** (prd-faithful / human-feedback / adr-faithful + fun/ui/incremental/economy/laziness):
  **prd 9 / adr 8.5 / human-fb 8** — *the game the PRD promised is built and honest.* 7 self-resolvable findings
  applied. (`project/audit/reports/2026-06-29-v03-fidelity-battery.md`)
- **Post-M2·8 adversarial re-audit** (3 cold-read lenses) — independently confirmed the fork-retirement is
  functionally CLEAN; only 3 comment nits, fixed. (raw: `project/brainstorms/raw/2026-06-30-v03-overnight-adversarial-verify.json`)
- **Hands-on QA visual sweep** of all 15 surfaces (0 console errors) → 1 real bug fixed (a "the the" map header).
- **End-to-end proofs (RED-able):** the full arc closes via real intents (`t0-arc.test.ts`); the breadth seams —
  quest/market/map — work within a real playthrough (`breadth-arc.test.ts`); structural invariants hold across the
  whole arc — no numeric corruption, write-once latch, monotonic clock (`invariants.test.ts`).
- **Ship-readiness:** the prod build is clean and the DEV cheat-API is verifiably **stripped from the prod bundle**;
  a deploy-gate guard now refuses to publish a bundle that leaked it.

## For the human (the only things left — all yours)

- **R1** — playtest the T0 M0–M4 build (`npm run dev`; `__qa.speed(8)` / `jumpToPhase2()` / `jumpToAscension()`).
  The pacing is liquid — tell me which rungs feel off and I'll tune them.
- **R4** — the design/taste calls the battery surfaced: the D-053 wall-time-clock ADR contradiction (#1), auto-combat
  tension (#3), breadth-as-chrome (#5), koku↔win-condition coupling (#6). (R4#2, the fork, is now closed.)
- **R2 / R3** — confirm/override the diverge self-picks (influence panel / breadth surfaces; non-blocking).

## Rough edges flagged (tuning/depth, not blockers)

- **Post-ascension panel** shows the stale T0 framing ("Reach Excellent to ascend 480/480") — the thin-T1-content
  edge (T1 isn't built); a depth call, R4#5.
- **The T0 pace is real now** (~5-min cold-open of raking, hands-off via the auto-loop / speed toggle) — confirm
  the feel; the magnitudes are liquid.
- **Surplus crafting materials** accumulate after the one-time craft (no second sink yet) — R4-adjacent.
