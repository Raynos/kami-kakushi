---
name: v03-fidelity-battery
description: v0.3 state-of-the-game fidelity battery — PRD / human-feedback / ADR + fun/ui/incremental/economy/laziness
metadata:
  type: audit
  battery: v0.3
---

# v0.3 fidelity battery — state of the game (T0 M0–M4)

> **What this is.** A full `battery` (8 cold-read lenses + convergence critic, `wf_4010f653-0fb`): is the BUILT
> v0.3 faithful to the **PRD**, **human feedback**, and the **ADRs** (+ fun/ui/incremental/economy/laziness)?
> Raw verbatim: [`../../brainstorms/raw/2026-06-29-v03-fidelity-battery.json`](../../brainstorms/raw/2026-06-29-v03-fidelity-battery.json).
> Suite at audit time: **220 tests / 23 files green** (the build advanced past the now-stale roadmap-respect report).
> Scores: prd-faithful **9** · adr-faithful **8.5** · human-feedback **8** · ui-polish 7.5 · incremental 7 ·
> economy-arithmetic 6.5 · fun 6 · laziness 4.5 (↑=lazier). **Verdict: faithful to locked intent; the gaps are
> 2 known-pending locked-ADR items, 2 false-green test guards, and the moment-to-moment T0 fun/pacing.**

Verified. The key reconciliation holds: `main.ts:205` is `if (paused || document.hidden || crashed) return;` with a comment still asserting the *old* "strictly active-only (no offline catch-up)" model; `resolveBootProfile` returns `'demo'` ("NOT the H1 call"); `decisions.md:444` D-053 explicitly says "do NOT pause on `document.hidden`"; `decisions.md:157` frames D-053 as *reaffirming* active-only — so the two lenses are reading the same code against different rungs of the same lock. The ratchet narrowing (`integrity.test.ts:22-25`) and the tautological no-stranding assertion (`m2.test.ts:282-304`) both confirm. Here is the converged report.

---

## Verdict

The **built v0.3 is faithful to locked intent**: all seven signed PRD acceptance criteria, every load-bearing spine ADR (D-049/D-050/D-052/D-055/D-057/D-062), and the breadth + infra ADRs (D-037/D-039/D-063/D-015/D-065/D-013a/D-067) are implemented in real, wired, RED-able code — the suite is 220/220 green, well past the **stale** roadmap-respect reference report (which still sees 139 tests and calls the breadth modules "absent"; four lenses independently flag it as a pre-reshape snapshot). Fidelity is high (avg of the seven fidelity lenses ≈ 7.5/10); the gaps are **two known-pending locked-ADR contradictions** (D-056 DEMO-default, and — newly caught — D-053 wall-time clock), **two genuine integrity holes** that quietly rot (a hollow no-dead-value ratchet and a tautological no-stranding test), and **the moment-to-moment T0 fun**, which no shipped profile yet delivers. Headline: **the game the PRD promised is built and honest; the game a player *feels* in the first ten minutes is not yet tuned, and two test "guards" can't go RED.**

## Scorecard

| Lens | Score /10 | One-line verdict |
|---|---|---|
| prd-faithful | **9** | All seven LOCKED criteria honored with code+test; only the pending DEMO-pacing gap + one test-debt ding. |
| adr-faithful | **8.5** | Every load-bearing spine/breadth ADR wired & tested; lone contradiction is the known-pending D-056. |
| human-feedback-faithful | **8** | Nearly all signed locks verified-real; one real missed contradiction (D-053) the prior audit overlooked. |
| ui-polish | **7.5** | Intentional woodblock/ink, not slop; undercut by one collision-prone seal overlay + minor discipline slips. |
| incremental | **7** | Strong compounding spine + one no-dead-tail currency; koku dead-tail + a ratchet blind to new currencies. |
| economy-arithmetic | **6.5** | Core arithmetic sound & faithful; the headline 70/30 split realizes 0% before the only ascension. |
| fun | **6** | Honest foundations + real payoff; T0 hook mis-paced in *both* profiles, new-mechanic tension auto-defused. |
| laziness (↑ = lazier) | **4.5** | Spine honestly built/tested, but two false-green guards + broad UI/DEV test-debt + dead stub content. |

## Cross-lens agreement

Problems found independently by ≥2 lenses — the strongest signal, ordered by how many lenses converged:

1. **DEMO/REAL fork un-retired; DEMO is the shipped default (D-056) — 4 lenses.** prd-faithful, human-feedback, adr-faithful, and fun all land on `balance.ts:53` `DEFAULT_BALANCE_PROFILE='demo'` + `main.ts` `resolveBootProfile → 'demo'`. adr-faithful calls it "the lone locked-ADR contradiction"; fun shows it ruins the first-ascension weight; prd shows it misses the ≥30-min pacing lock. Verified: `resolveBootProfile` returns `'demo'` ("shipped default — NOT the H1 call"). Known-pending/ripple-blocked (M2·8).

2. **G-NO-DEAD-VALUES ratchet silently doesn't cover this cycle's new loot currencies — 2 lenses, both MAJOR, same line.** incremental + laziness both cite `integrity.test.ts:22-25`: `SURFACED_CURRENCIES = {koku} ∪ ACTIVITIES.yields = {koku, wood, sansai}`, while `hardwood`/`beast_sinew` (`crafting.ts:25,32`, looted at `fight.ts:92-104`, surfaced at `render.ts:880-885`) escape it. Verified: the docstring claims "derived from the REAL registries … fails the build" but the set is narrowed to activity-yields only. A future dead loot/quest/market currency passes green.

3. **No-stranding guarantee is faked / un-RED-able — 2 lenses (`m2.test.ts:272-306`).** laziness (MAJOR) proves the assertion is *tautological*: the Broken/Battered branch repairs+`continue`s and the hp branch `continue`s, so the re-check at `:299` reads a durability just confirmed *not* Broken — `foughtBroken` is structurally unreachable and `expect(foughtBroken).toBe(false)` (`:304`) passes regardless of game behaviour. human-feedback (minor) independently flags that recovery is modeled by directly mutating `weaponDurability`/`hp` instead of dispatching the real `cook_meal`/`repair_weapon` intents, so the production recovery loop (`main.ts:218-249`) is untested. Verified.

4. **Spine + breadth UI surfaces ship with zero DOM tests — 2 lenses.** laziness (MAJOR) + human-feedback (minor): `render.test.ts` covers only formatLogText/settings-a11y/foe-fog; `renderHouseInfluence` (`render.ts:537`), `showAscension` (`:1079`), `renderMap` (`:1205`), `renderMarket` (`:1167`), `renderQuests` (`:1251`), craft panel (`:866-894`) rest entirely on static screenshots.

5. **Cold open over-teaches / text-wall on first click — 2 lenses.** fun (MAJOR): 268 words / 7 paragraphs (3 cold-open lines `intents.ts:115-124` + 4 ungated Genemon greet lines `dialogue.ts:37-57`) before the first rake — the literal §6/§7 T0 fun-risk. ui-polish (minor): the `genemon-greet.png` log is an 8-paragraph wall against §10 "maximum ma."

6. **T0 pacing / hook is wrong — 2+ lenses (fork-independent).** fun (MAJOR ×2): real profile R0=31min / 3500 rakes vs the ~10-15 min/rung fun-doc target *and* D-022's "R0 = exempt ~5-min cold-open"; R0 is a ~30-min single-verb reveal-desert. economy-arithmetic (MAJOR): the cadence mismatch is *why* the seasonal split never fires. This is distinct from the fork: even the intended REAL thresholds are mis-derived.

7. **DEV `__qa` harness untested + hardcoded literals — 2 lenses.** laziness (minor, `main.ts:286-422`) + human-feedback (nit): `speed`/`toTier`/`toRung`/`jumpToPhase2` pin literals (`'rank-r7'`,`'porters-knot'`,`'panel-house-influence'`) with zero coverage; prod-stripped (good) but it's the headless playtest harness.

8. **Breadth map reads as chrome — 2 lenses, different angles.** fun (minor): the map gates nothing — `canDoActivity` has no location check (`selectors.ts:115`), every activity is doable anywhere, movement only logs a blurb (`intents.ts:334-343`). ui-polish (minor): it ships as a `→ label kanji` button-list, not the §5.11 sumi-road/registration-node schematic (`render.ts:1205-1248`).

## Fidelity verdict

### PRD-faithful — confirmed vs gaps
- **Confirmed:** no-magic / growth-only-through-perseverance (`ascension.ts:33-63` carries state forward additively, no prestige/rebirth/wipe anywhere; reward is only EARNED standing). Mediocre-start (`state.ts:139-148`, worn `carrying_pole` `weapons.ts:22`). Trade ≤⅓ (`market.ts:75-82` capped buy-only sink, banks zero pillar deed, `intents.ts:314-333`). Four pillars + Estate spine, others UNNAMED silhouettes (`state.ts:47-49`, `render.ts:559-650`). Reveal-latch write-once (`unlock.ts:10-46`). Manual opt-in ascension (`ascension.ts:22-34`, fires only on the explicit intent `intents.ts:345-348`; `ascension.test.ts:43-47` proves it).
- **Gaps:** active-only is faithful to the *coarse* D-013 lock (no offline catch-up; `step.ts:4-5`), **but** the build pauses on `document.hidden` (`main.ts:205`), which the D-053 refinement forbids — see human-feedback below. Shipped DEMO profile (`balance.ts:53`) doesn't meet the LOCKED ≥30-min pacing (known-pending). Porter's-knot "ZERO mechanical bonus" is honored in code (`ascension.ts:55-58` reads it for dream TEXT only) but guarded only by *absence* — no test asserts the flag grants no stat (`m1.test.ts:67` only asserts it's set).

### Human-feedback-faithful — confirmed vs gaps
- **Confirmed:** 6-tier split + T0=Estate-tutorial → T1 gate (`ascension.ts:23`). HP-carries + heal-by-eating D-050 (`combat.ts:117`, `fight.ts:79/111/55`, `intents.ts:243-245`). No-stance-dominated (`m2.test.ts:129-154`). 20-35% single-fight band (`m2.test.ts:54-58`, monkey@L1=0.32). 70/30 seasonal judge ±10% never-net-negative (`pillars.ts:69-85`). Tier-gate=EXCELLENT + overshoot→bigger boon + first-ascension-big + porters-knot dream beat. Schema bump v1→v2 + real `migrate()`. Linear koku flywheel. 2nd weapon found/crafted, drillmaster grant RETIRED (`m2.test.ts:318-367`). Process locks: decision #3 → ADR D-057; displayed==tested==same-for-every-player invariant (`FORECAST_SEED` fixed, shared by `render.ts:929` + tests).
- **Gaps:** **D-053 contradicted (MAJOR, new — prior audit missed it):** signed "advance by wall-time, do NOT pause on `document.hidden`, a backgrounded tab catches up" (`decisions.md:444-450`) vs `main.ts:205` doing the literal opposite + a fixed `setInterval` with no dt accumulation (`main.ts:267`; the only `coreTick(state,dt)` is the DEV `__qa` hook). Marked code-application PENDING, so not an over-claim — but it is a *second* pending-locked-ADR gap, refuting the roadmap report's "D-056 is the only contradiction." No-stranding modeled abstractly (above). D-056 fork (above).

### ADR-faithful — confirmed vs gaps
- **Confirmed:** D-050 HP-carry/eat; D-052 grant retired → loot+craft+equip-gate (`crafting.ts`, `intents.ts:228`); D-055 silhouettes + the v0.1 write-only-flag bug genuinely fixed (`porters-knot` SET `ranks.ts:81` → READ `ascension.ts:46` + `surfaces.ts:183`); D-049/D-057 gate=EXCELLENT + overshoot + n=400 sampled display (`combat.ts:218`) with analytic kept for the gate (`combat.ts:148`); D-062 first-big ceremony; D-037 Quests top-level tab; D-039/D-063/D-015 diegetic data-mentor (no popups); D-065 walkable node-graph + `move_to`; D-013a tier STORED; D-067 SCHEMA_VERSION=2 + DEV tools prod-stripped.
- **Gaps:** D-056 CONTRADICTED (the one live contradiction). D-073 partial — `variants-log.md` registry is EMPTY though 5 surfaces ran diverge/diverge-LITE; only the influence panel got a true 2-variant contact sheet (lane: **marketing**). D-052 equip_weapon "never-gifted" gate has ZERO test coverage. D-054 milestone-integrity is still a norm, not a verify gate.

## Converged findings

De-duped, ordered by severity. No lens raised a CRITICAL; the cap is MAJOR.

**MAJOR — 1. DEMO/REAL fork un-retired; DEMO is the shipped default (D-056).** *4-lens consensus.* Evidence: `balance.ts:53` `DEFAULT_BALANCE_PROFILE='demo'`, `main.ts` `resolveBootProfile → 'demo'`, dual `RUNG_METER_THRESHOLDS` maps (`balance.ts:61-67`); the pacing gate checks `--profile=real` (`package.json:23`) but the game ships demo — exactly the leak D-056 (`decisions.md:488-494`) forbids. Fix: retire `resolveBootProfile`/`BalanceProfile`/the dual maps, ship REAL as the only world, add a single-profile pacing assertion. *Known-pending (M2·8), ripple-blocked — do it first once the ripple clears.* **Lane: engineering. Carryover.**

**MAJOR — 2. D-053 wall-time clock contradicted; sim PAUSES on `document.hidden` (NEW — uncaught by the prior audit).** *Reconciles the prd "active-only FAITHFUL" vs human-feedback disagreement: faithful to coarse D-013, contradicts the precise D-053 refinement.* Evidence: `main.ts:205` `if (paused || document.hidden || crashed) return;` + fixed `setInterval(AUTO_REPEAT_MS)` with no dt accumulation, vs `decisions.md:444-450` "advance by elapsed wall-time … do NOT pause on `document.hidden` … a backgrounded tab catches up." Fix: drive the production tick by wall-clock dt and drop `document.hidden` from the autoStep guard (keep paused/crashed), add a covering test. Locked-PENDING, so not an over-claim — but the live guard does the explicit opposite and must not ship as-is. **Lane: engineering. New.** *(See judgment queue — there's a doc-level tension to resolve first.)*

**MAJOR — 3. G-NO-DEAD-VALUES ratchet blind to this cycle's new currencies.** *incremental + laziness, same line.* Evidence: `integrity.test.ts:22-25` derives `SURFACED_CURRENCIES` from `koku ∪ ACTIVITIES.yields` only; `hardwood`/`beast_sinew` (`crafting.ts:25,32`), pillar value, market grants, and quest rewards all escape it though they're surfaced (`render.ts:869/882`). Fix: fold `MATERIAL_IDS` + market-grant resources + Influence ids into the surfaced set and ledger their consumers, so an un-consumed currency flips the gate RED as the docstring promises. **Lane: engineering. New.**

**MAJOR — 4. No-stranding test has a tautological, never-RED assertion.** *laziness (MAJOR) + human-feedback (minor).* Evidence: `m2.test.ts:282-304` — the Broken/Battered branch repairs+`continue`s and the hp branch `continue`s, so the `:299` re-check reads a durability just confirmed not-Broken; `foughtBroken` (`:300`) is dead code and `expect(foughtBroken).toBe(false)` (`:304`) is a tautology; the real recovery loop (`main.ts:218-249`) is untested. Fix: drive recovery through the real `repair_weapon`/`cook_meal` intents (or `autoStep`) and assert the durability-band-before-each-fight invariant on a reachable path. **Lane: engineering. New.**

**MAJOR — 5. T0 hook mis-paced in *both* profiles; no shipped pace delivers the designed tutorial feel.** *fun ×2, fork-independent.* Evidence: real R0=31.1/R1=35.43/R2=38.25 min (~3500 rakes for R0) vs the ~10-15 min/rung target (`fun-factor.md:84,290-299`) and D-022's "R0 = exempt ~5-min cold-open" (`decisions.md:200`); demo blitzes the whole tutorial in <1 min; `pacing:check` (`package.json:23`) codifies the ≥30 floor onto the floor-exempt tutorial. Plus R0 is a single-verb reveal-desert (`intents.ts:74-79`; next reveals not until the R1 reward `surfaces.ts:63-99`). Fix: re-derive REAL T0 thresholds to ~10-15 min/rung, exempt R0-R7 from the ≥30 floor in `pacing:check` (gate it from T1), front-load a mid-R0 reveal, and ship one profile. **Lane: engineering. Carryover.**

**MAJOR — 6. koku flywheel compounds but dead-ends — decoupled from the win-condition, finite sinks.** *incremental.* Evidence: the only koku consumers are `improve_estate` (1100 koku, capped at stage 3, `estate.ts:25/35/45`) and `buy_item` (~278 koku, `market.ts`); lifetime sink ≈1378, after which labour+combat keep minting koku with no consumer. The pillar accrues a FLAT `ESTATE_DEED_PER_ACT=8` (`pillars.ts:55-63`) independent of koku/yield/stage — so getting richer never brings ascension closer. Fix: couple wealth to the macro goal (a repeatable koku→Estate-deed commission past E3, capped per season; or scale the deed by estate stage) so the flywheel feeds ascension and koku never goes inert. **Lane: engineering. New.**

**MAJOR — 7. HP-carry + eat-to-heal tension (D-050) auto-defused in the dominant auto-grind.** *fun.* Evidence: `main.ts:215-249` — auto-combat auto-cooks/forages below `COMBAT_HEAL_FRAC=0.55` and auto-repairs, so the player sets auto-combat and watches; the §2.6 "a fight you might lose" tension fires only in manual play, which the optimal loop makes irrational. Fix: give pushing-on-hurt a real throughput-vs-setback tradeoff or a press-your-luck prompt, or make auto-combat strictly assistive so tension stays in the player's hands. **Lane: engineering. New.** *(Taste call — see judgment queue.)*

**MAJOR — 8. The 70/30 seasonal split realizes 0% (not 30%) on the played T0 spine.** *economy-arithmetic.* Evidence: a reducer-driven grind hits EXCELLENT in exactly 60 acts at in-game day 5, while the first Phase-2-gated season boundary (`day%28`, `step.ts:21,39`) lands ~16+ days / ~192 acts later — the judge fires **zero** times before the only ascension. The 3/7 math is correct in isolation; the cadence is the bug. Fix: judge per-week / per-N-acts in the thin T0 spine, and/or raise the EXCELLENT band so the gate sits past the first Phase-2 boundary — otherwise the whole `seasonalJudge` subsystem is dead on the optimal path. **Lane: engineering. New.**

**MAJOR — 9. Cold open over-teaches — 268 words / 7 paragraphs on the first click.** *fun (MAJOR) + ui-polish (minor).* Evidence: `open_eyes` delivers 3 cold-open lines (`intents.ts:115-124`) + all 4 ungated Genemon greet lines (`dialogue.ts:37-57`) before the first rake; `fun-factor.md:297` and ui-design §10 both name this. Fix: gate the steward's lines to fire AFTER the first rake (reveal-as-plot) so the opening click is wake + one hook line + the rake verb. **Lane: engineering. New.**

**MAJOR — 10. Rank-up / ascension seal overlay has no scrim → collides with the event log.** *ui-polish.* Evidence: `styles.css:867` `.rankup-seal{position:fixed;inset:0;…}` has NO background/scrim (vs `.modal-scrim{background:#26221ecc}` `:771`); `render.ts:1129/1149` append it viewport-centered over the left-aligned log. Screenshots: `project/audit/screens/diverge-influence/play-2-ceremony.png` (ascension title renders transparently over the near-identical log line → garbled double-text), `…/latest/qa-10-rankup-seal.png`, `…/latest/qa-06-mobile-r2.png` (collides with the nav tab). Contradicts ui-design §5.10/§6.7. Fix: reuse the modal pattern — a faint sumi scrim + opaque paper-card `.seal-inner`; full `.modal-scrim` for the ascension. The correct pattern already ships for Settings. **Lane: engineering. New.**

**MAJOR — 11. Spine + breadth UI surfaces ship with zero DOM tests.** *laziness (MAJOR) + human-feedback (minor).* Evidence: `render.test.ts` covers only formatLogText/settings-a11y/foe-fog; influence bar, ascension ceremony, map, market, quests, craft panel (`render.ts:537/1079/1205/1167/1251/866-894`) have no regression-catching assertion. Fix: add jsdom DOM tests that mount each surface and assert its wired button dispatches the right Intent + renders off real `GameState`. **Lane: engineering. Carryover.**

**minor — 12. DEV `__qa` harness untested + pins hardcoded flag/unlock literals.** *laziness + human-feedback.* `main.ts:286-422` — `toRung`/`toTier`/`speed`/`jumpToPhase2`/`jumpToAscension` fabricate state with literals (`'rank-r7'`,`'porters-knot'`) that silently rot if ids drift; prod-stripped but uncovered. Fix: a smoke test exercising `jumpToPhase2 → ascensionAvailable` + `__qa.speed` sets the multiplier the tick reads. **Lane: engineering. Carryover.**

**minor — 13. Breadth beats lean chrome; map gates nothing + reads as a button-list.** *fun + ui-polish.* `selectors.ts:115` has no location gate (every activity doable anywhere); `render.ts:1205-1248` is a `→ label kanji` list, not the §5.11 sumi-road schematic (kanji jam against romaji: `paddies田圃`). The lone quest auto-completes off tokens the reducers already emit (`quest-engine.ts:28-45`). Fix: make ≥1 breadth beat load-bearing (gate a deed behind a node, or give the quest a branch); add the ink-road schematic when the graph grows. **Lane: engineering. New.**

**minor — 14. Seasonal judge compounds on its own payout — phantom standing with no new deeds.** *economy-arithmetic.* `pillars.ts:80-84` sets `judged` to the PRE-bonus high-water while bumping `highWater` by the bonus, so next season re-judges the bonus (geometric → +75% of the first bonus over idle seasons), inflating overshoot boon points. Fix: set `judged` to the POST-bonus high-water; add a "judge twice, no intervening deeds → second bonus is 0" test. **Lane: engineering. New.**

**minor — 15. Surplus crafting materials dead-tail after the single one-time craft.** *incremental.* Materials drop on every grindable win (`fight.ts:92-104`) and accumulate unbounded; the only consumer is `craft_wood_axe` (re-craft is strictly redundant with the cheaper `repair_weapon`). Fix: a second recipe, a material-funded repair, or cap accrual. **Lane: engineering. New.** → **CLOSED 2026-07-01 as working-as-intended for the T0 miniature** (**D-095** / H11 → option C): every candidate sink grazed a lock (2nd weapon → D-052; mats→koku or mats-repair → D-092), so the surplus is accepted for the tutorial and a real material economy becomes a **T1** concern.

**minor — 16. equip_weapon "never-gifted" gate (D-052) has zero test coverage.** *adr-faithful.* `intents.ts:225-235` refuses `wood_axe` unless `hasFlag('crafted-wood_axe')`, but 0 test refs — a regression re-allowing the un-crafted axe passes green. Fix: a reduce-level test (no-op without the flag; succeeds after `craft_weapon`). **Lane: engineering. Carryover.**

**minor — 17. Porter's-knot "ZERO mechanical bonus" honored in code but not RED-able.** *prd-faithful.* `ascension.ts:55-58` reads the flag for dream TEXT only; `m1.test.ts:67` only asserts it's set. Fix: a small invariant test — two states differing only in the knot/dream flags produce byte-identical stats/yields/recipes. **Lane: engineering. New.**

**minor — 18. D-073 partial: breadth surfaces shipped diverge-LITE; the variants-log registry tracks NONE.** *adr-faithful.* `variants-log.md` is EMPTY though 5 surfaces ran this cycle; only the influence panel got a true 2-variant contact sheet. Fix: backfill deferred-single-idea rows for craft/market/quests/map; re-run a real diverge on any panel the human flags via R3. **Lane: marketing. New.**

**minor — 19. Four new write-only flags + dead stub dialogue re-introduce the anti-pattern the code claims to fix.** *laziness.* `ascension.ts:48` writes `tier-1-reached`/`ascended-t0`/`dream-t0-ascension` (none read by game logic; `tier-1-reached` redundant with `tier===1`); `combat-unlocked` (`ranks.ts:119`) never read; `dialogue.ts:68-104` Kihei/Sōan defs never routed yet pass "cast coverage" (`dialogue.test.ts:36-41`). Fix: drop the redundant flags or wire a consumer; gate-and-route the Kihei/Sōan lines or move them out of the shipped registry. **Lane: engineering. New.**

**minor — 20. D-054 milestone-integrity is still a norm, not a verify gate.** *adr-faithful.* `package.json:27` enumerates 9 gates, none a DoD-instrument manifest. Fix: wire a gate that fails on any DoD-named instrument unresolved to a test/tool (push the rule norm→gate). **Lane: engineering. Carryover.**

**nit — 21. Color-discipline slips on the House-Influence surface.** *ui-polish.* `styles.css:1102` colors the grade WORD gold and `:1117-1128` recolors the Estate bar off its fixed ochre identity at top grade (a deliberate diverge pick); `render.ts:567` puts a non-curated 🔒 (outside the §7 motif set, un-desaturated) on silhouette rows. Fix: render the grade word in ink and convey grade by the registration-tick crossed; drop the 🔒 (the greyed ghost-bar already reads as locked). **Lane: engineering. New.**

**nit — 22. Anti-spike per-deed cap never binds; cold open is light washi not "kura-dark."** *economy + ui-polish.* `perDeedCap=10` but the sole producer banks `8` (`balance.ts:90-92`), so the cap is decorative and any 60 labour acts reach EXCELLENT (deed not gated to estate work); cold open (`qa-01-cold-open.png`) is a light field, not §10's dark-storehouse-waking beat. Fix: gate the deed to estate-relevant activities (or document the cap as forward-headroom); optionally darken the cold-open field. **Lane: engineering. New.**

## Judgment queue for the human

Calls only a person can make — locked-design or taste. Plain language:

1. **The active-only / wall-time clock (D-053 vs D-013).** *Problem:* you signed D-053 ("don't pause a backgrounded tab; advance by wall-time so it catches up"), but `decisions.md:157` also says D-053 *reaffirms* active-only, and the PRD §1 acceptance criteria lock "no offline catch-up." The build implements the conservative reading (pauses on `document.hidden`), which directly contradicts D-053's explicit instruction. These pull in different directions and only you can say which is intended. *Proposed:* confirm the target — "open-but-hidden tab keeps simulating by wall-time; *closed* tab does not" — then build it (remove the `document.hidden` guard, drive the tick by dt). *Cost of inaction:* a signed lock sits unbuilt while the live code does its literal opposite, and the two ADRs stay ambiguous — a future reader can't tell which is canon.

2. **The shipped pace / fork retirement (D-056 + the REAL T0 numbers).** *Problem:* DEMO ships by default (tutorial in <1 min), but the REAL profile over-corrects — R0=31 min / ~3500 rakes — against both the ~10-15 min/rung fun target *and* D-022's "R0 = ~5-min cold-open." No profile a player can reach delivers the designed hook, and the `pacing:check` gate currently enforces the ≥30 floor onto the floor-exempt tutorial. *Proposed:* approve retiring the fork (ship one profile) AND sign off on re-derived T0 thresholds (~10-15 min/rung, R0 nearer 5 min) with the ≥30 floor gated from T1. *Cost of inaction:* the very-first-impression hook — the single highest-leverage fun surface — never lands; the "big first ascension" arrives weightless.

3. **Whether auto-combat may defuse the D-050 fight tension.** *Problem:* auto-combat silently heals-to-optimum and only fights the matchup you pick, so "a fight you might lose" becomes a passive sansai sink the bot resolves. This is a *design* choice about how much the autopilot is allowed to do, not a bug. *Proposed:* decide between (a) keep auto-combat fully autonomous and accept the eat-loop as background maintenance, or (b) make it strictly assistive / add a press-your-luck prompt so tension stays with the player. *Cost of inaction:* the headline new combat mechanic reads as chrome in the loop players actually run.

4. **Cold-open length / learn-by-doing.** *Problem:* the first click dumps 268 words of steward monologue before the player does anything — the "text wall instead of learn-by-doing" the fun-doc warns against. The voice is excellent; the volume is the issue. *Proposed:* gate most of Genemon's lines to fire after the first rake (wake + one hook line + the verb). *Cost of inaction:* the purest first impression (one verb + an empty log) is buried under a 7-paragraph wall.

5. **Breadth-as-chrome — accept or invest.** *Problem:* the map, the lone quest, and the market each deliver a one-time reveal but no repeat fun; the map gates nothing mechanically. *Proposed:* either bless them as deliberate T0 reveal-only beats, or fund making at least one load-bearing (a deed gated behind a node, or a quest branch). *Cost of inaction:* mid-loop T0 fun leans on breadth that doesn't carry it; fine if intended, a hole if not.

6. **koku coupling to the win-condition.** *Problem:* getting richer never brings ascension closer (the pillar deed is flat, independent of koku/yield/stage), and koku runs out of sinks (~1378 lifetime) — a dead primary currency at the T0 endgame, against the "no dead value" pillar. *Proposed:* approve coupling wealth to standing (a capped koku→deed commission, or deed scaling by estate stage). *Cost of inaction:* the wealth flywheel's payoff goes inert exactly when it maxes; the project's own no-dead-value discipline is breached in its primary currency.

*Files of record:* `src/app/main.ts:205` (D-053 guard) · `src/core/balance.ts:53` (DEMO default) · `src/core/integrity.test.ts:22-25` (hollow ratchet) · `src/core/m2.test.ts:282-304` (tautological no-stranding) · `src/ui/styles.css:867` (seal-overlay scrim) · `docs/living/decisions.md:444,488` (D-053, D-056).