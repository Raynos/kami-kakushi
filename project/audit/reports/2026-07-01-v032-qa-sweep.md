# v0.3.2 QA sweep — the assembled build, headless (2026-07-01, session-40)

**Verdict: the assembled v0.3.2 build is SOUND.** Every new surface renders, and the
full cold-open → R3-combat → R4 → R5 → R6 flow produces **zero console errors** across
every captured state (the headless `qa-shots.mjs` harness + a v0.3.2 sweep). One real
render snag was found and **fixed**; the rest are taste/pre-existing notes for the
human's playtest, not bugs.

## Method
Headless (playwright `headless:true`, hook-enforced — no headed browser): drove the DEV
`window.__qa` API to each state and screenshotted it. Galleries:
`project/audit/screens/latest/` (the standard pass) + `project/audit/screens/v032-sweep/`
(the v0.3.2-specific surfaces). Reviewed each shot by eye for look / legibility /
woodblock-intent / regressions.

## Fixed this pass
- **Training panel — the +1 buttons cascaded diagonally.** The 5-attribute rows reused
  `.labour-row` (plain flex, no `justify-content`), so each `+1` sat immediately after
  its variable-width label instead of aligning — a ragged staircase. Fixed with a
  dedicated `.attr-row` (label flexes/wraps, button right-aligned in a clean column).
  Confirmed fixed in the refreshed shots.

## Confirmed working (all rendered clean)
- **5-attribute training panel** (STR/AGI/INT/SPD/LUCK + glosses + aligned +1 buttons).
- **Bestiary** — default A (field-guide cards): "N of 7 recorded," faced foes inked with
  tell + haunt + win-rate, unfaced foes fogged. Variants **B** (danger ledger 危険帳) and
  **C** (図鑑 scroll) both render and are distinct (your R5 taste call).
- **Staggered reveals** — R3 shows weapon + fight + Bestiary but NOT durability/craft/
  stance; **R4** adds the durability band + repair + craft; **R5** adds the stance control.
- **Stances** — 下段 Guarded / 中段 Balanced / 上段 Aggressive with the atk/taken trade shown.
- **Quests** — all four kinds (PEST / HUNT / CLEAR / DEFEND) with steps + rewards.
- **Interior-house reveals** — "The house reopens 家" inks omoya/workshops/granary by R6.
- **Pedlar (market) reframe** — reads as the personal koku sink ("your purse, not the
  house's").

## Notes for the human (taste / pre-existing — NOT bugs)
- **Bestiary win-rate is HP-sensitive.** A hurt MC (the auto-policy left it at 1 HP)
  shows the monkey at **0% · Risky**; healthy it reads ~29%. Honest (a hurt fighter
  forecasts lower, D-050) but the *field-guide* framing could read as "this foe is
  unwinnable" rather than "you're hurt." A taste call (R5) — could show the foe's
  inherent difficulty separately.
- **Bestiary variant B truncates the foe name** ("Crop-raiding mo…") in the narrow
  ledger row. DEV-only (A ships); polish only if you pick B.
- **The "watch" fight-control buttons** (Fight · auto-to-end · auto-flee@20%) look
  cramped/overlapping in the narrow column. **Pre-existing** (not a v0.3.2 change), but
  worth a legibility pass.
- **The loss penalty bites carried koku hard** (~2.8K koku dropped in the sweep) — WAI
  (bank before a risky fight, D-076), but the sting is real; the auto-policy carries
  everything, so it over-states the risk vs. a player who banks.

Balance magnitudes are **liquid** (D-059) — name any number that feels off at playtest.
