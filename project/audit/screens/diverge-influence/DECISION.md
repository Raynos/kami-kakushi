# Diverge — the live House-Influence panel (M2·6, D-073)

**Surface:** the T0-M3 macro surface — `renderHouseInfluence` mutated from the static locked teaser into the
live **Estate 家産 pillar + grade bar + unnamed silhouettes (D-055) + the manual-opt-in Ascend CTA**.

**Why branch-preserve became folder-preserve:** the standard `diverge` skill keeps losing variants on a
`diverge/<surface>` branch, but the working tree is **shared with another live agent** this session, and a
`git switch` would clobber their uncommitted WIP (CLAUDE.md shared-tree safety rule). So the variants are
preserved here as committed screenshots + this note instead — same discipline (2 distinct approaches → contact
sheet → self-pick → R-item), zero `main` flag-debt.

## The two approaches

| | Variant A — continuous ink grade-bar (WINNER) | Variant B — segmented 3-band meter |
|---|---|---|
| Bar | one ink bar, fill indigo→gold by grade, ticks at Good/Great/Excellent | three labelled boxes (Good \| Great \| Excellent), each filling discretely |
| Read | a single sweep — at-a-glance "how far to Excellent" | step-wise; the labels add clutter |
| Aesthetic | woodblock-faithful (an ink rule with seal-marks) | reads as generic game-UI |
| State | renders correct | buggy (segment fill used `color`, not `background` → invisible) |

Shared across both: header `House Influence 家威`; active row `◆ Estate & Wealth 家産` + grade word
(Good 良 / Great 優 / Excellent 秀, ochre→indigo→gold); the season high-water readout; **3 unnamed silhouettes**
(D-055 — the pillars to come, never spoiled); the **vermilion 朱 Ascend CTA** when Estate ≥ EXCELLENT (the one
key action, per the palette bible).

## Pick: **Variant A** (self-pick; R2 filed for human override, non-blocking)

A is more woodblock-faithful and legible, and correct. Shipped to `main` flag-free. The ascension **ceremony**
reuses the rank-up-seal beat, scaled up + gold (D-062, "first ascension always big").

Contact sheet: `A-{1-teaser,2-great,3-excellent-ascend}.png` vs `B-{1,2,3}.png` in this folder.
