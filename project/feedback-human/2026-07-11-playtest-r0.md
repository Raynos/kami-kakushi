# Playtest feedback — 2026-07-11 (async inbox capture, `r0` lane)

Source: the in-game capture inbox (`project/playtest-inbox/`), the **r0** bucket
lane (4 open items, all on the exhausted R0 rake row). FB-stamped at capture
time; no reserved block drawn. Drained in the 2026-07-10→11 all-lanes parallel
pass (ADR-171); human approved the batch wholesale ("land all").
Status legend: 🔲 open · 🔧 in progress · ✅ fixed · 🅿️ parked · 💬 needs-discussion.

## The exhausted rake row

### FB-365 · disabled rake button "can still be clicked" — ✅
**Verbatim:** _"This disabled button can still be clicked."_
**Reading:** repro showed the click was already doubly inert (render gate +
reducer refusal — no state change on real or programmatic click). What the
human saw was the disabled plate still playing the hover raise, which reads
as clickable — the same root as FB-366.
**Fixed in:** `f3bdfb2c` (shared with FB-366).

### FB-366 · disabled button still has a hover effect — ✅
**Verbatim:** _"This disabled button still has a hover effect."_
**Reading:** the `.verb:hover` raise rule had no disabled guard, so a dead
plate lit up like a live one.
**Fixed in:** `f3bdfb2c` — `.verb:not([disabled]):hover`; no
`pointer-events:none` (the title tooltip must keep carrying the disabled-why).
**Distilled rule:** a disabled control never plays interactive affordances —
hover feedback is a promise the click will do something (TST4).

### FB-367 · auto button should hide when the action is disabled — ✅
**Verbatim:** _"The auto button can be hidden / disabled if the main action is
disabled."_
**Reading:** with the spill exhausted, "▶ auto" stayed visible and enabled but
was instantly disarmed by the autoplay heartbeat — a click that visibly did
nothing. The labour rows already had the right idiom (ADR-148); the rake row
was built without it.
**Fixed in:** implementation reached HEAD in `b3921564` (shared-tree sweep
alongside the estate works commit); RED-able tests pin it in `4c943deb`.
Exhausted → auto hides unconditionally; merely out-of-strength → visible only
while armed, showing "⏸ waiting".

### FB-368 · disabled button needs a real non-DEV hover saying why — ✅
**Verbatim:** _"The disabled button can have a real non-dev hover saying why
it's disabled, beyond the story flavor text."_
**Reading:** the prod `title` already carried the why, but a native tooltip is
a 1s-delayed whisper; the labour rows show the reason as visible inline text
(`lock-hint`). The rake row lacked that span — an inconsistency, not a missing
mechanism.
**Fixed in:** `b3921564` (swept, as FB-367) + tests `4c943deb` — the rake row
carries the labour-row `lock-hint`, reading the SAME predicate the title uses
(AC-6).
**Distilled rule:** one shared primitive per idiom (TST1) — a row that gates
must carry the row idiom's full kit (auto policy + lock-hint), not a subset.
