# 2026-07-09 — storywave-closure plan rulings (session 134)

The human ran the plan-review ask (AskUserQuestion) before execution and
ruled the plan's four surfaced forks + one follow-up, then went AFK with
the session executing the whole plan end-to-end autonomously.

## Rulings (verbatim options picked)

1. **HD-31 · Autumn exit-gate semantics → "Refusing gate per G1 spec"** —
   NOT the plan's recommended keep-as-built default. Implement the true
   refusing exit-gate: attempting Autumn's exit unreckoned refuses and
   triggers the nengu scene; completion sets the flag. → **ADR-166**.
2. **HD-32 · Supplemental fiction wave → "Everything now incl. unit 5"** —
   the full wave ships now: five season VN overlays, three named hidden
   discoveries, wolf-flees/new-moon line (if no authored source exists),
   per-grade day-book judge lines, AND the per-season node flavor.
   → **ADR-167**.
3. **Routing → "Fable builds it all here"** — this Fable session executes
   C0→C5 itself (overrides the plan's 80%-Opus routing table for this
   execution; the plan's routing stands as the record of what it proposed).
4. **prd-drift gate scope → "Widen to the living docs"** — the retired-terms
   scan extends beyond `docs/living/prd/*.md` to `fun-factor.md`,
   `ui-design.md`, `qa-playtesting.md`, with allowance rows as needed.
5. **Unit-5 diverge granularity → "Per-season"** (follow-up ask): 6 units
   × 3 takes; each take = one season's atmosphere across all ~15 nodes
   (coherent seasonal mood over per-node voice or whole-set picks).

## Standing steers given with the rulings

- *"If you see anything else we need to fix that the plan missed then you
  can fix that too"* — grant of discretion for adjacent mechanical fixes;
  design-shaped finds still surface as HD/HR-items.
- *"You are working on your own through the whole plan end to end
  autonomously, I'm going AFK"* — no blocking asks for the rest of the
  session; any further fork proceeds on the recommended default and is
  surfaced as an HD/HR-item for async override.
