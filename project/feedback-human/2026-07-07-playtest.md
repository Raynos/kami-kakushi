# 2026-07-07 — playtest feedback (async inbox capture)

Feedback captured **in-game** via the FB-3 capture overlay during the human's
2026-07-07 morning session (`2026-07-07T09-03-29-855814`), drained via
`/drain-inbox` as an interactive pass: the human steered the whole batch
(sequencing, the timed-actions design forks) via AskUserQuestion before any
fix landed. Every item was reproduced against the running game first (PH2).

**Status legend:** 🔲 open · 🔧 in progress · ✅ fixed · 🅿️ parked ·
💬 needs-discussion. **Routing:** bug / UX / feel / visual → fix + log here;
_what the game is_ → also an ADR; general taste → graduate to a living doc.

---

## Feedback log

### FB-173 · the CSS "stick" above the version button — ✅
**Verbatim:** _"Do you see in the bottom right of this element, there's a
little bar, like a CSS bug, just a tiny stick of a border we want to remove
that"_ / _"That stick i was talking baout the CSS bug, its right above the
version / about button here, it's like sticking up and out and it doesnt
makes sense, it looks like a CSS bug can you remove it."_ / _"This is the
stick for the CSS we want to get rid off."_ (3 captures, same bug)
**Reading:** a stray ~14px vertical hairline at the workspace's bottom-left,
above the v0.3.9 footer button.
**Root cause:** the FB-172 calendar dock — pre-unlock (`readout-clock`) the
clock inside `.clock-dock` is `[hidden]`, but the dock still painted its
padding + 1px gold `border-right`: an empty box whose border reads as a
stick. Fix: `.clock-dock:has(> .clock[hidden]) { display: none; }`.
Regression-checked at rung-R3 — the dock still renders once the clock
unlocks.
**Fixed in:** see this drain's commit.

### FB-174 · ALL actions become timed actions — 💬 → plan
**Verbatim:** _"So all actions in the game, I no longer like the idea of
pressing the button and it just doing the action. / I prefer now for ALL
actions to be timed actions, / You press the button and the timer starts, an
action like 'Rake the spilled rice' takes 5 seconds. all actions take some
number of seconds or even minutes for larger actions / The button is then
disabled for the lifetime of the action, until it completes. / There will be
a small progress bar under the button or just within the border of the
button at the bottom showing the time. / once an action completes, it enters
some kind of cooldown state, maybe 2s / The button progress bar then goes
back right to left during the cooldown state, once the cooldown is complete
you can press it again to do the timed action. / Pressing the auto button
would then do that action in a loop basically, it would still auto press it,
it would basically say on cooldown complete go again."_
**Reading:** a design fork — the core interaction model changes from
instant-dispatch to duration + cooldown. Human locked the direction in this
drain (AskUserQuestion): core owns duration DATA / the shell owns the CLOCK;
scope = labour + meta verbs + travel + craft/eat; an explicit **instant**
action class (trade); combat deferred to its own review; fast-idle
magnitudes (small 3–5s · labour 5–10s · big 30–90s).
**Doc-update plan:** `docs/plans/fable-2026-07-07-timed-actions.md` (the
plan built with the human); ADR lands with the plan sign-off.

### FB-175 · Now tab needs an "active" lamp, not an unread dot — ✅
**Verbatim:** _"I think the now button is too subtle, when the auto is
playing there's actual text rendering in the Now button / So the now button
should have some kind of 'unread' state but not the same red notification as
everything else, / All the other tabs show show unread when new logs are
written to them ( except All ) / The now button should have a different
color / theme, basically an 'active' theme if text was written to it in the
last 10s and if not, because nothing is happening it can go back to the
normal colors."_
**Reading:** Now was deliberately excluded from the shu unread dots (FB-53 —
its lines self-fade), which left it inert. Wanted: an ambient ACTIVITY lamp.
**Fix:** a warm-gold `live` tint on the Now tab while any `nowSeen` stamp is
< 10s old — driven by the existing FB-115 expiry clock (no new timer);
lapses back to quiet on its own. Verified on during auto-rake, off after 12s
quiet.
**Fixed in:** see this drain's commit.

### FB-176 · Now stands alone at the bar's left — ✅
**Verbatim:** _"I think the organization of buttons here at the bottom
should be Now on the left as a standalone thing, then the group of buttons
next to it, basically now should be it's own UI element seperate from the
other 6"_
**Reading:** Now is not a channel filter — it's the fleeting-flavor scratch
view; its placement should say so.
**Fix:** the Now button moved to the bar's left as a standalone
fully-rounded element; the six channel tabs join inside a new
`.log-filter-group` (the F21 segmented variant preserved per-group).
**Fixed in:** see this drain's commit.

### FB-177 · fresh-entries divider missing during live writing — ✅
**Verbatim:** _"I saw new text being written here without the 'new' UI / We
used to have a UI element that rendered for a period of time to seperate
historical logs from new/realtime logs."_
**Reading:** the FB-27 `新 · new` divider regressed for slow batches.
**Root cause** (instrumented live repro): the divider armed its 4.5s fade at
batch ARRIVAL, but the typewriter cascade types a batch out over many
seconds (one long story line alone typed for ~9s) — lines kept landing
after the divider had already faded.
**Fix:** the fade re-arms on every appended/typed line, and the countdown
defers while the cascade is still busy (queue non-empty / typing live) — the
divider now fades 4.5s after the LAST written line. Verified across the full
19s cascade window that previously failed.
**Fixed in:** see this drain's commit.

### F122 · cursor flickers arrow↔hand across the tab-bar gaps — ✅
_(async inbox capture — drain, session `2026-07-07T13-06-19-66f8f3`)_
**Verbatim:** _"Mouse pointer over various clickable elements and buttons
including in the story/progress/chat tabs for the log flicker between pointer
and mouse as you move the mouse. I think the CSS is too fragile here."_
**Reading:** cursor is set per-button, never on the interactive containers, so
every flex-gap between buttons exposes the default arrow — motion across a bar
flips arrow↔hand.
**Root cause** (confirmed live, headless computed-style probe): `.nav`,
`.log-filter-bar`, `.log-filter-group` all compute `cursor: auto` (arrow) with a
3.6px gap between `cursor: pointer` child buttons.
**Fix:** the three interactive tab-row containers claim `cursor: pointer`, so the
gaps + padding no longer show the arrow — no boundary to flicker across (verified
live: containers now compute `pointer`). One shared CSS rule.
**Distilled rule:** an interactive tab-row/button-bar container should claim the
same cursor as its buttons — per-button cursor over a gapped flex row flickers.
**Fixed in:** see this drain's commit.

---

## Evening drain — the 17-capture map-sheet session (`2026-07-07T22-29-33-9aa00d`)

Drained as one interactive batch (the human approved "fix all" + settled
three forks via AskUserQuestion — T1 canon = hybrid · JP marginalia =
strip to player-legible · LOD = fade). Grounding note: the map-modal
screenshots came out EMPTY and the element picker returned null for every
capture (two tool bugs, logged below) — triage ran on the notes + the
live sheet.

### FB-178 · drag-pan selects SVG text — ✅
**Verbatim:** _"As you drag the map around, it basically causes text selection which looks ugly"_
**Fixed in:** 78532a4 (`user-select:none` on the map pane).

### FB-179 · zoom-out makes text vanish — ✅
**Verbatim:** _"As you zoom out certain text does not render anymore"_
**Reading:** the deliberate L10 zoom gate read as a rendering glitch.
**Fixed in:** 78532a4 (the fine register FADES — opacity+visibility, 180ms — same threshold; human picked "keep gate, fade smoothly").

### FB-180 · "Field margins" caption illegible — ✅
**Verbatim:** _"its not very legible, we need a different font color with far stronger contrast"_
**Fixed in:** 78532a4 (captions ink-soft → full ink, halo 4→5px).

### FB-181 · the kanji mark rows don't serve — ✅
**Verbatim:** _"all the little japanese symbols below the name dont serve much purpose… the margin bottom left is also not needed anymore"_
**Distilled rule:** the sheet is player-bound — no on-sheet grammar that needs a legend; state lives in the detail pane. → ui-design.md candidate (with FB-182/183/184).
**Fixed in:** 78532a4 (mark rows + filter pills + 凡例 box + aside legend removed).

### FB-182 · cartouche means nothing · the red square confuses — ✅
**Verbatim:** _"This japanese in top right doesnt mean antyhign to me. Also the red square underneath is confusing…"_
**Fixed in:** 78532a4 (red 改 hanko removed; cartouche keeps the vertical JP title + gains an English gloss "The Kurosawa lands — 1780/1782").

### FB-183 · random vertical JP text — ✅
**Verbatim:** _"Random japanese text here we can remove and clean up"_
**Fixed in:** 78532a4 (the period distance notes removed; NOTES data kept for a future glossed-hints feature — see the rung-reveal design).

### FB-184 · gold 新 badges unhelpful — ✅
**Verbatim:** _"the golden 'new zone' kanji is not very helpful for me"_
**Fixed in:** 78532a4 (on-sheet badges removed; the roster keeps its 新 chips).

### FB-185 · the ruin is too small vs the guest house — ✅
**Verbatim:** _"The ruined compound needs to be grander, or the guest house needs to be smaller. Or both"_
**Fixed in:** a63458c (both: fallen masses +18% + a sixth, barracks bigger; guest house scale 1.35→1.22).

### FB-186 · orchard sits ON the ruins — ✅
**Verbatim:** _"they need space to overlap and be close to each other"_
**Fixed in:** a63458c (orchard shifted off the rubble core; adjacent with slight overlap, rows still ruin-aligned).

### FB-187 · bamboo grove crammed in the mountains + not bamboo-like — ✅
**Verbatim:** _"there needs to be a bit more space, also the way bamboo is drawn can be better"_
**Fixed in:** a63458c (precinct N wall drops 45u; the grove gets its own band) + 82d35ff (taller culm groups with joint ticks, bigger nodding edge clumps).

### FB-188 · pools overlap the river · floating kanji · weird red line — ✅
**Verbatim:** _"the pools over lap the river, and kanjis are floating here"_
**Fixed in:** a63458c (pools re-seated west bank; 改・涸 re-anchored; the strike ends on the pool group).

### FB-189 · woodlot edge missing the woods — ✅
**Verbatim:** _"we should render a bit of woods in T0"_
**Fixed in:** a63458c (a forest tongue reaches into the T0 window's east edge, south of the burn-line anchor).

### FB-190 · the weir isn't visible — ✅
**Verbatim:** _"I see the riverbank but I don't physically see the weir itself"_
**Reading:** root cause was a DATA bug — the bar's angle ran it along the flow.
**Fixed in:** 82d35ff (bar crosses bank-to-bank: gold structural stroke, two stone courses, stakes, foam step; footbridge + fish weirs same fix).

### FB-191 · drill yard reads as a building — ✅
**Verbatim:** _"i expected some kind of outdoor training zone not a building"_
**Fixed in:** 82d35ff (the zone IS the yard: swept ground, rake sweeps, two straw dummies, rack; the stall range is its edge — the R8 "stalls for twenty horses" wrongness stays).

### FB-192 · "Kitchen threshold & board-side" too long — ✅
**Verbatim:** _"is a long name for a zone, needs a rename"_
**Fixed in:** 82d35ff ("The Kitchen threshold").

### FB-193 · T1's new work should show on the ruined main compound — ✅ (hybrid)
**Verbatim:** _"All the new areas are mounted on the guest house… the main house zone still looks like a ruin" / "The ruined compound needs to show as a rebuild shoin and east wing"_
**Reading:** contradicts built canon (T1 rebuilds only the lived guest compound; the ruin's reveal is T2). Surfaced as the drain's canon fork.
**Decision (human):** HYBRID — canon holds, but ONE fresh repair reaches into the precinct edge as a T2 promise.
**Fixed in:** 82d35ff (the precinct wall north of the lived corner re-stacked in fresh work, noted 改・繕).

### FB-194 · guest-house zone too busy · "didn't we build a new gate?" — ✅
**Verbatim:** _"Kura interior I guess can stay over there, but its too dense and too confusing"_
**Reading:** density handled by the strip (FB-181) + caption suppression + the smaller house; the remembered "new gate" folded into the FB-193 hybrid mend; workshops stay in the guest compound (canon).
**Fixed in:** 78532a4 / 82d35ff.

### FB-195 · capture tool: map-modal screenshots come out empty — 🔲
**Reading:** the FB-3 overlay's full-page screenshot pipeline misses the map modal entirely — the pen annotation floats on the bare game shell. Spatial context of every map capture was lost.
**Fix queued** (capture-overlay pass; not a map-sheets issue).

### FB-196 · capture tool: element picker returns null inside the modal — 🔲
**Reading:** `element: null` in every JSON from the map session — the pick-mode resolver can't name SVG/modal elements, so notes lose their anchor.
**Fix queued** (same pass as FB-195).
