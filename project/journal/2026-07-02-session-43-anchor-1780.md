# Session 43 — 2026-07-02 — Anchor the setting to 1780 (D-105 doc ripple)

**Summary:** Implemented the docs-only ripple for **ADR D-105** (anchor the game
to **1780 / An'ei 9** — real year, fictional geography & politics). Human steered
**A2** (light off-screen "hard times" texture) over the A1 default and **B1**
(relative Year-N calendar, no code change) held. Also authored a **tiny hand-off
`src/` plan** for the one code surface (the cold-open grounding line).

## What changed

- `docs/living/prd/01-vision.md` — §1.3 tone: now states **set in 1780 (An'ei 9)**
  + the real-year / fictional-geography-&-politics split (shōgun = unnamed office).
- `docs/living/prd/05-narrative.md` — T0.2 cold-open grounded to "spring, An'ei 9
  — 1780" (flavour only, calendar stays relative — D-105); added the **A2**
  "hard times down the road" optional rumour-board tidbit to the T2 content list
  (off-screen, no plot, no mechanics, never names the era).
- `docs/living/ui-design.md` — noted the "mid-Edo woodblock" register is anchored
  to 1780 (validates, doesn't change, the visual bible).
- `README.md` — anchor added to the **editable top-note** (not the preserved seed
  body — see Landmines).
- `docs/plans/2026-07-02-anchor-1780-setting.md` → `project/archive/` — plan done
  (Status ✅), A2/B1 recorded, DoD ticked; graduated.
- `docs/plans/2026-07-02-anchor-1780-code-surfacing.md` — NEW tiny hand-off plan:
  ground `COLD_OPEN.wake` in spring/An'ei 9, update its tests, leave `year()` +
  `SEASON_TAG` untouched (B1). A2 rumour + B2 nengō stay deferred.
- `project/todo-human.md` — removed the done anchor-setting plan from the reading
  queue (D-089); added the new code-surfacing plan.

## Next intended steps

1. Hand `2026-07-02-anchor-1780-code-surfacing.md` to an agent for the cold-open
   line + test update once the shared-tree red (intro WIP) clears.

## Landmines

- **Deviation from the ripple plan (§5 step 3):** the README anchor went into the
  editable top-note, **not** line 20 — the README explicitly preserves "the
  original seed text unchanged," so editing the seed body would break that
  contract. Same intent, contract respected.
- **Committed with `SKIP_VERIFY=1`, NOT pushed.** The working tree carried
  another agent's in-flight **red** (`src/ui/render.ts` intro WIP — eslint
  prefer-const / unused-vars). This commit is **docs-only** and staged by explicit
  path; per AGENTS.md "don't fight someone else's red," it stays **local** until
  that red clears. `SKIP_VERIFY` was for the local docs commit only — never a red
  push to main.
