# Session 94 — 2026-07-06 — HD-23 + HD-24: R3 mend-hint & cold-open restore

**Summary:** Implemented the two open s88 e2e decisions the human chose —
**HD-23 → option C** (a diegetic mend-path lock-hint at R3) and **HD-24 → option
B** (a "Restore a saved game" line on the cold-open card). Both graduated to ADRs
(**ADR-141**, **ADR-142**), archived, and covered by RED-able render tests +
verified live via `__qa`. The HD-23 hint line shipped from 3 blind ADR-139 takes
(HR-10 filed). This file is HISTORY — live state is
`project/status/project-status.md`.

## What changed
- `src/ui/render.ts` — HD-23: a `.lock-hint` (`wcHint`) on the combat weapon-card
  that shows *"A worn edge is mended by hands the house has come to trust."* when
  the blade is Battered/Broken and `verb-repair` (R4) isn't yet unlocked; wired in
  both the incremental and DEV-wholesale combat paths. HD-24: a `.coldopen-restore`
  line under the wake verb → `settings.open('saves')`, and the Settings/Saves
  **modal re-parented from `shell` to root** so it can overlay the pre-awake
  cold-open (the shell is `hidden` before waking); the restore line joins the
  cold-open reveal sequence.
- `src/ui/styles.css` — `.coldopen-restore` (muted, chromeless, underlined link on
  the dark kura field; AA-legible, hover→washi).
- `src/ui/render.test.ts` — two new RED-able tests: HD-23 (R3 Battered shows the
  hint + no Repair; R4 retires the hint + offers the CTA; Battered fixture derived
  from `DURABILITY_BANDS`, asserted) and HD-24 (pre-awake restore line opens the
  Saves modal as a root sibling on the Saves tab).
- `docs/living/decisions.md` — **ADR-141** (HD-23) + **ADR-142** (HD-24).
- `project/human-in-the-loop/decisions.md` — HD-23/HD-24 removed (now closed).
- `project/human-in-the-loop/archive.md` — HD-23/HD-24 archive rows.
- `project/human-in-the-loop/review.md` — **HR-10** (the 3 ADR-139 hint takes;
  shipped default take B, alternates A/C recorded).

## Next intended steps
1. Await the human's HR-10 verdict on the mend-hint line (take B shipped; A/C alternates).
2. Remaining open HD-items: none. Open HR queue unchanged otherwise.

## Landmines
- The Settings/Saves modal is now a **root-level sibling**, not a `shell` child —
  anything that assumed `shell` contains the modal (or toggled the modal via the
  shell subtree) must account for the move. `shell.hidden` no longer hides it (by
  design — that's what lets it overlay the cold-open).
- The `.lock-hint` element in the combat weapon-card is built once and toggled via
  `hidden` (reconcile `toggle`), so it's always present in the DOM — assert on
  `.hidden`, not presence.
- ADR-139: the HR-10 alternates live ONLY in the review doc (no DEV switcher for a
  one-line lock-hint). On an override, swap the single string in the weapon-card
  block of `render.ts`.
