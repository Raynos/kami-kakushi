# Session 80 — 2026-07-05 — v0.3.6: the first real /ship run

**Summary:** Ran the F9 `/ship` train end-to-end for real: patch bump
0.3.5 → 0.3.6, CHANGELOG drafted + confirmed, release commit `8369078`,
one fix-forward for a committed-tree red the isolated build caught, then
`ship.sh` built/deployed/proved. **Live-proven:**

> ✓ LIVE-PROVEN: https://raynos.github.io/kami-kakushi/ serves v0.3.6 ·
> build ce71cf4 (assets/index-737BIEcy.js)

- **Release commit:** `8369078` (`chore(release): v0.3.6 — Phase-2
  pacing, 10x saves`)
- **Shipped build SHA:** `ce71cf4` (HEAD had advanced with the co-agent's
  F10 commit before the deploy — it rode along, correctly on origin/main)
- **gh-pages deploy SHA:** `49c6d6b`

## What changed

- `package.json` — 0.3.5 → 0.3.6.
- `CHANGELOG.md` — `## [0.3.6] — 2026-07-05`: Phase-2 sub-koku deed
  pacing (D-133 stopgap), R10 economy tunings, ~10× smaller saves,
  intro click-advance + rung-popover fixes; F3/F5/F6/F7/F9 under
  Internal (the F7 cockpit entry moved down from Unreleased).
- `docs/plans/README.md` — fix-forward `57421d6`: the committed
  gen-region listed F8 as IN-PROGRESS while the committed F8 plan read
  PROPOSED (an earlier regen ran against working-tree WIP). Regenerated
  in a detached worktree of HEAD and committed via
  `git hash-object`/`update-index` so the co-agent's dirty working copy
  of the same file was never touched.
- `project/status/project-status.md` — deployed-version mentions →
  v0.3.6 / 2026-07-05.
- `project/journal/2026-07-05-session-80-v036-ship.md` — this file.

**Verified (R3):** the proof line above is `ship.sh`'s live fetch (exit
0), not a local assumption — the CDN served both `v0.3.6` and
`build ce71cf4` after ~30s of polling. Isolated `npm ci` + full verify
green (17 gates) at the shipped SHA; DEV-strip gate clean.

## Landmines

- **Local green hid committed red** (again): the ship-train's isolated
  verify is what caught the stale plans-README gen-region — pre-commit /
  pre-push verify run on the shared working tree and were green. The
  index-plumbing fix pattern (regen in a worktree of HEAD, commit the
  blob without touching the working file) is the shared-tree-safe cure.

## Next intended steps

1. F9 Ph2 hardening (stranded gh-pages push recovery, resume polish).
2. Phase-2 economy redesign or F8 telemetry — per the master plan.
