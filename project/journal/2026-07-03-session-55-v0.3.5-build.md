# Session 55 — 2026-07-03 — v0.3.5 slice: the seven agent-default deltas

## ☀️ SUMMARY (read this first)

Building the **v0.3.5 slice** (7 tasks) from
`docs/plans/opus-2026-07-03-v0.3.5-build-plan.md` in an isolated worktree
(branch `worktree-agent-a125ef980e547d3ef`). Each task ships with the test that
was asserting the OLD default updated so it could have gone RED. This file is
HISTORY, not live state — the live snapshot is
`project/status/project-status.md`.

The seven: (1) rice storage costs (spoilage + kura cap), (2) Now-view →
append-only, (3) Inventory tab → R3, (4) Quests → own tab at R5, (5) hearth
homes the cook verb, (6) chest → belongings storage, (7) R5 wall-weapon token.
**NOT** building the R7 capstone branch (split out, needs design).

---

## Landmines / notes

- **Worktree md-links quirk (verify infra):** running `npm run verify` from the
  SHARED checkout (`/Users/raynos/projects/games/kami-kakushi`) cries wolf on 72
  dead links because `check-md-links.ts` scans `.claude/` and recurses into the
  nested worktree copy's `project/journal/` (stale-by-design links). Running from
  the worktree root itself is clean (no nested worktrees dir). ALL work here is
  done from the worktree. (A `worktrees` exclude in check-md-links would harden
  this for shared-checkout runs — not applied, out of scope.)

## What changed

### Task 1 — rice storage costs something (D-118 §1)
- `src/core/content/balance.ts` — added `RICE_SPOILAGE_NUM/DEN` + `riceSpoilage()`
  (per-season decay, integer fixed-point, ≈10%) and `KURA_RICE_CAP_BASE/PER_STAGE`
  + `kuraRiceCap(estateStage)` (U0 120 → U4 440). Magnitudes liquid (D-059).
- `src/core/step.ts` — `onSeasonTurn` fires in `onDayRollover` on each
  `day % DAYS_PER_SEASON === 0` boundary; spoils carried AND banked rice (each
  floors independently), emits an ephemeral "some rice spoiled" Now line.
  Deterministic → fold-invariant (engine.test still green).
- `src/core/intents.ts` — `deposit` caps RICE at `kuraRiceCap(estateStage)`
  (only rice; coin banks freely); a full kura is a no-op.
- `src/ui/render.ts` — storehouse shows `stored X/N rice`, disables the rice-store
  button when the kura is full (title points at improving the estate), blurb notes
  spoilage + the cap.
- `src/core/economy.test.ts` — new tests: spoilage decays both piles by exactly
  `riceSpoilage(each)` on a season turn (and nothing mid-season); deposit caps at
  `kuraRiceCap(0)`, a full kura deposits nothing. Both derive from the source of
  truth → RED against the old free/lossless/uncapped kura.

### Task 2 — Now-view → append-only (D-123)
- `src/ui/reconcile.ts` — new `resetReconcile(container)` to forget a container's
  key→node map after a wholesale `textContent=''` clear on a path that SHARES the
  container with a reconcile path (the log lines).
- `src/ui/render.ts` — `renderNowView` reconciles the visible ephemeral entries
  (keyed by log key) instead of `textContent=''` + rebuild; placeholder no longer
  wholesale-clears (idempotent append); `resetReconcile(logLines)` at the two
  teardown points (`setLogFilter`, renderLog reset) so a shared-container wipe
  can't leave the Now map patching detached nodes.
- `src/ui/render.test.ts` — new zero-churn test: a fleeting Now line keeps node
  identity across an idle re-render and churns nothing. RED against the old
  rebuild (which recreated every node each ~480ms tick).
- Landmine: the wall-clock fade interval (`pruneNowViewDom`) still removes now-line
  nodes directly; reconcile self-heals on the next render (aged-out entries drop
  out of `visible`). A state-change render during a ~0.4s collapse animation can
  cut it short — cosmetic only, accepted.

### Tasks 3 + 4 — the seven-tab IA (Inventory R3, Quests own tab at R5) (D-119)
Committed together — one coherent IA reshape (the reveal-cadence test spans both).
- `src/ui/render.ts` — `tabHasContent('inventory')` now gates on `tab-combat` (R3,
  was panel-estate/panel-home R1); `Tab` type + TAB_LABEL + TAB_ORDER gain
  `quests` (glyph 用); `tabHasContent('quests')` gates on `tab-quests` (R5);
  `characterHasContent` drops the old quests branch; `renderQuests` re-gates to
  `activeTab === 'quests' && isUnlocked('tab-quests')`.
- `src/core/content/surfaces.ts` — new `tab-quests` surface (kind 'tab', R5 reveal
  line).
- `src/core/content/ranks.ts` — R5 `rewardOnReach.unlock` gains `tab-quests`.
- `src/core/content/rungBeats.ts` — R5 `motivates` gains `tab-quests` (beat↔unlock
  coherence).
- `docs/content/t0-content.md` — regenerated (gen:docs) for the new surface.
- Tests: new D-119 reveal-cadence tests (R1 = Map+Estate, NO Inventory/Quests; R3
  reveals Inventory but NOT Quests; R5 reveals Quests) — RED against the old
  Inventory-at-R1 triple-reveal / a Quests-at-R3 batch. Updated the storehouse +
  home + dev home-variant fixtures to add `tab-combat` (Inventory now R3), the
  renderQuests zero-churn test to the Quests tab, and the renderSkills F72 test to
  use `readout-combat-level` for the Character tab's "other content" (quests left).

### Tasks 5 + 6 — housing: hearth homes cook, chest is storage (D-120)
Committed together (both edit home.ts + home.test.ts + the belongings render).
- `src/core/content/home.ts` — `ComfortKind` widens to add `storage`; hearth's
  comfort → `null` + `homesCook: true` (diegetic cook locus, was +12 satietyMax);
  chest's comfort → `{ kind: 'storage', amount: CHEST_STORAGE_CAPACITY (6) }` (was
  +5 satietyMax). New `homeHasCookLocus()` helper.
- `src/core/selectors.ts` / `index.ts` — new `homeStorageBonus`, `homeHasCook`
  selectors + `CHEST_STORAGE_CAPACITY` export.
- `src/ui/render.ts` — `comfortLabel` + a shared `comfortSummaryText` handle
  storage + the cook note; a **cook-at-the-hearth affordance** appears in the home
  pane once the hearth is owned + cook is unlocked (dispatches `cook_meal`, heal
  cue when hurt).
- `src/ui/dev.ts` — the DEV home variants' comfort copy updated to match (storage +
  cook), so they stay honest (D-075: no buggy variants).
- `src/scripts/gen-docs.ts` + `docs/content/t0-content.md` — the belongings table
  shows hearth "cook here" / chest "storage +6".
- `home.test.ts` — hearth test now asserts it homes cook (comfort null, homesCook
  true, no satietyMax bump); new chest-is-storage test; the prestige-not-power
  invariant widened to `['rest','body','storage']`. All RED against the old stats.
- **Landmine / decision:** the core `cook_meal` verb is NOT hard-gated on owning the
  hearth — the Work-column cook stays reachable (combat healing must work before a
  120-coin hearth is bought / for existing tests + pacing). The hearth ADDS a
  home-context cook affordance ("homes the cook verb") without stranding the heal.
  The chest's `storage` capacity is a shown prestige buffer (a T1+ overflow seam),
  not yet a binding wall.

### Task 7 — R5 wall-weapon status token (D-122)
- `src/core/content/ranks.ts` — R5 `rewardOnReach.flags` gains `wall-weapon`.
- `src/core/ranks.ts` — `applyPromotion` emits a dynamic mount line naming the
  ACTUAL `equippedWeapon` (getWeapon) when a rung grants `wall-weapon` — data-driven
  off the flag, never a hard-coded R5 literal, never a generic sword.
- `src/ui/render.ts` — the home pane's status-mirror shows the mounted weapon LIVE
  (reads `equippedWeapon`, so re-equipping updates it), hidden until the flag.
- Tests: core (`rung-beats.test.ts`) — reaching R5 sets the flag + the mount names
  the wielded weapon (carrying-pole vs wood-axe proves it reads the weapon; a
  non-token rung mounts nothing); render — the status-mirror is hidden pre-R5 and
  names the wielded weapon at R5. RED against a hardcoded/generic-sword mount.
- Decision: the mount reads the weapon LIVE (updates on re-equip) rather than
  snapshotting at R5 — satisfies "your currently-wielded weapon / never a generic
  sword" and needs no schema field. NOT built: the full surname→swords→gōshi ladder
  (T1–T5). DEV home variants B/C don't render the mount (prod default does; they're
  review alternates) — minor, noted.

## Status: all 7 tasks landed, verify green. R7 capstone branch NOT built (out of scope).
