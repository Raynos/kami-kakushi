# Session 81 — 2026-07-05 — v0.3.7: tooling-only /ship run

**Summary:** Ran the `/ship` train patch-bumping 0.3.6 → 0.3.7. Every
commit since 0.3.6 was process/docs (F10 two-pass taste flow, /ship v2,
F8 plan lock, status checkpoints) — **nothing player-facing** — so this
cut just refreshes the deployed build to track HEAD. Confirmed the
internal-only payload with the human, shipped clean.

- **Release commit:** `b1b3403` (`chore(release): v0.3.7 — tooling-only,
  tracks HEAD`)
- **Shipped build SHA:** `b1b3403` (HEAD at deploy)
- **gh-pages deploy SHA:** `f4697b5` — **pushed** (not `--verify-live`'d
  this run)
- **Tag:** `v0.3.7` on `b1b3403`, pushed to origin

## What changed

- `package.json` — 0.3.6 → 0.3.7.
- `CHANGELOG.md` — `## [0.3.7] — 2026-07-05`: Internal-only section (F10
  taste flow, leaner tagged /ship v2, F8 plan lock), with an explicit
  "nothing changes for the player" lead.
- `project/status/project-status.md` — deployed-version mentions →
  v0.3.7; resume banner records "pushed @ gh-pages f4697b5 ← main
  b1b3403", flagged not-live-verified.
- `project/journal/2026-07-05-session-81-v037-ship.md` — this file.

## Verified (R3)

- Release commit's pre-commit + pre-push verify green (17 gates, ~4.4s);
  only `package.json` + `CHANGELOG.md` staged (shared-tree safe).
- `ship.sh` exit 0: isolated build (node_modules reused, lockfile
  unchanged), DEV-strip gate clean, gh-pages pushed. Per the F9 ratified
  rule, **exit 0 = pushed = done**; I did NOT run `--verify-live`, so the
  honest claim is **pushed**, not live-proven.

## Landmines

- Three co-agent WIP files were dirty in the shared tree at ship time
  (`docs/plans/fable-process-F8-play-telemetry.md`, a session-79 journal,
  `src/telemetry/`). They did not block the release commit's verify;
  staged only my own paths by pathspec, never touched theirs.

## Next intended steps

1. F8 play-telemetry build (per the locked plan) or Phase-2 economy
   redesign — per the master plan.
2. A `ship.sh --verify-live` pass on 0.3.7 is optional if confirmation
   the CDN is serving the new build is wanted.
