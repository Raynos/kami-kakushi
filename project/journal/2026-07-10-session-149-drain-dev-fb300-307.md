# Session 149 — 2026-07-10 — drain the `dev` bucket: FB-300…307 (the DEV-panel reshape)

**Summary:** Drained the `dev` lane's eight open captures (FB-300…FB-307) —
one coherent human ask: reshape the DEV panel. Combat/Auto retired, an
"NG (post open)" footer shortcut, a wider panel with three-up tabs, Balance
demoted to last, "Rungs" → "Rung info", a new Prototypes tab (the six
prototype launchers out of Story, grouped Map sheets / New UI / Parked), and
the Story diverge bundles grouped by rung like Variants (a `rung:` field
through the takes gen pipeline). Lane claimed and released per ADR-171;
whole-lane pass (the ≤5 cap is retired), wholesale proposal approved by the
human before any fix landed. The human also approved sweeping the cold-open
WIP seam (`subColdOpen`) already sitting in `dev.ts` into the first commit
touching it.

## What changed
- FB-300 — `src/ui/dev.ts` drops the Combat/Auto section + the `DevQa`
  `auto`/`autoCombat` members (+ `dev.test.ts` stubs); the headless
  `__qa.auto`/`__qa.autoCombat` drive methods stay (QA path, not buttons).
- FB-301 — footer gains `⟳ NG (post open)`: loads the existing
  `post-cold-open` fixture (backup-first).
- FB-302/303/304/305/306 — the tab bar reshape: 7 tabs, 3 per row, panel
  widened; order Settings · Variants · Scenarios / Story · Rung info ·
  Prototypes / Balance; Prototypes pane hosts the six `⤢` launchers under
  Map sheets / New UI / Parked UI prototypes headers.
- FB-307 — `bundle.md` headers gain `rung:`; `src/scripts/narrative/takes.ts`
  parses it into `StoryTakeBundle.rung`; the Story pane renders the same
  `— rung RX —` headers the Variants pane uses; `storyTakes.gen.ts`
  regenerated.
- `project/feedback-human/2026-07-10-playtest-dev.md` — the FB-300…307
  records; sidecars stamped done; bucket archived at end of pass.

## Next intended steps
- None for this lane — bucket archived. `r0` / `the-log` remain w3:p3's.

## Addendum — live steers FB-308…311 (same session)
Four direct-chat steers landed on top of the reshape: fixed expanded height
(`min(42rem, 82vh)`), the footer as a 2×2 grid, a `Variants (n)` count badge,
and fixed-third tab cells so Balance sits as one cell. Headless-verified
(per-tab height probe + shots). Committed with SKIP_VERIFY under the same
w1:p3 red-tree rationale; local until a green push window.

## Addendum 2 — FB-307 data + FB-312 (same session)
The human unblocked FB-307's data half by approving a snapshot of w1:p3's two
in-flight hd37-cold-open bundles (needed for the registry regen to survive
CI's byte-compare; w1:p3 had already stamped them `rung: R0` on my ping). A
fresh steer landed with it: FB-312 — the `— other —` catch-all is banned;
`rung:` is now REQUIRED (`R<n>` or `other · <reason>`, enforced RED at the
generator), each reason its own header. Fallout: the F95 footer test re-pinned
to the FB-309 grid; takes.test loc lines shifted by the fixture's rung line.
