# Session 196 — 2026-07-13 — HD-41 locks: take B, the world changed

**Summary:** The human asked what HD-41 was waiting on, couldn't find
the answer in the item ("nothing in HD-41 made it obvious it's waiting
on SV19"), then found the take live and **locked take B ("the world,
changed")** for the 31 Progress-tab objective lines. The lock is
**ADR-191**; HD-41 and HR-41 are archived; the
`opus-2026-07-12-rung-reward-legibility` plan graduates DONE. Two
process consequences: every open HD/HR item now leads with a
**Waiting on:** blocker line (README rule, applied to HD-40), and the
positional SV/V tag system that hid the bundle from the human in the
first place is ruled to die — tags stop being registry positions
(built this session, next commit).

## What changed

- `src/core/content/narrative/requirements.md` — all 31 `objective:`
  lines swapped to take B's (scripted swap from `take-b.md`, verified
  31/31); `gen:narrative` regenerated `requirements.gen.ts`,
  `storyTakes.gen.ts` (22 bundles), `docs/content/t0-story.md`.
- `src/core/content/narrative/takes/hd41-progress-objective/` —
  **pruned** (sign-off, ADR-075 zero flag-debt; A and C survive in the
  HR-41 archive row + git history).
- `docs/living/decisions.md` — **ADR-191** (the full HD-41 lock:
  treatment B, completion-driven pulse, voice = take B).
- `project/human-in-the-loop/` — HD-41 + HR-41 removed from the live
  queues, archive rows added; `README.md` gains the **Waiting on:**
  first-line rule; HD-40 gets its blocker line.
- `project/human-in-the-loop/review.md` — HR-40/27/33's SV tags
  renumbered from the `review-link` gate's report (the prune shifted
  every tag after position 19 down one — w1:p3 hit the same trap
  adding `adr190-nudge` hours earlier).
- `project/status/project-status.md` — HR-41 bullet dropped from the
  snapshot; HR-42's pointer re-cited by bundle id.
- `docs/plans/opus-2026-07-12-rung-reward-legibility.md` — Status ✅,
  moved to `project/archive/`; plans README regenerated by hand-equiv.
- `src/ui/dev-surfaces.ts` — the HD-41 header comment updated (closed
  → ADR-191; the stale SV18 citation gone).

## What happened

**The pick.** The human chose B over the self-picked A: the changed
physical fact of the estate as the evidence of work, over the
day-book record. ADR-191 records the why.

**The findability failure, twice.** (1) HD-41's entry said "awaiting
the human" but named the actual actionable thing (HR-41's take-picker
in the DEV panel) three bullets deep — the human couldn't tell what it
was blocked on. Hence the **Waiting on:** rule. (2) The human then
looked for the bundle at its old tag **SV18** and concluded it was
gone — `adr190-nudge` landing at SV1 had renumbered it to SV19.
Verified live (headless) that the row existed; but two agents hitting
the same positional-tag trap in one day is the system's fault, not
theirs.

**The ruling that follows:** tags must not be position-indexed
("Review & audit that whole system and make it simple and dead
simple"). The KISS rework — stable id-based references, no numeric
tags, gate simplified — lands as this session's next commit.

## Next intended steps

- Build the id-based review-reference rework (this session).
- HD-40 remains the open decision; HR-42 the fresh review.
