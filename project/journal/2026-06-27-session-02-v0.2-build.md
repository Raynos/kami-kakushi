# Session — 2026-06-27 (cont.) — v0.2 build (the audit-fix prototype)

## ☀️ SUMMARY (read this first)

Autonomously built **v0.2** of the M0-M2 prototype from the morning's state-of-the-game audit (human AFK at a
theme park, explicit go-ahead to "build a v0.2 I can review instead of v0.1"). v0.1 baseline tagged **`v0.1`**
(`bd4ac17`). v0.2 = **9 feat commits + 2 fixes** on `main`, **all `npm run verify` GREEN** (51 → **98 tests**),
console-clean, Lighthouse a11y **100**. Full mapping in
[`project/audit/reports/v0.2-changelog.md`](../audit/reports/v0.2-changelog.md).

**Pipeline (orchestrated):** (1) a **design workflow** — 6 subsystem specs incl. a real combat-sim parameter
sweep that found a graded curve, + a conflict-aware 8-commit sequencing plan; (2) Commit 1 (combat) via a
standalone engineer subagent (verified the pattern); (3) Commits 2-8 via a **sequential build workflow**
(each a fresh engineer, verify-green + commit, halt-on-failure — none halted); (4) a lead **browser playtest**
of every new surface + a bug-fix; (5) an adversarial **re-score workflow** (in flight at journal time).

**What v0.2 fixes (audit → build):** the binary 98/2/0/0 combat → a **graded rolling-frontier curve** + a
**kendo stance decision** (27pt swing on even fights); the missing **reinvestment loop** (skill→yield) + real
**sinks** (cook sansai→satiety, koku→estate E0→E3, the phantom `attributePoints`→Might/Guard/Vigor); the silent
R3 dead-end → a **chapter-close beat + dream-2 mystery payoff + a greyed House Influence 家威 4-pillar macro
teaser**; the dead `.coldopen` CSS → a **wired cold-open title screen**; log-spam → **×N coalescing**; unjuiced
deeds → **number-pop**; areas-as-dead-data → **labour grouped by place**; the **false-green tests** → real
RED→GREEN curve/no-dead-value/log tests; the **dead `migrate()`** → wired + tested + schemaVersion footgun
closed; **DEMO/REAL pacing profiles** + a headless `npm run pacing` report (REAL clears the ≥30-min floor:
R0/R1/R2 = 31/35/38 min); + the a11y/label/touch-target/unit-bug fixes.

**Scope discipline:** NONE of the 6 human H-items were decided (DEMO stays the default; the REAL profile is
merely *reachable*). Every balance number tagged `provisional (v0.2) — tune by playtest`.

---

## Log

### 1 · Design wave — `wf_7c25f884-7a0` (7 agents, 45 min)
Verified combat sweep produced the rolling-1 frontier (monkey L1 0.33, wolf 0.04→0.85, bandit a wall to L8).
8-commit plan with reconciled cross-spec numbers + 12 resolved conflicts. Plan → `tmp/v02-plan.md`; raw →
`brainstorms/raw/2026-06-27-v02-design-specs.json`.

### 2 · Build — Commit 1 standalone (`946aab0`) + Commits 2-8 workflow `wf_32abe6ba-32a` (7 agents, 57 min)
All 8 feat commits landed verify-green, no halt: `946aab0` combat · `1d7bd13` economy · `3e8236a` pacing ·
`30bb2c6` log · `4913c98` persistence · `72dc94a` narrative · `c442146` feature-UI · `e756f56` feel-UI.
Plus `2261a19` (eslint ignore tmp/ — pre-empt a snag the agents hit). Tests 51→98.

### 3 · Lead playtest (browser, firsthand) + fix `493c5dd`
Drove every new surface and reviewed screenshots with my own vision — all verified working (see the changelog's
"Playtest-verified" column): cold-open screen, graded curve (monkey 48/81/90, wolf 8/19/44-Even, bandit a wall),
stance 27pt swing, Training/estate/cook panes (estateStage 0→1 confirmed), R3 chapter-close + dream-2 payoff +
家威 teaser, log ×N (×38/×32), number-pop, "Combat level" label, ◇-Unknown foe gating.
**Bug caught & fixed (`493c5dd`):** New Game / Import left the previous run's log lines in the DOM
(`renderLog` never reset `lastKey`/`lastSeq`); now it clears on a `log.seq` rewind. Console clean, a11y 100.

### 4 · Rough edges flagged for the human (tuning, not blockers)
Durability × the tighter curve can soft-stick the L1 grind (broken pole, no wood → monkey unwinnable until you
woodcut); Balanced stance dominated on the wolf at some levels; slower XP makes the grind longer (cheap knob:
`COMBAT_XP_BASE 40→32`); stance pulled forward to R3 vs the PRD's R5 (wants a 1-line ADR). All in the changelog.

### 5 · Deferred (recorded, not folded)
WebAudio SFX; the font woff2 **binaries** (@font-face + subset script shipped, system-Mincho fallback until
vendored); `pacing:check` in `verify` (= the H1 policy call); a cooking skill; the koku-reward rebalance.

### 6 · Re-score + post-re-score must-fixes — `wf_685b7891-991` (5 agents) ✅
Independent adversarial re-score verified the fixes are real in code+tests and produced the deltas: **Fun
4.5→6.5 · UI 7→8.5 · PRD-faithful 6.5→8 · README-spirit 7→7.5 · human-feedback 7.5→8 · Incremental 4.5→7 ·
Laziness 4.5→3.** Verdict: *"a high-integrity audit-fix build whose headline fixes are verified real, not merely
claimed."* It caught real issues — all FIXED (verify-green, 98→99 tests):
- **`0bf2d5d` [HIGH]** seed-robust forecast — the displayed win-rate was run-seed-coupled at n=48, so monkey@L1
  read 0.48 at the *shipped* seed (vs 0.33 at the test's seed-1) → the guard test protected the wrong
  experience (the *same* flaw the v0.1 audit caught). `foeForecasts` now uses a fixed seed + n=400 → every
  player sees the true **~0.32**, in the signed 20–35% band; bands re-aimed.
- **`ae20a2d` [MED]** durability soft-stuck — auto-combat now auto-repairs (wood permitting) and STOPS rather
  than silently grind a Broken weapon at ~0%.
- **`ae20a2d` [LOW]** legacy-save false "we mended a problem" notice — additive might/guard/vigor absence no
  longer flags `coerced` (+ a guarding test); the no-dead-value ratchet now covers the attributes.
- Docs: `D-047` ADR (v0.2 + the stance R5→R3 pull-forward); changelog corrected (fonts ARE vendored; the
  combat numbers were the old run-seed values; cook +40-clamped; estate one-stage/commission). The
  Balanced-stance-dominated-on-some-foes item is flagged **for the human** (tuning).

**DONE.** v0.2 is tagged, verify-green, re-scored. **NEXT is the human's:** playtest v0.2, action H1–H6, and
decide the next milestone (the real four-pillar macro engine is the biggest remaining piece).
