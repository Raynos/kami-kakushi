# Session 36 — 2026-07-01 — the v0.3.1 autonomous build (/loop)

## ☀️ SUMMARY (read this first)

A long autonomous `/loop` to build **v0.3.1** end-to-end — the full plan plus a
**batch-2 design expansion** the human dropped during priming. _This file is
HISTORY, not live state; the live snapshot is
[`../status/project-status.md`](../status/project-status.md)._

The human primed the session (two `AskUserQuestion` batches) then went AFK with:
_"build the plan and everything we discussed… don't stop until it's complete…
everything we discussed will ripple back into the PRD."_ Locked design calls
(full capture:
[`../human-feedback/2026-06-30-v0.3.1-priming-decisions.md`](../human-feedback/2026-06-30-v0.3.1-priming-decisions.md)):

- **Strict plan order, all variants, no corners.** Combat log **summarised**. Koku
  sinks **all three** directions.
- **Map = the spatial spine** — every activity on ONE node (no default), enemies
  physically on nodes, walk-to-do (expands Step 5).
- **Combat deadliness** — HP accumulates / no free regen; two auto-modes
  (fight-to-death vs auto-retreat@20%, per-turn); retreat = a 3rd outcome.
- **The bank** — carried (at-risk) vs banked (safe) resources, deposit at the kura;
  the loss penalty ("real bite") hits carried only.

**Build order:** Step 1 DEV panel + variant infra → 2 craft/market/quests variants
→ 3 combat + bank + auto-modes → 4 koku + §4 balance → 5 spatial map → 7 battery
leftovers → final PRD-consistency + R1 hand-off. Each mechanic ripples into the
PRD + an ADR in its own step (no build-ahead-of-canon). Progress also tracked in
the harness task list (7 tasks mirroring these steps).

---

## 1 · Orientation + the priming design session

**Orientation (verify-don't-trust, R2).** Read the plan, `project-status`,
`review.md`, and the code I'll touch (`render.ts`, `main.ts`, the deploy
strip-guard, vite config). Key finding: **no variant code exists** — the old
diverge process kept only the winner (A) and deleted B/C (the exact "diverge-LITE"
anti-pattern D-075 was written to kill). `grep variant src/**` → one comment at
`render.ts:638` ("diverge winner — variant A"). So `review.md` R2 is a shopping
list of variants OWED, not a gallery to review — Step 1–2 build them. (The human
independently noticed the same gap — "where are the variants lol".)

**Combat recon (Explore subagent).** The fight is **already an HP-attrition sim**:
`resolveFight` (combat.ts:234-262) trades HP turn-by-turn until 0; HP carries
(D-050); a loss soft-sets HP to `SETBACK_HP=1`. So Step 3 is **new features**, not
a math rewrite — summarised log, loss penalty, the bank, the two auto-modes, and
killing the auto-loop's auto-heal (main.ts:189-209). Constants live in
`balance.ts:116-251`; forecast = `foeForecasts`/`sampledWinRate` (must stay routed
through the same fn as the fight — A6).

**Priming.** Two `AskUserQuestion` batches resolved the four plan open-Qs then
expanded scope with three new locked calls (above). Captured to disk before any
code (compaction insurance).

**Docs landed this entry:** the priming-decisions capture, the plan's "Batch-2
scope expansion" block + a PRD-ripple DoD line, this journal.

## 2 · Step 1 — DEV panel + variant-toggle infra (DONE, verify-green + pushed)

Built the strip-safe variant infra + the in-UI DEV panel.

- **`src/ui/dev.ts` (new, DEV-only).** The variant registry (`SURFACES`), `createDevApi()`
  (holds `variant: Record<surface,id>`, defaulting to each surface's first = the prod default),
  the alternate variant renderers (influence B/C), and `mountDevPanel()` (Speed 1/2/4/8× ·
  Jump→Phase2/Ascend · Rung R3/R5/R7 · Face-wolf/Auto · New-game · the per-surface VARIANT
  toggle). Stamped with `DEV_SENTINEL = __KAMI_DEV_PANEL__`.
- **`src/ui/render.ts`.** Exported `el`; `mount(..., dev?: DevApi)` (type-only import of
  `DevApi`, erased); the House-Influence grade visual now routes through
  `import.meta.env.DEV && dev.renderVariant('influence', …)` — A (continuous bar) is the inline
  default that SHIPS; B/C live in dev.ts.
- **`src/app/main.ts`.** `const dev = import.meta.env.DEV ? createDevApi() : undefined` →
  threaded to mount; the DEV block mounts the panel on `document.body`.
- **`src/scripts/gh-pages.sh`.** Strip-guard extended to grep the prod bundle for
  `__KAMI_DEV_PANEL__` too (not just `__qa`).
- **Influence variants:** A (continuous ink bar, default) / B (segmented 3-band boxes — old
  `color`/`background` bug fixed) / C (◆◇ standing-marks). Each a review.md line item; R2 updated.

**Verified (R3 — done is earned):**
- `npm run verify` GREEN (10 gates, ~2.2s). New RED-able `src/ui/dev.test.ts` drives the real
  renderer and asserts the routing (A default / B / C) + `createDevApi` defaults.
- **Prod strip PROVEN:** `npm run build` then grep dist — `__qa`, `__KAMI_DEV_PANEL__`,
  `influence-seg`, `influence-marks`, `mountDevPanel` → **0 hits**; default A's `influence-bar`
  /`influence-fill` → present. ui/dev.ts tree-shakes out; prod ships only the default.
- **Live in-browser (R6):** drove `localhost:5173` with Playwright — the panel mounts and the
  real toggle buttons swap influence A→B→C live (`tmp/step1-variant-{A,B,C}.png`).

## 3 · Step 2 — full variants for craft / market / quests (DONE, verify-green + pushed)

Authored FULL 2–3 working, on-constraint variants for the three diverge-LITE surfaces via a
parallel diverge **Workflow** (one agent per surface, grounded in ui-design §9 + the dev.ts
pattern). Self-picked **A** (the current rendering) as the prod default for all three per A19
(keep the shipped rendering unless an alternate is clearly better); B/C are DEV-only alternates.

- **Craft:** A work-order checklist (default) · B continuous fill-gauges (A19 ink) · C diegetic
  assembly (each material = the part it becomes; 整/未 verdict).
- **Market:** A price-button list (default) · B posted price-board (品書, dotted-leader ledger) ·
  C pedlar's ground-cloth (emoji goods + continuous ochre stock-ink).
- **Quests:** A woodblock cards (default) · B 高札 notice-board (continuous-ink deeds stroke) ·
  C 用帳 field-ledger (aligned tabular koku column + 合計 foot).

**Integration (verify-don't-trust the agent code — R2):** checked each renderer against the real
data shapes (MaterialDef.blurb ✓, QuestKind='PEST'|'HUNT'|'CLEAR' ✓, market ids ✓) before wiring.
One architectural improvement over the agent draft: threaded the real `dispatch` through
`renderVariant(surface, container, state, dispatch)` for the interactive market/quests buttons,
replacing a `window.__qa.dispatch` global reach-through (cleaner + testable). render.ts seams:
craft/market wrap the item loop (variant renders into the shared card); quests early-returns
after the title (variant renders the whole body).

**DEV-panel gutter fix:** the panel overlapped the right work column (variants rendered behind it
— bad for live R2 review). Reserved a 16rem right gutter on `#app` so the centered shell
(`max-width:980px;margin:0 auto`) re-centers clear of the panel; proven `shell.right(944) ≤
panel.left(952)`. The panel never covers content now.

**Verified (R3):** `npm run verify` GREEN. **Prod strip RE-PROVEN:** `npm run build` + grep dist →
every new renderer + diegetic class/string = **0 hits**, defaults (craft-mat/market-row/quest-card)
ship, bundle size unchanged (tree-shaken). **Live (R6 / D-075 no-buggy):** drove all 6 new variants
in-browser — each renders without a crash, replaces its default, correct element counts
(`tmp/step2-{market,craft,quests}-{B,C}*.png`). Raw workflow output snapshotted to
`brainstorms/raw/2026-07-01-step2-variant-diverge-w30vfsskf.json`.

## 4 · Step 3 — combat rework + bank + auto-modes (IN PROGRESS, sub-step commits)

The big game-feel step (D-076 + batch-2 calls 4/6/7). The fight is ALREADY HP-attrition (combat
recon), so this is the new FEATURES, in verify-green sub-commits.

**3a — summarised log + loss-stops-autopilot + remove auto-heal (DONE, pushed).**

- `fight.ts`: a grind WIN is now ONE summarised combat line carrying the HP swing + loot
  (`You bring down the boar 猪. ✓ (HP 18→9 · +8 koku, +1 hide)`) — the old 2-3 lines folded into
  one; loot rolled first so it lands inline. A LOSS is one summarised line AND nulls `autoCombat`
  (D-076: 0 HP ⇒ autopilot off). The scripted wolf keeps its charged narrative (the human's
  execution call). HP still carries (D-050).
- `main.ts`: removed the auto-loop's auto-heal (cook/forage block) — HP now accumulates; a ≥1-dmg
  foe grinds you down, a loss stops the autopilot. (Kept satiety-rest + durability-repair; the
  retreat-@20% mode lands in 3d.) Dropped the now-unused `hpMax`/`isUnlocked` imports.
- Tests: `combat-rework.test.ts` (4 RED-able guards) — win = one line w/ HP swing, loss = one line
  + autoCombat→null, win keeps autopilot. Fixtures from the curve (mc(5) vs monkey = win, mc(1) vs
  bandit = loss), not magic seeds. `verify` GREEN (249 tests).

**3b + 3c — the bank + the carried-loss penalty (DONE, pushed).**

- `state.ts`: added `banked: Record<ResourceId, number>` (the kura storehouse) + a `withBanked`
  helper. Two-pool: `resources` = carried (existing spending/earning untouched), `banked` = a safe
  reserve. `validate.ts`: added to the compile-time field-ledger + a `{}` default, so pre-bank
  saves hydrate clean (additive — no migration).
- `intents.ts`: `deposit`/`withdraw` intents move ALL of a resource carried ↔ banked (gated on the
  estate economy; spatially gated to the kura in Step 5). `render.ts`: a "The storehouse 蔵" panel
  (carried vs stored koku + Store-all / Withdraw-all), reusing the frame/card vocabulary. (Not
  diverged — a small functional panel, not a design-rich surface; noted as a judgment.)
- `fight.ts` + `balance.ts`: a LOST fight now drops `LOSS_KOKU_FRAC` (0.2, "real bite") of CARRIED
  koku + `LOSS_MATERIAL_FRAC` (0.34, floored) of carried materials — **banked is untouched**; the
  summarised loss line names the drop. Magnitude liquid (D-059).
- Tests: 5 new RED-able guards (deposit/withdraw round-trip, no-op gate, loss bites carried-not-
  banked by the source frac, a win never touches banked). `verify` GREEN (254 tests). **Live (R6):**
  drove the storehouse (deposit 120→banked, withdraw round-trips) + a real bandit loss (carried koku
  100→80, banked 50→50 SAFE, hardwood 3→2, the penalty line renders right).

**3d — the two auto-modes / auto-retreat (DONE, pushed).**

- `combat.ts`: `resolveFight` gains a `retreatHp` param + a `fled` outcome — break off on a turn
  where HP drops below the threshold but is still > 0 (a killing blow is a LOSS, so a burst foe
  still wins — the per-turn semantic the human specified). The forecast keeps `retreatHp=0`, so the
  shown win-rate stays the honest to-death rate (A6).
- `state.autoCombatRetreat` (additive, validate-defaulted); `fight`/`set_auto_combat` carry a
  `retreat` flag; `applyGrindFight` handles the flee (no reward, **no penalty**, autopilot STOPS —
  you mend + re-engage). `main.ts` auto-loop passes the mode. `render.ts`: per foe, two toggles —
  **▶ auto** (to the end) and **▶ auto·flee** (retreat@20%).
- Tests: 4 new RED-able guards (resolveFight flees on a survivable grind-down; retreatHp=0 never
  flees; a 40-seed sweep proves flees occur + every flee is no-penalty/autopilot-off; the mode flag
  threads). `verify` GREEN (258 tests). **Live (R6):** the two toggles render + set the mode.

**Combat mechanics (3a–3d) COMPLETE.**

**Ripple (DONE this commit) — Step 3 CLOSED.** Rippled the new model into the PRD: §4.6.6b rewritten
from the superseded "self-recovering auto-retreat loop" → no-auto-heal + accumulating HP + two
auto-modes + loss-stops-autopilot; §4.6.6 setback → the defined carried-wealth bite; **new §4.6.6c**
the kura storehouse (carried vs banked). ADRs **D-090** (a lost fight bites carried, the bank
shelters banked) + **D-091** (the two auto-modes), both refining D-076. `verify` GREEN. **Step 3 done.**

## 5 · Step 4 — koku economy tightening (game part DONE, pushed)

Tightened the T0 koku economy for scarcity (D-086 / batch-1 call 4) with sinks across all three
directions the human named — a lighter pass than the plan's full §4 re-derivation, for a reason
found on the way (scope calls below).

- **Repair costs koku** — a recurring combat-upkeep sink (`REPAIR_KOKU_COST` = 6, liquid). Learned
  the hard way (the m2 no-stranding test): a HARD koku gate softlocks an unlucky fresh L1 (few early
  wins → no koku → can't repair → stranded), violating **D-061/D-086** (tension must never push the
  player out). So it's a **soft FEE** — repair needs wood (hard) + up to 6 koku **waived when you
  can't pay** (the smith extends a broke goshi credit). A real sink normally, zero softlock risk.
- **A 4th estate stage E4** ("Raise the long-house", 1400 koku) — the flywheel's biggest turn, a
  deeper late-T0 koku sink priming the house's rise to T1.
- **Market depth** — 2 pricier capped goods (road-rations, smith's billet), inside the D-008
  minority-lane cap (a RED-able test asserts total market spend ≤ ⅓ of the estate sink).
- Tests: RED-able guards in economy.test (the fee bites / is waived, ≥4 ascending estate stages,
  market ≤⅓ estate) + updated the market-count + m2 no-stranding tests. Regenerated the content
  docs. `verify` GREEN (263 tests). **Live (R6):** E4 offered at E3, 6 market items, the 6-koku fee.

**Scope calls (surfaced, bias-to-motion):** (1) the plan's **§4 6-tier re-derivation** is **deferred
to the final PRD-consistency pass** — a finding: §4 is T1+ *pillar/tier* balance, NOT the T0
koku-sink *content* constants, so the plan's "both move the same numbers" coupling is looser than
stated; the relabel is separable doc-hygiene, done once in the final pass. (2) **battery #15**
(surplus materials / 2nd recipe) → **Step 7** — a 2nd recipe ripples into Step 2's craft variants +
it's literally a battery item.

**Step 4 CLOSED** — ADR **D-092** (koku scarcity tightening; the soft repair-fee no-softlock design)
+ a PRD §4.6.6d note landed. `verify` GREEN.

## 6 · Step 5a — activities are spatial (batch-2 call 1) DONE

The first slab of the spatial map (the human's biggest batch-2 call): **every labour now belongs to
exactly one map node, and there is no default node.** You walk to the work.

- **Core gate** — `canDoActivity`/`availableLabours` (`src/core/selectors.ts`) now require
  `activity.area === state.location`; the work tab lists only the current node's labours. The
  `activity.area` bindings were already in `content/activities.ts` (farm→home-paddies,
  haul→gate-forecourt, woodcut→woodlot-edge, forage→near-satoyama); this makes them load-bearing.
- **The harness learned to walk** — the shared `focusedOptimalIntent` driver (`pacing-report.ts`,
  the SINGLE policy both pacing + playcheck drive) now BFS-hops the *revealed* graph to a labour's
  node before working it (`nextHopToward` + `cheapestEligibleGlobal`). This is why the macro e2e /
  pacing / invariants / playcheck all stay green under the spatial model — the optimal path is
  *navigate-then-do*, not teleport-and-do.
- **UI** — renderActions shows a "no work where you stand — open the 地図 map and walk" hint, guarded
  so the cold-open kura (gate not yet revealed) still just offers rake-rice.
- **Test ripple** — 7 core test files drove `do_activity` from the old default node; each now sets
  `location` to the labour's node first (assertions unchanged, fights untouched — foes aren't spatial
  until 5b). Fixed via a 7-agent workflow, one file each.
- **`verify` GREEN — all 10 gates** (tsc + 263 tests + pacing/playcheck/invariants).
- **Live (R6):** through the real reducer — farm @ kura → *rejected*; walk kura→gate-forecourt→
  home-paddies → *both hops land*; farm @ home-paddies → *works*; woodcut @ home-paddies (wrong
  node) → *rejected*. The walk-to-work loop is reachable.

Next in Step 5: **5b** foes on nodes (home-paddies: monkey+boar, near-satoyama: wolf, woodlot-edge:
bandit, kura: scripted wolf) → **5c** spatial banking (deposit/withdraw at the kura) → **5d** the
load-bearing deed node + post-ascension after-state → **5e** map-presentation variants (diverge
A/B/C) → PRD ripple + ADR.

## 7 · Step 5b — foes are spatial (walk to the fight) DONE

Foes now live on map nodes — you walk to a foe's ground to fight it, the natural companion to 5a.

- **Content** — `MobDef` gained `area: AreaId`: crop-raiders (monkey + boar) on the home paddies,
  the lean wolf at the near satoyama, the road bandit on the woodlot road, the scripted wolf
  cornered in the kura where you woke.
- **Two selectors, cleanly split** — `foeForecasts` stays *location-independent* (it's "my win-rate
  if I fought this foe" — the curve the balance gates read; filtering it would've broken ~5 curve
  tests and misframed the concept). A NEW `foesHere(state)` does the spatial filter for the UI. This
  confined the ripple to the intent gate + the driver — no curve-test churn.
- **Reducer gates** — `fight` no-ops unless you stand on the foe's node; `face_wolf` is faced only
  at the kura; `move_to` clears `autoCombat` (walking away ends the auto-grind you left behind).
- **UI** — the combat "watch" lists only `foesHere`, with a "no foe holds this ground — walk the 地図
  map" hint when empty; the work-tab wolf beat shows at the kura, a walk-back summons elsewhere.
- **The autoplay policy moved to core (R2 single-source)** — `focusedOptimalIntent` + `nextHopToward`
  + `cheapestEligibleGlobal` now live in `core/autoplay.ts`, exported and shared by the pacing gate,
  playcheck, the full-arc/invariants e2e, AND the DEV `__qa.toRung` climber. This FIXED a real
  regression: the old `toRung` used the now-spatial `availableLabours` and STALLED at R1 (couldn't
  reach the paddies to farm). The DEV `__qa` also gained `goto(node)` (BFS-walk the revealed graph)
  and `fight`/`faceWolf` now auto-navigate to the foe first.
- **Tests** — 5 RED-able 5b guards (fight works on-node / no-ops off-node, `foesHere` is
  node-scoped, walking clears auto-combat, the wolf is kura-only), driven from the source-of-truth
  `getMob(...).area`. m2 + render tests set the foe's node. `verify` GREEN — all 10 gates, 267 tests.
- **Live (R6):** through the real reducer/UI — `toRung('R3')` now climbs the WHOLE arc (walks to
  farm, back to the kura for the wolf, reaches R3); fight auto-walks to the foe's node; the watch
  shows monkey+boar at the paddies and the empty-ground hint at the forecourt; walking off clears
  auto-combat. The played monkey@L1 win-rate measured **0.333**, matching the **0.32 forecast** — the
  forecast is honest (no reality gap).

**QA scripts made spatial-honest** — `qa-shots.mjs` stands on the paddies for the combat shots (the
kura watch is empty by design); `playtest.mjs`'s stability grind now cooks (heal) + repairs
(sharpen) between bouts — without it a labour-climbed R3 MC is combat-untrained at 1 HP and just
loses forever (D-076 deadliness working). With maintenance it shows monotonic progress (L1→L3, no
anomalies). *(Screenshot-gallery regen deferred to 5e, after the map UI settles — regenerating now
then again at 5e is wasted work.)*

## Next intended steps (current)

1. **Step 5 (cont.)** — 5c spatial banking (deposit/withdraw at the kura) → 5d deed node +
   after-state → 5e map variants (+ screenshot-gallery regen) → PRD ripple + ADR. Then Step 7
   (battery, incl. #15 + the deferred diverge-skill rewrite) → final PRD-consistency + R1 hand-off.

## Landmines (current)

- **Variant strip-safety is load-bearing** — every new surface variant follows the Step-1
  pattern: default inline in render.ts (ships); alternates in `ui/dev.ts` behind
  `import.meta.env.DEV && dev.renderVariant(surface, …)`; add the surface to `SURFACES`; the
  gh-pages guard greps `__KAMI_DEV_PANEL__`. (Proven: `npm run build` + grep dist = 0 leak.)
- **DEV panel** — fixed top-right in a reserved 16rem `#app` gutter so it never covers content
  (the shell re-centers clear of it); collapsible.
- **B/C a11y** — DEV-only until picked; if the human picks one, re-check WCAG contrast (B's
  dark ink label on the indigo "Good 良" box).
- **Shared-tree safety** — another agent may edit the tree; stage only my own files by explicit
  path, never `git add -A`.
- **The bank is a real state-model change** (resources → carried+banked pair) — it rides
  Step 3 because the loss penalty needs it; Step 5 makes deposit spatial.
