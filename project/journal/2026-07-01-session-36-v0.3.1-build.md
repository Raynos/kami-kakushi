# Session 36 — 2026-07-01 — the v0.3.1 autonomous build (/loop)

## ☀️ SUMMARY (read this first)

A long autonomous `/loop` to build **v0.3.1** end-to-end — the full plan plus a
**batch-2 design expansion** the human dropped during priming. _This file is
HISTORY, not live state; the live snapshot is
[`../status/project-status.md`](../status/project-status.md)._

The human primed the session (two `AskUserQuestion` batches) then went AFK with:
_"build the plan and everything we discussed… don't stop until it's complete…
everything we discussed will ripple back into the PRD."_ Locked design calls
(full capture:
[`../human-feedback/2026-06-30-v0.3.1-priming-decisions.md`](../human-feedback/2026-06-30-v0.3.1-priming-decisions.md)):

- **Strict plan order, all variants, no corners.** Combat log **summarised**. Koku
  sinks **all three** directions.
- **Map = the spatial spine** — every activity on ONE node (no default), enemies
  physically on nodes, walk-to-do (expands Step 5).
- **Combat deadliness** — HP accumulates / no free regen; two auto-modes
  (fight-to-death vs auto-retreat@20%, per-turn); retreat = a 3rd outcome.
- **The bank** — carried (at-risk) vs banked (safe) resources, deposit at the kura;
  the loss penalty ("real bite") hits carried only.

**Build order:** Step 1 DEV panel + variant infra → 2 craft/market/quests variants
→ 3 combat + bank + auto-modes → 4 koku + §4 balance → 5 spatial map → 7 battery
leftovers → final PRD-consistency + R1 hand-off. Each mechanic ripples into the
PRD + an ADR in its own step (no build-ahead-of-canon). Progress also tracked in
the harness task list (7 tasks mirroring these steps).

---

## 1 · Orientation + the priming design session

**Orientation (verify-don't-trust, R2).** Read the plan, `project-status`,
`review.md`, and the code I'll touch (`render.ts`, `main.ts`, the deploy
strip-guard, vite config). Key finding: **no variant code exists** — the old
diverge process kept only the winner (A) and deleted B/C (the exact "diverge-LITE"
anti-pattern D-075 was written to kill). `grep variant src/**` → one comment at
`render.ts:638` ("diverge winner — variant A"). So `review.md` R2 is a shopping
list of variants OWED, not a gallery to review — Step 1–2 build them. (The human
independently noticed the same gap — "where are the variants lol".)

**Combat recon (Explore subagent).** The fight is **already an HP-attrition sim**:
`resolveFight` (combat.ts:234-262) trades HP turn-by-turn until 0; HP carries
(D-050); a loss soft-sets HP to `SETBACK_HP=1`. So Step 3 is **new features**, not
a math rewrite — summarised log, loss penalty, the bank, the two auto-modes, and
killing the auto-loop's auto-heal (main.ts:189-209). Constants live in
`balance.ts:116-251`; forecast = `foeForecasts`/`sampledWinRate` (must stay routed
through the same fn as the fight — A6).

**Priming.** Two `AskUserQuestion` batches resolved the four plan open-Qs then
expanded scope with three new locked calls (above). Captured to disk before any
code (compaction insurance).

**Docs landed this entry:** the priming-decisions capture, the plan's "Batch-2
scope expansion" block + a PRD-ripple DoD line, this journal.

## 2 · Step 1 — DEV panel + variant-toggle infra (DONE, verify-green + pushed)

Built the strip-safe variant infra + the in-UI DEV panel.

- **`src/ui/dev.ts` (new, DEV-only).** The variant registry (`SURFACES`), `createDevApi()`
  (holds `variant: Record<surface,id>`, defaulting to each surface's first = the prod default),
  the alternate variant renderers (influence B/C), and `mountDevPanel()` (Speed 1/2/4/8× ·
  Jump→Phase2/Ascend · Rung R3/R5/R7 · Face-wolf/Auto · New-game · the per-surface VARIANT
  toggle). Stamped with `DEV_SENTINEL = __KAMI_DEV_PANEL__`.
- **`src/ui/render.ts`.** Exported `el`; `mount(..., dev?: DevApi)` (type-only import of
  `DevApi`, erased); the House-Influence grade visual now routes through
  `import.meta.env.DEV && dev.renderVariant('influence', …)` — A (continuous bar) is the inline
  default that SHIPS; B/C live in dev.ts.
- **`src/app/main.ts`.** `const dev = import.meta.env.DEV ? createDevApi() : undefined` →
  threaded to mount; the DEV block mounts the panel on `document.body`.
- **`src/scripts/gh-pages.sh`.** Strip-guard extended to grep the prod bundle for
  `__KAMI_DEV_PANEL__` too (not just `__qa`).
- **Influence variants:** A (continuous ink bar, default) / B (segmented 3-band boxes — old
  `color`/`background` bug fixed) / C (◆◇ standing-marks). Each a review.md line item; R2 updated.

**Verified (R3 — done is earned):**
- `npm run verify` GREEN (10 gates, ~2.2s). New RED-able `src/ui/dev.test.ts` drives the real
  renderer and asserts the routing (A default / B / C) + `createDevApi` defaults.
- **Prod strip PROVEN:** `npm run build` then grep dist — `__qa`, `__KAMI_DEV_PANEL__`,
  `influence-seg`, `influence-marks`, `mountDevPanel` → **0 hits**; default A's `influence-bar`
  /`influence-fill` → present. ui/dev.ts tree-shakes out; prod ships only the default.
- **Live in-browser (R6):** drove `localhost:5173` with Playwright — the panel mounts and the
  real toggle buttons swap influence A→B→C live (`tmp/step1-variant-{A,B,C}.png`).

## 3 · Step 2 — full variants for craft / market / quests (DONE, verify-green + pushed)

Authored FULL 2–3 working, on-constraint variants for the three diverge-LITE surfaces via a
parallel diverge **Workflow** (one agent per surface, grounded in ui-design §9 + the dev.ts
pattern). Self-picked **A** (the current rendering) as the prod default for all three per A19
(keep the shipped rendering unless an alternate is clearly better); B/C are DEV-only alternates.

- **Craft:** A work-order checklist (default) · B continuous fill-gauges (A19 ink) · C diegetic
  assembly (each material = the part it becomes; 整/未 verdict).
- **Market:** A price-button list (default) · B posted price-board (品書, dotted-leader ledger) ·
  C pedlar's ground-cloth (emoji goods + continuous ochre stock-ink).
- **Quests:** A woodblock cards (default) · B 高札 notice-board (continuous-ink deeds stroke) ·
  C 用帳 field-ledger (aligned tabular koku column + 合計 foot).

**Integration (verify-don't-trust the agent code — R2):** checked each renderer against the real
data shapes (MaterialDef.blurb ✓, QuestKind='PEST'|'HUNT'|'CLEAR' ✓, market ids ✓) before wiring.
One architectural improvement over the agent draft: threaded the real `dispatch` through
`renderVariant(surface, container, state, dispatch)` for the interactive market/quests buttons,
replacing a `window.__qa.dispatch` global reach-through (cleaner + testable). render.ts seams:
craft/market wrap the item loop (variant renders into the shared card); quests early-returns
after the title (variant renders the whole body).

**DEV-panel gutter fix:** the panel overlapped the right work column (variants rendered behind it
— bad for live R2 review). Reserved a 16rem right gutter on `#app` so the centered shell
(`max-width:980px;margin:0 auto`) re-centers clear of the panel; proven `shell.right(944) ≤
panel.left(952)`. The panel never covers content now.

**Verified (R3):** `npm run verify` GREEN. **Prod strip RE-PROVEN:** `npm run build` + grep dist →
every new renderer + diegetic class/string = **0 hits**, defaults (craft-mat/market-row/quest-card)
ship, bundle size unchanged (tree-shaken). **Live (R6 / D-075 no-buggy):** drove all 6 new variants
in-browser — each renders without a crash, replaces its default, correct element counts
(`tmp/step2-{market,craft,quests}-{B,C}*.png`). Raw workflow output snapshotted to
`brainstorms/raw/2026-07-01-step2-variant-diverge-w30vfsskf.json`.

## 4 · Step 3 — combat rework + bank + auto-modes (IN PROGRESS, sub-step commits)

The big game-feel step (D-076 + batch-2 calls 4/6/7). The fight is ALREADY HP-attrition (combat
recon), so this is the new FEATURES, in verify-green sub-commits.

**3a — summarised log + loss-stops-autopilot + remove auto-heal (DONE, pushed).**

- `fight.ts`: a grind WIN is now ONE summarised combat line carrying the HP swing + loot
  (`You bring down the boar 猪. ✓ (HP 18→9 · +8 koku, +1 hide)`) — the old 2-3 lines folded into
  one; loot rolled first so it lands inline. A LOSS is one summarised line AND nulls `autoCombat`
  (D-076: 0 HP ⇒ autopilot off). The scripted wolf keeps its charged narrative (the human's
  execution call). HP still carries (D-050).
- `main.ts`: removed the auto-loop's auto-heal (cook/forage block) — HP now accumulates; a ≥1-dmg
  foe grinds you down, a loss stops the autopilot. (Kept satiety-rest + durability-repair; the
  retreat-@20% mode lands in 3d.) Dropped the now-unused `hpMax`/`isUnlocked` imports.
- Tests: `combat-rework.test.ts` (4 RED-able guards) — win = one line w/ HP swing, loss = one line
  + autoCombat→null, win keeps autopilot. Fixtures from the curve (mc(5) vs monkey = win, mc(1) vs
  bandit = loss), not magic seeds. `verify` GREEN (249 tests).

**3b + 3c — the bank + the carried-loss penalty (DONE, pushed).**

- `state.ts`: added `banked: Record<ResourceId, number>` (the kura storehouse) + a `withBanked`
  helper. Two-pool: `resources` = carried (existing spending/earning untouched), `banked` = a safe
  reserve. `validate.ts`: added to the compile-time field-ledger + a `{}` default, so pre-bank
  saves hydrate clean (additive — no migration).
- `intents.ts`: `deposit`/`withdraw` intents move ALL of a resource carried ↔ banked (gated on the
  estate economy; spatially gated to the kura in Step 5). `render.ts`: a "The storehouse 蔵" panel
  (carried vs stored koku + Store-all / Withdraw-all), reusing the frame/card vocabulary. (Not
  diverged — a small functional panel, not a design-rich surface; noted as a judgment.)
- `fight.ts` + `balance.ts`: a LOST fight now drops `LOSS_KOKU_FRAC` (0.2, "real bite") of CARRIED
  koku + `LOSS_MATERIAL_FRAC` (0.34, floored) of carried materials — **banked is untouched**; the
  summarised loss line names the drop. Magnitude liquid (D-059).
- Tests: 5 new RED-able guards (deposit/withdraw round-trip, no-op gate, loss bites carried-not-
  banked by the source frac, a win never touches banked). `verify` GREEN (254 tests). **Live (R6):**
  drove the storehouse (deposit 120→banked, withdraw round-trips) + a real bandit loss (carried koku
  100→80, banked 50→50 SAFE, hardwood 3→2, the penalty line renders right).

**3d — the two auto-modes / auto-retreat (DONE, pushed).**

- `combat.ts`: `resolveFight` gains a `retreatHp` param + a `fled` outcome — break off on a turn
  where HP drops below the threshold but is still > 0 (a killing blow is a LOSS, so a burst foe
  still wins — the per-turn semantic the human specified). The forecast keeps `retreatHp=0`, so the
  shown win-rate stays the honest to-death rate (A6).
- `state.autoCombatRetreat` (additive, validate-defaulted); `fight`/`set_auto_combat` carry a
  `retreat` flag; `applyGrindFight` handles the flee (no reward, **no penalty**, autopilot STOPS —
  you mend + re-engage). `main.ts` auto-loop passes the mode. `render.ts`: per foe, two toggles —
  **▶ auto** (to the end) and **▶ auto·flee** (retreat@20%).
- Tests: 4 new RED-able guards (resolveFight flees on a survivable grind-down; retreatHp=0 never
  flees; a 40-seed sweep proves flees occur + every flee is no-penalty/autopilot-off; the mode flag
  threads). `verify` GREEN (258 tests). **Live (R6):** the two toggles render + set the mode.

**Combat mechanics (3a–3d) COMPLETE.**

**Ripple (DONE this commit) — Step 3 CLOSED.** Rippled the new model into the PRD: §4.6.6b rewritten
from the superseded "self-recovering auto-retreat loop" → no-auto-heal + accumulating HP + two
auto-modes + loss-stops-autopilot; §4.6.6 setback → the defined carried-wealth bite; **new §4.6.6c**
the kura storehouse (carried vs banked). ADRs **D-090** (a lost fight bites carried, the bank
shelters banked) + **D-091** (the two auto-modes), both refining D-076. `verify` GREEN. **Step 3 done.**

## 5 · Step 4 — koku economy tightening (game part DONE, pushed)

Tightened the T0 koku economy for scarcity (D-086 / batch-1 call 4) with sinks across all three
directions the human named — a lighter pass than the plan's full §4 re-derivation, for a reason
found on the way (scope calls below).

- **Repair costs koku** — a recurring combat-upkeep sink (`REPAIR_KOKU_COST` = 6, liquid). Learned
  the hard way (the m2 no-stranding test): a HARD koku gate softlocks an unlucky fresh L1 (few early
  wins → no koku → can't repair → stranded), violating **D-061/D-086** (tension must never push the
  player out). So it's a **soft FEE** — repair needs wood (hard) + up to 6 koku **waived when you
  can't pay** (the smith extends a broke goshi credit). A real sink normally, zero softlock risk.
- **A 4th estate stage E4** ("Raise the long-house", 1400 koku) — the flywheel's biggest turn, a
  deeper late-T0 koku sink priming the house's rise to T1.
- **Market depth** — 2 pricier capped goods (road-rations, smith's billet), inside the D-008
  minority-lane cap (a RED-able test asserts total market spend ≤ ⅓ of the estate sink).
- Tests: RED-able guards in economy.test (the fee bites / is waived, ≥4 ascending estate stages,
  market ≤⅓ estate) + updated the market-count + m2 no-stranding tests. Regenerated the content
  docs. `verify` GREEN (263 tests). **Live (R6):** E4 offered at E3, 6 market items, the 6-koku fee.

**Scope calls (surfaced, bias-to-motion):** (1) the plan's **§4 6-tier re-derivation** is **deferred
to the final PRD-consistency pass** — a finding: §4 is T1+ *pillar/tier* balance, NOT the T0
koku-sink *content* constants, so the plan's "both move the same numbers" coupling is looser than
stated; the relabel is separable doc-hygiene, done once in the final pass. (2) **battery #15**
(surplus materials / 2nd recipe) → **Step 7** — a 2nd recipe ripples into Step 2's craft variants +
it's literally a battery item.

**Step 4 CLOSED** — ADR **D-092** (koku scarcity tightening; the soft repair-fee no-softlock design)
+ a PRD §4.6.6d note landed. `verify` GREEN.

## 6 · Step 5a — activities are spatial (batch-2 call 1) DONE

The first slab of the spatial map (the human's biggest batch-2 call): **every labour now belongs to
exactly one map node, and there is no default node.** You walk to the work.

- **Core gate** — `canDoActivity`/`availableLabours` (`src/core/selectors.ts`) now require
  `activity.area === state.location`; the work tab lists only the current node's labours. The
  `activity.area` bindings were already in `content/activities.ts` (farm→home-paddies,
  haul→gate-forecourt, woodcut→woodlot-edge, forage→near-satoyama); this makes them load-bearing.
- **The harness learned to walk** — the shared `focusedOptimalIntent` driver (`pacing-report.ts`,
  the SINGLE policy both pacing + playcheck drive) now BFS-hops the *revealed* graph to a labour's
  node before working it (`nextHopToward` + `cheapestEligibleGlobal`). This is why the macro e2e /
  pacing / invariants / playcheck all stay green under the spatial model — the optimal path is
  *navigate-then-do*, not teleport-and-do.
- **UI** — renderActions shows a "no work where you stand — open the 地図 map and walk" hint, guarded
  so the cold-open kura (gate not yet revealed) still just offers rake-rice.
- **Test ripple** — 7 core test files drove `do_activity` from the old default node; each now sets
  `location` to the labour's node first (assertions unchanged, fights untouched — foes aren't spatial
  until 5b). Fixed via a 7-agent workflow, one file each.
- **`verify` GREEN — all 10 gates** (tsc + 263 tests + pacing/playcheck/invariants).
- **Live (R6):** through the real reducer — farm @ kura → *rejected*; walk kura→gate-forecourt→
  home-paddies → *both hops land*; farm @ home-paddies → *works*; woodcut @ home-paddies (wrong
  node) → *rejected*. The walk-to-work loop is reachable.

Next in Step 5: **5b** foes on nodes (home-paddies: monkey+boar, near-satoyama: wolf, woodlot-edge:
bandit, kura: scripted wolf) → **5c** spatial banking (deposit/withdraw at the kura) → **5d** the
load-bearing deed node + post-ascension after-state → **5e** map-presentation variants (diverge
A/B/C) → PRD ripple + ADR.

## 7 · Step 5b — foes are spatial (walk to the fight) DONE

Foes now live on map nodes — you walk to a foe's ground to fight it, the natural companion to 5a.

- **Content** — `MobDef` gained `area: AreaId`: crop-raiders (monkey + boar) on the home paddies,
  the lean wolf at the near satoyama, the road bandit on the woodlot road, the scripted wolf
  cornered in the kura where you woke.
- **Two selectors, cleanly split** — `foeForecasts` stays *location-independent* (it's "my win-rate
  if I fought this foe" — the curve the balance gates read; filtering it would've broken ~5 curve
  tests and misframed the concept). A NEW `foesHere(state)` does the spatial filter for the UI. This
  confined the ripple to the intent gate + the driver — no curve-test churn.
- **Reducer gates** — `fight` no-ops unless you stand on the foe's node; `face_wolf` is faced only
  at the kura; `move_to` clears `autoCombat` (walking away ends the auto-grind you left behind).
- **UI** — the combat "watch" lists only `foesHere`, with a "no foe holds this ground — walk the 地図
  map" hint when empty; the work-tab wolf beat shows at the kura, a walk-back summons elsewhere.
- **The autoplay policy moved to core (R2 single-source)** — `focusedOptimalIntent` + `nextHopToward`
  + `cheapestEligibleGlobal` now live in `core/autoplay.ts`, exported and shared by the pacing gate,
  playcheck, the full-arc/invariants e2e, AND the DEV `__qa.toRung` climber. This FIXED a real
  regression: the old `toRung` used the now-spatial `availableLabours` and STALLED at R1 (couldn't
  reach the paddies to farm). The DEV `__qa` also gained `goto(node)` (BFS-walk the revealed graph)
  and `fight`/`faceWolf` now auto-navigate to the foe first.
- **Tests** — 5 RED-able 5b guards (fight works on-node / no-ops off-node, `foesHere` is
  node-scoped, walking clears auto-combat, the wolf is kura-only), driven from the source-of-truth
  `getMob(...).area`. m2 + render tests set the foe's node. `verify` GREEN — all 10 gates, 267 tests.
- **Live (R6):** through the real reducer/UI — `toRung('R3')` now climbs the WHOLE arc (walks to
  farm, back to the kura for the wolf, reaches R3); fight auto-walks to the foe's node; the watch
  shows monkey+boar at the paddies and the empty-ground hint at the forecourt; walking off clears
  auto-combat. The played monkey@L1 win-rate measured **0.333**, matching the **0.32 forecast** — the
  forecast is honest (no reality gap).

**QA scripts made spatial-honest** — `qa-shots.mjs` stands on the paddies for the combat shots (the
kura watch is empty by design); `playtest.mjs`'s stability grind now cooks (heal) + repairs
(sharpen) between bouts — without it a labour-climbed R3 MC is combat-untrained at 1 HP and just
loses forever (D-076 deadliness working). With maintenance it shows monotonic progress (L1→L3, no
anomalies). *(Screenshot-gallery regen deferred to 5e, after the map UI settles — regenerating now
then again at 5e is wasted work.)*

## 8 · Step 5c — banking is spatial (bank at the kura) DONE

The bank (carried↔banked, built in Step 3) is now a PLACE: the storehouse IS the kura, so you
deposit/withdraw only while standing at the grain-store where you woke.

- **Reducer** — `deposit`/`withdraw` no-op unless `location === 'kura'` (the gate the Step-3 comments
  already promised).
- **UI** — the storehouse card still shows the carried/banked balance EVERYWHERE (your safe reserve
  is worth seeing on the road), but the Store/Withdraw buttons render only at the kura; elsewhere a
  "walk back to the kura (蔵)" hint replaces them.
- **Tests** — 2 RED-able 5c guards (deposit/withdraw no-op off the kura, work at it; banked stays
  safe until you walk back). The Step-3 bank tests already run at the kura (createInitialState
  default), so no churn. `verify` GREEN — 10 gates, 269 tests.
- **Live (R6):** deposit off the kura is a no-op (500 carried, 0 banked); walk to the kura, deposit →
  500 banked; the UI shows the buttons at the kura and the walk-back hint at the forecourt.

## 9 · Step 5d — the load-bearing map node (D-078) + the ascension after-state DONE

The meatiest slab of Step 5 — the map finally GATES something, and the game's biggest payoff resolves
into a real next-state.

**Part 1 — a load-bearing node (D-078, clears battery #13).** A new 7th node **deep satoyama (奥山)**,
one hill beyond the near satoyama, gates a materially **richer forage**: `forage_deepwoods` yields
sansai 4 / koku 2 vs the near sansai 2 / koku 1 — you walk one hill farther (past the danger ring,
at a higher satiety cost) for a better haul. This ties the spatial spine to the Step-4 koku economy
AND the combat cook-loop (more sansai → more healing). The **boar dens there** (moved from the home
paddies — fits the quest's "come down out of the satoyama"; the paddies keep the intro monkey), so
it's a true hunting ground with the better combat payoff too. Reveals at R2 with the other satoyama
rooms; both satoyama nodes are danger rings.

**Part 2 — the ascension after-state (F2: "design the AFTER of every payoff").** The T0→T1 ceremony
used to land on a STALE panel — the House Influence pane fell through to "Reach Excellent standing to
ascend (480/480)", telling you to do what you'd just done. Now, once ascended (`tier ≥ 1`), the panel
RESOLVES: "You are a man of the house. 家産 stands Risen — the Estate is yours to raise, no longer
merely to save," the lord's boon prompt ("N points to spend on the Skills tab"), and a "to be
continued" frontier (mirrors the rung-ladder frontier). The big moment now has a satisfying AFTER.

- **Tests** — RED-able: the deep forage out-yields the near on both resources + costs more to earn
  (economy.test); the boar/monkey node split + `foesHere` (combat-rework.test); the danger-ring set
  (map.test); the after-state DOM guard (render.test — asserts "man of the house"/"Risen"/boon and
  NOT the stale prompt). Regenerated `docs/content/t0-content.md`. `verify` GREEN — 10 gates, 275 tests.
- **Live (R6):** walked past the danger gate to the deep node; deep forage = sansai 4 / koku 2 vs
  near 2 / 1; 1 foe at deep (boar), 1 at the paddies (monkey). Ascended tier 0→1: the panel shows the
  resolved man-of-the-house after-state + the 5-point boon, and the 480/480 stale prompt is GONE.

## 10 · Step 5e — map-presentation variants (D-075 diverge) DONE

The map is now the load-bearing spatial spine, so per **D-075** it ships as **2 alternates behind the
DEV toggle** (self-picked default A ships; B/C DEV-only, each a review.md R-item, zero prod flag-debt):

- **A · paths list** _(default, ships)_ — the existing you-are-here card + "Paths lead to →" move list.
- **B · 絵地図 estate schematic** _(DEV-only)_ — the estate as a spatial TRAIL: revealed nodes laid out
  in columns by BFS distance from the kura (left→right, outward), you-are-here lit gold, walkable
  neighbours live ("Walk here →"), danger-gated marked ⚠ + the conditioning need. Graph-derived, never
  drifts from the map.
- **C · 道中記 traveller's ledger** _(DEV-only)_ — INFORMED routes: each path shows the destination's
  blurb + what awaits (⛏ labours, ⚔ foes, ⚠ danger), so you choose your road from the map, not blind.

Built on the Step-1 variant infra: the `map` surface + its 3 variants in `SURFACES`; B/C in
`renderMapVariant` (ui/dev.ts, inline-styled, DEV-only); render.ts's `renderMap` falls through to the
variant then draws default A. **Strip-safe** — `npm run build` + grep dist = **0 leak** of the DEV
markers / variant strings. **Live (R6):** toggled A/B/C in the DEV panel — all three render
intentionally (woodblock/ink, kanji, the danger lock); B's schematic lays out 蔵→門前→田圃→里山→奥山 with
you-are-here + walkable tags; C's ledger shows the routes + ⛏/⚔ what-awaits. R-items updated
(review.md R2 "Walkable map" → all three LIVE). `verify` GREEN — 10 gates.

**Step 5 is now COMPLETE (5a–5e) + rippled to canon.** The spatial-map model landed in the PRD (§2.6(f)
+ the `LabourNode.area` data-shape field) and as **ADR D-093** (T0 activities/foes/banking are spatial;
no default node; the load-bearing deep node; the after-state; the navigation-aware autoplay policy) —
implementing D-078, composing with D-090 (bank) + D-075 (map variants).

## 11 · Step 7 — battery engineering leftovers (IN PROGRESS)

Grab-bag of deferred engineering (no design decisions). Landing item-by-item, each its own
verify-green commit.

- **✅ #8 — the seasonal judge fired 0× on the optimal path (REAL bug).** Measured it precisely: the
  Phase-2 Estate deed-grind reaches EXCELLENT in **~5 days / 60 acts**, but the judge fired only on the
  **28-day** season boundary — so it **never fired** before ascension (and a 7-day weekly cadence would
  also miss, 5 < 7). The A4 self-inflation (re-judging its own payout) was **already fixed** (pillars.ts
  advances `judged` to the post-bonus high-water). **Fix:** decouple the judge from the 28-day calendar
  — a new `PHASE2_JUDGE_INTERVAL_DAYS = 3` (liquid, D-059) reckons on a cadence **≤ the grind span**, so
  a judge is GUARANTEED before the gate; the 28-day `season()` calendar is untouched. Renamed the step
  hook `onSeasonBoundary → onReckoning`, reframed the log ("The accounts are reckoned…", dropping "the
  season turns" since 3 days isn't a season). Now it fires **1×** at day 3 (+119) — and that reckoning
  is what **tips the estate to EXCELLENT**, a clean showcase beat (the periodic judgment certifies you
  for ascension). RED-able integration test in `ascension.test.ts` drives the real grind + asserts the
  judge fires + banks (`judged > 0`) + logs before EXCELLENT (goes RED under the old season cadence).
  _(T1+, when Phase 2 is a long game, scales this back toward the 28-day season — a per-tier concern.)_

- **✅ #21 / #22 — UI nits (the LOOK):** (a) dropped the non-curated 🔒 on the House-Influence
  silhouette rows (outside the woodblock motif — the greyed ◆ ———— ghost-row already reads as locked;
  the a11y label moved to the row). (b) The **cold-open is now the "kura-dark" waking beat** (§10) — a
  deep sumi field dimming to black at the edges, the 神隠し title + lede in pale washi emerging from the
  darkness (transparent frame, faint hairline), "Open your eyes" the one lit action — was a flat light
  field. Live-verified (light-on-dark, high contrast). (c) The **per-deed cap** (never binds — cap 10 >
  the sole T0 producer 8) is now **documented as intentional forward-headroom** for T1+ (the anti-spike
  guardrail for when multiple larger deed producers land) — a comment, not a balance change.
  - _Left as taste/design calls (noted, not changed solo):_ the grade-WORD gold→ink recolour (#21 — a
    taste call, already a11y-100 + a deliberate diverge-adjacent choice); gating estate-deeds to
    estate-only labour (#22 — a T1 balance/design call).
- **⏸ #15 — the material-surplus sink → SURFACED as H11 (not built).** A 2nd craft recipe grazes two
  locks (a 2nd weapon breaks **D-052** one-craftable; a materials→koku or materials-repair path loosens
  **D-092** koku-scarcity; a consumable adds a new item system) — so it's a **taste call the human
  owns**, not a solo pick. Surfaced as **H11** with an option-map (my rec: accept the surplus as
  fine-for-a-tutorial, deepen the material economy at T1). Continuing the lock-neutral items.

- **✅ #11 / #12 — DOM interaction tests + DEV-harness smoke.** #11: RED-able jsdom tests that the
  spine surfaces' buttons dispatch the RIGHT Intent off real `GameState`, targeting v0.3.1's new
  SPATIAL behaviour — a labour button → `do_activity` at its node, a map path → `move_to`, the kura
  storehouse → `deposit`, a foe's Fight → `fight{mob}` on that node (render.test.ts). #12: the map
  variants (5e, previously DOM-untested) now route + render under the DEV toggle — A the paths card, B
  the 絵地図 schematic ("You are here"/"Walk here"), C the 道中記 ledger ("You stand at"/"Set out")
  (dev.test.ts). `verify` GREEN — 10 gates, 279+ tests.

- **✅ #19 — dead write-only flags + unrouted dialogue: AUDITED → documented, not deleted.** An
  Explore agent scanned every flag set-site for a reader; then **verify-don't-trust (R2) checked each
  claim** — and it paid off: most "dead" flags are NOT safe to delete. `ascended-t0` +
  `dream-t0-ascension` are **asserted by the ascension e2e**; `rank-r3`/`combat-unlocked`/
  `quest_..._done` are **test fixtures/assertions**; the `rank-rN` flags are a **symmetric per-rung
  marker set** (r1 gates a reveal); `dream-t0-ascension` is a **forward-hook** (the dream chain). Only
  a few (`tier-1-reached`, `rank-r2/4/5/6`) are zero-ref — and deleting a few of a symmetric set makes
  it ragged. The dialogue stubs (`kihei-intro`, `soan-intro`) are **authored-but-not-yet-routed**
  forward content (already commented). So the right fix was **clarifying comments** (ascension flags,
  the rank-marker convention, the dialogue stubs) that resolve the "these look dead" confusion the
  battery flagged — no risky deletion of intentional state. `verify` GREEN.

- **✅ #20 — milestone-integrity as a verify GATE (D-054, the D-088 teeth).** Pushed the
  milestone-integrity NORM up to the **11th `verify` gate**: [`milestone-integrity.ts`](../../src/scripts/milestone-integrity.ts)
  — kept LEAN per the "build the game" steer (no fragile full-markdown parse). A hand-curated MANIFEST
  maps each named DoD forward-contract (G-CURVE, G-NO-DEAD-VALUES, NO-STANCE-DOMINATED,
  DISPLAYED==TESTED, playcheck) + D-088's per-tier e2e/invariants tests → the file+marker that
  RESOLVES it; the gate asserts (1) every instrument resolves (file exists + carries its marker) and
  (2) every named contract the roadmap MENTIONS is manifested (a DoD can't name an un-wired
  instrument). Fails only on a genuine unresolved instrument → never cries wolf (A11). RED-able (delete
  a DoD-named test → RED). Rippled: D-054 consequence (BUILT), the `10→11 gates` count in
  project-status + working-agreements. `verify` GREEN — 11 gates, still under the 5s budget.

- **✅ diverge-skill doc reconciled to the built reality (deferred rewrite).** The `diverge` SKILL.md
  body still described the RETIRED branch/`?variant=`/contact-sheet/GC model (a header band-aid pointed
  at the new model but said the §§ were "being re-worked"). Reconciled the ACTIONABLE sections to the
  DEV-panel reality: the header is now authoritative + scopes what's current (§1/§2/§5/§6) vs
  superseded-rationale (§0/§3/§4/§7/§8); **§2 is the built procedure** (author A inline in render.ts →
  add a `SURFACES` entry + `renderVariant` alternate in `dev.ts` → the `import.meta.env.DEV` fall-through
  → prove strip-safety → one R-item per variant); **§6's R-item shape** is the live-in-the-DEV-panel
  form (no contact sheet / no branch / no `variants-log.md`). Per the "build the game not meta" steer,
  did NOT gold-plate a full 253-line rewrite of infrastructure that never existed — the dead-infra
  sections are unambiguously flagged "do not follow", collapsed under a `<details>`, kept only as the
  caps/anti-slop rationale. `verify` GREEN.

**Step 7 is COMPLETE** — every item landed (#8 the judge bug · #11/#12 DOM tests · #19 the flag audit ·
#20 the milestone-integrity gate · #21/#22 the UI nits · the diverge-doc) or was honestly surfaced
(#15 → H11, a taste call the human owns).

## 12 · Final pass — PRD-consistency + the diff re-audit

- **Diff re-audit (independent eyes on my own work, R3).** A `general-purpose` subagent audited the
  whole session diff (`ddb450a..HEAD`, 13 commits) — it reproduced the green (built the prod bundle,
  grepped it, ran `verify`), and reported: **strip-safety AIRTIGHT** (0 DEV leaks — `__KAMI_DEV_PANEL__`,
  `renderMapVariant`, 絵地図/道中記, `__qa`, `focusedOptimalIntent` all 0 in `dist/`); **pure-core clean**;
  **no scaffold/debug**; the **spatial model correctly gated** with e2e proof the harness reaches
  tier-1 ascension (not a tripped guard); the **new tests all genuinely RED-able** (spot-checked 6).
  - **The one SHOULD-FIX it caught (R3 — my false partial-green):** the #8 seasonal-judge cadence
    change landed with only a **code comment** — the PRD's §4.2.2 ("fires every season / canon-fixed")
    + §6 ("runs every season" + a `pendingAppraisals` counter) were **stale/contradicted** and there was
    **no ADR**. FIXED: **ADR D-094** (the reckoning cadence is a per-tier LEVER; the up-only high-water
    SHAPE stays canon) + rippled **§4.2.2** (shape=canon vs cadence=lever) and **§6** (per-reckoning hook;
    also corrected the stale `pendingAppraisals`-counter prose — the code folds one day at a time, no
    counter). This is the per-step-mandate ripple I'd missed for #8.
  - **NIT b (fixed for consistency):** `set_auto_combat` didn't gate on the foe's node (a DEV-reachable
    no-op) — now it mirrors the `fight` gate (arm only where the foe stands; clearing works anywhere),
    with a RED-able guard.
  - **NITs a & c (noted, no change):** (a) BFS pathfinding doesn't pre-apply the danger-ring gate, but
    `walkTo` already breaks on no-progress and `cheapestEligibleGlobal` excludes danger-gated labours at
    low conditioning, so it never strands — mitigated. (c) `forage_deepwoods` isn't rung-eligible + the
    deep node reveals at R2, so it's **optional MANUAL breadth** (a player CHOOSES to walk deeper for the
    richer yield), not on the auto-critical-path — which is correct: D-078 "load-bearing" means "the map
    gates a real yield" (clears battery #13), not "mandatory".
- **§4 6-tier re-derivation → reasoned-deferred to the T1 build** (the Step-4/D-092 finding: §4 is T1+
  tier balance decoupled from the T0 koku work; inventing T1 numbers now — T1 unbuilt, no playtest data
  — would be speculative, not "correct", R1). Updated the §4 deferral note + project-status.
- **Screenshot gallery regenerated** (`qa-shots.mjs`, spatial-world fixes) — 11 shots, no console
  errors; the kura-dark cold-open confirmed. **Human queue reconciled** (R1 now names the v0.3.1 build,
  R2 adds the map variants, H11 surfaced).

## 13 · Fun/feel playtest pass (the human re-launched the loop — "build a fun game")

v0.3.1's plan is built + checkpointed; the human re-invoked `/loop` ("build a fun game… actually
building the game, not meta"). R5 says a compiling build is only the FLOOR — so I'm PLAYING the spatial
build for feel and fixing where fun is absent (proxies prove absence; the human certifies presence, R1).

- **✅ FOUND + FIXED: the labour was buried below the fold.** Playing the Work tab at a labour node,
  the render order was `ladder → estate → market → storehouse → influence → actions` — so at
  home-paddies "Work the home paddies" (the HERO action you walked there to do) sat at **y=1517px, ~865px
  below a 652px fold**, under four global "spend-koku" cards. The spatial model makes you walk to a node
  to work it, then **hid the work button** — undermining the whole loop. **Fix:** the node-specific
  `actions` (labour) now LEADS "What you can do" (y=1517 → **y=517, above the fold**), with the rung
  ladder as progress-context right below and the global estate/market/storehouse panes beneath. Pure
  DOM-order change (each pane renders into its own div by reference; tests query by class/text). Now you
  walk to a node and immediately see + do its work. `verify` GREEN.
- **✅ FOUND + FIXED: the MC's HP was INVISIBLE.** The combat tab showed the monkey at "0%" — the honest
  low-HP forecast (the R3-via-climb MC is at 1 HP) — but **HP was displayed NOWHERE**: the header vitals
  are koku · "body"(satiety) · wood · sansai; the combat pane shows level/weapon but no current HP; no
  HP number anywhere. So the whole **D-076 combat rework** (HP accumulates, no auto-heal, die if hurt, a
  loss bites carried koku) was **illegible** — you can't see you're hurt, can't tell when to heal, can't
  read WHY a foe is 0%. **Fix:** an HP **"life" meter** in the header beside satiety — a verdigris bar +
  the **exact number** (`1/32`) turning **vermilion when ≤30%** — revealed the moment combat first
  matters (the R2 wolf). Live-verified: `life 1/32` red when hurt → `32/32` green when healed. RED-able
  DOM test guards it. `verify` GREEN. Makes the deadly combat the human designed actually readable.

- **✅ FOUND + FIXED: the heal cue was buried.** Played the real combat-heal loop — the untrained L1
  MC drops 32→1 HP over 5 monkey fights (the intended D-076 deadliness), then cooking mends 1→15. With
  the new life bar the "watch HP drop → heal" loop is now legible — but the **only heal** ("Cook a meal")
  was framed as a satiety sink (its own code comment said so) and didn't signal it mends HP. **Fix:** the
  Cook button now carries a tooltip ("…eating is the only way to heal") and becomes the **PRIMARY
  (prominent) action when the MC is hurt** — the "heal now" companion to the red life bar; its
  can't-afford tooltip points you to forage sansai. RED-able DOM guard. Live-verified. `verify` GREEN.

- **✅ FOUND + FIXED: moving between nodes was a tab-switching chore.** Traced a real bank run — farm at
  home-paddies, then protect the koku before combat — it cost ~8 interactions and **4 tab-switches**
  (work → 地図 tab → 2 hops → work → deposit → 地図 tab → 2 hops → work), because the work tab had **no
  move affordance**. The spatial model *encourages* movement (labour/foes/bank spread across nodes) but
  made every move a tab-switch to the map. Movement is only fun if it's smooth. **Fix:** a compact
  **"Walk on 道"** strip on the Work tab — the current node's reachable paths (danger ⚠ + the
  conditioning lock), right under the labour — so you move + work from ONE tab; the Estate 地図 tab stays
  the fuller view. Extracted a shared `moveStrip()` (DRY — the map tab + the work strip use the same
  builder). Live-verified: click "→ Gate & forecourt" on the work tab → you move, still on the work tab.
  The bank run's 4 tab-switches → **0**. RED-able DOM guard. `verify` GREEN.

**Fun/feel pass so far (4 fixes, all found by PLAYING):** labour above the fold · the HP "life" meter ·
the Cook heal-cue · the "Walk on" smooth-nav strip. The spatial + deadly-combat loop the human designed
is now legible AND smooth.

- **✅ VERIFIED the PROD build (the shipped artifact, R6).** Not just the DEV build + a strip grep —
  built `dist/`, served it via `vite preview`, and PLAYED it as a real player (no `__qa`, no DEV panel):
  `__qa` is undefined, the DEV panel is absent, the **kura-dark cold-open renders**, and the core loop
  works (Open your eyes → rake → koku appears) with **0 console errors/warnings**. Grepped the prod
  bundle: all 4 fun fixes SHIPPED (`Walk on`, `only way to heal`, `vital health`, `map-move`) while the
  DEV harness + variants stayed STRIPPED (`__KAMI_DEV_PANEL__`/`絵地図`/`renderMapVariant` = 0). So a real
  player gets the full, working, polished game and nothing DEV-only leaks — the build is solid end-to-end.

## 14 · Multi-lens fun/quality audit (ultracode workflow) + the fix pass

The human said "speed up the loop" (ultracode ON). Instead of solo serial playtesting, ran a **6-lens
fun/quality audit WORKFLOW** (30 agents): combat / nav / economy / visual / onboarding / robustness
each reviewed the build vs the design docs → each finding **adversarially verified** (skeptical,
default-reject) → synthesized. **19 confirmed** (of 23), nearly all render-only or small pure-core. Raw
snapshot: `project/brainstorms/raw/2026-07-01-v031-fun-quality-audit.json`. Implementing in priority order.

- **✅ Pure-core batch (5 fixes, RED-able tested):**
  - **[MAJOR robustness] Weapon-swap free-repair EXPLOIT** — `equip_weapon` refilled durability to max
    on every swap → tapping equip = a free repair, voiding the wood+koku scarcity economy. Now
    switching CARRIES the current durability clamped to the new max (fresh durability only at CRAFT);
    re-equip is a no-op.
  - **[MAJOR robustness] Corrupt-save CRASH** — `validate.ts` accepted any string as `location`, so a
    bad node id reached `getNode()` and crashed the UI on load. Now clamped to `MAP_NODE_IDS` (fallback
    the kura), coercing only a present-but-invalid value (old saves missing it default cleanly).
  - **[MAJOR onboarding] First-rake monologue dump** — all three raked-gated mentor lines fired on the
    FIRST rake (text-wall; the "kept on" payoff pre-empted). `deliverDialogue` now takes a per-call cap;
    the rake path passes 1 → gen-rake #1, gen-keep #2, gen-kept #3 (one teach per moment).
  - **[MODERATE combat] Auto-combat halts SILENTLY on a broken weapon** — the "leave it running" mode's
    natural end-state just stopped with no message (R3 false-silence). Now logs why.
  - **[MINOR combat] Auto-flee-while-hurt PHANTOM flee** — arming auto·flee at ≤20% HP made
    `resolveFight` "flee" on turn 1, logging "winded, blade up, but whole" for a non-fight + silently
    un-arming. Now guarded: refuse honestly ("too hurt to hold the line — eat and mend"), no
    fight/clock/wear. `verify` GREEN (11 gates, 286 tests).

- **✅ render/styles legibility batch (11 fixes, live-verified):** the **estate flywheel payoff** now
  shows (`+15% labour output · +20 max body` — the invisible koku sink motivation, R6); the map/Walk-on
  strip now says **what's at each node** (`rice`, `greens · rice · a foe stirs`, `the storehouse` —
  derived from content, scout-fog preserved); the danger-ring lock reason is **VISIBLE**
  (`Needs Conditioning Lv2`, not hover-only on a disabled button — ui-design §5.9/§8); the **HP number**
  bumped off the smallest type tier (`--fs-micro → --fs-small`, tabular); the combat **auto-mode labels**
  carry the risk on their face (`▶ auto · to the end` vs `▶ auto · flee @20%`); the banked-koku tooltip
  stops lying (`Draw koku from the kura storehouse first`); the **market blurb** renders (trade wasn't a
  bare price list); the **full-but-stuck rung bar** shows the story cue (`Service enough — but the
  grain-store wolf still waits`) instead of a dead `N/N`; the dead `.influence-when` class got its rule;
  the generic-web `⚙`/`⚠` became the ink `設定`/`険`; the stale "open the 地図 map" hints point at
  `Walk on 道`. `verify` GREEN.

- **✅ [MAJOR onboarding] R0 blind ~550-click rake grind** — the make-or-break first 5 min was a blind,
  un-automatable rake grind with NO visible progress (the ladder hid until R1, and rake — a meta verb —
  had no auto-toggle like every later labour). Fixed both: (1) the **rung ladder now shows during R0**
  (once you've raked → "Estate service · 12/1100", a visible next goal); (2) an **auto-rake toggle**
  (new `autoRake` state field + `set_auto_rake` intent + a main.ts auto-loop branch), revealed after a
  few manual rakes (meter ≥ 10, so the first rakes still land as juice) — tap ▶ auto and it rakes toward
  R1, resting when starving, clearing itself at R1. Live-verified: meter climbs automatically once
  armed. RED-able test. `verify` GREEN — 11 gates, 288 tests.

**Audit fix pass COMPLETE — all 19 confirmed findings landed** (3 MAJOR robustness/onboarding + the
render/styles legibility batch + the R0 grind). The v0.3.1 build is materially more fun + legible +
robust than the plan alone produced.

## 15 · Exit checkpoint (2026-07-01) + the plan marked DONE

`/prepare-to-exit` ritual: tree clean, `origin/main == ae85200` (everything pushed). Marked the build
**plan DONE** (`docs/plans/2026-06-30-v0.3.1-build.md` Status line was stale — "not started" — now ✅ DONE;
per convention a completed plan LINGERS in `docs/plans/` with a DONE status, not archived). Reading queue
already empty. A **second, deeper audit pass** (regression-check + pacing/a11y/completeness/arc-coherence)
was in flight — resumed to finish (its confirmed findings are the next fix batch).

## H11 closed → D-095 (the material-surplus sink)

The human resolved **H11** ("C for now"): the post-craft hardwood/sinew surplus is accepted as
**working-as-intended for the T0 miniature** — every candidate sink grazed a lock (2nd weapon → D-052;
mats→koku / mats-repair → D-092), so a real material economy becomes a **T1** concern. Graduated to
**ADR D-095**; no code change. Rippled: verbatim intent captured
(`project/human-feedback/2026-07-01-h11-material-surplus.md`), H11 removed from the open queue,
archived (crosswalk row → D-095), and battery **#15** annotated CLOSED-WAI in the fidelity report.
The open human queue is now **R1** (play/taste) + **R2** (variants) only.

## Next intended steps (current)

1. **Done this session:** audit pass-2 landed (10 fixes), the archive-convention flip, and the loop
   wound down at the human's call. v0.3.1 is built + twice-audited (19 + 10 fixes), all green on
   `origin/main`. The only open work is the **human's**: **R1** (play/taste — the real unblock) and
   **R2** (variant review, live in the DEV panel). **H11** closed (D-095). A future session could run
   an optional pass-3 / deeper single-lens dive, but the build is ship-quality as-is.

## Landmines (current)

- **Variant strip-safety is load-bearing** — every new surface variant follows the Step-1
  pattern: default inline in render.ts (ships); alternates in `ui/dev.ts` behind
  `import.meta.env.DEV && dev.renderVariant(surface, …)`; add the surface to `SURFACES`; the
  gh-pages guard greps `__KAMI_DEV_PANEL__`. (Proven: `npm run build` + grep dist = 0 leak.)
- **DEV panel** — fixed top-right in a reserved 16rem `#app` gutter so it never covers content
  (the shell re-centers clear of it); collapsible.
- **B/C a11y** — DEV-only until picked; if the human picks one, re-check WCAG contrast (B's
  dark ink label on the indigo "Good 良" box).
- **Shared-tree safety** — another agent may edit the tree; stage only my own files by explicit
  path, never `git add -A`.
- **The bank is a real state-model change** (resources → carried+banked pair) — it rides
  Step 3 because the loss penalty needs it; Step 5 makes deposit spatial.

## Convention flip — done plans ARCHIVE, they don't linger (human steer)

The human overrode the standing "done plans linger in `docs/plans/` — trust the Status line,
not the filename" convention: **a finished plan graduates to `project/archive/` the moment it's
done**, so `docs/plans/` lists only *live* plans. Rippled everywhere the old rule was stated —
`AGENTS.md`, `repo-map.md`, `docs/README.md`, the `session-brief.sh` hook (a DONE plan left in
`docs/plans/` now prints "archive it", not "skip"), and the plan's own header. `git mv`'d the
v0.3.1 build plan to `project/archive/2026-06-30-v0.3.1-build.md` and fixed every reference to its
old path (md-links stays green). Added `docs/plans/README.md` documenting the convention (and
keeping the dir tracked when it holds no active plans); exempted that README from the
reading-queue hard-block gate (it's the dir's own doc, never a for-the-human plan). Commit `be9e17c`.

## Audit pass-2 landed — 10 confirmed fixes (all render/CSS/a11y + one flag swap)

The resumed 5-lens pass-2 workflow (regression-check + combat-pacing + a11y + completeness +
arc-coherence; 20 agents) confirmed **10 findings** out of 14 — and crucially **no core / logic /
balance / determinism defect survived pass-1's 19 fixes**. Every confirmed item was low-risk polish:

- **MAJOR** — ascension boon pointed to the *Skills* tab (dead-end); attribute points spend at
  Training 鍛錬 on the *Combat* tab → string fixed (`render.ts`).
- **MAJOR** — `dream-2` + the R3 frontier capstone gated on the *transient* `s.rung === 'R3'`, which
  goes dead forever once R3→R4 promotes (at combat lvl 1); swapped both to the *latched* `rank-r3`
  flag so the beats back-reveal when combat level hits the gate at any rung ≥ R3 (`surfaces.ts`).
  **Added a RED-able test** (`m2.test.ts`) — a state promoted to R4 with `rank-r3` latched still
  reveals both beats (impossible under the old rung check).
- **MAJOR** — manual save-**import** failure was silent (modal just closed → looked like success);
  added a `note()` on the `{ok:false}` branch (`main.ts`).
- **MINOR (a11y)** — `.map-move-hint` (`--ink-faint`, ~3.7:1) and the 険 danger glyph (raw `--beni`,
  4.42:1) both failed WCAG AA on washi-shade → deepened to `#685b48` / `#96303c` (the D-045 pattern);
  move-button yields/foe/gate context tied to the button via `aria-describedby` (unique per strip so
  Map + Work don't collide), 険 span gets `lang="ja"`.
- **MINOR** — Skills tab hid each labour skill's yield accelerator → added a derived `+N% labour yield`
  line (conditioning excluded — it's the gate skill); **NIT** — quests were accepted blind → steps +
  koku reward now preview before "Take this on"; **NIT** — retired the dead `COMBAT_HEAL_FRAC` (its
  doc claimed an auto-heal D-076 removed); **MINOR** — R5 milestone pre-spent the ascension's "man of
  the house" identity beat → retranslated to a humbler "onto the household staff proper" step.

Verified live (Playwright + `__qa` at R3): 0 application console errors, `aria-describedby` wired with
no duplicate ids, hint colour = `#685b48`, Farming shows "+84% labour yield" while Conditioning shows
none. Prod re-built: DEV markers 0 in the executable `.js` (only in the sourcemap, which the gh-pages
guard correctly ignores), all four fix strings present in the bundle. `verify` green (11 gates).

The human then called the loop to wind down. v0.3.1 is **built + twice-audited (19 + 10 fixes)**; the
only open work is the human's — **R1** (play/taste) + **R2** (variant review).
